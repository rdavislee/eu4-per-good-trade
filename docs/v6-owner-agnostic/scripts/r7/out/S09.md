## §2.4

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y143 | second-order term + pinned tol: 76/290\to0 (tol pinned both), 12/290\to0 (full cost both) | CONFIRMED | measurement (`p3_relabel_pergood.py`, rerun) | `prg.out`: "first-order only, LP_OPTS per-good: 76 of 290 runs move" / "full cost, HiGHS default tol per-good: 12 of 290 runs move" / "full cost, LP_OPTS 1e-10 per-good: 0 of 290 runs move" (this last row is the "0" for both comparisons) |
| Y144 | v6.0's 580-of-580 sweep measures the unit-cost objective, superseded not contradicted | CONFIRMED | file read + rerun (`../v5-owner-agnostic/scripts/_audit_b_1444perm.py`) | line 58: `res = linprog(c=np.ones(len(arcs)), ...)` (unit cost, no tie-break term); rerun output: "1444, 29 goods x 20 full node relabellings = 580 runs" / "orientation changed : 580" |
| Y484 | tradenodes file generated once from campaign-start `Φ_w`, owned by DLL in memory, no per-session regen, merchants recalled only on rebuild | UNTESTABLE | gap | no emitter/generator or DLL exists anywhere in the tree (`scripts/` has no writer for `00_tradenodes.txt`, no DLL project); this is a forward design spec for unbuilt components, nothing to check it against |
| Y485 | engine logs `[tradenodedefinition.cpp:61]: X=>y ( ERROR: Trade nodes must always be defined so that an outgoing is defined after in the file, or we get processing errors)` once per violating link but tolerates violations | CONFIRMED | file read (eu4.exe string table) + file read (error.log) | eu4.exe string table (offset ~30311818): `" ( ERROR: Trade nodes must always be defined so that an outgoing is defined after in the file, or we get processing errors)"` verbatim; `crashes/eu4_20260820_134250/logs/error.log:64`: `[tradenodedefinition.cpp:61]: Valencia=>sevilla ( ERROR: Trade nodes must always be defined so that an outgoing is defined after in the file, or we get processing errors)` |
| Y486 | all-159-backwards file logs exactly 159 such errors, then loads/plays normally, `total`/`retention` unchanged, power-dependent fields vary only within run-to-run noise | CONFIRMED | file read (EU4 install logs) + file read (`../v2-drain/game-session.md`) | `logs/error.w_permute.log`: `grep -c "tradenodedefinition.cpp:61"` = 159 (0 in `error.w_vanilla.log`); `game-session.md` §B2 table: `total` 1/79 differ max 0.012% in both the vanilla-vs-vanilla noise control and the vanilla-vs-permuted test; `retention` 0/80 differ in both; `current`/`local_value`/`outgoing` differ by comparable magnitude (noise ~9%) in both |
| Y487 | 2-node cycle produced `EXCEPTION_STACK_OVERFLOW` at `0x00007FF6DDE6A8B4` under 1002 recorded `eu4.exe` frames, reproduced 3x, vanilla and reversed-order file both fine as controls | CONFIRMED | file read (3 crash dumps) + file read (`game-session.md`) | all 3 dumps' `exception.txt` (`.../crashes/eu4_20260820_{134250,134617,165621}/exception.txt`): `Unhandled Exception C00000FD (EXCEPTION_STACK_OVERFLOW) at address 0x00007FF6DDE6A8B4`, 1002 numbered `eu4.exe` frames each; all 3 `meta.yml` show `Mods: mod/pgt_cycle.mod`; `game-session.md` §B1 control table: vanilla 0/159 order-violations loads&runs, `pgt_permute` 159/159 loads&runs, `pgt_cycle` (the cycle) crashes |
| Y488 | the crash dump records no per-frame addresses | CONFIRMED | file read (crash dump exception.txt) | every one of the 1002 stack-trace lines in all 3 dumps reads `eu4.exe (function-name not available) (+ 0)` — a fixed zero offset placeholder, no actual per-frame address; only the single top-level exception address is given |
| Y489 | acyclicity is a hard correctness requirement established by observation, not assumed | CONFIRMED | derivation from the cycle-crash finding | follows directly from Y487's observation (crash reproduced on a hand-authored cycle, not inferred); `game-session.md` §B1: "It has now run... Acyclicity is enforced because the engine provably cannot survive its absence." |
| Y490 | reversed sevilla\to valencia link (outgoing block moved, path+control reversed) loaded with zero errors and rebuilt the economy | CONFIRMED | engine test (`game-session.md` §B3) | "`logs/error.log`: zero non-descriptor lines. The engine accepts a hand-reversed link silently"; setup: "`sevilla → valencia` removed from sevilla and `valencia → sevilla` added to valencia, path and control reversed by §2.4 item 3's recipe" |
| Y491 | Valencia moved to Sevilla's incoming side, Sevilla became a zero-outgoing end node, Castile's merchant switched steering\to collecting, Aragon+France disappeared from the node | CONFIRMED | engine test (`game-session.md` §B3) | node-window table: outgoing tab vanilla "Valencia" \to flipped "— none —"; Outgoing value −0.45\to0; "Castile's merchant: steering... \to collecting"; "countries listed: Castile, Portugal, Morocco, Granada, Aragon, France \to Castile, Portugal, Morocco, Granada" |
| Y492 | every provincial power figure was unchanged in that test | CONFIRMED | engine test (`game-session.md` §B3) | "every *provincial* power figure stayed identical (Castile 129.9, Portugal 88.5, Morocco 23.4, Granada 11.6)" |
| Y493 | that test is the mod's core premise verified end to end | CONFIRMED | observation cited by claim (`game-session.md` §B3 conclusion) | "This is the mod's core premise demonstrated end to end: change direction in the file, and EU4 rebuilds the trade economy around the new direction." |
| Y494 | emit in decreasing `Φ_w` order; shipped vanilla file follows the declaration-order convention, 0 of 159 links violate it | CONFIRMED | file read + parse of `common/tradenodes/00_tradenodes.txt` (own script) | own parser on the shipped file: 80 top-level node blocks, 159 `outgoing` links total; for every one of the 159 links the destination node is declared strictly after the source node in file order — 0 of 159 violate the "outgoing defined after" convention; corroborated by `error.w_vanilla.log` containing 0 `tradenodedefinition.cpp:61` lines |
| Y138 | canonical node order still required but no longer decides the installed map; both order-invariance results are measurements not proofs; §2.1 propagates the per-good economy and writes it back | CONFIRMED | derivation check over the item's own measurements + file read (§2.1, line 912) | assembles Y139/Y140/Y147/Y141/Y143 (all independently confirmed below); §2.1 line 912: "solves per good, propagates the per-good economy externally, and writes the result and the orientation back into the engine's own structures" — matches the claim's paraphrase exactly |
| Y495 | Phase 2's min-cost b-flow is degenerate under unit arc costs: many supports share the optimal cost, and the returned one depends on presentation order | CONFIRMED | algebraic/LP-theory derivation, corroborated by measurement | standard LP-degeneracy fact; `rl.out` shows unit-cost objective deviation across permutations of only 5.626e-16 (same cost to double precision) while 40 of 40 permutations return a different support — exactly the degenerate-face signature |
| Y139 | measured: 40 of 40 permutations return a different optimal support (unit-cost objective) | CONFIRMED | measurement (`relabel6.py`, already-fresh `rl.out`) | `rl.out`: "unit cost (former) ... permutations returning a DIFFERENT optimal support: 40 of 40" |
| Y140 | those permutations reach an objective identical to within a few units in the last place | CONFIRMED | measurement (`rl.out`) | `rl.out`: "unit cost (former) objective 0.789420000928 max rel deviation 5.626e-16" (~2.5 ULP of a double) |
| Y147 | §2.3 now breaks those ties inside the objective | CONFIRMED | file read (`flowop.py`) + measurement (`rl.out`) | `flowop.py` lines 18-19,74: `TIE_EPS`, `TIE_EPS2`, `TIE_COST = 1.0 + TIE_EPS*(...)+ TIE_EPS2*(...)`; `rl.out` tie-break-cost row: "permutations returning a DIFFERENT optimal support: 0 of 40" |
| Y141 | tie-break cost: 0/40 differ; 180 relabellings (three seeds of 60) moved 0/159 edges; `relabel6.py` validates on identity and aborted when tie-break went in, disagreeing on 26/159 edges | CONFIRMED | measurement, own rerun + file read (`p3_unpatched.py`) | `python relabel6.py 60 4242 7 999` (own rerun, matching "three seeds of 60"): "pooled over 180 relabellings / orientation changed : 0 of 180"; `python p3_unpatched.py` (own rerun): "edges agreeing : 133 of 159 / edges disagreeing : 26" — exactly the abort figure |
| Y146 | counts are HiGHS-specific in detail but not kind: any simplex returns a vertex of a degenerate face, tie-break's job is to leave one vertex | CONFIRMED | algebraic/LP-theory derivation | standard simplex-method fact (every run terminates at a basic feasible solution/vertex; a generic perturbation added to a degenerate face's objective yields a unique optimal vertex); consistent with the observed 40/40\to0/40 pattern |
| Y148 | §1.1's key ties in more places than documented, but fires nowhere on 1444: 0 exact (DEF,β) ties on free edges 29/29 goods, 0 within-cluster β ties, 0 tied cluster masses | CONFIRMED | measurement (`props6.py`, already-fresh `p6.out`) | `p6.out`: "core nodes summed over goods: 2320 ; exact (DEF,b) key collisions: 0"; "Phase-1 within-cluster argmin ties: 0 ; top-k cut ties: 0"; "acyclic goods: 29 of 29" |
| Y496 | node window renders incoming/outgoing link lists in file declaration order | CONFIRMED | engine test (`game-session.md` Observation 4) + file read (`tradeinterface.gui`) | Sevilla incoming tabs vanilla "Tunis, Safi" vs permuted-file "Safi, Tunis" — same links/directions, only enumeration order changed; `tradeinterface.gui` lines 90/110: `incoming_nodes_listbox`/`outgoing_nodes_listbox` are plain listboxes with no independent sort key shown |
| Y149 | end-flag list is a function of the world not node order: across the 180 relabellings the end set came back as `{genua, hangzhou}` every time, a change from v6.0 | CONFIRMED | measurement, own rerun + file read (`edits6.json`) | own rerun (180 relabellings): "baseline sink set returned : 180 (100.0%)", holders `[('genua', 180), ('hangzhou', 180)]`; `edits6.json` preserves the old v6.0 text: under the unpatched objective `hangzhou` was end in 100/100 but `english_channel` only 40/100, count ranging 1-5 |
| Y497 | item 2: `end=yes` on every `Φ_w` sink, stripped from any former end node that gains outgoing links | CONFIRMED | derivation, consistent with measured/engine-test behaviour | matches how `drain.py`'s `run_drain` computes sinks dynamically as outdegree-0 nodes (not a static list); directly demonstrated by Y491: Sevilla gained `end` status only after losing its outgoing link in the B3 reversal test |
| Y150 | 1444 has two end nodes, `genua` and `hangzhou`, against vanilla's three | CONFIRMED | measurement (`rl.out`) + own parse of `00_tradenodes.txt` | `rl.out` identity-permutation baseline / sink-count distribution `{2: 400}`, holders genua+hangzhou; own parse of shipped file: `end=yes` nodes = `['genua', 'venice', 'english_channel']`, count 3 |
| Y498 | end count is not fixed, follows wealth field and `α_Φ`, so emitter reads it from the solve rather than assuming a number | PARTIAL | measurement (`p6.out`) for the premise; gap for the emitter behaviour | `p6.out`: "sinks per good min/max/mean: 2/8/3.69" — count genuinely varies, confirming the premise; but no emitter exists in `scripts/` to check whether it "reads it from the solve" — that half is a design statement about an unbuilt component |
| Y499 | item 3: link reversal recipe (move `outgoing` block, reverse `path`, reverse `control`); verify one hand-flipped link before generator code | CONFIRMED | engine test (`game-session.md` §B3) | setup matches the recipe exactly: "path and control reversed by §2.4 item 3's recipe"; conclusion: "The spec's instruction to verify one hand-flipped link before writing generator code is now done, and it passed" |
| Y500 | item 4: `location`, `members`, `inland=yes`, `ai_will_propagate_through_trade`, unrecognized keys round-trip byte-faithfully | UNTESTABLE | gap (searched `scripts/` for an emitter) | `grep`-searched all of `scripts/` for a tradenodes writer/serializer and for "round-trip"/"byte-faithful"; none found — `pdx.py` is parse-only (`tokenize`/`parse`/`load`, no writer). No emitter exists to check byte-faithful round-tripping against |
| Y1171 | `../v2-drain/game-session.md` covers only the first two of the crash launches | CONFIRMED | file read (`game-session.md`) | §B1 explicitly documents 2 launches only: "reproduced on two independent launches. Crash dumps at `crashes/eu4_20260820_134250/` and `crashes/eu4_20260820_134617/`" — no mention of a third |
| Y1172 | the three dumps `eu4_20260820_134250`, `_134617`, `_165621` are identical in exception address and frame depth, citation for "three" | CONFIRMED | file read (crash dump directories) | all 3 exist under `C:\Users\rdavi\OneDrive\Documents\Paradox Interactive\Europa Universalis IV\crashes\`; each `exception.txt` shows the same `C00000FD` at `0x00007FF6DDE6A8B4` with exactly 1002 numbered frames; each `meta.yml` shows `Mods: mod/pgt_cycle.mod` |

## §2.5

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y501 | attachment uses pattern scanning + function hooking following the EU4dll precedent, which provides attach scaffolding but nothing about trade structures | UNTESTABLE | gap | "EU4dll" appears nowhere in the tree except this one sentence in the spec itself; no primary source describing what it provides is present in the materials (a wiki would not count as a source even if one existed) |
| Y502 | mod ships a runtime-patching DLL rather than a modified executable | UNTESTABLE | gap | no `.dll`/`.cpp`/`.h` project exists anywhere under `per-good-trade/` (`find . -iname "*.dll" -o -iname "*.cpp" -o -iname "*.h"` returns nothing); forward design statement for an unbuilt component |
| Y503 | the binary is frozen, so offsets found stay found | PARTIAL | file read (3 crash dumps + eu4.exe) | confirmed for the currently-installed build: identical exception address `0x00007FF6DDE6A8B4` and identical `SCMCommit 835bfdf8ca24c291a1b3f1b5bc72d47e7df1ae18` across 3 launches spanning 13:42-16:56 on 2026-08-20; `eu4.exe` unchanged (38,462,504 bytes) throughout. The unqualified/permanent reading ("stay found" across future patches) is untested — no second build exists in the materials |
| Y504 | nation-pair direction gates of §1.10 are hooked and returned true at the call site rather than by a shared predicate | UNTESTABLE | gap | no hooking/attachment code exists anywhere in the tree to check this implementation choice against |

## §2.6

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y505 | monthly trade tick runs in three passes: static power/modifiers; end-node pass (modified power + propagation); value pass (node value\to collect/steer split\to collect division\to outgoing division) | UNTESTABLE | gap | no decompiled engine control-flow source or developer documentation of the tick's internal pass order exists in the materials; only string-table and log evidence is accessible, which does not reveal execution order. (It is used consistently as a premise in the §3.10 factoring, which is separately confirmed below.) |
| Y506 | written each tick: node trade value = `Σ_g value_g(n)` | CONFIRMED | derivation check (definitional identity) | matches the doc's own table verbatim (line 1311); `value_g(n)` used consistently in the adjacent `collect_pool` row and in §3.10's identity (line 1795) |
| Y507 | written each tick: node collectible pool = `Σ_g value_g(n)·collected_share(n,g)` | CONFIRMED | derivation check (definitional identity) | matches doc's own table verbatim (line 1312); `collected_share(n,g)` independently defined at lines 773-774 as a per-node-per-good share in [0,1], consistent with this usage |
| Y508 | written each tick: per-link value = net `Σ_g` realized flow in the installed `Φ_w` direction | CONFIRMED | derivation check (definitional identity) | matches doc's own table verbatim (line 1313) |
| Y509 | country trade income derived by the engine from the written fields, unless stored | UNTESTABLE | gap | asserts a fact about the real EU4 engine's income-derivation logic; no decompiled engine income-calculation code is present in the materials to check against |
| Y510 | `collect_pool` is itself per good on the inside, because `collected_share(n,g)` depends on `P_transfer(g)`, made commodity-specific by §1.8 | CONFIRMED | derivation check (step-by-step) | line 777: "Transfer eligibility is per good... A country's power counts toward `P_transfer(g)` only if..." — confirms `P_transfer` carries a `g` argument, so `collected_share(n,g)` genuinely varies by good, and so does the pool's per-good decomposition; step is valid |
| Y511 | `powershare_C` (good-independent) factors out of the per-good sum, collapsing it to one scalar; engine's vanilla collection math reproduces every country's per-good income exactly | CONFIRMED | derivation check (algebraic) | `income_C(n) = Σ_g value_g(n)·collected_share(n,g)·powershare_C(n) = powershare_C(n)·Σ_g[value_g(n)·collected_share(n,g)] = powershare_C(n)·collect_pool(n)` is valid distributive-law algebra given `powershare_C(n)` carries no `g` subscript (asserted as a merchant-or-home property); matches doc's own §3.10 derivation (lines 1795-1799), which itself calls it "an identity, not a measurement" |
| Y512 | two deadlines: display immediately after the value pass, because AI consumers read these figures during the month | UNTESTABLE | gap | asserts EU4 AI's internal read timing; no decompiled AI-logic source is present in the materials |
| Y513 | payment bounded by the month boundary, since the treasury reconciles at the start of each month against the previous month's income | UNTESTABLE | gap | asserts a specific internal engine reconciliation timing; no decompiled treasury/tick source is present in the materials |
| Y514 | per-link values written net, which can be negative where realized flow opposes the drawn arrow | CONFIRMED | derivation check (definitional consequence) | a signed "net" value in a fixed installed direction (table row 3, line 1313) is negative by definition whenever actual flow opposes that fixed direction; consistent with the live game's own display convention observed in `game-session.md` §B3 (Sevilla's vanilla "Outgoing value −0.45") |
