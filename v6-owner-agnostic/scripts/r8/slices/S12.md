# Slice S12  (47 claims)


## §2.2a

- **Y454** [UNCHANGED, type MODEL, spec line 1037]
  CLAIM: v2 called the target "map-agnostic" while proving its central properties only for the map it was measured on; …

- **Y455** [UNCHANGED, type MODEL, spec line 1041]
  CLAIM: Premise 1 is that the node graph is connected: reachability is LP feasibility, and the LP is feasible because …

- **Y456** [UNCHANGED, type MODEL, spec line 1042]
  CLAIM: On a graph with more than one component the global balance is not enough — each component must balance …

- **Y457** [UNCHANGED, type MEASURED, spec line 1045]
  CLAIM: Vanilla 1444 is one component.

- **Y458** [UNCHANGED, type DESIGN, spec line 1047]
  CLAIM: The solver must compute components once at load; on a single component it proceeds, and on more than one it …

- **Y459** [UNCHANGED, type DESIGN, spec line 1050]
  CLAIM: The solver must not silently hand an infeasible program to the LP.

- **Y460** [UNCHANGED, type PROCESS, spec line 1051]
  CLAIM: v1 carried per-component renormalisation and v2 dropped it without replacement; v3 restores the requirement.

- **Y461** [UNCHANGED, type MODEL, spec line 1054]
  CLAIM: Premise 2 is that Phase 0 is a no-op, or the map-dependent properties are read as measurements: several §1.1 …

- **Y462** [UNCHANGED, type DESIGN, spec line 1056]
  CLAIM: Where Phase 0 acts, three properties weaken and the spec says so rather than asserting through it.

- **Y463** [UNCHANGED, type MODEL, spec line 1061]
  CLAIM: Global DAG is proved on a 2-core map and still proved where Phase 0 acts, because pendant edges are bridges …

- **Y464** [UNCHANGED, type MEASURED, spec line 1062]
  CLAIM: Sink-set equality is measured exact 29/29 on a 2-core map and fails where Phase 0 acts, because a pendant …

- **Y465** [UNCHANGED, type MODEL, spec line 1063]
  CLAIM: Marking order reproduces the DAG on a 2-core map and fails where Phase 0 acts, because pendants have no marking order …

- **Y135** [UNCHANGED, type MODEL, spec line 1064]
  CLAIM: Where Phase 0 acts, free-edge determinism is unaffected but index-independence is not: the key reads the …

- **Y466** [UNCHANGED, type MEASURED, spec line 1066]
  CLAIM: Two further cases are independent of Phase 0 and both break sink-set equality inside the 2-core: a selected …

- **Y467** [UNCHANGED, type DESIGN, spec line 1071]
  CLAIM: The stated target is connected maps: on a connected map with minimum degree at least 2 every §1.1 property is …

- **Y468** [UNCHANGED, type MODEL, spec line 1072]
  CLAIM: On a connected map with pendants the algorithm still runs and still produces an acyclic, fully-oriented, …

- **Y469** [UNCHANGED, type DESIGN, spec line 1074]
  CLAIM: On a disconnected map the solver must renormalise per component or refuse.


## §2.3

- **Y1169** [CHANGED, type MEASURED, spec line 1185]  provenance: `p3_bisect.py`; `round6.py`
  CLAIM: The `copper` flip count over four column permutations is a function of the seed that draws the permutations; `p3_bisect.py`’s three seeds (20260821, 7, 4242) give 2, 8 and 6.
  CHANGE: old: "the flip count over four permutations is **seed-dependent (2, 8 or 6)**" — three values, no instrument or seeds named → new: it is "**a function of the seed that draws the permutations**", with the instrument and the three seeds named (`p3_bisect.py`; 20260821, 7, 4242) and the 2/8/6 attached to them

- **Y470** [UNCHANGED, type DESIGN, spec line 1079]
  CLAIM: Constants are read at runtime and never hardcoded.

- **Y471** [UNCHANGED, type ENGINE, spec line 1081]
  CLAIM: The nine runtime-read uses map to named defines: `TRADE_PROPAGATE_DIVIDER`, `TRADE_PROPAGATE_THRESHOLD`, …

- **Y472** [UNCHANGED, type ENGINE, spec line 1088]
  CLAIM: `TRADE_MERCHANT_PRESENT` is a bonus on income, not trade efficiency.

- **Y136** [UNCHANGED, type DESIGN, spec line 1093]
  CLAIM: The two wealth coefficients are not the same kind of constant: the emitter reads `GP_COEFF` rather than …

- **Y137** [UNCHANGED, type PROCESS, spec line 1099]
  CLAIM: v3.0 through v5.0 said neither wealth coefficient was in a file; v4.0’s tree ships the one sweep script, `audit_modifiers.py`, which walked past the block holding one of them, and v3.0 and v5.0 repeated the claim without shipping a sweep.

- **Y473** [UNCHANGED, type ENGINE, spec line 1106]
  CLAIM: `GP_COEFF` is 0.2 goods produced per point of `base_production`, measured on four provinces at four …

- **Y474** [UNCHANGED, type ENGINE, spec line 1107]
  CLAIM: `TAX_COEFF` is 1.0 ducat per year per point of `base_tax`, measured on two provinces at two development …

- **Y475** [UNCHANGED, type ENGINE, spec line 1109]
  CLAIM: Both coefficients are read off the tooltips' base lines, which carry no owner term — Garnatah also has …

- **Y476** [UNCHANGED, type ENGINE, spec line 1110]
  CLAIM: Neither coefficient is read off a province window, because a window figure carries the owner's modifiers and …

- **Y477** [UNCHANGED, type ENGINE, spec line 1111]
  CLAIM: Prices come from `common/prices/00_prices.txt` at runtime and are never hardcoded.

- **Y478** [UNCHANGED, type DESIGN, spec line 1114]
  CLAIM: The design constants are the excluded-goods list (defaulting to gold), the α price anchor `P₀ = 2.0`, the …

- **Y480** [UNCHANGED, type MODEL, spec line 1117]
  CLAIM: two knobs — `r = 0` and the 1e-11 tolerance; `ρ` withdrawn as never shipped

- **Y481** [UNCHANGED, type MODEL, spec line 1118]
  CLAIM: "an absolute threshold rather than a relative one; §3.13 records why that is settled rather than open"

- **Y482** [UNCHANGED, type DESIGN, spec line 1121]
  CLAIM: the option "replaces Phase 1, moves the zero-flow tolerance and removes α's clamp"; still recorded in §3.13, still not …

- **Y479** [UNCHANGED, type MODEL, spec line 1124]
  CLAIM: `α_Φ`, `TIE_EPS` and `TIE_EPS2` are hyperparameters whose values are developer taste, the document offers no …

- **Y339** [UNCHANGED, type DESIGN, spec line 1127]
  CLAIM: Changing any of the three hyperparameters is a design decision, and §1.6 records how the field responds …

- **Y996** [UNCHANGED, type MODEL, spec line 1132]
  CLAIM: `TIE_EPS` and `TIE_EPS2` together set the Phase-2 objective, `cost(u,v) = 1 + TIE_EPS·(w[u]+w[v])/2 + …

- **Y1026** [UNCHANGED, type DESIGN, spec line 1143]
  CLAIM: The two cost terms do different jobs and only the first means anything: the first-order term is the design …

- **Y1027** [UNCHANGED, type DESIGN, spec line 1145]
  CLAIM: The second-order term is tie-breaking and nothing else; its form is arbitrary and no reading should be …

- **Y998** [UNCHANGED, type MODEL, spec line 1148]
  CLAIM: A single cost vector does not make every solve unique, because uniqueness of an LP optimum depends on `b` as …

- **Y1028** [UNCHANGED, type MEASURED, spec line 1151]
  CLAIM: Measured on zero-reduced-cost arcs outside the support: the aggregate `b_w` goes from 40 under unit costs to …

- **Y1029** [UNCHANGED, type MEASURED, spec line 1153]
  CLAIM: Adding the second-order term takes the zero-reduced-cost arcs to 1 arc on 1 good.

- **Y1166** [UNCHANGED, type MEASURED, spec line 1157]
  CLAIM: The earlier 84 → 13 printings mixed seeds: 84 at seeds 1 and 2024, 13 at seed 606, with no single seed yielding both.

- **Y1167** [UNCHANGED, type PROCESS, spec line 1158]
  CLAIM: Those earlier figures were measured at the HiGHS default tolerance, which the shipped solver does not use.

- **Y1168** [UNCHANGED, type PROCESS, spec line 1159]
  CLAIM: They are replaced rather than repaired.

- **Y999** [UNCHANGED, type MEASURED, spec line 1161]
  CLAIM: BOTH costs make all 159 edge costs distinct (the 3-pairs-equal claim gone); the structured term leaves 11 of 29 goods …

- **Y1115** [UNCHANGED, type MODEL, spec line 1162]
  CLAIM: `\|w[u] − w[v]\|` telescopes: summed along a path it collapses to a function of the endpoints, so two routings between t …

- **Y1030** [UNCHANGED, type MODEL, spec line 1170]
  CLAIM: The solver's optimality tolerance is a correctness requirement rather than a performance knob, and HiGHS …

- **Y1053** [UNCHANGED, type MODEL, spec line 1174]
  CLAIM: `scipy.optimize.linprog`'s `method="highs"` options document that default as `1e-07`, for both the dual and …
