# Slice S14  (50 claims)


## §2.5

- **Y503** [CHANGED, type ENGINE, spec line 1322]  provenance: `eu4_rev.txt`; the three crash dumps
  CLAIM: The binary is frozen at build `835bfdf8`, so offsets found stay found for that binary, and a patch is a new binary.
  CHANGE: old: "**The binary is frozen** — offsets found stay found" → new: "frozen **at build `835bfdf8`**" with the guarantee scoped — "offsets found stay found **for that binary**, and **a patch is a new binary**"

- **Y501** [UNCHANGED, type DESIGN, spec line 1322]
  CLAIM: Attachment uses pattern scanning and function hooking, following the EU4dll precedent, which provides the …

- **Y502** [UNCHANGED, type DESIGN, spec line 1322]
  CLAIM: The mod ships a runtime-patching DLL rather than a modified executable.

- **Y504** [UNCHANGED, type DESIGN, spec line 1324]
  CLAIM: The nation-pair direction gates of §1.10 are hooked and returned true at the call site rather than by forcing …

- **Y1212** [NEW, type ENGINE, spec line 1322]  provenance: `eu4_rev.txt`; the three crash dumps
  CLAIM: The binary is at build `835bfdf8`, SCM timestamp 2024-10-03, constant across `eu4_rev.txt` and all three crash dumps.

- **Y1213** [NEW, type DESIGN, spec line 1322]  provenance: §2.3
  CLAIM: A patch is a new binary — §2.3’s re-measure-on-patch rule applied to offsets.


## §2.6

- **Y505** [UNCHANGED, type ENGINE, spec line 1328]
  CLAIM: The monthly trade tick runs in three passes: static power and modifiers; a pass from the end nodes …

- **Y506** [UNCHANGED, type MODEL, spec line 1334]
  CLAIM: Written each tick: node trade value as the sum over goods of `value_g(n)`.

- **Y507** [UNCHANGED, type MODEL, spec line 1335]
  CLAIM: Written each tick: node collectible pool as the sum over goods of `value_g(n) · collected_share(n,g)`.

- **Y508** [UNCHANGED, type MODEL, spec line 1336]
  CLAIM: Written each tick: per-link value as net realized flow summed over goods, in the installed `Φ_w` direction.

- **Y509** [UNCHANGED, type ENGINE, spec line 1337]
  CLAIM: Country trade income is derived by the engine from the written fields, unless stored.

- **Y510** [UNCHANGED, type MODEL, spec line 1339]
  CLAIM: Feeding the engine the collectible pool is sufficient for a narrower reason than it looks: `collect_pool` is …

- **Y511** [UNCHANGED, type MODEL, spec line 1339]
  CLAIM: What factors out is `powershare_C`, a country's share among collectors, and whether a country collects is a …

- **Y512** [UNCHANGED, type ENGINE, spec line 1343]
  CLAIM: There are two deadlines, not one window: display immediately after the value pass, because AI consumers read …

- **Y513** [UNCHANGED, type ENGINE, spec line 1344]
  CLAIM: Payment is bounded by the month boundary, since the treasury reconciles at the start of each month against …

- **Y514** [UNCHANGED, type MODEL, spec line 1346]
  CLAIM: Per-link values are written net, which can be negative where realized flow opposes the drawn arrow.


## §2.7

- **Y533** [CHANGED, type ENGINE, spec line 1385]  provenance: the binary’s string table
  CLAIM: Static string-table analysis already yields three named direction call sites — `DIPLO_SELLPROV_NOT_UPSTREAM`, `TREASURE_FLEET_TOOLTIP_CANT_REACH` and `TRADE_POWER_UPSTREAM` — and no colonisation refusal string exists.
  CHANGE: old: "**both nation-pair gates compare trade capitals**" → new: the two gates are separated — "the **treasure-fleet gate compares two trade capitals**; the **sell-province gate compares the buying country’s main trading port against the province being sold**". The three names and the no-colonisation-string clause are unchanged

- **Y515** [UNCHANGED, type DESIGN, spec line 1350]
  CLAIM: Items 1–10 are the v1 probe set, settled with a debugger on a vanilla install in one session, though the …

- **Y516** [UNCHANGED, type PROCESS, spec line 1353]
  CLAIM: Items 13–15 were run against 1.37.5, with results folded into §1.9, §2.4 and §3.6.

- **Y517** [UNCHANGED, type DESIGN, spec line 1354]
  CLAIM: Item 12 was dropped rather than run, because under owner-agnostic wealth the per-province production-income …

- **Y518** [UNCHANGED, type ENGINE, spec line 1359]
  CLAIM: Probe 13 settled and reversed the hedge: the engine does not tolerate a cycle — `EXCEPTION_STACK_OVERFLOW`, 1002 frames at one address, on three launches.

- **Y1173** [UNCHANGED, type PROCESS, spec line 1359]
  CLAIM: §2.4's citation note reconciles the session record's two launches with the three dumps.

- **Y519** [UNCHANGED, type ENGINE, spec line 1361]
  CLAIM: Probe 14 settled and confirmed the spec: the incoming-link entry only navigates, and clicking `Safi` in …

- **Y520** [UNCHANGED, type ENGINE, spec line 1363]
  CLAIM: Probe 15 settled and reversed the spec's caution: the tooltip's "where it already has power" is not a …

- **Y151** [UNCHANGED, type ENGINE, spec line 1366]
  CLAIM: §1.9’s upstream-propagation claim is consistent with probe 15’s reading — one observation on one node, enough to retire the cautionary case and not enough to promote the rule to a measurement.

- **Y1174** [UNCHANGED, type PROCESS, spec line 1368]
  CLAIM: §1.9 has since gained the 272-pair save census that bounds the propagation rule from the other side (probe 17).

- **Y521** [UNCHANGED, type ENGINE, spec line 1370]
  CLAIM: The §2.4 item 3 link-reversal check is done and passed: a hand-flipped link loaded with zero errors and …

- **Y522** [UNCHANGED, type ENGINE, spec line 1373]
  CLAIM: The declaration-order companion question is settled: the engine validates order and logs one error per …

- **Y523** [UNCHANGED, type DESIGN, spec line 1376]
  CLAIM: Probe 1 is pass caching: for each of the three passes independently, does flipping a link crash, produce …

- **Y524** [UNCHANGED, type DESIGN, spec line 1377]
  CLAIM: Probe 2 is pass 2's content: what imposes its ordering, given that propagation is one hop and cannot chain.

- **Y525** [UNCHANGED, type DESIGN, spec line 1378]
  CLAIM: Probe 3 is write windows: where income accumulation sits relative to the value pass, and whether writing …

- **Y526** [UNCHANGED, type DESIGN, spec line 1379]
  CLAIM: Probe 4 is negative link values: write one and observe arrow rendering and protect-trade allocation.

- **Y527** [UNCHANGED, type DESIGN, spec line 1380]
  CLAIM: Probe 5 is merchant storage: flip a link hosting a steering merchant and see whether the assignment dangles, …

- **Y528** [UNCHANGED, type DESIGN, spec line 1381]
  CLAIM: Probe 6 is caravan, twice: does the engine grant it for a merchant assigned to a link that is incoming in …

- **Y529** [UNCHANGED, type DESIGN, spec line 1382]
  CLAIM: Probe 7 is render data: is arrow render state separate from the economic link.

- **Y530** [UNCHANGED, type DESIGN, spec line 1383]
  CLAIM: Probe 8 is `TRADE_PROPAGATE_THRESHOLD` semantics: set it to 4 and check whether the raw requirement doubles.

- **Y531** [UNCHANGED, type DESIGN, spec line 1384]
  CLAIM: Probe 9 is diverted gold: does diverted colonial gold still appear in the per-province production income …

- **Y532** [UNCHANGED, type DESIGN, spec line 1385]
  CLAIM: Probe 10 is caller enumeration: disassemble and list every call site of "is X downstream of Y", classified as …

- **Y534** [UNCHANGED, type DESIGN, spec line 1386]
  CLAIM: Probe 11 is the caravan recipient: place a merchant in a coastal node steering toward an inland one, read …

- **Y535** [UNCHANGED, type ENGINE, spec line 1386]
  CLAIM: The engine tooltip and the identifier `merchant_steering_to_inland` both read as the inland node, and if that …

- **Y1040** [UNCHANGED, type DESIGN, spec line 1388]
  CLAIM: Probe 16 asks whether EU4's 1/1000 quantisation happens in the simulation or in the serialiser, to be settled …

- **Y1041** [UNCHANGED, type ENGINE, spec line 1390]
  CLAIM: If the rounding happens in the simulation the engine erases sub-milli-ducat divergence every tick, which is a …

- **Y1042** [UNCHANGED, type DESIGN, spec line 1393]
  CLAIM: Probe 16 settles what §2.1 may claim about the engine's own defence and whether the mod should round at its …

- **Y1175** [UNCHANGED, type DESIGN, spec line 1400]
  CLAIM: Probe 17 asks whether trade range gates propagated receipt.

- **Y1176** [UNCHANGED, type DESIGN, spec line 1403]
  CLAIM: Probe 17's method: observe one near pair and one far pair for the same country, and check whether receipt appears exactly where the upstream node is in trade range.

- **Y536** [UNCHANGED, type DESIGN, spec line 1411]
  CLAIM: All writes land atomically at the tick hook with the sim paused.

- **Y1214** [NEW, type ENGINE, spec line 1385]  provenance: the binary’s string table
  CLAIM: `$WHO$` in the sell-province strings is the buyer, established by its sibling keys `DIPLO_SELLPROV_ABOVE_TREASURY` ("Price is above $WHO$ treasury") and `_NOPORT_NOCORE` ("cannot be made into a core of $WHO$").

- **Y1215** [NEW, type ENGINE, spec line 1385]  provenance: the binary’s string table
  CLAIM: Those sell-province keys are all AI-acceptance reasons evaluated on the counterparty.

- **Y1216** [NEW, type DESIGN, spec line 1406]  provenance: §1.3; the wiki
  CLAIM: Probe 18 is the devastation scaling law: §1.3’s `devastation` row applies `-2 × level/100` on the wiki’s word, the one scaling in that table resting on community documentation.

- **Y1217** [NEW, type DESIGN, spec line 1408]  provenance: unsourced
  CLAIM: Probe 18’s method: read `goods_produced` in the two 1444 devastation windows — Bohemia 50, Erzgebirge and Moravia 20 — and check the penalty scales linearly through them.
