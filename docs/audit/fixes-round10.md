# Round-10 fixes — draft for negotiation, nothing applied

Two sections with two different standings. **Section A is a user-directed design change**
(2026-08-22): the routed money becomes the engine's own, read live each tick, and the trade UI is
specified around it. It is not findings-driven; negotiation verifies its factual assertions, its
internal coherence and the completeness of its edit sites — the design decision itself is the
user's and is not up for confirmation or rejection. **Section B answers `validation-round9.md`**:
1,055 CONFIRMED · 14 PARTIAL · 0 REFUTED · 8 UNTESTABLE over 1,077 claims, and its rows carry the
usual bar — CONFIRMED or REJECTED, no conditions.

The spec is frozen at `0989f4dc54d31514123eed24f0aae5c5` (2,190 lines) until this list is
negotiated to unconditional confirmation and separately preconfirmed. The 8 UNTESTABLE rows are
probe-class engine facts (Y350 the one-merchant-per-node cap; Y370–Y373 the outgoing-split rules;
Y505/Y512/Y513 the §2.6 tick probes 1–3) — the loop's floor, not its backlog. **R1 is untouched by
every row**: Section A adds a second quantity beside the wealth calc; no orientation input moves
by one byte.

## A. The routed economy is the engine's own (user-directed)

| # | fix |
|---|---|
| A1 | §0 gains the **v6.5** note: v6.5 changes the money and the display, not the map. The value the network routes is the engine's own, read live each tick, decomposed per good and routed through the per-good graphs; the trade UI shows the aggregate two-way traffic and, on selecting a good, that good's own numbers in the same widgets. Every orientation input — §1.3's wealth, §1.2's shares, `α`, `Φ_w` — is unchanged. |
| A2 | §1.8 gains an opening block, **"What the network routes."** `inject_g(n)` is the engine's own produced trade value at **node × good**: the engine's `trade_goods_size` quantity for `g` at `n` — carrying every modifier vanilla applies (buildings, trade-company investments, owner goods-produced modifiers; production efficiency and autonomy are outside vanilla's trade value by vanilla's own construction) — times the current price. The DLL reads it **live from engine memory each tick**; the reference implementation's counterpart is the save's per-node `trade_goods_size` array — 33 slots in each of the 80 node blocks, slot *k* ↔ `00_tradegoods.txt` index *k−1*, the slot map verified at Jaccard 1.000 against node good-membership, slots 0, 30 and 32 all-zero — times the save's current price — an **annual** quantity, like every value in this document (§1.3's time-basis note): the engine's own node fields are monthly twelfths of it, measured at exactly 12.00× on reconciling nodes. The granularity is already exactly what routing needs: node × good, no per-province step (a province produces one good, and the engine has summed them). **This is a different quantity from §1.3's `trade_value(p)`**, which is an orientation input (a wealth component) reading only development, the trade good and province condition. A manufactory moves `inject` and with it the money; it does not move the arrows. |
| A3 | §1.3 gains the separation sentence after its formula block: the `trade_value(p)` defined here is a component of `wealth(p)` and feeds orientation only; the money the network routes is §1.8's `inject`, the engine's own quantity. Conflating the two is named as the error the split exists to prevent. |
| A4 | §1.6's `V_g` is redefined to the injected economy: `V_g = Σ_n inject_g(n)` — the world injected value of `g`, serving display, link values and AI scoring as before (`V_g` is not an orientation input anywhere). Consequences handled in the same row: §1.5's latent-good statement (`V_g = 0`) survives trivially; the **value-weighted agreement figure is re-measured at apply time** under the new weights, computed from the save's `trade_goods_size` arrays, and scoped to this save's roll (ruler personalities are rolled at start — §1.3's Garnatah note); the unweighted 55.1% cannot move. **[instrument]** `measure6.py`'s agreement weight moves to `inject`-based `V_g` in the same pass — today it implements v6.4's `V_g` exactly, through `solver.py`'s **misnamed** `prod_income = gp · price` (goods_produced × price, identical to `build_sc`'s `V` to 5.7e-14; the name gains a correcting comment at apply, the §3.4 distinction being exactly what the misnomer blurs) — so the document's redefinition and the instrument's weight move together, with the re-measure expected at **54.9%** weighted / 55.1% unweighted. The figure sits at **two** sites, updated together — §1.6's agreement sentence and §2.8's disagreement-baseline bullet — and §2.3's "0.10 value-weighted" second-order-cost figure is checked at apply for whether its base moves. |
| A5 | §3.4 is re-scoped to the two-quantity design: the pre-modifier argument governs the **orientation shares** — where a good comes from is a property of the place, so owner choices must not move the arrows — while the routed economy deliberately carries the engine's own goods-produced modifiers, because Goal 7 makes the game's money the model's money. The refusal of production income is unchanged and now holds by construction: vanilla's own trade value excludes production efficiency and autonomy (verified two ways on the save), so `inject` inherits exactly the property §3.4 demands. **The same row rewrites §3.4's production-income paragraph** ("This is also why the aggregate uses trade value…"): it stops attributing the aggregate's weighting to `V_g` — `Φ_w` reads §1.3's wealth field and no `V_g` anywhere; the weighting that sentence described belonged to **v2.0's** aggregate and left at **v2.1**'s `Φ_w` adoption (§0, §1.5's own history note) — and its owner-independence rationale narrows to the two channels that actually stay out (efficiency, autonomy), since `V_g` now deliberately carries owner goods-produced modifiers. |
| A6 | §2.2, three fixes: **item 2's parse list is corrected** — the save carries no per-province produced quantity (58 distinct province-record keys, none of them one), so item 2's "goods produced" becomes the per-node `trade_goods_size` array, named as node-level; the solver list's DLL half gains the live injection read (joining items 4–7's live-memory implementation); and the reference-coverage sentence — at **both** its sites, §2.2's paragraph and §2.9's solver track — updates: the reference reads the save for the twenty rolled trade goods **and the node `trade_goods_size` arrays**. |
| A7 | §2.6 names the origination of `value_g(n)`: `inject_g(n)`, read live at the tick, routed per §1.8 over the per-good graphs — all in the document's annual basis — **with the ÷12 conversion at the engine-write boundary**, since the engine's node fields hold monthly twelfths (the same annual-over-twelve relation §1.3 established from the engine's own tooltips); the written fields are otherwise unchanged. |
| A8 | §2.8's income-balance row gains the redesign's expectation: origination is identical to vanilla's by construction, so **total world collected income is expected to track vanilla's** up to steering boosts, routing differences and merchant placement — and the distribution across historical great powers remains the gating metric, with the null-run caveat standing. One reference-side caveat stated in the row: the save-based reconstruction (Σ `trade_goods_size` × price ÷ 12) reproduces the engine's `local_value` exactly on 58 of 80 nodes and runs **3.4% low** in aggregate, short almost entirely in New World nodes — so the comparison is defined against the DLL's live read, and the reconstruction's known gap is recorded so nobody chases it as a model defect. |
| A9 | §2.9's memory track gains the build task: locate the engine's live produced-quantity fields — the node trade-goods arrays and/or the per-province quantities behind them (§2.5's pattern-scanning scope). |
| A10 | §1.12 is rewritten around the confirmed UI design. **Aggregate trade view**: vanilla's node panels — the map-mode box shows the node's total trade value (`total_value` in `interface/mapicons.gui`'s `trade_small_mapicon`/`trade_big_mapicon`), the node window its six fields — `incoming_value`, `local_value`, `total_value`, `outgoing_value`, `our_from_this` (the country's own take, the "collected" line) and `piracy_value`, all in `tradeinterface.gui` — now Σ_g of the per-good economy; **every incident link is shown, with the value flowing in each direction along it** (two directed figures per physical link, never one net scalar), `Φ_w` remaining the drawn installed direction. **Per-good view** (click a province): the vanilla trade-goods colouring for that good plus that good's arrow layer, and the **same widgets repopulate with that good's numbers** — node box the good's value at the node, window all six fields for the good alone (`piracy_value` as the good's share of the node's skim), each edge (one direction each, the good's DAG) the good's realized flow; a sink is a node with no outgoing arrows for the good; the node icon clears back to aggregate. **Node-window tabs**: vanilla's layout is kept — connected-node tabs across the top, incoming on the left, outgoing on the right — grouped by the active view's graph (`Φ_w` in the aggregate view, the selected good's orientation in a per-good view). **Merchant assignment happens in these tabs**: panels in **both** groups carry the assign button, because assignment is to a link **end** at this node and steers exactly the goods oriented away from this node on that link (§1.7's rule — this is where §1.7's incoming-entry assignability is delivered). Bookkeeping the rewrite carries: the companion-overlay list shrinks to the per-country effective-power-by-good aggregate — per-good value display and two-way traffic move into the main UI by **swap-on-view** (only the selected good's numbers are displayed at a time, so no thirty-field storage is needed); the **negative-link display question is settled by design** (both directions shown as positive flows, so no negative net is displayed — **both** deferral sites update to record the decision, §2.9's "decide the negative-link display policy" item and §3.9's "deferred to data" sentence, with §2.8's disagreement measurement still collected); the "only UI change" sentence updates to name the three changes (incoming-entry assignability, directional value panels, per-good repopulation of existing widgets) while its neighbour "No new art, sprites, shaders, or map-mode chrome" **survives** — the panels and numbers reuse the existing `TradeNodeLink` and map-icon widget classes; and the feasibility dependencies are named — §2.7 probe 7 (arrow render state vs economic link) and a node-window data-source hook, added hooking surface under §2.5. |
| A11 | §3.14 makes the AI's candidate enumeration explicit, because the reversed-edge question falls out of it: candidates are **(node, incident-link-end) pairs** — both tab groups, not `Φ_w`-outgoing links — and a candidate's active good set is the goods oriented away from that node on that link, which is the solver's own output; the survival table scores it, and a candidate steering nothing scores zero and is never chosen. One sentence, stating that the AI reads the per-good orientations directly and never infers from the drawn map. |

## B. The 14 round-9 PARTIALs

| # | ID | fix |
|---|---|---|
| B1 | Y1144, Y137 | Both "one shipped sweep script" sites — §0's v6.0 note and §2.3's coefficient parenthetical — corrected with the criterion named: **three** of v4.0's scripts sweep both modifier directories (`audit_modifiers.py`, `audit_alpha.py`, `audit_bands.py`); further scripts read the same files without sweeping them (`audit_seventh.py` loads one named file; `validate_v4.py` walks all of `common/`), so the count holds under the directory-sweep criterion and no looser one. The walked-past-`GP_COEFF` half of §2.3's sentence is untouched (confirmed exactly). |
| B2 | Y1195 | §1.1's `props6.py` parenthetical: the added-line count is dropped rather than updated — it was 27, round 9's own comment edit made it 29, which is the enumerated-figure lesson again. The applied text keeps "zero deletions" (stable and load-bearing) and says the added block carries the permutation loop, `diff`-checkable, with no line count quoted. |
| B3 | Y1099 | §1.6's withdrawn-table aside: "The direction and the widest interval survive that difference" is false and becomes: the direction survives; **the widest interval did not** — the bisection's ×1.38–×1.95 and the grid's ×1.973–×2.456 do not even overlap — which sharpens the aside's own point: bisection rows were sampling artifacts, and only the grid's figures are maintained. |
| B4 | Y375 | The both-ends rule keeps its probe-class scoping and gains its probe: **§2.7 item 19** — observe whether transfer enters a node where the receiving side holds power at only one end (one session, node window). Two side effects carried: §2.9's memory-track count moves "fourteen (1–11 and 16–18)" → **"fifteen (1–11 and 16–19)"**, and §1.8's parenthetical points at the item. |
| B5 | Y384 | §1.9's ship-propagation bullet: the conditional "only where the country has a ship-propagation modifier" is marked at its evidence class — the modifier and its grants are file-stated (`ship_power_propagation`, static modifiers plus idea/reform/age grants); that propagation occurs **only** under it is the model's reading of the modifier's own semantics, unprobed. |
| B6 | Y447 | §2.2's cost sentence: "an O(V+E) sweep" → "an **O((V+E) log V)** priority sweep" — the shipped `sweep_priority` is a heap-driven Kahn queue. |
| B7 | Y1115 | §2.3's telescoping sentence scoped to where it is true: `\|w[u] − w[v]\|` telescopes on **monotone stretches** — there it collapses to the endpoint difference — so routings traversing similar wealth profiles can still total the same and the term cancels where ties need breaking; the measured consequence (11 of 29 goods keep an alternative optimum against the shipped term's 1) is unchanged and carries the argument. |
| B8 | Y483 | §2.3's DLC axis states its file evidence precisely: shipped content does carry a DLC-gated **modifier** of caravan power — `common/disasters/decline_of_mali.txt` gates a `caravan_power = -0.33` disaster on `has_dlc = "Origins"` — but no shipped file gates the caravan **grant mechanic** or treasure-fleet **diversion** themselves on a DLC flag: that conditionality is engine-side, the flag must be read via the engine, and a `dlc_load.json` toggle run is the probe-class confirmation; "caravan modifier values are readable even when inert" stays (confirmed). |
| B9 | Y486 | §2.4's reversed-order paragraph aligned with §2.8 and the session record: `retention` unchanged on 80 of 80 and `total` on 78 of 79, the exception (`zambezi`) drifting 0.012%, within the engine's own run-to-run variance. The two sections stop disagreeing. |
| B10 | Y591 | §3.2's `s − c` figures re-measured: **16.7%** of world demand unweighted (per-good mean), **7.6%** value-weighted (weighted by the model's trade value — the base is named, since v6.5 introduces a second value quantity), with the weight base **pinned in the applied text**: the orientation model's per-good trade value (`price(g)·Σ_m goods_produced(m,g)`, `solver.build_sc`) — v6.4's `V_g`, **not** §1.8's `inject` — so A4's redefinition cannot silently re-base it. Instrument: `scripts/r9/lead/L_rank2.py` (companion `L_rank.py`, shipped `scripts/rankop.py`). |
| B11 | Y652 | §3.6's dump sentence carries what the dumps can show: a stack overflow under 1002 recorded identical `eu4.exe` frames — **consistent with** same-module recursion, not proof of it, the dumps carrying no per-frame addresses. The acyclicity requirement and the three-launch record are unchanged. |
| B12 | Y683 | §3.9's gloss on vanilla's authored ends: "the biggest cities and richest areas" is cut to what the field shows — two of the three authored ends sit in the top node-wealth ranks and `venice` (rank 21 of 80, 180.6) is the historical pick rather than the wealth pick, which is itself the point: authored ends encode intent, not measurement. |
| B13 | Y790 | §3.16 item 1's "the 75% overseas autonomy floor is pre-Common-Sense" gains the primary source the install itself ships: `patchnotes/1.8 Patchnotes.txt` L40 — "Overseas provinces now have a minimum autonomy of 75% instead of the 'distant overseas' penalty" — against Common Sense at 1.12 (its own patchnotes name the expansion), so the claim is **file-verified inside the current tree** rather than carried as unverifiable history — and the same directory dates the replacement: 1.12's notes carry no overseas-floor change, and `1.16 Patchnotes.txt` L42 introduces States & Territories, so the applied text reads "introduced at 1.8, superseded by States & Territories at 1.16" rather than leaving "pre-Common-Sense" to imply Common Sense removed it. |

## What negotiation must settle

- Section A: verify every factual assertion (the save's `goods_produced` field and its parse, vanilla
  trade value excluding efficiency/autonomy, `V_g`'s consumer inventory, the widget claims against
  `tradeinterface.gui`, the figure impacts) and that the edit-site list is complete and coherent;
  the design itself is the user's and is not for confirmation or rejection.
- Section B: every row CONFIRMED or REJECTED, no conditions; anything conditional comes back.
- B2 drops the count, which you held is right; B10 carries your instrument path and figures with
  the weight base pinned to v6.4's `V_g`.
- Section A carries your six repairs: the node × good `trade_goods_size` source with its verified
  slot map (A2/A4/A6), §2.2 item 2's matching correction and §2.9's second twenty-rolled site
  (A6), §3.4's production-income paragraph (A5), `measure6.py`'s weight (A4 [instrument]), the
  reference reconstruction's 3.4% gap (A8), and the `mapicons.gui` citation plus the six-field
  window and the surviving no-new-art sentence (A10).
