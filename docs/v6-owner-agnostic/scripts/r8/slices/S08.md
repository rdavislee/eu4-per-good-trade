# Slice S08  (58 claims)


## §1.6

- **Y338** [UNCHANGED, type MEASURED, spec line 608]
  CLAIM: `Φ_w`'s marking order is a per-node scalar whose descending comparison reproduces the DAG (0 violations), so …

- **Y097** [UNCHANGED, type MEASURED, spec line 611]
  CLAIM: Per good on the same field: 2–8 sinks, mean 3.69, 29/29 acyclic, 0 fallbacks fired, and 90.6% of ordered node …

- **Y098** [UNCHANGED, type MEASURED, spec line 614]
  CLAIM: Agreement with the per-good graphs is 55.1% of edge-goods and 54.8% value-weighted.

- **Y1094** [UNCHANGED, type DESIGN, spec line 614]
  CLAIM: The agreement figure is a description of how often one power map coincides with twenty-nine commodity maps, not a qual …

- **Y100** [UNCHANGED, type DESIGN, spec line 619]
  CLAIM: `α_Φ = 2.0`, `TIE_EPS = 1e-3` and `TIE_EPS2 = 1e-6` are hyperparameters; the choice is developer taste and …

- **Y102** [UNCHANGED, type DESIGN, spec line 621]
  CLAIM: No derivation is claimed, none is implied, and none should be reconstructed from the figures below: they …

- **Y103** [UNCHANGED, type MEASURED, spec line 625]
  CLAIM: Across `α_Φ` = 1.00…8.00 at 0.01 the sink set is a step function, and `α_Φ = 2.0` sits in the band [1.63, …

- **Y104** [UNCHANGED, type MEASURED, spec line 627]
  CLAIM: Sampled at six values the sink count is non-monotone: 3 → 1 → 2 → 2 → 1 → 1 across `α_Φ` in {1, 1.5, 2, 3, 4, …

- **Y992** [UNCHANGED, type MEASURED, spec line 628]
  CLAIM: The sink set is unchanged at every `TIE_EPS` value tried from 1e-13 to 1e+12 — 24 grid points.

- **Y1151** [UNCHANGED, type MEASURED, spec line 629]
  CLAIM: `epsilon6.py` validates by reproducing the shipped map 159/159 at eps = `TIE_EPS`.

- **Y1152** [UNCHANGED, type MODEL, spec line 630]
  CLAIM: For the sinks, `TIE_EPS` is a switch with nothing to tune anywhere in reach.

- **Y1153** [UNCHANGED, type MEASURED, spec line 631]
  CLAIM: What moves at the `TIE_EPS` extremes is edge orientation: up to 16 edges below 1e-4 and up to 25 above 3.

- **Y1154** [UNCHANGED, type PROCESS, spec line 632]
  CLAIM: An earlier printing bounded the `TIE_EPS` band at about 1e-6 and about 1 and supplied each edge with a mechanism; both edges are withdrawn, because no sink transition exists at either.

- **Y1010** [UNCHANGED, type MEASURED, spec line 635]
  CLAIM: `TIE_EPS2` behaves the same way as `TIE_EPS` and was measured at 1e-7, 1e-6 and 1e-5, all three leaving the …

- **Y105** [UNCHANGED, type DESIGN, spec line 639]
  CLAIM: A written warning against reintroducing the withdrawn justifications for `α_Φ` — resemblance to vanilla's …

- **Y106** [UNCHANGED, type DESIGN, spec line 644]
  CLAIM: "Europe becomes the centre of trade as it develops" is the design claim, and it is what §3.1's first goal …

- **Y107** [UNCHANGED, type MEASURED, spec line 645]
  CLAIM: At 1444 the map ends in Genoa and in Hangzhou, and as European development compounds Europe gains ends and …

- **Y108** [UNCHANGED, type MODEL, spec line 646]
  CLAIM: The mechanism carrying that is that wealth is linear in development, so developing a region moves its `c_w` …

- **Y109** [UNCHANGED, type MEASURED, spec line 650]
  CLAIM: the table is withdrawn ("A table of interval boundaries was published here and is withdrawn"); the sweep is re-reporte …

- **Y1095** [UNCHANGED, type MEASURED, spec line 651]
  CLAIM: The European sweep samples uniformly on a 0.001 grid from ×1.000 to ×2.600.

- **Y110** [UNCHANGED, type DESIGN, spec line 653]
  CLAIM: The table is to be read as a direction rather than a trajectory: it scales all 824 counted European provinces by one f …

- **Y1096** [UNCHANGED, type MEASURED, spec line 654]
  CLAIM: The widest interval carrying three European ends and none in Asia runs ×1.973 to ×2.456, with `english_channel`, `genu …

- **Y111** [UNCHANGED, type MEASURED, spec line 657]
  CLAIM: The European-scaling path is not monotone: `hangzhou` leaves, returns and leaves again, `gulf_of_siam` holds an end over one stretch and nowhere else, and of the three intervals narrower than ×0.01, one — ×1.702–×1.709 — carries a set that appears nowhere else in the sweep.

- **Y1097** [UNCHANGED, type PROCESS, spec line 661]
  CLAIM: A table of interval boundaries was published here and is withdrawn: its rows came from bisection and disagree with a u …

- **Y1098** [UNCHANGED, type MODEL, spec line 662]
  CLAIM: The effect of a boundary that sits between samples is a row that looks like a fact and is an artifact of the sampling.

- **Y1099** [UNCHANGED, type PROCESS, spec line 663]
  CLAIM: The direction and the widest interval survive the bisection-versus-grid difference; the row boundaries did not, and qu …

- **Y340** [UNCHANGED, type DESIGN, spec line 672]
  CLAIM: These are properties of this snapshot rather than constants of the model — what one field yielded under one scaling, w …

- **Y112** [UNCHANGED, type MEASURED, spec line 673]
  CLAIM: Because §1.3's wealth is linear in development, scaling development and scaling wealth are the same operation here — m …

- **Y341** [UNCHANGED, type ENGINE, spec line 677]
  CLAIM: All three institutions the period is named for begin in Europe between 1450 and 1550: Renaissance `1450.1.1` …

- **Y342** [UNCHANGED, type ENGINE, spec line 680]
  CLAIM: The Renaissance's embracement bonus is `development_cost = -0.05`, a standing discount on every subsequent …

- **Y343** [UNCHANGED, type MODEL, spec line 681]
  CLAIM: Those institution bonuses are country-scoped, so §1.3 excludes them from wealth directly; they reach the map …

- **Y344** [UNCHANGED, type MEASURED, spec line 685]
  CLAIM: "draws A RECOGNISABLE pre-Columbian trade geography" — the definite claim and "unprompted" both softened

- **Y345** [UNCHANGED, type MEASURED, spec line 686]
  CLAIM: From the north the route to the Asian end is the Volga and the steppe: `white_sea → novgorod → kazan → …

- **Y113** [UNCHANGED, type MODEL, spec line 688]
  CLAIM: From Iberia the route is the African coast and the Red Sea: `sevilla → safi → timbuktu → katsina → ethiopia → …

- **Y114** [UNCHANGED, type MEASURED, spec line 690]
  CLAIM: No route leaves `genua` at all — it is a sink, out-degree 0 against in-degree 5, so the western …

- **Y994** [UNCHANGED, type MODEL, spec line 691]
  CLAIM: `english_channel` is not an end at this α: it drains to `genua` in two hops through `champagne`, and reaches …

- **Y115** [UNCHANGED, type MEASURED, spec line 695]
  CLAIM: No Europe→sink route passes the Cape of Good Hope, checked exhaustively rather than sampled: of the 23 …

- **Y346** [UNCHANGED, type DESIGN, spec line 696]
  CLAIM: "asserted as a universal because the whole set was enumerated rather than sampled — which is the only ground on which …

- **Y116** [UNCHANGED, type MEASURED, spec line 701]
  CLAIM: The Cape is a live conduit rather than an idle one: in-degree 2, out-degree 2, with 81 ordered node pairs for …

- **Y117** [UNCHANGED, type MEASURED, spec line 705]
  CLAIM: The Cape’s 81 is a count of pairs `(a, b)` where `a` reaches the Cape, the Cape reaches `b`, and `a` reaches `b` — not pairs where some shortest path transits it, which gives 71.

- **Y1155** [UNCHANGED, type MEASURED, spec line 706]
  CLAIM: Requiring *every* shortest path to transit the Cape gives 60 pairs on the same field.

- **Y1156** [UNCHANGED, type MEASURED, spec line 707]
  CLAIM: Requiring a *unique* shortest path through the Cape gives 43 pairs on the same field.

- **Y118** [UNCHANGED, type MODEL, spec line 708]
  CLAIM: In the per-good graphs the Cape also carries Asian spices to Europe; `Φ_w` models power, not cargo.

- **Y119** [UNCHANGED, type MEASURED, spec line 712]
  CLAIM: sole sink from ×1.52, continuous to the top of the swept range; all-22 gives no sole sink below ×25, the set settling …

- **Y1100** [UNCHANGED, type MEASURED, spec line 714]
  CLAIM: Swept from ×2.50 to ×25.00 on the all-22 scaling, none of the eastern four holds an end at any multiple, and both surv …

- **Y1101** [UNCHANGED, type PROCESS, spec line 716]
  CLAIM: The earlier draft's clause attributing the no-sole-sink behaviour to "the eastern four pulling ends of their own" was …

- **Y120** [UNCHANGED, type MEASURED, spec line 717]
  CLAIM: The Cape reverses under the same growth — 1444's `ivory_coast`/`zanzibar`→Cape→`comorin_cape`/`malacca` …

- **Y347** [UNCHANGED, type MODEL, spec line 722]
  CLAIM: The 22 European nodes are the 18 western and central ones (`english_channel`, `north_sea`, `baltic_sea`, …

- **Y121** [UNCHANGED, type MODEL, spec line 726]
  CLAIM: Dev-stacking a single node's top province concentrates the map on that node, and extra sinks at intermediate …

- **Y1102** [UNCHANGED, type MODEL, spec line 729]
  CLAIM: This section's figures are measured under the shipped sweep key (DEF ascending, β ascending, index), which is a design …

- **Y1103** [UNCHANGED, type MEASURED, spec line 731]
  CLAIM: Measured against DEF-descending on the same field: of the 19 aggregate-graph facts `round6.py` checks, 6 move and 13 d …

- **Y1104** [UNCHANGED, type MEASURED, spec line 732]
  CLAIM: The thirteen that hold: the sink set and its count, the promotion count, the fallback count, acyclicity, the number of …

- **Y1105** [UNCHANGED, type MEASURED, spec line 735]
  CLAIM: The six that move: the source count (5 against 10), the sources' `c_w` rank range, their mean degree, the Cape's order …

- **Y1106** [UNCHANGED, type MEASURED, spec line 737]
  CLAIM: Under the descending key `sevilla` reaches neither `ganges_delta` nor the Asian end, so the Iberian route ceases to ex …

- **Y1107** [UNCHANGED, type MEASURED, spec line 738]
  CLAIM: The northern route's endpoints stay connected under both keys — the check is that `white_sea` still reaches `hangzhou` …

- **Y1108** [UNCHANGED, type MODEL, spec line 741]
  CLAIM: The two long routes are properties of this field and this key; the sink set is a property of the field alone.

- **Y348** [UNCHANGED, type MODEL, spec line 745]
  CLAIM: The v1 diagnostic identity (`Φ ≡ φ₀` at α = 1) does not survive the operator change, because DRAIN performs …

- **Y349** [UNCHANGED, type DESIGN, spec line 746]
  CLAIM: Its replacement as the end-to-end correctness check is exact orientation equality between the reference and …
