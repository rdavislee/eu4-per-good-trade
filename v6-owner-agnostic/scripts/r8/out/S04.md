## §1.1

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y023 | Where the wealth key ties, the node index decides | CONFIRMED | file read (drain.py) | `drain.py:174,245`: `s_star = max(gated, key=lambda v: (NODEW[v], -v))` — ties on `NODEW[v]` are broken by `-v`, i.e. the node index. |
| Y024 | §2.8's containment set includes fallbacks because of T3, not the wealth tie | CONFIRMED | file read | Spec line 179-181 matches §2.8's "Sink set, 2-core" row (line 1440): "Asserting containment in `{selected} ∪ {promoted}` alone would halt on **T3** (§3.2), which is correct behaviour, so the fallback set is part of the assertion." |
| Y246 | Free edges orient from later-marked to earlier-marked | CONFIRMED | file read (drain.py) | Spec line 182-183; `drain.py:269-270` `compile_dirs`: `directed.append((u, v) if order[u] > order[v] else (v, u))` — higher `order` = later marked, so the arc runs later→earlier. |
| Y247 | Phase 4 un-peels the Phase-0 pendants in reverse order | CONFIRMED | file read (drain.py) | Spec line 185; `drain.py:271` `compile_dirs`: `for (v, u, bv) in reversed(Plog): ...` — `Plog` is the peel log, iterated in reverse. |
| Y1078 | Phase 4 emits the orientations Phase 0 determined for the pendants | CONFIRMED | file read (drain.py) | Spec line 185-187; `drain.py:271-272`: direction is `(v,u) if bv>=0 else (u,v)` using `bv`, the balance **captured during Phase 0's peel**, so Phase 0 decided it and Phase 4 (`compile_dirs`, run after the sweep) only emits it. |
| Y1079 | A pendant sink is visible only after Phase 4 (T1, §3.2) | CONFIRMED | file read (drain.py) | Spec line 186-187; pendant nodes are removed from `core` in `phase0` and never enter the Phase-3 `sweep`; their edges are appended only inside `compile_dirs`, which `run_drain` calls after the sweep — consistent with T1 in §3.2 (line 1544-1546). |
| Y248 | Each §1.1 property is labelled proved, measured, or true-by-construction, never interchangeably | CONFIRMED | file read | Spec lines 189-194, cross-checked against §2.2a's table (line 1059-1064: "Global DAG — proved", "Sink-set equality — measured exact 29/29", "Marking order — proved", "Free-edge determinism — proved... measured...") and §1.1's Efficiency bullet ("carries no measurement and wants none... follows from the construction", line 243-244). |
| Y249 | That labelling discipline caught four over-claims between v2.0 and v3.0 | CONFIRMED | file read (diff v2-drain vs v3-owner-agnostic spec) | `v2-drain/per-good-trade-spec.md` §1.1 properties list vs `v3-owner-agnostic/per-good-trade-spec.md`: four corrections in that one block — (1) sink placement "final sinks are exactly {...}" → "not a theorem... v2 asserted it as one"; (2) reachability "a feasibility theorem, not an aspiration" (unconditioned) → "on a connected map" with the premise stated; (3) efficiency "make the certificate flow a fewest-hop routing" (flat) → "in aggregate... no per-unit shortest-path claim is made, and none holds"; (4) flow-support "acyclic by theorem" (unconditioned) → "when the solver returns a basic (vertex) optimum... an interior-point solve... can return a support with an undirected cycle in it." |
| Y251 | Global DAG: every arc points later-marked→earlier-marked, reversed marking order is topological; pendant edges are bridges | CONFIRMED | derivation check + file read | Spec line 196-198; verified against `drain.py`'s sweep (a node is ready only once all its flow-out-neighbours are marked, so flow arcs also run later→earlier) and Phase 0's peel (degree-1 removal is a bridge by definition). |
| Y252 | Measured acyclic on 29 of 29 goods | CONFIRMED | measurement (props6.out) | `scripts/r8/props6.out`: "acyclic goods: 29 of 29". |
| Y253 | Every sink is one of four kinds: selected&flow-terminal, stall-promoted, fallback-promoted, or Phase-0 pendant | CONFIRMED | file read + derivation check | Spec line 199-201, verbatim; exhaustive by construction (every node is either peeled in Phase 0 or processed by the Phase-3 sweep, which only marks nodes ready/promoted/fallback-promoted). |
| Y025 | On 1444 the fallback and pendant cases are empty and sink set = {selected∩flow-terminal} ∪ {promoted} | CONFIRMED | measurement (props6.out, measure6.out) | `props6.out`: "fallbacks fired: 0", "sink-set identity holds on 29 of 29 goods; failures []", "min degree 2 ... bridges=0" (Phase 0 no-op ⇒ pendant case empty). |
| Y026 | That equality is a measurement on this input, not a theorem, and v2 asserted it as one | CONFIRMED | file read (v2-drain spec) | `v2-drain/per-good-trade-spec.md` line 61-63 states the sink set flatly with no theorem/measurement hedge ("the final sinks are exactly `{...}`"); current spec line 203-205 adds the correction. |
| Y027 | "Phase 0 no-op and no fallback firing" is necessary but not sufficient — T2 satisfies both and still breaks the equality | CONFIRMED | engine test (toys.py, run fresh) | `python toys.py` T2 (5-cycle + chord, all core degrees ≥2, no pendants; output shows `promoted=[]`, no fallbacks): actual sinks `{u2}` vs formula set `{u1,u2}` — both conditions hold and the equality still fails. |
| Y254 | Three constructed cases (T1, T2, T3) break the sink-set equality | CONFIRMED | engine test (toys.py, run fresh) | `python toys.py`: T1 actual `{C}` vs formula `{B}`; T2 actual `{u2}` vs formula `{u1,u2}`; T3 actual `{A}` vs formula `{}` (fallback fires, promotes A) — matches spec line 206-210 exactly. |
| Y255 | A node with no outgoing links for `g` is a sink for `g`; sinks differ per good; no global end node | CONFIRMED | file read + measurement | Spec line 209-210, verbatim (definitional); "sinks differ per good" independently confirmed by `val62p1_a.py` (fresh run): "distinct per-good sink sets: 27 of 29 goods; nodes that are sinks for ALL goods: []". |
| Y256 | Orientation serves 100% of every good's demand because the LP imposes node balance and Σ_n b_g(n)=0 identically | CONFIRMED | derivation check | Spec line 211-213, verbatim; LP feasibility argument checked step by step (both `s`,`c` are world shares so they each sum to 1, hence `Σb=0`). |
| Y257 | The LP-feasibility premise is connectedness: on a disconnected map balance must hold per component and share normalisation doesn't deliver it | CONFIRMED | derivation check | Spec line 213-216, cross-checked against §2.2a Premise 1 (line 1041-1045), same argument stated in full there. |
| Y258 | §2.2 states the connectedness requirement and what the solver does when it is violated | REFUTED | file read | Spec line 216 in the **current** document reads "**§2.2a** states the connectedness requirement..." (verified `sed -n '216p' per-good-trade-spec.md`), not §2.2. §2.2 (lines 988-1035) contains no mention of connectedness/components at all (grepped); only §2.2a (line 1041-1045, "Premise 1 — the node graph is connected") does. |
| Y259 | Measured on 1444 (one component): 100.0% of demand reachable, 29/29 goods, zero orphan sinks | CONFIRMED | measurement (props6.out) | `props6.out`: "components=1", "demand reachable: min 100.0000% max 100.0000% ; orphan sinks 0". |
| Y260 | Ready-marking is a monotone closure, so the stall sequence and both promotion branches are provably independent of scheduling | CONFIRMED | derivation check | Spec line 219-220, verbatim; matches §2.2a's characterisation that scheduling-independence of the promotion branches is proved (closure argument), distinct from the index-tiebreak half which is only measured. |
| Y261 | Free-edge direction is deterministic by the same closure argument plus the priority key's index tiebreak | CONFIRMED | derivation check | Spec line 219-221, verbatim; consistent with `drain.py`'s `sweep_priority` keying `(DEF, beta, index)`. |
| Y262 | Zero orientation changes over 145 scheduler permutations (29 goods × 5), cited to props6.py | CONFIRMED | measurement (props6.out) | `props6.out`: "scheduler permutations: 0 orientation changes over 145 trials (29 goods x 5)". |
| Y1005 | Zero exact (DEF, b) key collisions across all 2,320 core nodes of the 29 per-good solves | CONFIRMED | measurement (props6.out) | `props6.out`: "core nodes summed over goods: 2320 ; exact (DEF,b) key collisions: 0". |
| Y1006 | Phase 1's within-cluster argmin and top-k cluster cut are untied on the same field | CONFIRMED | measurement (props6.out) | `props6.out`: "Phase-1 within-cluster argmin ties: 0 ; top-k cut ties: 0". |
| Y1080 | The determinism bullet's figures are all props6.py's, core-node count included | CONFIRMED | file read | Spec line 228-230 (parenthetical): "`props6.py` for every figure here — the core-node count included — along with the permutations, the argmin and cut ties..."; matches `props6.out`'s single run producing all these figures. |
| Y263 | The certificate flow is a near-fewest-hop routing in aggregate; unit costs → objective = Σ(flow×hops) | CONFIRMED | derivation check | Spec line 233-236; arithmetic: with unit arc cost 1, `Σ(flow×cost) = Σ(flow×1)` over used arcs = total flow-hops, matching §2.3's cost formula (`cost(u,v)=1+...`, line 1136-1141). |
| Y978 | The bound is the interval [1, 1+TIE_EPS+TIE_EPS2] itself; no percentage is derived | CONFIRMED | arithmetic check | §2.3 (line 1115, 1137-1140): `TIE_EPS=1e-3`, `TIE_EPS2=1e-6`; `cost=1+TIE_EPS·(w[u]+w[v])/2+TIE_EPS2·frac(...)` with `w∈[0,1]`, `frac∈[0,1)`, giving exactly `cost∈[1, 1+1e-3+1e-6]` — matches spec line 236-238 verbatim. |
| Y264 | No per-unit shortest-path claim is made, and none holds: a unit may detour when sink assignment demands it | CONFIRMED | file read | Spec line 240-241, verbatim. |
| Y265 | The efficiency property carries no measurement and wants none: it follows from the LP's construction | CONFIRMED | file read | Spec line 243-244, verbatim ("This one carries no measurement and wants none: it follows from the construction of the LP..."). |
| Y266 | The §3.13 calibration deliberately degrades efficiency, a change to the program being solved, not evidence about the property | CONFIRMED | file read + derivation check | Spec line 244-246, verbatim; consistent with §3.13's "sink-count-span option" (line 1950-1961): its twig tolerance "re-routes arcs carrying a small fraction of a good's mass" — a deliberate departure from the pure min-cost objective. |
| Y267 | Recomputed on a fixed monthly tick, aligned to the vanilla trade tick | CONFIRMED | file read | Spec line 248; §2.6 (line 1328): "The monthly trade tick runs in three passes..." — same tick the mod attaches to. |
| Y268 | Orientation is read from the current solve every time, with no memory of the previous one | CONFIRMED | file read (drain.py, solver.py) | Spec line 248-249; grepped `drain.py`/`solver.py` for any cache/persistence mechanism — none found; each `run_drain(b)` call recomputes from scratch. |
| Y269 | The LP is deterministic on one machine/build: six identical solves gave one orientation on the reference impl | CONFIRMED | measurement (final.py, val62p1_a.py, run fresh) | `final.out`: "V037 six identical solves -> one orientation (spices): True"; fresh run of `val62p1_a.py` in scratch dir: "six identical aggregate solves -> distinct orientations: 1", "six identical spices solves -> distinct orientations: 1". |
| Y1082 | The six identical solves are six solves inside one process, blind to what varies between processes; fingerprint6.py covers that separately | CONFIRMED | file read (fingerprint6.py) | Spec line 249-251 matches §2.8's Determinism row (line 1437: "six identical solves inside one process (§1.1) test the solve, not the process...") and `fingerprint6.py`'s docstring: "Repeat under different PYTHONHASHSEED values to test what a single process cannot see -- iteration order that varies between processes rather than within one." |
| Y270 | What remains open across machines is §3.13's build-discipline question, not LP determinism, which §2.1 retires | CONFIRMED | file read | Spec line 253, verbatim; matches §3.13's "Multiplayer build discipline" bullet (line 1964-1969: "Not LP pivot determinism, which §2.1 retires... What is open is whether the shipped solver build does runtime CPU dispatch or threads its reductions"). |
| Y1195 | `val5_pergood.py` is still on disk; diff shows `props6.py` is that file verbatim (zero deletions) plus 27 added lines | CONFIRMED | measurement (diff, run fresh) | `diff val5_pergood.py props6.py` in scratch copy: 0 removed lines (`^<` count 0), 27 added lines (`^>` count 27), all forming the scheduler-permutation loop. |
