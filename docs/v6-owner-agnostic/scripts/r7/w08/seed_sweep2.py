import sys, os, numpy as np
sys.path.insert(0, ".")
import p3_relabel_pergood as M

# reuse M's build(), B, GL, IDENT, trial from p3_relabel_pergood module (import side-effect already ran the whole script though!)
