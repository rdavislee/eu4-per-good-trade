# Round-7 fixes — draft for negotiation, nothing applied

Answering `validation-v62.md`: **873 CONFIRMED · 41 PARTIAL · 0 REFUTED · 45 UNTESTABLE** over 959
claims (the tally after the validator's own Y353 correction, verified by machine count of its 959
graded rows). The spec is frozen at `5ec633ef46e7fd68bb118e10e83692eb`, **2,079 lines**, and stays
frozen until this list is negotiated to
unconditional confirmation and separately preconfirmed. Every figure below is written from the
validation's evidence cells or from my own re-checks, and will be re-measured at write time.

Zero REFUTED means every row here is a precision, scope, attribution or instrument repair — no fact
about the world changes. R1 (the wealth calc) is untouched by every row. Rows marked **[instrument]**
change a script; the rest change prose.

## A. Wrong attributions and citations

| # | ID | fix |
|---|---|---|
| A1 | Y1080 | §1.1's citation: the 2,320 core-node count is computed by `props6.py`, not `measure6.py`. The parenthetical cites `props6.py` for **every figure it covers, the core-node count included** — no numeral is asserted for how many that is — and `measure6.py` leaves the parenthetical. |
| A2 | Y702 | §3.10's retraction: "v1 through v4.0 quoted" → **"v1 through v3.0"**; add that v4.0 quoted its own **1.3e-16** on a construction it **does** state (`gulf_of_siam`, 13 goods carrying local value, per-good eligibility, the off-home penalty on two of three collectors — v4 L1074). |
| A3 | Y487 | §2.4's cycle-crash provenance: the three dumps in `crashes/` (2026-08-20, identical address, 1002 frames, cycle mod loaded) become the citation for "three launches"; `game-session.md` stays cited for the session but is noted as recording the first **two**. The count is artifact-true; only the citation moves. |
| A4 | Y137 | §2.3: "v3.0 through v5.0 … shipped a whole-install modifier sweep" → v4.0's tree ships the sweep (`audit_modifiers.py`); v3.0 and v5.0 repeated the claim without shipping it. |
| A5 | Y109 **[instrument]** | `europe.py` is updated to the documented experiment (α_Φ = 2.0, 0.001 grid, ×1.000–×2.600) so the §1.6 citation is producible. Acceptance: the repaired instrument's own output must reproduce **every §1.6 figure cited to it** — ×1.973–×2.456; the three sub-0.01 intervals with ×1.702–×1.709 as the sole one-off (B7's corrected form); the hangzhou episode boundaries 1.000–1.368 / 1.948–1.972 and gulf_of_siam 1.187–1.381 where retained; and "settles back at two" at the top of the range — before the citation stands. |
| A6 | Y412 **[instrument]** | `round6.py:232`'s family regex gains the `all_` variant its own §1.10 text lists: `(?:any|random|every|all)_trade_node_member_province`. Counts re-measured; the document quotes "several hundred uses", so no prose figure moves. |
| A7 | Y353 | §1.7's collect line cites **`TRADE_NON_CAPITAL_OFFICE = -0.50`** (`common/defines.lua:1200`) — the define §2.3's table already names. *The validation's "no located file source" is corrected by this row: the constant exists at that line.* |

## B. Figures replaced by what the shipped instruments print

| # | ID | fix |
|---|---|---|
| B1 | Y991, Y1007, Y143 | The relabelling passages at all three sites (§1.6, §2.3, §2.4 item 1) are rewritten to quote the **three measured configs as a triple** — first-order term with `LP_OPTS` pinned: **76 of 290**; full cost at the default tolerance: **12 of 290**; full cost with `LP_OPTS` pinned: **0 of 290** (`p3_relabel_pergood.py`) — and to draw only the **isolating comparisons** the instrument supports: the second-order term takes 76 → 0 *with the tolerance pinned in both*, and pinning the tolerance takes 12 → 0 *with the full cost in both*. The old figures (84 → 13) go for the reason the preconfirmation established by reproducing them: they come from `val62p3_pergood.py` at the **HiGHS default tolerance** — a tolerance the shipped solver does not use — and they **mix seeds** (84 at seeds 1 and 2024; 13 at seed 606; no single seed yields both). The jump they describe is single-variable (second-order term added, default tolerance in both), so the defect is provenance, not structure: seed-mixed figures at a non-shipped tolerance, replaced by the shipped-instrument triple above. The 18 → 1 goods-with-alternative-optimum halves stay (exact). |
| B2 | Y1032 | §2.3's sentence is recast in the instrument's own unit — **orientation flips summed over the six permutations**: `copper` **4** (on 4 distinct slots), `paper` **8** (on 2 distinct slots, flipped in four of six permutations) — replacing "differing on 12 and 8 edge-slots", which is wrong for copper's total and wrong for paper's slot count. The 7.7e-10 relative objective spread stays (exact). |
| B3 | Y1055 | §2.3's bisection sentence rescoped: unset ≡ 1e-7 **exactly, at every seed tried** (the load-bearing identity); the flip count over four permutations is seed-dependent (2/8/6), so no single count is quoted; "1e-8 gives 0" stays (all seeds). |
| B4 | Y1035 | §2.3: the second-order term's self-coherence cost "0.1–0.2 points" → **"at most a tenth of a point (0.04 edge-goods, 0.10 value-weighted)"**. Everything else in that sentence stays (exact). Measurement note that must survive into any citation: the figures reproduce only by patching **`drain.TIE_COST`** — `drain.py:18` rebinds the name at import, so patching `flowop.TIE_COST` is a no-op that silently prints the shipped figures for both configs; `scripts/val62p4_selfcoh.py` has exactly that defect and is **not** cited anywhere. |
| B5 | Y992 **[instrument]** | §1.6's TIE_EPS sensitivity rewritten to the measurement: the **sink set is unchanged at every value tried from 1e-13 to 1e+12** (24 grid points, shipped path); what moves at the extremes is edge orientation (up to 16 edges below 1e-4, up to 25 above 3). The claimed band edges (tolerance floor below, base-cost ceiling above) are withdrawn — no sink transition exists at either. `epsilon6.py` is repaired so it can actually meet the acceptance test, which its current shape cannot: the repair (i) fixes the `mcf(cost=…)` TypeError and the α = 1.5 docstring pin, (ii) **replaces its grid** (34 points over 1e-12…1e+4 today) with the 24-point 1e-13…1e+12 grid the figures come from, (iii) **adds the `TIE_EPS2` second-order term** to its patched cost so it runs the shipped cost shape rather than the first-order-only one, and (iv) rewrites its validation branch to compare against `drain.run_drain`'s orientation directly at **eps = TIE_EPS = 1e-3**, the one point where the swept cost equals the shipped cost — today the branch restores the shipped `TIE_COST` and validates nothing, and an eps = 0 match is unsatisfiable by construction (the shipped map carries TIE_EPS = 1e-3; measured, eps = 0 agrees on 143 of 159, the sweep's own low-end drift). Acceptance: the repaired instrument reproduces the 24-point sink-set result and the edge-drift maxima (16 below 1e-4, 25 above 3), and its eps = 1e-3 point matches the shipped map **159/159**. If the repair is not taken, the citation moves to the instrument that measured this. |
| B6 | Y117 | §1.6's Cape stricter-reading figure: "69" is replaced by a **named construction and its measured value** — pairs where *some shortest path* transits the Cape: **71**. (Every-shortest-path gives 60, unique-shortest 43; no construction yields 69.) |
| B7 | Y111 | §1.6: "several intervals … carry sets that appear once" → **three** intervals narrower than ×0.01, **one** of which (×1.702–×1.709) carries a set appearing nowhere else. |
| B8 | Y591 | §3.2: "one sixth of world demand" → **17.1% of demand (unweighted per-good mean; 7.7% value-weighted)** under the tested s−c operator. Genoa-as-unreachable-cloves-sink stays (exact). |
| B9 | Y015 | §0's tokeniser aside: the unreproduced endpoints "279 to 326" go, and **no replacement endpoints are quoted** — across the 84 tokenisation variants tried in preconfirmation the distinct counts spanned **48–359** with no privileged pair (the whole-document family alone spans 159–359), and neither 279 nor 326 appears in any — which is the ill-definedness the sentence exists to state. The sentence keeps only the point: the count varies by **hundreds of tokens** with the tokenisation, so no denominator is well-defined. |

## C. Universals and scope

| # | ID | fix |
|---|---|---|
| C1 | Y214 | §0's lineage sentence states the process record and the verification actually performed, asserting neither more nor less: the corrections settleable from files **were folded in at v2 and the sentence has been carried unchanged since** (byte-identical at line 10 of the v2 through v6 specs; `changes-v6.md` shows v6.0 did not touch it); **spot-verified on 7 of 60 in this round's audit, none missing; no exhaustive re-check has been made**. |
| C2 | Y217 | §0: "Measured figures carry the script that produced them" → carry the script **where one is named**, with the coverage paragraph recording the exceptions in the direction it actually states them — **some figures carry a script attribution instead of a guard, and some carry neither** (spec L93–94, Y017) — so guard and attribution stay distinct and the concession is quoted, not inverted. |
| C3 | Y270 | §1.1's closing: "it is the open question of §3.13" → what remains across machines is **§3.13's build-discipline question** (§2.1) — not LP determinism, which §2.1 retires. |
| C4 | Y516 | §2.7: "Items 12–15 are done. They were run…" → **"Items 13–15 were run** against 1.37.5 …; **item 12 was dropped rather than run** (below)." Same facts, no overstatement. |
| C5 | Y732 | §3.13: the three-cheap-probes sentence → of the three, the cyclic file **changed** what the spec says, the incoming-link button **confirmed** it, and the caravan recipient is **pending** — "change what this spec says" no longer covers all three. |
| C6 | Y652, Y653 | §3.6: "walks the node graph recursively and a cycle never terminates" → the dumps show an **unbounded same-module recursion on the tested two-node cycle**; "provably cannot survive its absence" → **demonstrably did not survive the cycle class tested — established by observation (§2.4), not proof over all cyclic inputs**. |
| C7 | Y653-adjacent | §2.4 already says "established by observation"; §3.6 aligns to it (covered by C6; listed so the site count is honest). |
| C8 | Y537 | §2.8's spice/cloves row: "Source in Indonesia, and both source there alone" → graph-sources stated per good — `cloves` from `the_moluccas` **alone** (Indonesian); `spices` from `the_moluccas` **and `kongo`** (Central Africa). The figures stay (exact); only the geography framing moves. |
| C9 | Y667 | §3.7 drops every frequency/popularity word: "The misstatement — that any non-collecting country with trade power is transferring — is wrong", citing the save-ledger evidence that the engine itself distinguishes transferring from inert non-collectors (`sevilla`: `pull_power` 17.642 = FRA 3.319 + ARA 14.323 exactly; `venice`: 15.199 of non-collector power with no `pull_power` at all). Wrongness is proven; commonness is asserted nowhere. |
| C10 | Y445 | §2.2 item 7: state what each implementation computes — the census as specified (30 solves, per-entry good counts) is the **DLL deliverable**; the reference implementation ships the **boolean projection over the 29 live goods** (`measure6.py`), which is what feeds §3.8's 5,723. |
| C11 | Y379 | §1.9's first bullet keeps the France observation (no power-or-merchant precondition on the receiving country) and gains the save-measured limitation: **72 of 272 threshold-qualifying (country, upstream-node) pairs receive nothing**, concentrated in distant colonial pairs (ENG qualifies in `english_channel` yet propagates nothing into `chesapeake_bay`), **consistent with trade range gating receipt** — an unstated gate, added to §2.7's probe questions. "No condition on the receiving node" is narrowed to what the observation established. |
| C12 | Y375 | §1.8's both-ends gate: marked as stated from interface behaviour with **no located file source** — a probe-class fact per §3.16's own rule, listed accordingly. |
| C13 | Y790 | §3.16 mechanism 1: of the 1.37 floors, **`COLONY_MIN_AUTONOMY = 50` is the file-exposed one**; the 90/20/0 regime floors are **not in `defines.lua` or any file that has been searched** — stated in the searched form, not as a universal over the install. The mechanism (a remembered pre-Common-Sense value) survives on the 50. |
| C14 | A4-adjacent | §0's parenthetical (spec **L18–20**; the sentence itself spans **L19–20**) "*The whole-install sweep is v5.0's alone.*" is reconciled with A4 before both stand in one document: the sweep **as documented §1.3 apparatus** is v5.0's alone (v5 L201 records that v4.0 stated the rule and swept only `common/tradegoods/`); the only **shipped sweep script** is v4.0's `audit_modifiers.py` (§2.3). The §0 clause gains that distinction in-line so §0 and §2.3 assert compatible things. |

## D. Observation-sourced claims scoped as observations

| # | ID | fix |
|---|---|---|
| D1 | Y041, Y046, Y288, Y291 | §1.3's tooltip block gains one scoping sentence: the `Base`-line schema, the 0.62 truncation ordering and the window's modifier composition are **recorded observations from the tooltip sessions**; what the materials re-verify is the save-side inputs and the arithmetic (re-verified again this round), and the on-screen strings are the observation itself — per §3.16, a named observation is a source, and these are named. |
| D2 | Y293, Y295 | The Clergy term: "+5%, estate" is the **observed tooltip instance**; the file value is loyalty-scaled (`01_church.txt` `global_tax_modifier = 0.2`), so the 5 is derivable only through loyalty arithmetic, and the sentence says so. The file-confirmed components (75+25+5+15 = 120 of the 125) are noted as such. |
| D3 | Y420, Y421, Y422 | §1.12 gains a one-line lead-in: **this section specifies display behaviour the DLL must deliver; present tense is specification, not observation of a build that does not yet exist.** All three rows then read as what they are. |
| D4 | Y708 | §3.11: "with nothing checking whether value moves" is scoped as an **absence-of-strings finding** in the binary's table (both trigger strings verified; no third found), with §2.7 item 6 as the probe that settles the internal logic. |
| D5 | Y721 | §3.12's inflation clause: gold receipts inflate (`GOLD_INFLATION`/`TREASURE_FLEET_INFLATION` = 0.5, flat); the income-relative normalisation is file-stated **only for peace gold** (`INFLATION_FROM_PEACE_GOLD`, "per month of income"), and for treasure fleets it is engine arithmetic the files do not state. The sentence is cut to what carries. |

## What negotiation must settle

- Every row: CONFIRMED or REJECTED, no conditions. Anything conditional comes back for another pass.
- A7 corrects the validation itself (the define exists); the validator should verify `defines.lua:1200` and either withdraw Y353's gap or refute my reading.
- B5 and A5 are instrument repairs; their acceptance tests are the ones stated **in their own rows** — B5's full set (24-point sink-set result, edge-drift maxima 16/25, and the eps = 1e-3 point matching the shipped map 159/159), and A5's every-cited-figure list. This bullet defers to the rows so it cannot drift from them again.
- B1's configs must be stated inline in the spec so the figures are reproducible as quoted — that is the defect being fixed, and it must not be reintroduced by quoting bare numbers.
