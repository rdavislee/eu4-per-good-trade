## §2.5 — Runtime attachment (4 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y501 | Attachment uses pattern scanning/hooking, following the "EU4dll precedent" | UNTESTABLE | searched the whole `per-good-trade` tree and the EU4 install for a copy of the EU4dll project | No copy of EU4dll exists anywhere on disk (only the phrase itself, verbatim, across v1-v6 spec versions). Its actual scanning/hooking method and scope cannot be checked; only `eu4.exe` itself (38,462,504 bytes, matches the v1 audit's recorded size) is available as a target |
| Y502 | Mod ships a runtime-patching DLL, not a modified executable | CONFIRMED | consistency check: grepped `per-good-trade-spec.md` for every "the DLL" reference | Every other mention of the runtime component ("owned by the DLL in memory" L1186, "the DLL never reads a save" L989, "the DLL that would prove it does not exist yet" L947) is consistent with a DLL, never a modified executable |
| Y503 | The binary is frozen, so offsets found stay found | CONFIRMED | file metadata check (no execution): `Get-Item eu4.exe .Length` | `eu4.exe` is a static 38,462,504-byte file, matching the size recorded in the v1-laplacian audit (`38,462,504-byte PE at version v1.37.5.0`) — a fixed installed binary that does not change between sessions |
| Y504 | §1.10's nation-pair gates are hooked and returned true at the call site, not via a shared predicate | CONFIRMED | consistency check against §1.10 | §1.10 (L793): "Any mechanism gated on one nation being upstream or downstream of another evaluates TRUE" — Y504 restates this as an implementation mechanism (hardcode true rather than compute a predicate); no contradiction anywhere in the document |

## §2.6 — Writing to the engine (10 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y505 | Monthly trade tick runs in three passes (power/modifiers; propagation; value) | UNTESTABLE | checked §2.7/§3.13 for probe status; checked v1 spec for the claim's origin | §3.13 (L1815): "only pass caching, pass-2 content, write windows... truly need the debugger" — still listed as open in §2.9 (twelve items 1-11 and 16 still open). No debugger session exists in our materials; RUNNING GAME excluded by the brief |
| Y506 | Written each tick: node trade value = Σ_g value_g(n) | CONFIRMED | derivation: cross-checked against §1.8's node-wide/per-good framework and §3.10's income identity, which use the same `value_g(n)` term consistently (L1752) | No contradiction; a per-good sum aggregating to a node total is the natural consequence of the 29-separate-graphs design used throughout |
| Y507 | Written each tick: node collectible pool = Σ_g value_g(n)·collected_share(n,g) | CONFIRMED | derivation, same cross-check as Y506 | Identical term used identically at L1278 and L1752 (§3.10's derivation) |
| Y508 | Written each tick: per-link value = net realized flow summed over goods, in the installed Φ_w direction | CONFIRMED | consistency check against §1.6/§3.9 (Φ_w as the one installed graph) | Matches the document's single-installed-graph design throughout; also consistent with §2.8's measured Φ_w-vs-realized sign disagreement (54.8%), which presupposes per-link values are written in the Φ_w direction and can therefore be negative |
| Y509 | Country trade income is derived by the engine from the written fields, unless stored | UNTESTABLE | no built DLL, no decompiled engine income-calculation code in our materials | This is an assumption about vanilla engine internals underlying the whole §2.6/§3.10 design; nothing in the game files, saves, or scripts observes the engine's own country-income computation directly |
| Y510 | `collect_pool` is per-good on the inside via `collected_share(n,g)`/`P_transfer(g)` (§1.8, commodity-specific) | CONFIRMED | derivation: read §1.8 (L753-759) and §3.10 (L1749-1756) | §1.8: "Trade power and collect/transfer intent are node-wide. What varies per good is what they produce," and "Transfer eligibility is per good" — supports the premise exactly as stated |
| Y511 | `powershare_C` is good-independent (merchant-or-home property), so it factors out of the per-good sum | CONFIRMED | derivation: same §1.8/§3.10 read; algebra checked step by step | §3.10 (L1756): "Every term that feeds a collector's power at a node is node-wide... so none of them can reintroduce a `g`" — the factoring step is algebraically valid given §1.8's stated premises |
| Y512 | Two deadlines: display immediately after the value pass; AI reads during the month | UNTESTABLE | checked §3.13's probe-status list | Part of the still-open pass-caching/write-window probes (§3.13, §2.9); requires debugger observation not in our materials |
| Y513 | Payment bounded by month boundary; treasury reconciles against previous month's income | UNTESTABLE | same as Y512 | Same open-probe status; no primary source available to settle engine reconciliation timing |
| Y514 | Per-link values are written net, which can be negative when realized flow opposes the drawn arrow | CONFIRMED | consistency + cross-reference to §2.8's measured sign-disagreement figures | §2.8/§1.6: Φ_w agrees with per-good graphs on only 54.8% of edge-goods (value-weighted) — realized flow genuinely opposes the installed direction on a large minority of edges, which is exactly the condition under which a net-written value would be negative |

## §2.7 — Probes (26 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y515 | Items 1-10 are v1's probe set, "settled with a debugger... in one session," though several are observable without one | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md` L284; cross-check against `../v2-drain/game-session.md` | v1 §2.7 opens verbatim "Settled with a debugger on a vanilla install, in one session." `game-session.md` (the claim audit's own session) settled probes 13-15 and the link-reversal/declaration-order questions using screen capture and save-file parsing — explicitly *not* a debugger — directly supporting "several are observable without one" |
| Y516 | Items 12-15 are done: run against 1.37.5 in `game-session.md`, folded into §1.9, §2.4, §3.6 | PARTIAL | file read: `game-session.md`; cross-checked §1.9 (L780-787), §2.4 (L1188-1197), §3.6 (L1619-1636) | Items 13-15 were run and are folded into exactly those three sections (verified: France-power example in §1.9, cyclic-crash/declaration-order text in §2.4, cyclic-crash text in §3.6). But item 12 was *not* run — the document's own next sentence (Y517) says it was dropped rather than run, so "they were run" overstates for item 12 |
| Y517 | Item 12 dropped rather than run: under owner-agnostic wealth the per-province production-income field is unread | CONFIRMED | derivation: read §1.3 (L253-282) | §1.3's wealth formula reads only `base_tax`, `base_production`, trade good and province-state modifiers — it never reads a precomputed production-income field; consistent with probe item 9's own framing of that field as a distinct, separately-checkable vanilla concept |
| Y518 | Probe 13 (cyclic node file): engine does not tolerate a cycle, `EXCEPTION_STACK_OVERFLOW`, 1002 frames, twice | CONFIRMED | file read: `game-session.md` §B1 | "`EXCEPTION_STACK_OVERFLOW` (C00000FD), reproduced on two independent launches... 1002 recorded frames, all at one identical return address" — matches exactly |
| Y519 | Probe 14: incoming-link entry only navigates; clicking Safi switched the window, dispatched nothing | CONFIRMED | file read: `game-session.md` §A1 | "The incoming entry is a pure navigation control" — clicking navigated Sevilla→Safi, no merchant dispatched, matches verbatim |
| Y520 | Probe 15: "where it already has power" is not a precondition; France 0 provinces/0 merchants, 3.3 power in Sevilla | CONFIRMED | file read: `game-session.md` §A2 | France: "0.0" provinces and merchant columns, 3.3 total power, itemised "Transfers from traders downstream: +3.1" — matches exactly |
| Y151 | §1.9's "every immediately upstream node" is consistent with probe 15's reading | CONFIRMED | file read: `game-session.md` §A2 conclusion; §1.9 L781 | game-session.md: "§1.9 is correct as written and needs no change... with no condition on the receiving node" — matches §1.9's actual wording exactly |
| Y521 | §2.4 item 3 link-reversal check: done and passed, hand-flipped link loaded with zero errors and rebuilt the economy | CONFIRMED | file read: `game-session.md` §B3 | "`logs/error.log`: zero non-descriptor lines"; Valencia moved outgoing→incoming, Sevilla's merchant switched steering→collecting, power shares recomputed — matches |
| Y522 | Declaration-order question settled: engine validates order, logs one error per violation, tolerates violations | CONFIRMED | file read: `game-session.md` §B2 | "exactly 159 lines, one per backwards link"; game loaded, ran to Dec 1444 without incident — matches exactly |
| Y523 | Probe 1: pass caching — does flipping a link crash/stale/rebuild per pass | UNTESTABLE | checked §3.13's probe-status list | Explicitly still debugger-only and open (§2.9's "twelve items still open (1-11 and 16)") |
| Y524 | Probe 2: what imposes pass 2's ordering, given propagation is one hop | UNTESTABLE | same | Same open-probe status |
| Y525 | Probe 3: write windows relative to the value pass | UNTESTABLE | same | Same open-probe status |
| Y526 | Probe 4: negative link values — write one, observe arrow rendering/protect-trade | UNTESTABLE | same | No live game observation available; still open |
| Y527 | Probe 5: merchant storage — flip a link hosting a steering merchant | UNTESTABLE | same | Still open; B3's flip test used an *unassigned* corridor, not a steering merchant, so it does not settle this |
| Y528 | Probe 6: caravan, twice — incoming-in-Φ_w link and a no-goods link | UNTESTABLE | same | Still open |
| Y529 | Probe 7: is arrow render state separate from the economic link | UNTESTABLE | same | Still open |
| Y530 | Probe 8: `TRADE_PROPAGATE_THRESHOLD` semantics — does the raw requirement double | UNTESTABLE | same | Still open; §3.13 files it "Derived — probably right, cheaply falsifiable," i.e. not yet run |
| Y531 | Probe 9: diverted colonial gold in the per-province production-income field | UNTESTABLE | same | Still open |
| Y532 | Probe 10: caller enumeration — disassemble every "is X downstream of Y" call site | UNTESTABLE | same, distinct from Y533's partial static-analysis result | The *full* disassembly/enumeration task is unstarted; only a partial static string-table scan (Y533) exists |
| Y533 | Static string-table analysis already yields three named call sites (DIPLO_SELLPROV_NOT_UPSTREAM, TREASURE_FLEET_TOOLTIP_CANT_REACH, TRADE_POWER_UPSTREAM); no colonisation refusal string | CONFIRMED | file read: grepped EU4 `localisation/*.yml` for all three identifiers, and for colonization+upstream/downstream text | All three found verbatim: `eldorado_l_english.yml:1074`, `tmm_l_english.yml:1311`, `tradenodes_l_english.yml:142` — text of each confirms "compare trade capitals" reading. No colonization-refusal string combining "colon"+"upstream/downstream" found in any localisation file |
| Y534 | Probe 11: caravan recipient — merchant in coastal node steering to inland one | UNTESTABLE | same open-probe status | Still open (item 11 of the twelve) |
| Y535 | Engine tooltip and identifier `merchant_steering_to_inland` both read as the inland node; if so §3.11 inverts | PARTIAL | file read: grepped `eu4.exe` binary for the identifier string (no execution) | The identifier exists verbatim in `eu4.exe`'s string pool, confirming its name. But which node the tooltip actually credits is exactly what still-open probe 11 would determine — no live observation confirms the "reads as inland" behaviour |
| Y1040 | Probe 16: is EU4's 1/1000 quantisation in the simulation or the serialiser | UNTESTABLE | checked §2.9's still-open list; own re-run of a similar grid check on 4 saves | §2.9 lists item 16 among the still-open twelve. `scripts/val62p5_g.py` confirms every trade field on all 4 available saves sits exactly on the 1/1000 grid (3354-3810 values, 0 off-grid per save) but this cannot distinguish simulation-side vs serialiser-side rounding without a live memory read |
| Y1041 | If rounding is in the simulation it defends lockstep MP; if only in the writer it says nothing about determinism | CONFIRMED | derivation: conditional-logic check | Both branches of the if/then are sound engineering claims independent of which antecedent holds; the sentence asserts the conditional, not the antecedent |
| Y1042 | Probe 16 settles what §2.1 may claim about the engine's defence, and whether the mod should round at its write boundary | UNTESTABLE | checked probe 16's status | Describes what an unexecuted probe would determine; nothing to check until it runs |
| Y536 | All writes land atomically at the tick hook with the sim paused | UNTESTABLE | checked §3.13's probe-status list | Requires debugger observation of the write hook; not settled by any file, save, or the game-session.md probes actually run |

## §2.8 — Validation (49 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y537 | Spice/cloves 1444: "source in Indonesia, and both source there alone" — spices from the_moluccas+kongo, cloves from the_moluccas only | PARTIAL | measurement: `scripts/val62p5_a.py`/`b.py`, graph in-degree-0 nodes per good | Graph-source lists match exactly (spices: `['kongo','the_moluccas']`; cloves: `['the_moluccas']`), and sink figures (Genoa/Brazil ranks 1/73; Genoa/Kongo/Brazil ranks 2/55/72) match exactly. But `kongo` is not in Indonesia (it is Central Africa) — the "source in Indonesia, and both source there alone" framing is inconsistent with its own very next clause naming kongo as a second spices source |
| Y1043 | v6.0 listed Australia, Venice, Deccan among the termini; none holds either sink on this field | PARTIAL | measurement (`val62p5_a.py`) confirms the field fact; checked for a v6.0 file to verify attribution | Computed: australia/venice/deccan are sinks for neither spices nor cloves — confirmed exactly. But no `v6.0` spec file exists anywhere on disk (only v6.1-frozen onward) to independently verify what v6.0 itself said; the same sentence already appears verbatim in v6.1-frozen, one version removed from the source being cited |
| Y540 | Malacca↔Cape post-1500: spice routes Malacca→Cape→Europe | CONFIRMED | measurement: `val62p5_a.py`, flow-into-cape share | 24.09% of world spice supply already routes through the Cape on the static 1444 field, confirming the routing mechanism exists independent of any date gate |
| Y541 | Malacca↔Cape pre-1500: withheld by range and power-at-both-ends gate, not by direction | CONFIRMED | derivation, cross-checked against §1.10's power-at-both-ends framing | Consistent with the measured fact that the edge orientation/flow already exists on the static field (Y540); realization is gated by historical range/power presence, not by the model's direction assignment |
| Y542 | 1000 AD start puts sinks in the Muslim world and Song China, "with no era data" | UNTESTABLE | checked `solver.py`'s province source | `solver.py` reads only `prov1444.json` (1444.11.11 data); the document's own qualifier ("no era data") concedes this is unverifiable with available materials |
| Y155 | hangzhou, not beijing, is China's wealth pole: 226.7 vs 143.0, ranks 12/39 of 79 | CONFIRMED | measurement: `val62p5_a.py`, node-wealth ranking | Output: "hangzhou wealth 226.7 rank 12 of 79 / beijing wealth 143.0 rank 39 of 79" — exact match; also matches `measure6.out`'s "hangzhou c_w/node-wealth rank (1,12)" |
| Y156 | Zeroing beijing also moves the map — 8 flips | CONFIRMED | measurement: `round6.py` | "zeroing beijing -> sinks ['genua','hangzhou'] 8 of 159 edges flip" — exact match |
| Y153 | On the razed field, 40 of 40 relabellings return {genua, gulf_of_siam}, hangzhou an end in none | CONFIRMED | measurement: `scripts/p3_razed_relabel.py` (pre-existing instrument, re-run) | "razed hangzhou 40 relabellings: baseline sink set returned 40/40 \| hangzhou an end in 0 \| sets {('genua','gulf_of_siam'): 40}" — exact match |
| Y544 | Ming loses Mandate: nothing moves that day, since §1.3 reads no owner property | CONFIRMED | derivation: §1.3 (L253-282) | §1.3's wealth formula reads development, trade good, province-state modifiers only — no institutional/owner flag such as the Mandate |
| Y545 | This row is the owner-agnosticism check, not a responsiveness check | CONFIRMED | consistency with §1.3's owner-agnosticism theme | Directly consistent framing |
| Y546 | Major war in China shifts corridors for the duration, reverts as devastation heals | CONFIRMED | derivation: §1.2 (devastation as a live input) + §3.6 (no-hysteresis, pure function of current state) + analogy to the razed-hangzhou test | §1.2 lists devastation as a live province-state modifier; §3.6 establishes the model is a pure function with no hysteresis, so a reverted input necessarily reverts the output; the razed-hangzhou test (Y153/Y154) is a concrete instance of exactly this mechanism |
| Y547 | Many poor provinces vs few rich: luxury demand to the rich node, bulk to the many-province node | CONFIRMED | derivation: direct restatement of §1.4's regime split (L443-451) | §1.4: "α>1... Luxuries concentrate on individually rich provinces" / "α<1... Bulk goods spread toward populous regions" |
| Y548 | Price crash: α falls below 1, regional sinks reappear | CONFIRMED | derivation + measurement: α-sweep on spices (ad hoc script) | §1.4's α(g) formula is monotone increasing in price, so a crash necessarily lowers α; at α=0.5 (below spices' baseline 1.5), spices sinks became `['baltic_sea','saxony']` instead of `{genua,brazil}` — geographically incidental "regional" sinks reappearing, matching the claim |
| Y549 | Caribbean 1650: sugar production income makes it a sink for cloth, tools, wine | UNTESTABLE | checked `solver.py`'s province source | Only 1444 province data is available (`prov1444.json`); no 1650 province/history state exists in our materials |
| Y550 | Kilwa 1000: ivory income makes it a sink for Indian textiles | UNTESTABLE | same | No 1000 AD province data available; consistent with Y542's own "no era data" admission |
| Y551 | A consuming leaf terminates the DAG of every good it consumes but does not produce | CONFIRMED | derivation: read `drain.py`'s `compile_dirs` peeled-edge rule; demonstrated via `toys.py`'s T1 | `compile_dirs`: a peeled (degree-1) node's single edge is oriented `(u,v)` [into the leaf] iff its absorbed balance `bv<0`, which by construction makes a net-importing leaf a sink; T1 demonstrates this exactly. (1444's own map has zero literal degree-1 nodes, so this is a construction-level guarantee, not a 1444 measurement) |
| Y552 | An inert merchant's goods take the even split as if the node were empty; node-wide bonuses still apply | CONFIRMED | cross-reference: §1.7 (L722) and §3.10 (L1739) | §1.7: merchant's +2 power/+10% income bonus applies "node-wide, regardless of what it is doing"; §3.10 references an even split as vanilla's default un-steered behaviour |
| Y553 | A node sinking spice but not cloth collects spice fully, cloth at the ratio with remainder pushed | CONFIRMED | derivation: direct application of §1.8's `collected_share(n,g)` formula (L755-756) | `collected_share=1` if sink for `g`, else `P_collect/(P_collect+P_transfer(g))` — applied independently per good, exactly as the claim describes |
| Y554 | A near-balanced link may flip monthly, carries near-zero either way, assignments survive | CONFIRMED | cross-reference: §3.6 (L1631-1634), already measured there | "zero support-membership changes moved more than 1e-6 of flow" under demand nudges; "merchant assignments are to links, so they survive flips untouched" |
| Y555 | Two-way Atlantic corridor: merchants at both ends on disjoint good sets, neither blocking the other | CONFIRMED | measurement: ad hoc script over all 29 per-good `run_drain` results | All 159 of 159 undirected edges carry flow in *both* directions across different goods, including every edge touching `english_channel` (e.g. ivory_coast↔EC: 21 goods one way, 8 the other) |
| Y556 | Economy tab vs overlay: matches to the ducat; self-consistency check, not vs stock EU4 | CONFIRMED | consistency check against `game-session.md`'s own recommendation | game-session.md explicitly recommends exactly this framing after finding vanilla values non-reproducible run to run |
| Y557 | Stock trade values not reproducible run to run: 49/80 nodes differ by up to 8.96% | CONFIRMED | measurement: `scripts/val62p5_e.py` on VANILLA_start.eu4 vs VANILLA2_start.eu4 | "nodes differing on ANY: 49 of 80; overall max 8.959%" — matches "49 of 80... up to 8.96%" exactly |
| Y558 | AI merchant placement randomised; power-dependent fields inherit it; retention 80/80, total 78/79 (zambezi 0.012%) | CONFIRMED | same measurement | "retention 0 of 80 differ... total 1 of 79 differ, max 0.012% (zambezi)" — exact match |
| Y559 | Any comparison against unmodded numbers needs a tolerance and a null run | CONFIRMED | cross-reference: `game-session.md`'s own explicit conclusion | Verbatim conclusion of the B2 investigation |
| Y560 | Reachability: 100% of demand reachable, asserted every tick, zero orphan sinks | CONFIRMED | measurement: `scripts/final.py` PART A | "V032 reach==100%: 29/29 \| orphan sinks total: 0" |
| Y561 | Conservation: Phase-4 Σunserved == Σstranded to machine precision, every good every tick | CONFIRMED | measurement: `val62p5_f.py` + `final.py` | "max \|sum unserved - sum stranded\| over 29 goods: 0 (eps=2.22e-16)"; `final.py`: "V089 unserved==stranded (<1e-9): 29/29" |
| Y562 | Determinism: re-running a tick reproduces the orientation bit-for-bit; one fingerprint across seeds | CONFIRMED | measurement: `scripts/fingerprint6.py` re-run under PYTHONHASHSEED 0,1,2,42,12345,random and unset | All 7 runs produced the identical hash `8b13dddb9a345aa6d1990cac70d64e0b649b01f8cab1a9ec41cf9c87faa1012c` |
| Y1044 | New row: LP configured tighter than the tie-break margin — `flowop.LP_OPTS` sets both tolerances 1e-10 | CONFIRMED | file read: `flowop.py` L85 | `LP_OPTS = {"dual_feasibility_tolerance": 1e-10, "primal_feasibility_tolerance": 1e-10}` — exact match |
| Y563 | Acyclicity asserted on every per-good graph, on Φ_w, and on the emitted file's declaration order | CONFIRMED | measurement: `final.py`/`val62p5_a.py` | "V028 acyclic: 29/29"; Φ_w "acyclic: True" (`val62p5_a.py`) |
| Y564 | Sink-set 2-core containment is a hard assertion every tick, unconditional | CONFIRMED | measurement: `val62p5_f.py` | "core sinks outside {selected} U {promoted} U {fallbacks}: 0" over all 29 goods |
| Y565 | Asserting containment in {selected}∪{promoted} alone would halt on T3 (correct behaviour) | CONFIRMED | measurement: `toys.py` T3 | "sink inside {selected} U {promoted}: False inside U {fallbacks}: True" — exactly the halt-on-correct-behaviour case described |
| Y566 | Sink-set equality is monitored, not asserted: exact on 1444, 29/29 goods, zero fallbacks | CONFIRMED | measurement: ad hoc equality script | "equality {selected cap flow-terminal} U promoted == actual sinks: 29/29; goods using fallback: 0" |
| Y567 | Where Phase 0 acts, equality doesn't apply; the check is the Phase-4 orientation rule; T1 is expected output | CONFIRMED | measurement: `toys.py` T1 | Matches T1's construction and stated expected mismatch exactly |
| Y568 | Colonization check: observer run to 1600, roughly vanilla pace | UNTESTABLE | checked available saves | Only 1444-dated saves (plus one Dec-1444 save) exist in our materials; no 1600 observation possible without the live game |
| Y569 | AI convergence check: greedy assignment settles with damping rather than oscillating | UNTESTABLE | same | Requires live multi-tick observation not available |
| Y570 | Latent-good check: repricing 45 owned latent-coal provinces flips 16 of 159 Φ_w edges | CONFIRMED | measurement: `measure6.py`/`measure6.out` | "latent-coal owned and counted 45... coal activation edge flips 16" — exact match |
| Y571 | Cross-implementation check: DLL and reference implementation agree on orientation exactly | UNTESTABLE | checked for a built DLL | No DLL exists in our materials ("the DLL that would prove it does not exist yet," L947); nothing to compare |
| Y572 | Φ_w-vs-realized sign disagreement measured, not asserted: 55.1% edge-goods, 54.8% value-weighted | CONFIRMED | measurement: `val62p5_f.py` | "edge-good agreement 55.1%; value-weighted 54.8%" — exact match; also matches `measure6.out` |
| Y573 | Flip behaviour measured per decade, peace vs war, reversion as occupation lifts | UNTESTABLE | checked for any such measurement | This is a stated future-measurement plan ("Measured, not asserted") — no such data exists in our materials |
| Y574 | Propagated-share change per node measured on each flip, alongside power/in-degree covariance | UNTESTABLE | same | Future-measurement plan, no data collected |
| Y575 | Total propagated power is not the quantity to watch: Σindeg=\|E\| is invariant under reorientation | CONFIRMED | derivation: trivial graph-theoretic fact | Reorientation relabels a fixed 159-edge set; it cannot change edge count, so the sum of in-degrees over all nodes is definitionally |E| regardless of orientation |
| Y576 | Income balance measured on two metrics: total world income and distribution across great powers | CONFIRMED | consistency check against §2.9 | §2.9 (L1399): "gate income balance on both metrics" — matches the "two metrics" framing |
| Y538 | Under §3.13 calibration, spices sinks at doab/genua; cloves moves to beijing; no Chinese spices sink | CONFIRMED | measurement: re-ran `scripts/final.py` PART B (the calibration instrument) | "V107 spices sinks under calibration: ['doab','genua']"; "V107 China nodes among calibration spices sinks: []"; "V179 cloves alpha=16 sinks: ['beijing']" — all exact matches |
| Y152 | Sinks are {selected∩flow-terminal}∪promoted, 2-8 per good; top-8 demanders sink at 19.4% (45/232), bottom-8 at 7.3% (17/232) | CONFIRMED | measurement: `round6.py` | "sinks among each good's top-8 demanders: 45 of 232 = 19.4%" / "bottom-8: 17 of 232 = 7.3%" — exact match |
| Y154 | Zeroing hangzhou moves Φ_w sinks {genua,hangzhou}→{genua,gulf_of_siam}, 32 of 159 edges flip | CONFIRMED | measurement: `round6.py` | "zeroing hangzhou -> sinks ['genua','gulf_of_siam'] 32 of 159 edges flip" — exact match (current document already carries the corrected 32, not the superseded 30) |
| Y543 | v4.0 (not v2-v4.0) said zeroing beijing "moves nothing"; it does — asymmetry is which node keeps its end | CONFIRMED | file read: `v2-drain/`, `v3-owner-agnostic/`, `v4-owner-agnostic/`, `v5-owner-agnostic/` spec files | v4-owner-agnostic L754 literally says "Zeroing beijing... moves nothing"; v2 and v3 instead zeroed *beijing* as their primary razed-China test and reported it *relocates* the sink — they never said "moves nothing," confirming the narrowed attribution is correct where the old broader "v2 through v4.0" wording was not |
| Y1045 | Configuration can regress silently on solver upgrade; three-branch reduced-cost classification (halt/report/halt) | CONFIRMED | measurement: `round6.py` §"2.3/3.13 what HiGHS does..." | Column-permutation test: default/unset tolerance moves 8 edge-slots on `copper`, 1e-8/1e-10 move 0 — demonstrating the silent-regression mechanism the three-branch check exists to catch |
| Y1119 | Calibration figures moved when §2.3's tie-break cost reached the calibration's Phase 2, previously reading a different vertex | CONFIRMED | file read: `scripts/final.py` PART B, code + comment | Comment: "the calibration read a vertex the solver happened to return rather than the one DRAIN installs"; code now calls `mincost_flow(..., cost=TIE_COST)` where the comment marks it as "the last surviving unit-cost Phase-2 call" |
| Y1120 | `paper` is in the genuine-tie state: zero reduced cost on an arc carrying no flow in any optimum | CONFIRMED | measurement: `scripts/val62p5_a.py`/`b.py` | One off-support zero-reduced-cost arc found (cape_of_good_hope→malacca); maximizing its flow subject to staying at the optimal objective gives ~1e-12 (numerically zero, confirming it is a genuine tie, not an exploitable alternative optimum) |

## §2.9 — Build order (8 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y577 | The build is not phases but two tracks run in parallel | CONFIRMED | consistency check | The following text literally implements this: "Solver track —..." / "Memory track —..." |
| Y579 | Then the b-flow and sweep with their per-tick assertions (reachability, conservation, acyclicity...) | CONFIRMED | cross-reference to §2.8's assertion list | All named assertions (reachability, conservation, acyclicity, determinism, 2-core containment) appear as rows in §2.8's table |
| Y581 | Then: write §1.10's classified call-site list, gate income balance on both metrics, decide negative-link display policy | CONFIRMED | cross-reference to §1.10 item 10 (still incomplete) and §2.8's income-balance metrics | Consistent with probe 10's unfinished status and Y576's two metrics |
| Y578 | Solver track starts with the defines parser: every define the model reads is a runtime read, except TAX_COEFF | CONFIRMED | file read: §2.3 (L1037-1060) | §2.3's table lists 9 runtime-read defines plus GP_COEFF (shipped file value, read at runtime); TAX_COEFF explicitly "is not in any file that has been found... remains a measured constant" — exact match |
| Y580 | Memory track is the §2.7 probe session: the twelve items still open there (1-11 and 16) on one trace | CONFIRMED | count check against §2.7's current probe list | Items 1-11 (pass caching through caravan recipient) + item 16 (quantisation) = 12 items, all confirmed still open (13-15 struck, 12 dropped) — matches exactly |
| Y1121 | Each per-tick assertion paired with a negative fixture that makes it fail | UNTESTABLE | checked for the actual per-tick assertion implementation | No built solver/DLL with per-tick assertions exists yet; `redtest6.py` (Y1123) is explicitly only "the reference-side version" of this requirement, i.e. an analogue for the spec-audit harness, not the thing itself |
| Y1122 | Four of the round-5 audit's defects were checks that could not fail | CONFIRMED | file read: `redtest6.py` docstring; cross-checked against `validation-v6-round5-part2.md` | `redtest6.py`'s own docstring: "Four defects found in the round-5 audit shared one signature" and implements exactly 4 fixtures; round-5's own finding "A1 — epsilon6.py: the gate cannot fail by construction" (L410) matches fixture 1 directly |
| Y1123 | `scripts/redtest6.py` is the reference-side version of the negative-fixture requirement | CONFIRMED | measurement: re-ran `redtest6.py` in full | "RESULT: 6 fixtures, 0 did not produce the required red" — the harness runs and does exactly what the claim describes |

## §3.1 — Goals (7 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y582 | Goal 1, world responsiveness: direction follows current state; razing hangzhou moves the sink | CONFIRMED | cross-reference to the already-measured razed-hangzhou test (Y154) | Zeroing hangzhou's wealth measurably moves the Φ_w sink set (32/159 edges flip) — direct demonstration |
| Y583 | Goal 2, realism: China is a silk source and spice sink at once — impossible under one graph | CONFIRMED | measurement: ad hoc per-node source/sink check across all 29 goods | `hangzhou` is both a raw silk *source* (S>0) and a sink for 8 other goods simultaneously (cocoa, copper, cotton, dyes, gems, glass, sugar, tobacco) — demonstrates the underlying source/sink co-location mechanism the Goal illustrates |
| Y584 | Goal 3, preserve feedback loop: sinks accumulate, fund development, reinforce | CONFIRMED | cross-reference to §1.8's `collected_share=1` at sinks | Full local collection at a sink (nothing pushed further) preserves the accumulation channel the goal describes |
| Y585 | Goal 4, return flows: export regions historically imported manufactures; vanilla cannot express this | CONFIRMED | derivation: §1.3's wealth formula creates cross-good demand from production income | wealth(p) feeds `c(n,g)` for every good g, not only the good produced — a sugar island's income genuinely creates cloth/tools demand; vanilla has no per-good demand concept at all to compare against |
| Y586 | Goal 5, route-aware direction: reflects reachability, not which neighbour is richer | CONFIRMED | cross-reference to §3.2's monotonicity critique and DRAIN's flow-based design | Directly matches the "Local comparison is monotone" argument and DRAIN's LP-feasibility approach, both already established in §3.2 |
| Y587 | Goal 6: zero authored data | CONFIRMED | broad consistency check across §1.x-§2.x | No hardcoded trade direction or value found anywhere; every figure is formula-derived from game files or defines |
| Y588 | Goal 7: the game's own numbers are the model's numbers | CONFIRMED | cross-reference to §2.6/§3.10 | §3.10: "the engine's own vanilla collection math reproduces every country's per-good income exactly" |

## §3.2 — Why a flow and a drainage sweep (38 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y589 | Two orientation families fail before DRAIN: first by theorem, second by exact rule whose consequence is measured; v2 called both theorems | CONFIRMED | file read: `v2-drain/per-good-trade-spec.md` L514 | "Two families of orientation fail before this one, and both failures are theorems, not taste" — v2 verbatim, confirming the overstatement |
| Y590 | Local comparison is monotone; Malacca→Cape→Europe requires a dip through a low-value intermediary | CONFIRMED | derivation + cross-reference to Cape's measured s=c=0 | A locally-monotone orientation cannot dip; the Cape (s=c=0, confirmed via ad hoc script) sits between richer neighbours, exactly the dip the route requires |
| Y591 | Monotonicity killed v1's rank strawman and the s-c operator: "one sixth" of world demand unreachable, Genoa crowned an unreachable cloves sink | PARTIAL | measurement: `val62p5_f.py` + follow-up scripts | Genoa-as-unreachable-cloves-sink confirmed exactly ("genua is a sink: True; genua reachable from supply: False"). But "one sixth" (16.67%) does not reproduce: unweighted per-good mean unreachable demand is 17.07%, value-weighted is 7.7%, cloves-specific is 67.6% — no natural construction gives exactly 1/6 |
| Y592 | Merchants cannot repair a wrong orientation — select among existing arrows, cannot reverse one | CONFIRMED | cross-reference to `game-session.md` §B3 | When Sevilla's outgoing link vanished, Castile's merchant switched steering→collecting rather than somehow retaining an outgoing option — merchants react to, but cannot alter, the graph |
| Y593 | v1's Laplacian sink rule is exactly (c-s)/deg > mean(nbr φ) - min(nbr φ), verified on every (good,node) pair | CONFIRMED | measurement: `val62p5_d.py` | "sink rule matches actual sinkhood on 2320 of 2320 (good,node) pairs; mismatches: []" — exact |
| Y594 | Supply sparse where demand dense: spices in 18/80 nodes, cloves in 1; every owned-province node carries demand | CONFIRMED | measurement: `val62p5_a.py`/`f.py` | "spices produced in 18 nodes... cloves produced in 1 node"; "nodes with owned provinces: 79... with c>0 for spices: 79" — exact match |
| Y595 | v1 Laplacian: highest-demand node never a spices sink; a zero-demand node outranked Genoa/Beijing; deleting demand variation left the sink unmoved | CONFIRMED | measurement: `val62p5_f.py` | "highest-demand node = genua (a sink: False)"; zero-demand node `cape_of_good_hope` has φ=0.0618, exceeding genua's -0.0583 and beijing's 0.0348; uniform-c spices sinks still include `saxony` (the unmoved v1 baseline sink) |
| Y596 | v1/v2 quantified asymmetry as "supply contrast 10⁷ vs demand contrast 10²-10³"; ratio was max(s)/ε-floor, removed by §1.2 | CONFIRMED | file read: `v1-laplacian/diagnosis.md` L327; §1.2 (L240-248); measurement `val62p5_f.py` | diagnosis.md verbatim: "supply contrast is 2.5×10⁷ and demand contrast is ~10²-10³"; measured eps-floor ratio (2.52e7/8e7) matches the 10⁷ order; §1.2: "No regularizer... DRAIN does not need it" |
| Y157 | Ratio metric cannot see sparsity: spices in 18/80 nodes, cloves in 1; (c-s)/deg dominated by where supply exists | CONFIRMED | measurement: `val62p5_a.py` | Matches Y594's figures exactly; logical argument follows directly from the sink-rule identity (Y593) |
| Y158 | On the contrast metric itself, demand is the wider side, not supply | CONFIRMED | measurement: `val62p5_f.py` | "supply contrast over producing nodes: 1..97" vs "demand contrast: 211..15010" — even the *minimum* per-good demand contrast (211) exceeds the *maximum* per-good supply contrast (97) |
| Y597 | No parameter fixes it: α strong enough to matter destroys §1.4's regime split | CONFIRMED | measurement: ad hoc α-sweep on spices, v1 Laplacian sinks | Within the legitimate clamp [0.2,3.0], the sink never reaches a Chinese node (α=3.0 gives `rheinland`); reaching `hangzhou` requires α≈5-8, far outside §1.4's stated clamp range |
| Y159 | Better wealth inputs move Genoa to a co-sink at ~×1.7 | CONFIRMED | measurement: `val62p5_c.py` | "genua minimal wealth multiple to become a spices sink: 1.724" — matches "roughly ×1.7" exactly |
| Y160 | Moving the sink to a Chinese node needs 3.6-4.8×: beijing 3.63, hangzhou 4.13, xian 4.61, canton 4.78 | CONFIRMED | measurement: `val62p5_c.py` | "beijing 3.626 / hangzhou 4.125 / xian 4.606 / canton 4.775" — exact match to quoted precision |
| Y161 | These are wealth multiples, not demand multiples — demand (wealth^α, α=1.5 for spices) makes the effective factor larger | CONFIRMED | derivation: k^α > k for α=1.5>1 | spices' α = min(3,(price/2))=1.5 (price=3.0); at k=1.724, k^1.5≈2.26 > 1.724, confirming the demand-side factor is larger |
| Y162 | Four named Chinese nodes are not the cheapest: girin needs 3.89×, yumen 4.49×, both inside the range | CONFIRMED | measurement: `val62p5_h.py`/`i.py` | girin 3.888, yumen 4.493 — exact match; both slot between the named nodes in the full ranking (beijing<girin<hangzhou<yumen<xian<canton), so the four named are not collectively the 4 cheapest |
| Y598 | v2 wrote "1.7× where 4-5× is needed," compressing two quantities into one comparison | CONFIRMED | file read: `v2-drain/per-good-trade-spec.md` L533 | "better wealth inputs move the threshold by 1.7× where 4-5× is needed" — verbatim match |
| Y599 | Conservation lesson: node-balance-imposing operators serve 100% of demand as a theorem; others strand it | CONFIRMED | derivation: standard flow-feasibility fact | An operator enforcing exact node balance guarantees full service by construction (given feasibility); non-balance heuristics (rank, seeded basins) carry no such guarantee |
| Y600 | DRAIN takes conservation from the b-flow; reachability is LP feasibility, not aspiration | CONFIRMED | cross-reference to measured 100% reachability (Y560/Y611) | Directly demonstrated: 29/29 goods, 100.0% reachability, zero orphan sinks |
| Y601 | v1 did state aggregate acyclicity as C061; its ε-machinery decided dead-branch direction | CONFIRMED | file read: `v1-laplacian/claims.md` L210; spec L477 | C061: "`Φ` is a potential, so orienting edges by it is acyclic" — verbatim; "ε magnitude: small enough to be invisible... large enough to decide dead branches against floating-point noise" — verbatim |
| Y163 | Sink placement is a measurement on one input: on 1444, final sinks = {selected∩flow-terminal}∪promoted, exact 29/29 | CONFIRMED | measurement: ad hoc equality script | "equality... == actual sinks: 29/29; goods using fallback: 0" |
| Y164 | v5.0's two rescue conditions (Phase 0 no-op, no fallback) are necessary but not sufficient: T2 satisfies both, still breaks equality | CONFIRMED | measurement: `toys.py` T2 | T2's output shows `promoted=[]` (no fallback) and is core-only (Phase 0 no-op by construction) yet "EQUAL: False" |
| Y602 | T1, pendant importer: triangle A(+5),B(-3),D(0) with leaf C(-2); actual sinks {C}, formula {B} | CONFIRMED | measurement: `toys.py` T1 | "directed: [('A','B'),('A','D'),('B','C'),('D','B')]... actual sinks = ['C'] \| formula set = ['B'] \| EQUAL: False" — exact match |
| Y603 | T2, free-edge race: 5-cycle+chord; actual {u2}, formula {u1,u2} | CONFIRMED | measurement: `toys.py` T2 | "actual sinks = ['u2'] \| formula set = ['u1','u2'] \| EQUAL: False" — exact match |
| Y604 | T3, fallback branch: triangle with b=0 everywhere, wealth 3/2/1; actual {A}, formula empty | CONFIRMED | measurement: `toys.py` T3 | "actual sinks = ['A'] \| formula set = [] \| EQUAL: False \| FALLBACK promoted: ['A']" — exact match |
| Y605 | What survives unconditionally: every 2-core node not selected/promoted/fallback-promoted gets an out-arc | CONFIRMED | derivation (sweep's `ready()`/termination logic) + measurement (0 violations, 29/29 goods) | Structural guarantee of the sweep's construction, confirmed with zero exceptions across all 29 goods (`val62p5_f.py`) |
| Y606 | Pendant net-importers are the only sinks outside that set | CONFIRMED | derivation: `compile_dirs`'s peeled-edge rule | An exporting pendant (bv≥0) always gets an outgoing edge; only a net-importing pendant (bv<0) becomes a sink — the only category of sink possible outside the 2-core containment guarantee |
| Y607 | §2.8 therefore carries two runtime checks, not one weakened one | CONFIRMED | cross-reference to §2.8's actual two rows ("Sink set, 2-core" and monitored equality) | Matches exactly what §2.8's table implements |
| Y608 | On pendant edges the Phase-4 orientation rule is the check; T1 is expected output | CONFIRMED | measurement: `toys.py` T1 | T1's result is exactly the described expected (non-violation) output |
| Y609 | Written as a single assertion with an escape clause, all three counterexamples vanish; against the narrower set T3 would halt on correct behaviour | CONFIRMED | measurement: `toys.py` T3 | T3's sink is outside {selected}∪{promoted} but inside ∪{fallbacks} — a narrower assertion would misfire on this legitimate case |
| Y610 | Free-edge direction is marking order under (DEF asc, b asc, index); zero exact ties on 1444, 29/29 goods | CONFIRMED | measurement: ad hoc tie-count script | "total free edges across 29 goods: 2321, exact (DEF,beta) ties: 0" |
| Y611 | Reachability: orientation contains the LP certificate; measured 100.0%, 29/29 goods, zero orphan sinks | CONFIRMED | measurement: `final.py` | "V032 reach==100%: 29/29 \| orphan sinks total: 0" |
| Y612 | Aggregate acyclicity: Φ_w is itself a DRAIN orientation, acyclic by the same marking-order argument | CONFIRMED | measurement: `val62p5_a.py` | "phi_w: core 80 of 80 \| every arc later->earlier marked: True \| acyclic: True" |
| Y613 | Φ_w's marking order is a per-node scalar reproducing the DAG, for any potential-needing consumer | CONFIRMED | measurement: `val62p5_a.py` | Same output: order-consistency confirmed for Φ_w and separately for 29/29 per-good graphs |
| Y615 | Corridor runs through the Cape: Malacca reaches the Channel in 3 hops through Cape vs 7 via Alexandria; 24% of world spice supply | CONFIRMED | measurement: `val62p5_a.py` (BFS + flow share) | "via cape: 1+2=3... via alexandria: 4+3=7"; "spice flow INTO cape: 0.2409" — both exact matches |
| Y616 | Peripheral termini still exist; value only arrives where someone holds power at both ends | CONFIRMED | cross-reference to §1.8/§1.9's collection/propagation framework | Consistent with the collected_share/P_transfer mechanism, which requires power presence for collection or transfer at either end |
| Y614 | Conduits still work: a node with s=c=0 (the 1444 Cape) carries flow through | CONFIRMED | measurement: ad hoc script | Cape's summed s and c are both exactly 0.0 across all 29 goods; in- and out-degree nonzero for all 29 goods |
| Y1124 | Degree is the weaker evidence for conduit function; an oriented edge is not a routed unit | CONFIRMED | derivation, consistent with the flow-vs-degree distinction already established | Directly follows from the LP-flow vs orientation-only distinction used throughout §2.1/§3.2 |
| Y1125 | On the certificate flow, Cape has both incoming and outgoing flow on 28/29 goods; exception is paper (routes none) | CONFIRMED | measurement: ad hoc script over `run_drain`'s flow_arc/net for all 29 goods | "goods with BOTH incoming and outgoing FLOW at cape: 28/29... without both: [('paper', 0, 0)]" — exact match |
