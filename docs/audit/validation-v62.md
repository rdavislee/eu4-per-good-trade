# Validation — Per-Good Trade Network Spec v6.2

**Document under test.** `per-good-trade-spec.md`, 2,079 lines, MD5 `5ec633ef46e7fd68bb118e10e83692eb` — verified at audit start and at assembly; unchanged throughout.

**Inventory.** `claims-delta-v62.md` — every row of its UNCHANGED (809), REWORDED (20), CHANGED (46) and NEW (84) tables: **959 live claims**, all graded. REMOVED rows are not claims and are not graded. Full census text from `claims-v6.md` where a delta label is compressed; the current document's wording governs, and CHANGED rows are graded on the new assertion.

**Sources.** The EU4 1.37.5 install; `scripts/` (instruments re-run, never quoted); `../v1-laplacian/` through `../v5-owner-agnostic/`; EU4 saves as engine-produced evidence (`VANILLA_start.eu4`, `Castile1444_12_22.eu4`, and their trade ledgers); the engine's own crash artifacts (`crashes/eu4_20260820_*`) and the surviving probe mods (`mod/pgt_cycle`, `mod/pgt_flip*`). The running game was not among the materials: claims only a live observation can settle are UNTESTABLE with the missing observation named.

**Process.** Seven validators graded disjoint slices in parallel, re-deriving every claim from sources. Every REFUTED and PARTIAL verdict — and every boundary case — was then re-verified by the coordinating auditor directly against the primary sources before entering this file; rows carrying "rechecked" notes record that re-derivation. Twelve verdicts changed under recheck, including one validator REFUTED downgraded to PARTIAL (Y992) and two UNTESTABLEs settled by engine crash artifacts the validator had not found (Y651, Y652).

**Verdicts.**

| verdict | claims |
|---|---|
| CONFIRMED | 873 |
| PARTIAL | 41 |
| REFUTED | 0 |
| UNTESTABLE | 45 |
| **total** | **959** |

Per section:

| § | CONFIRMED | PARTIAL | REFUTED | UNTESTABLE | total |
|---|---|---|---|---|---|
| §0 | 60 | 3 | 0 | 1 | 64 |
| §1.1 | 69 | 2 | 0 | 2 | 73 |
| §1.2 | 6 | 0 | 0 | 0 | 6 |
| §1.3 | 73 | 6 | 0 | 2 | 81 |
| §1.4 | 5 | 0 | 0 | 0 | 5 |
| §1.5 | 20 | 0 | 0 | 0 | 20 |
| §1.6 | 81 | 6 | 0 | 0 | 87 |
| §1.7 | 16 | 0 | 0 | 0 | 16 |
| §1.8 | 6 | 1 | 0 | 6 | 13 |
| §1.9 | 8 | 1 | 0 | 0 | 9 |
| §1.10 | 38 | 1 | 0 | 0 | 39 |
| §1.11 | 4 | 0 | 0 | 0 | 4 |
| §1.12 | 4 | 3 | 0 | 0 | 7 |
| §2.1 | 27 | 0 | 0 | 0 | 27 |
| §2.2 | 22 | 1 | 0 | 0 | 23 |
| §2.2a | 17 | 0 | 0 | 0 | 17 |
| §2.3 | 40 | 4 | 0 | 0 | 44 |
| §2.4 | 26 | 2 | 0 | 0 | 28 |
| §2.5 | 3 | 0 | 0 | 1 | 4 |
| §2.6 | 6 | 0 | 0 | 4 | 10 |
| §2.7 | 11 | 1 | 0 | 14 | 26 |
| §2.8 | 40 | 1 | 0 | 8 | 49 |
| §2.9 | 7 | 0 | 0 | 1 | 8 |
| §3.1 | 7 | 0 | 0 | 0 | 7 |
| §3.2 | 37 | 1 | 0 | 0 | 38 |
| §3.3 | 18 | 0 | 0 | 0 | 18 |
| §3.4 | 5 | 0 | 0 | 0 | 5 |
| §3.5 | 17 | 0 | 0 | 2 | 19 |
| §3.6 | 22 | 2 | 0 | 0 | 24 |
| §3.7 | 4 | 1 | 0 | 0 | 5 |
| §3.8 | 14 | 0 | 0 | 0 | 14 |
| §3.9 | 22 | 0 | 0 | 0 | 22 |
| §3.10 | 18 | 1 | 0 | 0 | 19 |
| §3.11 | 11 | 1 | 0 | 0 | 12 |
| §3.12 | 6 | 1 | 0 | 0 | 7 |
| §3.13 | 31 | 1 | 0 | 3 | 35 |
| §3.14 | 14 | 0 | 0 | 1 | 15 |
| §3.15 | 35 | 0 | 0 | 0 | 35 |
| §3.16 | 23 | 1 | 0 | 0 | 24 |

---

## §0 — front matter (64 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y207 | Target build is EU4 1.37.5 Inca | CONFIRMED | file read: `launcher-settings.json` | `"version": "EU4 v1.37.5.0 Inca (491d)"`, `"rawVersion": "v1.37.5.0"` |
| Y208 | Design is extended-timeline compatible | UNTESTABLE | searched materials for an ET check | no Extended Timeline install/mod among primary sources, and no date-range logic in `solver.py`/`drain.py` to test against; would need an ET install or a named compatibility check |
| Y209 | Design targets connected maps only | CONFIRMED | file read: spec §1.1 Reachability bullet + §2.2a | disconnected map called "infeasible outright" (line 200-202); §2.2a (line 995) exists and states "Premise 1 — the node graph is connected" with explicit solver behaviour on violation |
| Y210 | Supersedes v1.3 in `../v1-laplacian/` | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md` | header `**Version:** 1.3` at that path |
| Y211 | v1 oriented each good by a Laplacian potential | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:17` | "For each good `g`, solve over the unweighted graph Laplacian `L`" |
| Y212 | v1 sink placement topological not economic | CONFIRMED | file read: `v1-laplacian/diagnosis.md` §7 verdict | "C: confirmed, dominant... the field's shape is set by supply geometry; demand only chooses among nodes the geometry has already flattened" — sinks are local minima of `(c-s)/deg`, not a function of demand |
| Y213 | Four-operator bake-off replaced core with DRAIN | CONFIRMED | file read: `v2-drain/drain-orientation.md:1-4` | "tested against the Laplacian... the strongest candidate of the four tested" — DRAIN vs FLOW, RANK, BASIN against the LAP baseline |
| Y214 | Every settleable v1 claim-audit correction folded in | PARTIAL | file read: `v1-laplacian/validation.md` (23 REFUTED + 37 PARTIAL = 60 corrections), spot-checked 2 | C037/C038's "75% overseas autonomy floor" text is fully absent from the current wealth model (autonomy excluded entirely) — fold-in confirmed on this sample; the universal over all 60 corrections was not exhaustively re-checked — [rechecked: coordinator extended the sample to 7 of 60 (C037, C038, C049 gold invisible to wealth at L457-463, C101 no trade supply range at L778, C128 50/40 at L814, C130 50/50 at L815, C132 reversal at L817-820) — all folded in; the universal over all 60 remains unexhausted] |
| Y215 | v2.1 replaced installed aggregate with `Φ_w` | CONFIRMED | file read: spec.md:10-11 + §3.15 graveyard entry | current wording "v2.1 replaces the installed aggregate with `Φ_w`" matches §3.15's "A value-weighted aggregate of the per-good marking orders as the installed graph. *(v2.0 entry, superseded in v2.1.)*" |
| Y216 | Keeps v3.0's owner-agnostic wealth | CONFIRMED | file read: `v3-owner-agnostic/per-good-trade-spec.md:14` | "v3.0 makes three changes. (a) Wealth becomes owner-agnostic" |
| Y001 | v6.0 makes owner-agnosticism true by construction | CONFIRMED | file read: spec.md:264 (§1.3) + `solver.py` | "Owner-agnosticism is true by construction here, not by a rule that has to be policed"; no two-test-classifier code exists, `STATE_TAX_MOD = {}` |
| Y002 | Substantive change is §1.3: wealth = f(dev, good, condition) | CONFIRMED | file read: spec.md:257 | "Wealth is owner-agnostic, and it reads three things about the province: its development, its trade good, and its own current condition" |
| Y003 | Two-test classifier and everything it governed deleted | CONFIRMED | file read: `solver.py` `province_table()`/`build_sc()` | no classifier code; no buildings/great-projects/centres-of-trade inputs; only `base_tax`, `base_production`, `devastation`, trade good |
| Y004 | Two-test classifier is v4.0's; v3.0 used a structural rule | CONFIRMED | file read: `v3-owner-agnostic/spec.md:163` vs `v4-owner-agnostic/spec.md:186` | v3: "`province = {…}` block... `modifier = {…}` block" (structural); v4: "is local... enters wealth" (two-test) |
| Y005 | Deleted apparatus worth 105.30 ducats, 0.98%/0.99% | CONFIRMED | measurement: `python measure6.py` | `deleted apparatus in ducats 105.3`, `apparatus pct of on-total 0.98`, `apparatus pct of v6 field 0.99` |
| Y006 | Classification wrong in both independent audits | CONFIRMED | file read: `v3-owner-agnostic/validation-v3.md` W041 + `v5-owner-agnostic/validation-v5.md` X035 | both REFUTED; X035: "the enumeration is incomplete and miscounts" |
| Y007 | Three start-state reads corrected (on_startup devastation, dated add_base_*, is_city filter) | CONFIRMED | file read: `solver.py` `ON_STARTUP_DEVASTATION`/`province_table()`; `scripts/provinces.py:37-51` | dated `add_base_*` accumulation logic present (M1b comment); is_city not required; on_startup devastation dict populated |
| Y965 | v6.1 changes the operator, not the field | CONFIRMED | file read: spec.md:27-47 (v6.1 paragraph) | text exclusively describes Phase-2 tie-break/tolerance/`α_Φ` changes; no wealth/supply/demand formula change mentioned |
| Y008 | Phase 2 degenerate under unit arc costs | CONFIRMED | measurement: `python relabel6.py 45` | "unit cost (former)... permutations returning a DIFFERENT optimal support: 40 of 40" |
| Y966 | §2.3 breaks tie in two terms, one design-intent one generic | CONFIRMED | file read: `flowop.py` `TIE_COST` formula | first term (wealth-based) commented as carrying design intent ("wealth as a destination"); second (`_gen` modf term) commented "carries no design meaning" |
| Y1000 | §2.3 pins optimality tolerance as a correctness requirement | CONFIRMED | file read: `flowop.py` `LP_OPTS` comment; measurement: `p3_perm.py` | comment: "correctness requirement, not a performance knob"; default-tolerance solves differ by up to 7.66e-10 relative, six orders above float noise |
| Y1001 | Margin 3.8e-8 vs HiGHS default 1e-7 | CONFIRMED | measurement: `python p3_margin.py`; file read: scipy `_linprog_highs.py` docstring | `full ... worst per-good margin 3.765e-08`; docstring: "Default is 1e-07" for both feasibility tolerances |
| Y967 | 0/180 aggregate, 0/290 per-good relabellings unchanged | CONFIRMED | measurement: `python relabel6.py 45` (180 aggregate trials); `python p3_relabel_pergood.py` (290 per-good trials) | "orientation changed: 0 of 180"; "full cost, LP_OPTS 1e-10 per-good: 0 of 290 runs move... aggregate 0 of 10" |
| Y1002 | Unchanged under LP column-order permutation | CONFIRMED | measurement: `python p3_perm.py` | at `LP_OPTS`: "aggregate: flips [0,0,0,0,0,0]... goods/aggregate with any flip over 6 permutations: 0 of 30" |
| Y968 | Canonical node order still an emitter requirement (measurement not proof) | CONFIRMED | file read: spec.md:34-36; cross-ref §2.1 table labels these rows "Measured" | matches the empirical (not proved) status of the 0/180, 0/290, 0-flip results above |
| Y969 | `α_Φ` moves from 1.5 to 2.0 | CONFIRMED | file read: `fixes-agreed.md` (v6.0, `α_Φ = 1.5` throughout) vs `per-good-trade-spec-v6.1-frozen.md`/current spec (`α_Φ = 2.0`) | v6.0 checklist "α_Φ = 1.5's band is [1.38, 1.63]..."; v6.1-frozen and current both "α_Φ = 2.0" |
| Y970 | `α_Φ`/`TIE_EPS`/`TIE_EPS2` are developer taste, no justification offered | CONFIRMED | file read: spec.md:592-593 | "The choice is developer taste, and this document offers no justification for any of them beyond that" |
| Y971 | Every derivation for `α_Φ` withdrawn without replacement | CONFIRMED | file read: spec.md:594; `fixes-agreed.md:111` | "No derivation is claimed, none is implied"; v6.0's own prior "widest band" justification already called "false on both fields" |
| Y972 | 1444 sink set moves `{english_channel,hangzhou}`→`{genua,hangzhou}` | CONFIRMED | file read: `fixes-agreed.md` (α=1.5: `english_channel`+`hangzhou`); measurement: `measure6.py` (α=2.0: `genua`+`hangzhou`) | `fixes-agreed.md`: "`Φ_w` sinks `english_channel`... `hangzhou`..."; `measure6.out`: `Phi_w sinks ['genua', 'hangzhou']` |
| Y1058 | Sink-set move carries every aggregate-derived figure | CONFIRMED | file read: spec.md:41-43; structural cross-check | ranks/sources/bands/European-scaling are all computed downstream of the same aggregate DRAIN solve in `measure6.py` |
| Y1059 | What holds: wealth field, totals, price census, per-good graphs (`α(g)` doesn't read `α_Φ`) | CONFIRMED | file read: spec.md:43-44; `solver.py`/`flowop.py` `ALPHA` lambda | `ALPHA = lambda g: max(0.2, min(3.0, (PRICES[g]/2.0)**1.0))` — no `α_Φ` term |
| Y1060 | Per-good `α(g)` does not read `α_Φ` | CONFIRMED | file read: `solver.py`/`flowop.py` `ALPHA` definition | same as above — price-only formula |
| Y1061 | Quoted figure-count not maintained; grows for reasons unrelated to `α_Φ` | CONFIRMED | measurement: `fixes-agreed.md` ("`measure6.py` prints 57 labelled figures") vs current `python measure6.py` ("wrote measure6.out with 66 labelled figures") | count moved +9 (apparatus/tie-break-margin guards added later), unrelated to the `α_Φ` value |
| Y1003 | §2.1 records multiplayer need as build discipline, not a design change | CONFIRMED | file read: spec.md:896-947 (§2.1) | "it is no longer 'vertex selection is machine-dependent' but 'the build discipline is unverified'" |
| Y1062 | v6.2 narrows wealth rule, changes no number on 1444 field | CONFIRMED | file read: `per-good-trade-spec-v6.1-frozen.md:963` vs current `measure6.out` | both give world wealth **10,607.40** |
| Y1063 | `unrest` dropped from §1.3's table, not carried as excluded row | CONFIRMED | file read: spec.md:340-346 | table has exactly 4 rows (devastation/prosperity/under_siege/occupied); `unrest` appears only in prose below, not as a row |
| Y1064 | Both changes follow same reading: wealth = what a province can buy | CONFIRMED | file read: spec.md:51-52 + §1.3 formula | neither `unrest` nor `occupied`'s tax half enters `tax_value` |
| Y1065 | An occupying army is a fact about a war | CONFIRMED | file read: spec.md:54 (definitional) | matches doc's own framing; no further checkable content beyond internal consistency |
| Y1066 | What revolt/occupation cost the owner is real, and §1.3 declines to model it | CONFIRMED | file read: §1.3 formula | excludes `unrest`, national ideas, estate/government modifiers, `occupied`'s tax half — all owner-side effects |
| Y1067 | No province occupied at a start date with no wars | CONFIRMED | save evidence: `VANILLA_start.eu4` gamestate | 0 `active_war=` blocks; 0 occurrences of an `occupier=` field anywhere; the 3 controller≠owner provinces carry `occupying_rebel_faction`, a distinct field from war-occupation |
| Y1068 | What moves is during-campaign behaviour, where v6.2 differs from v6.1 | CONFIRMED | file read: `per-good-trade-spec-v6.1-frozen.md:304,315-318` vs current §1.3 | v6.1's `STATE_TAX_MOD` carried `occupied` (`local_tax_modifier = -0.5` applied during occupation); current `STATE_TAX_MOD = {}` (occupied's tax half "not read") — real divergence mid-campaign |
| Y1069 | Retired `unrest` figures withdrawn not repaired; `revolt_risk` parse gone | CONFIRMED | file read: grep for "12.23"/"9.40"/`revolt_risk` in current spec and in `solver.py`/`drain.py`/`flowop.py`/`measure6.py` | v6.1-frozen's exclusion figures (12.23 ducats, 9.40 ducats, "4 of 159 edges") are absent from current spec; zero `revolt_risk` hits in the reference implementation |
| Y009 | Prose convention: no empirical absolutes | CONFIRMED | file read: spot-check of §0/§1.1 prose; cross-ref `fixes-agreed.md` R2 | consistently hedged ("measured... not a theorem", scoped-to-field observations) throughout the graded slice; no counterexample found in it |
| Y010 | Graveyard covers gravity kernels, v1 Laplacian, RANK, seeded basins | CONFIRMED | file read: spec.md §3.15 headings | "The v1 Laplacian potential...", "Ranked orientation...", "Seeded basin growth...", "Pinned-count wealth fields (top-k seeding, gravity kernels)" all present |
| Y011 | Rejected-operator numbers re-measured/re-refuted in three successive audits | CONFIRMED | file read: `v3-/v4-/v5-owner-agnostic/validation-v*.md` exist as three successive rounds | §3.15 entries note numbers were "measured on a superseded wealth field and each audit spent its effort recounting them" |
| Y220 | Load-bearing comparison stated as direction not figure | CONFIRMED | file read: spec.md §3.15 gravity-kernel entry | "a pure `wealth^α` edge comparison... does not concentrate ends at all... No figures are maintained for it" — directional argument, no maintained figure |
| Y012 | Every v5 validation claim folded through: 22 refuted/39 partial/1 unverifiable | CONFIRMED | file read: `v5-owner-agnostic/validation-v5.md:28-31` | CONFIRMED 134 / REFUTED 22 / PARTIAL 39 / UNVERIFIABLE 1 — exact match |
| Y1070 | `fixes-agreed.md` carries a row for all 62 graded claims | CONFIRMED | file read: `fixes-agreed.md` §5 table | `grep -c "^| X"` over that table = 62 |
| Y1071 | `fixes-agreed.md` frozen at v6.0 | CONFIRMED | file read: `fixes-agreed.md:3` | "FROZEN AT v6.0. This file records what v6.0 changed relative to v5.0 and is not maintained against later versions" |
| Y1072 | Where a `fixes-agreed.md` figure has moved, the spec is the live document | CONFIRMED | file read: `fixes-agreed.md:1-2` states the rule; demonstrated concretely | its world-wealth figure (10,594.70) differs from the spec's current one (10,607.40) — the spec value is the one in force |
| Y1073 | Neither harness targets `fixes-agreed.md` by default | CONFIRMED | file read: `fixes-agreed.md:4-5`; measurement: `python mutate6.py` (no arg) | text: "Neither harness targets it by default"; `mutate6.py` refuses to run without an explicit target |
| Y218 | Deleted text quoted in `changes-v6.md` | CONFIRMED | file read: `changes-v6.md` | multiple `**Removed:**` code-fenced blocks quoting deleted spec prose verbatim (e.g. the R1 classification-rule table, ~line 550) |
| Y217 | Measured figures carry the script that produced them | PARTIAL | file read: spec.md:81-96 | same paragraph immediately qualifies this: "Some figures carry a script attribution instead of a guard, and some carry neither" — the blanket claim has document-acknowledged exceptions — [rechecked: L81 blanket sentence vs L94-95 concession re-read; Y1080's misattribution is a concrete instance] |
| Y013 | `verify6.py` reads figures, fails when they disagree with the install | CONFIRMED | measurement: `python verify6.py ../per-good-trade-spec.md` (36 checks, 0 failed); `python mutate6.py ../per-good-trade-spec.md` (12/12 planted errors caught) | harness passes when consistent and fails on deliberately wrong figures, as designed |
| Y974 | `verify6.py`'s coverage of printed figures is partial | CONFIRMED | measurement: `python coverage6.py` | "coverage: 4 of 7 uniquely-locatable spec figures are protected (57%)"; 29 further figures unscored |
| Y1051 | Neither count nor proportion given, for two reasons; "partial" is as far as it goes | CONFIRMED | file read: spec.md:84-96 | matches exactly — no count/proportion appears in that paragraph |
| Y975 | No count given because checks are generated per matching phrase | CONFIRMED | measurement: `verify6.py` run output | e.g. "every site: connected pairs   all 2 sites carry 5723" — a phrase-count-dependent check, confirming the mechanism |
| Y015 | No proportion because the denominator is ill-defined (279–326) | PARTIAL | measurement: `python val62p1_c.py` (numeric-tokenisation sweep) | reasonable decimal-aware tokenisations give a distinct-count range of roughly 285–359 (full range 170–2,449 across all variants tried); qualitative "ill-defined" point fully confirmed, but the exact bounds 279/326 were not reproduced by any tried pattern and no script in `scripts/` computes them by name — [rechecked: coordinator's three tokenisations give 307/261/130 unique (1817/1208/316 raw) — endpoints 279/326 not reproduced by any construction tried] |
| Y1052 | Earlier draft said "well under half" then refused a ratio | CONFIRMED | file read: `scripts/v61p.py` old-text patch | prior draft literally read "...covers well under half of what the document prints. No count is given here..." two sentences apart |
| Y016 | `coverage6.py` corrupts every spec-printed figure regardless of guard | CONFIRMED | file read + measurement: `python coverage6.py` | source and output directly implement/demonstrate CAUGHT/MISSED/NO SITE/unscored categories |
| Y017 | Some figures carry script attribution instead of a guard, some carry neither | CONFIRMED | measurement: `coverage6.py` output | shows exactly this three-way split (12 guarded, 3 MISSED + 2 NO SITE unguarded-but-computed, 29 further unscored) |
| Y1074 | A figure with no script named has not been reproduced by anything in `scripts/` | CONFIRMED | derivation (definitional) | tautological given "reproduced by `scripts/`" is defined by script attribution |
| Y018 | `mutate6.py`'s higher score is not coverage; it only checks already-guarded figures | CONFIRMED | measurement: `python mutate6.py ../per-good-trade-spec.md` | "caught 12 of 12 planted errors" — all mutations land on figures `verify6.py` already guards, vs `coverage6.py`'s 57%/unscored figures |
| Y219 | Document has three sections: §1/§2/§3 | CONFIRMED | file read: spec.md headings | `# 1. Mechanics` (105), `# 2. Implementation` (885), `# 3. Reasoning` (1403) |

## §1.1 — Trade direction (73 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y221 | Every good has its own directed network over the same adjacency | CONFIRMED | file read: `drain.py` `run_drain()` | computes a per-good directed graph over the shared `EDGES_UND` adjacency independently per good |
| Y222 | Direction is computed, never authored | CONFIRMED | file read: `drain.py` | no hardcoded/authored edge directions; all orientation follows from phase0/2/3 balance-driven logic |
| Y223 | Balance `b_g=s_g-c_g`; DRAIN peel/select/route/sweep | CONFIRMED | file read: `drain.py` `run_drain()` | calls `phase0` (peel), `phase1` (select), `phase2` (route), `sweep_priority` (sweep) in that order |
| Y224 | Phase 0 determines each pendant edge's orientation from subtree balance sign | CONFIRMED | file read: `drain.py` `phase0()` | matches spec.md:114-116 exactly |
| Y225 | Phase 0 exact: removed edges are bridges, tree flow determined by conservation | CONFIRMED | derivation (graph theory) | any edge incident to a degree-1 node is necessarily a bridge; flow on a tree with fixed node balances is uniquely determined by conservation, matching `phase0`'s peeling |
| Y226 | Vanilla Phase 0 is a no-op: min degree 2, zero bridges | CONFIRMED | measurement: `python props6.py` | "min degree 2 max 8 mean 3.98", "components=1 bridges=0" |
| Y227 | Phase 0 exists for modded maps | CONFIRMED | file read: `drain.py` `phase0()` | graph-generic (any adjacency with degree-1 nodes), not vanilla-specific |
| Y228 | Phase 1: cluster net demanders, HHI, `k=clamp(round(1/HHI),1,\|clusters\|)`, heaviest per top-k | CONFIRMED | file read: `drain.py` `phase1()` (lines 52-102) | implements exactly this |
| Y229 | Phase 1 carries one knob: dilation radius `r` (default 0) | CONFIRMED | file read: `drain.py` `phase1(core, beta, dilate_r=0)` signature | only one tunable parameter beyond `core`/`beta` |
| Y1075 | Dilation radius `r` links demanders within `r` hops before clustering | CONFIRMED | file read: `drain.py` `phase1()` dilation block (lines 71-92) | BFS-merges clusters whose demander members are within `r` hops |
| Y230 | `k=1` for 27 of 29 goods at defaults | CONFIRMED | measurement: `props6.py` | "Phase1 k==1 on 27 of 29 goods (k values Counter({1: 27, 2: 2}))" |
| Y231 | Phase 1 selection deliberately weak; Phase 3 self-corrects upward | CONFIRMED | measurement: `measure6.out` | Phase-1 selection 1 node vs final 2-sink `Φ_w` set (1 promotion); per-good mean 3.69 sinks vs mostly-k=1 selection |
| Y1076 | `ρ` not shipped; `drain.py`'s Phase 1 clusters every demander | CONFIRMED | file read: `drain.py` `phase1()` | `Dset` = all core nodes with `beta<0`, no quantile filter anywhere |
| Y1077 | §3.13 calibration carries its own Phase 1 implementing a quantile | CONFIRMED | file read: spec.md §3.13 "sink-count-span option" | "a demand-mass quantile `ρ = 0.5` in its own Phase 1" |
| Y232 | Phase 2 solves uncapacitated min-cost flow serving `b_g`, orients by net flow | CONFIRMED | file read: `drain.py` `phase2()` | calls `mincost_flow(b, 0, cost=TIE_COST)`, orients by sign of net flow |
| Y976 | Phase 2 costs near-unit, symmetric, read from node wealth | CONFIRMED | file read: `flowop.py` `TIE_COST` | `1 + TIE_EPS·(w[u]+w[v])/2 + …` — symmetric in u,v, wealth-derived, near 1 |
| Y977 | Costs not unit because unit optimum non-unique/order-dependent | CONFIRMED | measurement: `relabel6.py` | unit-cost objective: "permutations returning a DIFFERENT optimal support: 40 of 40" |
| Y233 | Support is spanning-tree basis ≤N-1 edges for a basic (vertex) optimum | CONFIRMED | measurement: direct LP re-solve (`method="highs"`, crossover on) of aggregate `b_w` | support size 79 = N−1 exactly (core size 80) |
| Y234 | Interior-point without crossover can split flow, giving an undirected cycle | CONFIRMED | measurement: forced `method="highs-ipm"`, `run_crossover="off"` | support size 119 (>N−1=79) and an undirected cycle detected in it |
| Y235 | §2.2 requires network simplex or simplex LP | CONFIRMED | file read: spec.md:963-964 | "network simplex or a simplex LP, not interior-point without crossover" |
| Y1004 | §2.3 requires tolerance tighter than the tie-break margin | CONFIRMED | file read: `flowop.py` `LP_OPTS=1e-10` (tighter than the measured 3.8e-8 margin and HiGHS's 1e-7 default) | consistent with §2.1's table |
| Y236 | For any optimum, support has no directed cycle | CONFIRMED | derivation (standard min-cost-flow fact) + measurement | even the degenerate IPM/no-crossover support (with an undirected cycle) has zero directed cycle when oriented (`drain.has_cycle` → `None`) |
| Y237 | Zero-net-flow edges are free, deferred to Phase 3 | CONFIRMED | file read: `drain.py` `phase2()` | `else` branch appends to `free` when `\|phi\|<=ZERO_TOL` |
| Y238 | Phase 3 Kahn-style readiness rule | CONFIRMED | file read: `drain.py` `sweep_priority()` `ready()` | matches the spec's ready-condition exactly |
| Y239 | Priority key (DEF ascending, b ascending, index) | CONFIRMED | file read: `drain.py` `run_drain()` calls `sweep_priority(..., "defasc_beta")` | `keyfn = (DEF[v], beta[v], pid[v])` |
| Y240 | Flow-arc subgraph acyclic and fixed before free edges; `DEF` has no circularity | CONFIRMED | file read: `drain.py` `flow_def()` | topological sort over the flow-arc-only subgraph, fixed before any free-edge orientation |
| Y241 | Stall promotes heaviest flow-terminal demander | CONFIRMED | file read: `drain.py` `sweep_priority()` | `terminals=[u ... len(outs[u])==0 and inflow[u]>ZERO_TOL]; s_star=min(terminals,key=(beta[v],v))` |
| Y242 | Fallback promotes highest-wealth candidate, ties by index | CONFIRMED | file read: `drain.py` | `s_star = max(gated, key=lambda v:(NODEW[v],-v))` |
| Y243 | Node wealth good-independent; fallback needs no bootstrap | CONFIRMED | file read: `drain.py` `NODEW` | computed once at module level from `ROWS`, independent of any per-good `b` |
| Y244 | Candidates = unmarked nodes whose flow out-neighbours are all marked | CONFIRMED | file read: `drain.py` | `gated=[u for u in core if u not in marked and cnt[u]==0]` |
| Y245 | Candidate with a flow out-arc is ready; candidate with inflow is flow-terminal demander | CONFIRMED | derivation from code | matches `ready()`/`terminals` logic directly |
| Y019 | Fallback fires only when every candidate is support-isolated, zero post-peel balance | CONFIRMED | derivation from `drain.py` structure + measurement: `toys.py` T3 | T3 constructs exactly this condition and fires the fallback |
| Y020 | Priority key reads post-peel folded balance, not raw `b` | CONFIRMED | file read: `drain.py` `phase0()` folds pendant balance into parent; `sweep_priority` keys on the returned `beta` | not the original `b` argument |
| Y021 | Connected core needs folded balance to vanish across the whole core for the fallback | CONFIRMED | derivation from spec's own math + `measure6.out` | aggregate node sums are non-uniform (0-72 provinces/node), so the aggregate fallback condition is not trivially satisfied |
| Y022 | 0–72 counted provinces per node | CONFIRMED | measurement: `python val62p1_a.py` | "provinces per node: min 0 max 72; zero-count nodes: ['cape_of_good_hope']" |
| Y023 | Wealth-key tie → node index decides | CONFIRMED | file read: `drain.py` | `max(gated, key=lambda v:(NODEW[v],-v))` — ties on NODEW broken by index |
| Y024 | §2.8 containment includes fallbacks because of T3 | CONFIRMED | file read: spec.md §2.8 | "Asserting containment in `{selected} ∪ {promoted}` alone would halt on **T3**... so the fallback set is part of the assertion" |
| Y246 | Free edges orient later-marked → earlier-marked | CONFIRMED | file read: `drain.py` `compile_dirs()` | `directed.append((u,v) if order[u]>order[v] else (v,u))` |
| Y247 | Phase 4 un-peels Phase-0 pendants in reverse order | CONFIRMED | file read: `drain.py` `compile_dirs()` | `for (v,u,bv) in reversed(Plog): ...` — `Plog` built in peel order, reversed to un-peel |
| Y1078 | Phase 4 emits the orientations Phase 0 determined | CONFIRMED | file read: `drain.py` `compile_dirs()` | Phase 4 only replays `Plog`'s sign decisions; Phase 0 already computed them |
| Y1079 | Pendant sink visible only after Phase 4 | CONFIRMED | derivation from `compile_dirs()` structure | pendant edges are absent from `directed` until the reversed-`Plog` loop runs |
| Y248 | Each property labelled proved/measured/true-by-construction, never conflated | CONFIRMED | file read: spec.md properties bullets (182-231) | each explicitly says "Measured:", gives a proof argument, or "carries no measurement and wants none" |
| Y250 | §1.1 property measurements regenerated for v6.0 by `measure6.py` | CONFIRMED | measurement: `measure6.py` exists and reproduces the cited figures | acyclic-goods, sinks-per-good, connectivity-pct etc. all present in its output |
| Y249 | Labelling discipline caught four over-claims v2.0→v3.0 | UNTESTABLE | searched `v2-drain/`, `v3-owner-agnostic/` for an enumeration | found only the same unsourced restatement already verbatim in v3's own spec (`claims-v3.md` W011, provenance "derivation", no citation); no file names which four |
| Y251 | Global DAG: later→earlier marked, pendant edges are bridges, no cycle | CONFIRMED | derivation (same as Y246+Y225) + measurement | acyclic 29/29 (`props6.py`) |
| Y252 | Measured acyclic 29/29 goods | CONFIRMED | measurement: `props6.py` | "acyclic goods: 29 of 29" |
| Y253 | Every sink is one of four kinds | CONFIRMED | file read: `drain.py` sink sources match exactly; measurement: `toys.py` | T1 (pendant), T2 (stall-adjacent), T3 (fallback) all constructed as real cases |
| Y025 | 1444: fallback/pendant empty, sink set exact, 29/29, 2-8 per good mean 3.69, zero fallbacks | CONFIRMED | measurement: `props6.py`/`measure6.out` | "sink-set identity holds on 29 of 29 goods; failures []", "fallbacks fired: 0"; "sinks per good min/max/mean (2, 8, 3.69)" |
| Y026 | Equality is a measurement not a theorem; v2 asserted it as one | CONFIRMED | file read: `v2-drain/per-good-trade-spec.md:61` | "Sink placement is explicit: the final sinks are exactly `{…}`" stated unconditionally, unlike v6.2's explicit "measured, not proved" hedge |
| Y027 | Doesn't become a theorem with conditions; T2 satisfies both and still breaks it | CONFIRMED | measurement: `python toys.py` T2 | core-only (no pendants), zero fallback promotions, `EQUAL: False` |
| Y254 | Three constructed cases (T1/T2/T3) break sink-set equality | CONFIRMED | measurement: `python toys.py` | T1, T2, T3 all reproduced with `EQUAL: False`, matching the described mechanisms exactly |
| Y255 | Node with no outgoing links for `g` is a sink for `g`; sinks differ per good; no global end node | CONFIRMED | measurement: `python val62p1_a.py` | "distinct per-good sink sets: 27 of 29 goods... nodes that are sinks for ALL goods: []" |
| Y256 | Orientation serves 100% of demand; LP node balance + `Σb_g(n)=0` identically | CONFIRMED | derivation | `S`,`C` are world shares summing to 1 each in `solver.py` `build_sc()`, so `Σ(S-C)=0` identically |
| Y257 | Connectedness needed for LP feasibility; disconnected + imbalance is infeasible | CONFIRMED | measurement: `python toys.py` T5 | disconnected graph, per-component imbalance → "LP success: False... Infeasible" |
| Y258 | §2.2a states connectedness requirement and solver behaviour on violation | CONFIRMED | file read: spec.md §2.2a (995) | "Premise 1 — the node graph is connected... What the solver does about it: compute components once at load..." |
| Y259 | Measured 1444: 100.0% reachable, 29/29, zero orphan sinks | CONFIRMED | measurement: `props6.py` | "demand reachable: min 100.0000% max 100.0000%; orphan sinks 0" |
| Y260 | Ready-marking monotone closure → provably scheduler-independent | CONFIRMED | derivation from code (`ready()` depends only on marked-set/support graph) + measurement | `props6.py`: "0 orientation changes over 145 trials" |
| Y261 | Free-edge direction deterministic, same closure + index tiebreak | CONFIRMED | derivation from `drain.py` code | `ready()` depends only on the marked-set closure; ties broken by `pid` index in `keyfn` |
| Y262 | Free-edge direction is a function of graph+balances alone: 0/145 scheduler-permutation changes | CONFIRMED | measurement: `python props6.py` | "scheduler permutations: 0 orientation changes over 145 trials (29 goods x 5)" — exact match |
| Y1005 | Zero exact `(DEF,b)` key collisions across all 2,320 core nodes | CONFIRMED | measurement: `props6.py` | "core nodes summed over goods: 2320; exact (DEF,b) key collisions: 0" |
| Y1006 | Phase 1's within-cluster argmin and top-k cut are untied | CONFIRMED | measurement: `props6.py` | "Phase-1 within-cluster argmin ties: 0; top-k cut ties: 0" |
| Y263 | Certificate flow near-fewest-hop; unit-cost objective = `Σ(flow×hops)` | CONFIRMED | derivation | with unit arc cost, min-cost-flow objective is literally total flow-hops, a standard restatement |
| Y978 | Efficiency bound is the interval `[1, 1+TIE_EPS+TIE_EPS2]` itself | CONFIRMED | derivation from `flowop.py` `TIE_COST` | max extra cost `< TIE_EPS·1 + TIE_EPS2·1 = TIE_EPS+TIE_EPS2` by construction |
| Y264 | No per-unit shortest-path claim; a unit may detour | CONFIRMED | file read: spec.md:226-227 (design scoping) | direct statement, consistent with aggregate (not per-unit) LP optimisation |
| Y265 | Efficiency property carries no measurement, follows from LP construction | CONFIRMED | file read: spec.md:228-229 | "This one carries no measurement and wants none: it follows from the construction of the LP" |
| Y266 | §3.13 calibration deliberately degrades efficiency (program change, not evidence) | CONFIRMED | file read: spec.md §3.13 "sink-count-span option" | unclamped `α`, quantile `ρ=0.5`, twig tolerance 3e-4 explicitly change the objective/config; "not adopted" |
| Y267 | Recomputed on fixed monthly tick aligned to vanilla trade tick | UNTESTABLE | searched `common/defines.lua` and related files for a trade-tick period | no emitter/DLL exists yet to check against (§2.1's own admission); vanilla's recompute cadence is hardcoded engine behaviour, absent from any moddable file found |
| Y268 | Orientation read fresh each time, no memory of previous solve | CONFIRMED | file read: `drain.py` `run_drain()` | pure function of its `b` argument; no persistent/global mutable state carried between calls |
| Y269 | LP deterministic on one machine/build: six identical solves → one orientation | CONFIRMED | measurement: `python val62p1_a.py` | "six identical aggregate solves -> distinct orientations: 1"; same for spices |
| Y1082 | Six solves = single-process test, blind to inter-process variation; `fingerprint6.py` covers that separately | CONFIRMED | file read: `scripts/fingerprint6.py` docstring | "One run hashes ONE process. Repeat under different PYTHONHASHSEED values to test what a single process cannot see" |
| Y270 | Across machines, LP determinism is the open question of §3.13 | PARTIAL | file read: spec.md §3.13 "Multiplayer build discipline" bullet | that bullet explicitly states "Not LP pivot determinism, which §2.1 retires" — the open item is build/CPU-dispatch discipline, not "LP determinism" per se, so the label conflicts with §3.13's own framing — [rechecked: §3.13 L1879-1884 re-read — 'Not LP pivot determinism, which §2.1 retires'; the open items are build/CPU-dispatch bit-identity and DLL fidelity] |
| Y1080 | Attribution: `measure6.py` for core-node count; `props6.py` for permutations/ties/reachability/orphan-sinks | PARTIAL | measurement: grepped `measure6.py`/`measure6.out` for "core node"/"2320" (zero hits); `props6.py:126` computes it | the core-node count (2,320) is computed only by `props6.py`, not `measure6.py` — the first half of the attribution is misattributed; the `props6.py` half is correct — [rechecked: grep confirms measure6.py and measure6.out carry no core-node count; props6.py:126 computes it] |
| Y1081 | `props6.py` renamed from a round-5 file; permutation loop written for this citation | CONFIRMED | file read: `props6.py` docstring ("Per-good structural battery for the round-5 audit"); searched v2-/v3-/v4- scripts for the "145" permutation figure | no prior-version script computes 29×5 scheduler permutations |

## §1.2 — Supply (6 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y271 | `s(n,g) = goods_produced(n,g) / Σ goods_produced(m,g)` | CONFIRMED | file read | `scripts/solver.py:176-179` `build_sc`: `live = world > 0; S[live] = gp[live] / world[live][:, None]`, exact match to the formula at spec line 243. |
| Y272 | `goods_produced` is pre-production-efficiency, pre-autonomy | CONFIRMED | file read | `scripts/solver.py:154-156` computes `gp = max(0, GOODS_PRODUCED_FACTOR * base_production * (1+gmod))` — no production-efficiency or autonomy term anywhere in the province table. |
| Y273 | `goods_produced` moves with devastation/occupied/prosperity via `00_static_modifiers.txt` | CONFIRMED | file read | `common/static_modifiers/00_static_modifiers.txt` lines 433-486: `occupied`, `under_siege`, `devastation`, `prosperity` blocks all carry `trade_goods_size_modifier`. |
| Y274 | No regularizer; v1 mixed in `s ← (1-ε)s + ε/N` | CONFIRMED | file read | `../v1-laplacian/per-good-trade-spec.md:43-46`: "A regularizer is mixed in on every solve: `s ← (1 − ε)·s + ε/N` ε ≈ 10⁻⁶". `solver.py`'s v6 `build_sc` (used unregularized, `eps=0.0`, by `measure6.py`/`verify6.py`) carries no such term in the live field. |
| Y275 | Free edges oriented combinatorially by the sweep, not by comparing near-equal potentials | CONFIRMED | file read | `scripts/drain.py:143-176` `sweep()`: node-marking test is purely graph-structural (`cnt[u]==0`, `u in Sset`, free-edge adjacency to already-marked nodes) — no potential/φ value is read or compared anywhere in the function. |
| Y276 | One node has `b=0` exactly at 1444 — `cape_of_good_hope` — handled as ordinary conduit | CONFIRMED | measurement: ran `build_sc` with `ALPHA=lambda g: max(0.2,min(3.0,(price/2)^1))`, checked `S-C==0` across all 29 live goods | `scripts/val62p2_b0check.py`: `cape_of_good_hope` is the only node with `b==0`, and it is `b==0` for all 29 of 29 live goods; no other node is ever zero. Matches `drain.py`'s own docstring: "`beta(v)==0`... occurs at exactly one node (`cape_of_good_hope`) for all 29 goods." |

## §1.3 — Demand (81 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y277 | Demand assembled per province, then summed to the node | CONFIRMED | file read | `scripts/solver.py:183-190` `build_sc`: per-province `wealth`/`w` array, then `np.add.at(C[gi], pn, w/tot)` sums into node index `pn`. |
| Y028 | Wealth is owner-agnostic; reads development, trade good, condition | CONFIRMED | file read | `solver.py` `province_table()`: wealth built solely from `base_tax`, `base_production`, `good`(price), and `devastation` state — no owner-keyed term. |
| Y278 | Wealth is a property of the place | CONFIRMED | file read (design conformance) | Same: `wealth()` takes no owner parameter in `province_table()`; `s["owner"]` is carried in the row only for bookkeeping, never multiplies into `tax`/`prod_income`. |
| Y279 | Wealth reads no autonomy, production efficiency, national ideas, estate/government modifiers, technology | CONFIRMED | file read | `solver.py province_table()`/`wealth()` reads only `base_tax`, `base_production`, trade good and the four `STATE_GOODS_MOD` condition terms — no autonomy, production-efficiency, national-ideas, estate/government or technology field appears anywhere in the wealth path. |
| Y029 | Same dev/good/condition ⇒ same wealth whoever owns it | CONFIRMED | file read | Formula is owner-independent by construction (see Y028/Y278). |
| Y280 | A province's wealth does not change when conquered | CONFIRMED | file read | `wealth()` has no owner argument; conquest alone (ownership transfer without a condition change) leaves `tax`/`prod_income` unchanged in the code. |
| Y030 | Owner-agnosticism true by construction, not policed; v3-v5's two-test classifier wrong in both audits | CONFIRMED | file read across versions | `../v4-owner-agnostic/validation-v4.md:63` documents the "two-test procedure — local... value..." repair; `../v5-owner-agnostic/validation-v5.md:386` "W041 → §1.3's two-test table" (refuted); `../v5-owner-agnostic/fixes-agreed.md:173` "Deleted outright: the two-test classification rule". `apparatus6.py`'s docstring: "both audits' refutations (W041, X035) were of the classifier." |
| Y031 | `base_tax`, `base_production`, trade good are bare attributes needing no classification | CONFIRMED | file read | `solver.py province_table()` reads these three fields straight off `PROV[pid]` with no lookup table or classifier function. |
| Y032 | What's given up: gems' `local_tax_modifier`, incense's `trade_value_modifier`, no longer read | CONFIRMED | file read | `scripts/apparatus6.py:23-24`: `LOCAL_TAX_MOD={"gems":0.15}`, `LOCAL_TV_MOD={"incense":0.10}` — frozen, v5.0-only constants, absent from `solver.py`'s live wealth path. |
| Y033 | Apparatus live on 89 of 2,472: 43 gems + 31 incense + 16 great-project/permanent, minus 1 overlap | CONFIRMED | measurement: `scripts/val62p2_counts.py` | Output: `gems counted: 43 incense counted: 31 mon/perm counted: 16 overlap: {542} union: 89`. |
| Y034 | Count is field-dependent: 87 under withdrawn `is_city` filter; 89 not 88 because province 4856 rolled incense | CONFIRMED | measurement, same script | `apparatus provinces under is_city filter: 87`; `4856 rolled: incense` (history `trade_goods=unknown` at 4856, engine rolled `incense`). |
| Y1083 | Deleted-apparatus figures reproduced by `apparatus6.py`, frozen classifier constants | CONFIRMED | file read | `apparatus6.py` docstring: "FROZEN TABLE of twenty-two constants, copied verbatim from `../../v5-owner-agnostic/scripts/solver.py:59-75`"; reproduces "10,607.40 / 10,712.70 / 105.30 / 89" exactly, matching `measure6.out`. |
| Y1084 | Frozen constants record v5.0's worth, sit in own file so nothing wires them back | CONFIRMED | file read | `apparatus6.py` is a standalone file; `solver.py` (the live wealth path) does not import it — only `measure6.py` does, for the reporting figure only. |
| Y1085 | `measure6.py` imports the apparatus figures rather than restating | CONFIRMED | file read | `measure6.py:256,258`: "constants live in apparatus6.py so they cannot be wired back... `import apparatus6 as _AP`". |
| Y281 | Model trades fidelity for a classification-question-free input surface | CONFIRMED | file read (design conformance) | `province_table()` contains zero classifier logic — direct field reads only, matching Y031/Y281's framing. |
| Y035 | `goods_produced(p) = GP_COEFF·base_production(p)·(1+Σ state mods)` | CONFIRMED | file read | `solver.py:156`: `gp = max(0, GOODS_PRODUCED_FACTOR*base_production*(1+gmod))`, exact structural match. |
| Y036 | `trade_value(p) = goods_produced(p)·price(good(p))`, no trade-value modifier | CONFIRMED | file read | `solver.py:157`: `prod_income = gp * price` — no modifier term. |
| Y037 | `tax_value(p) = TAX_COEFF·base_tax(p)`, no modifier | CONFIRMED | file read + measurement | `solver.py:158`: `tax=TAX_COEFF*base_tax*(1.0+tmod)` with `tmod=0.0` hardcoded; `verify6.py`: `[PASS] tax term takes no modifier   0 of 2472 counted provinces disagree with TAX_COEFF*base_tax`. |
| Y282 | `wealth(p) = tax_value(p) + trade_value(p)` | CONFIRMED | file read | `solver.py`: rows carry `tax` and `prod_income`; `build_sc` uses `wealth = tax + prod_income`. |
| Y283 | `c(n,g)` = node's wealth^α(g) share of world wealth^α(g) | CONFIRMED | file read | `solver.py:183-190` `build_sc` computes exactly this. |
| Y038 | `GP_COEFF` is a shipped file value from `provincial_production_size` | CONFIRMED | file read | `common/static_modifiers/00_static_modifiers.txt:251-254`: `provincial_production_size = { trade_goods_size = 0.2 ... }`, localised "Base Production" (confirmed in `measure6.out`: "provincial_production_size block ... its localisation Base Production"). |
| Y284 | `GP_COEFF`/`TAX_COEFF` have different provenance | CONFIRMED | file read | `GP_COEFF` read at runtime from a file (`_read_gp_coeff()`); `TAX_COEFF=1.0` is a hardcoded measured constant — different sourcing. |
| Y039 | `GP_COEFF` moddable, read at runtime not hardcoded | CONFIRMED | file read | `solver.py:59-67` `_read_gp_coeff()` opens and regexes the file at import time; not a literal constant. |
| Y040 | `TAX_COEFF` in no file — not defines.lua, not common/defines/, not that modifier block | CONFIRMED | measurement: `grep -rn TAX_COEFF common/` | Zero matches anywhere in the EU4 install; the name is the mod's own, not an engine token. |
| Y285 | Tax/trade terms share a time basis per the engine's tooltips (annual/12) | UNTESTABLE | tooltip observation | Live game display convention; no shipped file states a tooltip's display-time-basis. Would be settled by observing a province window's tax and production tooltips directly. |
| Y041 | Tax tooltip schema `Base: trunc(base_tax×0.0833333) (Yearly base_tax)`; observed 0.49/6.00 and 0.16/2.00 | PARTIAL | measurement (save-confirmed inputs) + arithmetic; live tooltip text not observable | `base_tax(223)=6.000`, `base_tax(1747)=2.000` confirmed in `VANILLA_start.eu4`; `trunc(6×0.0833333)=0.49`, `trunc(2×0.0833333)=0.16` reproduce the claimed numbers exactly. The literal on-screen "Base:"/"(Yearly...)" text cannot be confirmed without launching the game. — [rechecked: save fields and trunc arithmetic re-verified by coordinator; the on-screen strings remain out of reach of the materials] |
| Y042 | Parenthetical is `base_tax` itself, truncated twelfth, not ×12 of displayed figure | CONFIRMED | derivation | `0.49×12=5.88≠6.00`; `0.16×12=1.92≠2.00` — both computed directly, matching the claim's own counter-figures exactly. |
| Y043 | v4.0/v5.0 wrote `Base: X (Yearly 12·X)`, false on its own data; v3.0 has neither | CONFIRMED | file read across versions | `../v4-owner-agnostic/per-good-trade-spec.md:163` and `../v5-owner-agnostic/per-good-trade-spec.md:170` both literally read `Base: X (Yearly 12·X)` — `12×0.49=5.88≠6.00` shown on the same line, self-contradictory. `grep` for this schema in `../v3-owner-agnostic/per-good-trade-spec.md` returns nothing. |
| Y044 | Production tooltip `3.52 → +0.29` fixes the divisor to (11.73, 12.14] | CONFIRMED | derivation | `3.52/0.30=11.733`, `3.52/0.29=12.138`, matching the stated bound exactly; and `3.52` itself reproduces from `goods_produced(223)=0.2×4=0.8`, `price(silk)=4.00` (`common/prices/00_prices.txt:100-102`), `×1.10=3.52`; `3.52/12=0.2933→trunc 0.29`. |
| Y045 | Both monthly figures are annual/12, letting the annual forms add directly | CONFIRMED | derivation | Follows from Y042 (tax) and Y044 (production) both resolving to the annual-over-twelve relation on independently-confirmed base figures. |
| Y286 | Coefficients measured on Garnatah (223: 6/4/silk, autonomy 0) and Caceres (1747: 2/2/wool) | CONFIRMED | measurement [rechecked by coordinator: direct save read] | VANILLA_start.eu4: province 223 = base_tax 6.000, base_production 4.000, trade_goods silk, owner GRA, `local_autonomy` absent; 1747 = 2.000 / 2.000 / wool / CAS with `local_autonomy=3.500` **present** — the same save writes the field when nonzero and omits it at zero, so 223's autonomy 0 is established by the file's own convention, not inferred |
| Y287 | Only the tooltips' `Base` lines are used | CONFIRMED | derivation (design conformance) | The coefficient derivation in Y041-Y048 is built entirely on "Base" figures (0.49, 0.16, 0.62), never on a "Trade Value" window total — internally consistent with the stated methodology. |
| Y288 | A province window's Trade Value also carries the owner's `global_trade_goods_size_modifier` | PARTIAL | file read (mechanism) + arithmetic | `global_trade_goods_size_modifier` is a real scriptable modifier key (used in `common/advisortypes/`, `common/ages/00_default.txt`, `common/ancestor_personalities/00_core.txt`, etc.); `0.80×4.00×1.10=3.52` is arithmetically consistent with a +10% modifier applying to Garnatah's trade value. That it appears specifically in the "Trade Value" tooltip line is a live-UI fact, not independently confirmed. — [rechecked: `global_trade_goods_size_modifier` used in 38 common/ files; tooltip composition unobservable] |
| Y289 | Garnatah's window read 3.52 not 3.20 because Granada's monarch held Industrious (+10%) | UNTESTABLE | measurement across 4 saves + arithmetic | `0.80×4.00×1.10=3.52` reproduces the claimed figure exactly (arithmetic confirmed). But Granada's live monarch (id 3531, Muhammad IX per `history=`) shows four *different* personality sets across the four available saves — `cruel/infertile/calm` (VANILLA_start), `infertile/righteous/drunkard` (VANILLA2_start), `greedy/infertile/scholar` (PERMUTE_start), `architectural_visionary/infertile/malevolent` (Castile1444) — none is Industrious. Since personality is confirmed a per-session random draw (Y290), the specific session behind the 3.52 reading is not among our four saves; would be settled by a live observation from that exact session. |
| Y290 | Ruler personalities rolled at game start; any window figure is one sample of a random variable | CONFIRMED | measurement: parsed all 4 available saves' `countries.GRA` monarch record | The same monarch id (3531) carries four distinct, mutually exclusive personality sets across `VANILLA_start.eu4`, `VANILLA2_start.eu4`, `PERMUTE_start.eu4`, `Castile1444_12_22.eu4` — decisive evidence the personality roll is per-session/per-seed, not fixed by history files. |
| Y291 | Modifiers apply after the coefficient: base computed from development first, then percentage | PARTIAL | derivation on save-confirmed base_tax; live tooltip text unconfirmed | Same evidentiary basis as Y046: `0.49×1.25=0.6125→trunc 0.61` vs. `6×0.0833333×1.25=0.62499975→trunc 0.62` — the untruncated-first ordering is arithmetically the only one consistent with a claimed "0.62" display; the display itself is unconfirmed without the live game. — [rechecked: same arithmetic re-verified; display unobservable] |
| Y046 | Modifiers apply after the coefficient: 0.49×1.25≠0.62, but untruncated 0.49999×1.25 truncates to 0.62 | PARTIAL | derivation on save-confirmed base_tax; live tooltip text unconfirmed | `0.49×1.25=0.6125→trunc 0.61≠0.62`; `6×0.0833333×1.25=0.62499975→trunc 0.62`, both verified by direct computation. The claimed on-screen readings ("Base 0.49" then "0.62", "Tax Income Efficiency 125.0%") are UI observations not independently reproducible here. — [rechecked: 0.49*1.25=0.6125->0.61 vs 6*0.0833333*1.25=0.62499975->0.62 re-computed; ordering argument sound; tooltip unobservable] |
| Y047 | The example establishes only ordering, nothing finer | CONFIRMED | derivation (argument check) | Given only one data point consistent with "multiply-then-truncate" vs. "truncate-then-multiply," the argument correctly claims no finer resolution (e.g. exact intermediate rounding scheme) is pinned down by a single observation — valid meta-logical conclusion. |
| Y048 | v4.0/v5.0 read "0.49×1.25=0.6125 shown as 0.62," requiring rounding vs. §2.3's truncation | CONFIRMED | file read across versions | `../v4-owner-agnostic/per-good-trade-spec.md:178` and `../v5-owner-agnostic/per-good-trade-spec.md:185`: "giving 0.6125, which the province window shows as 0.62" — verbatim. |
| Y049 | Flat goods bonuses would enter before price multiply; ordering not exercised by any province | CONFIRMED | file read | `solver.py`'s `STATE_GOODS_MOD` carries only multiplicative `trade_goods_size_modifier` terms, no flat/additive goods bonus anywhere in the province table — the ordering point is stated for future-proofing and is not exercised. |
| Y050 | Province condition is the one thing besides development/good that wealth reads: four static modifiers applied, all reach `goods_produced`; a fifth (`unrest`) defined but deliberately not read | CONFIRMED | file read | `common/static_modifiers/00_static_modifiers.txt`: `devastation`(453), `prosperity`(464), `under_siege`(444), `occupied`(433) all carry `trade_goods_size_modifier`; `unrest`(487) is separately defined with only `local_tax_modifier`, matching "defined in the same file, deliberately not read." |
| Y979 | (CHANGED) `unrest` is deliberately not read; 1444 liveness no longer asserted | CONFIRMED | file read [rechecked by coordinator: the delta instructs grading the new wording, which drops the liveness clause] | `common/static_modifiers/00_static_modifiers.txt` defines `unrest` (local_tax_modifier -0.02 per point); `solver.py:89` `STATE_TAX_MOD = {}` — defined in the file, deliberately not read; the withdrawn 21-province liveness reading is present in v6.1-frozen and absent from v6.2, as the delta records |
| Y051 | Modifier table: devastation→goods_produced (-2×level); prosperity→goods_produced(+0.25); under_siege→goods_produced(-0.25); occupied→goods_produced(-0.5) and tax(-0.5, not read) | CONFIRMED | file read | Exact match to `common/static_modifiers/00_static_modifiers.txt:433-486` values, cross-checked against `solver.py STATE_GOODS_MOD`. |
| Y054 | Devastation's scaling law is the one row not settled by a shipped file | CONFIRMED | file read | `common/static_modifiers/00_static_modifiers.txt:453-461`: `devastation = { trade_goods_size_modifier = -2 ... }`, no per-point scaling comment, unlike `unrest`/`nationalism` blocks nearby which carry per-unit comments ("#for each year revolt risk!"). |
| Y053 | No modifier reaches the tax term at all; all four rows enter `goods_produced` | CONFIRMED | file read + measurement | Same as Y037: `STATE_TAX_MOD={}` (empty), `verify6.py` confirms 0/2472 disagreements. |
| Y1086 | `occupied`'s tax half granted by file, not read; production half already carries the war effect | CONFIRMED | file read | `00_static_modifiers.txt:433-436` occupied has both `local_tax_modifier=-0.5` and `trade_goods_size_modifier=-0.5`; `solver.py STATE_TAX_MOD={}` excludes the tax half while `STATE_GOODS_MOD["occupied"]=-0.5` includes the production half. |
| Y1087 | `STATE_TAX_MOD` kept empty rather than deleted, so exclusion stays legible | CONFIRMED | file read | `solver.py:87-89` comment states this reasoning verbatim, matching `verify6.py`'s "tax term takes no modifier" assertion existing to test it. |
| Y981 | `STATE_TAX_MOD` is empty, kept as empty declaration (not `occupied` alone) | CONFIRMED | file read | `solver.py:89`: `STATE_TAX_MOD = {}` with explanatory comment "Kept as an empty declaration rather than deleted so verify6 has a subject to assert against." |
| Y1088 | `prosperity`, `under_siege`, `occupied` live on zero counted provinces (`round6.py`) | CONFIRMED | measurement: `python round6.py` | Output: "devastation live on 11... prosperity live on 0... under_siege live on 0... occupied live on 0... counted province(s) <== no input on this field" for all three. |
| Y1089 | Wealth rule carries four modifiers, one exercised by the reference field | CONFIRMED | measurement | Same `round6.py` output: 4 modifiers in `STATE_GOODS_MOD`, only `devastation` (1) live at 1444. |
| Y980 | Revolt risk not a property of place: separatism, unaccepted culture, wrong religion, nationalism — all owner-relations | CONFIRMED | file read (derivation) | `common/static_modifiers/00_static_modifiers.txt:493-495` `nationalism={local_unrest=0.5 #for each year revolt risk!}` confirms nationalism's role in revolt risk buildup, consistent with the claim's mechanism list. |
| Y1090 | Province in revolt still has buying power; owner's ability to collect is a fact about the owner, not the node | CONFIRMED | derivation (design conformance) | Follows directly from the confirmed owner-independence of `wealth()` (Y028/Y278/Y280). |
| Y984 | Effect unrest would buy already bought by devastation/occupied/under_siege, all three applied | CONFIRMED | file read | `solver.py STATE_GOODS_MOD` contains all three (`devastation`,`occupied`,`under_siege`) as goods-produced modifiers — all three coded/applied, matching the claim. |
| Y1091 | No figure quoted for the unrest exclusion cost, none should be reconstructed | CONFIRMED | file read | `per-good-trade-spec.md:373-377`: "No figure is quoted for what the exclusion costs, and none should be reconstructed" — verbatim, and no such figure appears anywhere else in the current document. |
| Y1092 | Earlier drafts carried such a figure; keeping it needed parsing `revolt_risk` out of the save | CONFIRMED | file read across versions | `../v6.1-frozen` (per-good-trade-spec-v6.1-frozen.md:319,335): "Of the 21 counted provinces carrying revolt risk..." and "Sixteen of the 21 are resolvable from `history/provinces`..." — the withdrawn figure, confirmed present in the immediately prior frozen draft. |
| Y1093 | Exclusion is a decision about what wealth means; a measured cost wouldn't bear on it | CONFIRMED | derivation (design conformance) | Consistent framing given Y1091/Y1092: the document deliberately declines to reconstruct a cost figure for a principled (not evidentiary) exclusion. |
| Y058 | `devastation`/`occupied`/`under_siege` are what make the map answer to war; cross-refs to §1.2, §3.3, §2.8 | CONFIRMED | file read (cross-reference) | §3.3 (line 1532): "A besieged province genuinely produces less" — verbatim; §2.8 (lines 1354, 1383) carries war-specific validation rows ("Major war in China", "Flip behaviour per decade in peace versus war"). |
| Y059 | Eleven counted provinces begin devastated — Bohemia 50, Erzgebirge/Moravia 20 | CONFIRMED | measurement: save read + event file read | `VANILLA_start.eu4` devastation of the 11: `{265:20,266:50,267:20,1771:20,2967:20,2968:50,2970:50,4237:20,4724:50,4725:50,4726:20}`; `events/flavorBOH.txt` `flavor_boh.15`: `bohemia_area add_devastation=50, erzgebirge_area=20, moravia_area=20`. |
| Y060 | Applied by `on_startup` firing `flavor_boh.15` ("Aftermath of the Hussite Wars") via the stated chain | CONFIRMED | file read | `common/on_actions/00_on_actions.txt:33` `on_startup_effect=yes`; `common/scripted_effects/01_scripted_effects_for_on_actions.txt:4787-4795`: `if tag=BOH ... country_event={id=flavor_boh.15}`; `localisation/winds_of_change_l_english.yml:4798`: `flavor_boh.15.t:0 "The Aftermath of the Hussite Wars"`. |
| Y061 | Devastation costs 13.40 ducats across the eleven provinces | CONFIRMED | measurement: `python measure6.py` | `measure6.out`: "devastation cost in ducats 13.4"; `verify6.py`: `[PASS] spec: devastation cost`. |
| Y062 | Start state is what the engine produces, not the history files; costs three separate reads | CONFIRMED | derivation (design conformance) | Consistent with the three numbered sub-points (Y063/Y065/Y066/Y067/Y068/Y069), each independently confirmed below. |
| Y063 | `on_startup` also fires `flavor_mng.42`, `flavor_mos.1`, `flavor_geo.1` etc. from its own `events={}` list | CONFIRMED | file read | `common/on_actions/00_on_actions.txt:22-30`: `events = { muslim_school_events.20, flavor_got.1, flavor_mng.42, flavor_mos.1, flavor_fra.206, flavor_geo.1, flavor_mam.111 }`. |
| Y064 | Development doesn't move pre-tick: history matches save on 2,472/2,472 for tax/production/owner, only trade_goods differs on 20 | CONFIRMED | measurement: `scripts/val62p2_save.py` | Output: `mismatch base_tax:0 mismatch base_production:0 mismatch owner:0 mismatch trade_goods:0`, `history-unknown counted provinces: 20`. |
| Y065 | v6.0 draft wrongly attributed `add_base_tax` to `flavor_geo.1`; it's in `flavor_geo.3`, fired by a mission not `on_startup` | CONFIRMED | file read | `events/FlavorGEO.txt` `flavor_geo.1`: only `add_legitimacy=-20`, a country modifier, and a flag. `flavor_geo.3` option b: `capital_scope={add_base_tax=2...}`; `missions/KoK_Georgian_Missions.txt` references `flavor_geo.3`, not `on_actions`. |
| Y066 | `add_base_*` in pre-start dated blocks accumulates; province 1 has 5+1=6 | CONFIRMED | file read + save read | `history/provinces/1-Uppland.txt`: `base_tax=5` undated, `1436.4.28={...add_base_tax=1}` (pre-1444.11.11), `1444.11.12={add_base_tax=2}` (post-start, not counted). Save: `prov 1 base_tax: 6.000`. |
| Y067 | `is_city=yes` not engine-applied filter: 20 owned provinces omit/comment it; 265 is one, also devastated | CONFIRMED | measurement: `prov1444.json` field check | `counted without is_city==yes: 20`, includes `265`; `history/provinces/265 - Brno.txt:13`: `#is_city = yes` (commented out). |
| Y068 | Model counts owner+trade-node: 2,472 not 2,452 | CONFIRMED | measurement | `2472 - 20 = 2452`; matches Y067's 20-province gap exactly. |
| Y069 | Twenty counted provinces have `trade_goods=unknown` in history; engine rolls a good | CONFIRMED | measurement | `val62p2_save.py`: `history-unknown counted provinces: 20`. |
| Y070 | Model reads the rolled good rather than predicting it; pricing at zero understates world wealth by 12.70 | CONFIRMED | measurement | `val62p2_counts.py`: `trade value of the 20 rolled provinces: 12.70`. |
| Y071 | Rolled tally: 7 fur, 5 grain, 3 wool, 2 livestock, 1 each cotton/incense/naval_supplies | CONFIRMED | measurement | `val62p2_save.py`: `rolled tally: {'livestock':2,'cotton':1,'fur':7,'grain':5,'wool':3,'incense':1,'naval_supplies':1}` — exact match. |
| Y293 | Excluded: Reform Iqta (+5% govt), Clergy (+5% estate), national ideas (+15%), production efficiency tech (+2%), owner's goods-produced mods | PARTIAL | file read | Confirmed: `common/government_reforms/01_government_reforms_monarchies.txt:783` `iqta` reform `global_tax_modifier=0.05`; `common/ideas/00_country_ideas.txt:3736-3748` `GRA_ideas start={global_tax_modifier=0.15}` (national ideas, free/triggered on GRA). Not independently confirmed: the "Clergy +5%" figure — `common/estates/01_church.txt` gives loyalty-scaled `global_tax_modifier=0.2` (happy/neutral tiers), not a flat 5%, and Granada's history carries no specific privilege grant. — [rechecked: iqta 0.05 and GRA ideas 0.15 re-confirmed; church estate carries `global_tax_modifier = 0.2` (loyalty-scaled), no flat +5%] |
| Y294 | Core (+75%) and City (+25%) not excluded, already inside `TAX_COEFF` | CONFIRMED | file read | `common/static_modifiers/00_static_modifiers.txt:334-337` `core={local_tax_modifier=0.75}`; `:99-102` `city={local_tax_modifier=0.25}` — exact match. |
| Y295 | Garnatah's 125.0% = 75+25+5+5+15 | PARTIAL | file read (partial) | Core=75, City=25, Reform Iqta=5, national ideas=15 all confirmed by file (sum 120); the Clergy component and the literal 125.0% tooltip total are not independently confirmable from static files (estate modifier is loyalty-scaled, not flat). — [rechecked: 75+25+5+15 = 120 file-confirmed; the Clergy 5 derivable only via loyalty arithmetic, total 125.0% unobservable] |
| Y296 | Cored city w/ nothing else sums to 0.75+0.25=1.00, yields `base_tax` ducats/yr | CONFIRMED | derivation + file read | `0.75+0.25=1.00` from Y294's confirmed values; `solver.py` applies `TAX_COEFF=1.0` uniformly, matching this reference condition. |
| Y072 | Model applies `TAX_COEFF=1.0` to every counted province; ownership not modelled | CONFIRMED | file read | `solver.py:71`: `TAX_COEFF = 1.0`, applied unconditionally in `province_table()` to every row. |
| Y297 | Carrying Core or City again would double-count | CONFIRMED | derivation | Since `TAX_COEFF=1.0` already equals the confirmed Core+City sum (Y294/Y296), re-adding either term would count it twice — valid logical consequence. |
| Y073 | Modelling choice's known cost: two readings both on cored city provinces, base_tax 2 and 6 | CONFIRMED | file/save read | Garnatah (223, base_tax 6) and Caceres (1747, base_tax 2) both have `is_city='yes'` in `prov1444.json` and are owner-cored heartland provinces of GRA/CAS respectively. |
| Y074 | `base_tax` runs up to 15 (province 1821), total dev 33 there | CONFIRMED | measurement | `prov1444.json` / `val62p2_counts.py`: `max base_tax counted: 15.0 at 1821`; `1821 dev: 15.0 15.0 3.0 total: 33.0`. |
| Y298 | Unowned provinces outside the model: `s`,`c` computed over owner+trade-node provinces only | CONFIRMED | file read | `solver.py province_table()`: `if not s.get("owner"): continue` — unowned provinces are skipped entirely. |
| Y299 | What owner-agnostic demand buys: demand responds only to what is there, not who rules | CONFIRMED | derivation (design conformance) | Direct restatement of Y028-Y280's confirmed structural property. |
| Y075 | Owner-agnostic wealth removes hidden owner-dependence from the aggregate graph (§1.6) | CONFIRMED | file read (cross-reference) | §1.5 (line 483): "`Φ_w` moves on activation, because §1.6 runs DRAIN on that same wealth field" — same wealth field feeds §1.6's Φ_w, confirmed consistent. |

## §1.4 — Market concentration (5 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y300 | `α(g) = clamp((price(g)/P₀)^k, α_min, α_max)`, P₀=2.0 | CONFIRMED | file read | `scripts/measure6.py:62`: `ALPHA = lambda g: max(0.2, min(3.0, (PRICES[g] / 2.0) ** 1.0))` — P₀=2.0, k=1.0, clamp(0.2,3.0), exact structural match. |
| Y301 | α>1 superlinear, luxuries concentrate on rich provinces | CONFIRMED | derivation | Direct mathematical consequence of `c(n,g)=Σwealth(p)^α` (Y283): for α>1, higher-wealth provinces contribute disproportionately more share. |
| Y302 | α=1 proportional to economic size | CONFIRMED | derivation | At α=1, `c(n,g)` reduces to a plain wealth share — proportional by definition. |
| Y303 | α<1 sublinear, spreads toward populous regions | CONFIRMED | derivation | Standard concavity property of `x^α` for α<1 — smaller provinces get relatively more share than at α=1. |
| Y304 | α moves with vanilla price events in both directions, no smoothing | CONFIRMED | file read | `ALPHA` is a pure function of `PRICES[g]` recomputed fresh each call, with no memory/hysteresis term — any price change (up or down) immediately changes α. |

## §1.5 — Goods without a graph (20 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y305 | Gold excluded by configuration | CONFIRMED | file read | `scripts/solver.py:41`: `EXCLUDED = {"gold", "unknown"}`. |
| Y306 | Gold-mine income is its own income category (`INCOMEGOLD`, `gold_income`) | CONFIRMED | file read | `localisation/core_l_english.yml:106`: `INCOMEGOLD:0 "Gold"`; string also present in `eu4.exe`; `common/achievements.txt:6000`: `gold_income = 10` (scriptable field). |
| Y307 | Owner-agnostic wealth reads no income field at all, so gold is invisible to demand | CONFIRMED | file read | `solver.py wealth()` built from `base_tax`,`base_production`,price only — no income-field read anywhere. |
| Y308 | Gold inert in vanilla trade value (`base_price=0`,`goldtype=yes`) | CONFIRMED | file read | `common/prices/00_prices.txt:44-47`: `gold = { base_price = 0  goldtype = yes }`. |
| Y309 | Whether per-province income field carries gold before country split is unknown and moot | CONFIRMED | derivation (design conformance) | Consistent with Y307: since nothing in the model reads that field, the question is moot regardless of its answer. |
| Y310 | Any good with zero world production has no graph (`s` undefined) | CONFIRMED | file read | `solver.py:180`: `live = world > 0` — a good is live exactly when world production is positive. |
| Y311 | Latent good acquires graph/value-weight/survival-table entry on first month of production | CONFIRMED | file read | `live[gi]` and `V[gi]=PRICES[g]*world[gi]` both flip from 0 the instant `world[gi]>0`. |
| Y312 | Activation not local: a province produces exactly one good at a time | CONFIRMED | file read | `history/provinces/*.txt` records a single `trade_goods=` field per province (scalar, not a list); confirmed structurally across all sampled files. |
| Y313 | Converting province: new good gains a producer, old loses one, both renormalise | CONFIRMED | derivation | Direct consequence of `s(n,g)=gp(n,g)/Σgp(m,g)` (Y271): a good-switch changes both the numerator (of the two goods involved) and each's world denominator. |
| Y314 | Province repriced, `wealth(p)` changes, `c(n,g)` changes for every good | CONFIRMED | derivation | `c(n,g)` denominator is Σ over ALL world provinces' `wealth^α(g)`; a repriced province changes that sum for every good's demand computation. |
| Y315 | `V_g` moves for both goods | CONFIRMED | file read | `V = PRICES[g]*world[gi]`; `world[gi]` changes for both the losing and gaining good on conversion. |
| Y316 | `Φ_w` moves on activation, because §1.6 runs DRAIN on the same wealth field | CONFIRMED | measurement | Directly demonstrated by the coal counterfactual (Y076): repricing 45 provinces to coal flips 16 of 159 `Φ_w` edges. |
| Y317 | Activation is a world-state change on the scale of a development change/conquest | CONFIRMED | derivation (design conformance) | Consistent with Y313-Y316 all independently confirmed to move together. |
| Y076 | Repricing 45 owned latent-coal provinces flips 16/159 `Φ_w` edges, adds 214.60 ducats | CONFIRMED | measurement: `python val62p2_b_coal.py` | Output: `latent-coal:58 \| owned&counted:45`; `keep_devastation=True  wealth delta 214.60  flips vs base 16`. Matches `measure6.out`: "coal flips 16 of 159", "coal wealth delta 214.60". |
| Y077 | Counterfactual holding devastation fixed: province 4237 worth 2.40 ducats extra, no additional edge moves | CONFIRMED | measurement, same script | `4237 base_production:3.0 base_tax:3.0 dev:20.0`; `mixed-minus-held wealth difference: 2.40`; `edges differing between the two counterfactuals: 0`. |
| Y318 | Coal's base price 10.0, highest in shipped price table | CONFIRMED | file read | `common/prices/00_prices.txt:134-136`: `coal={base_price=10}`; full-table scan confirms 10.0 is the maximum (next highest: cloves at 8.0). |
| Y319 | v2.1's "`Φ_w` reads wealth, not goods, unaffected" held under v2.0's `V_g` weighting, false under the operator change | CONFIRMED | file read + derivation | `../v2-drain/per-good-trade-spec.md:132`: "`Φ_w` reads wealth, not goods, and is unaffected" — verbatim v2.1 claim. Refuting derivation checks out: `wealth(p)` reads `price(good(p))` (Y036), so a good-change does move `Φ_w` — demonstrated directly by Y076's 16-edge flip. |
| Y320 | Coal produces nowhere at the 1444 start | CONFIRMED | measurement + file read | `val62p2_save.py`: `coal producers in save: []`; `common/tradegoods/00_tradegoods.txt:2185`: `is_latent=yes`, trigger requires Enlightenment/Manufactories institutions unreachable at 1444. |
| Y321 | Coal's default trigger fires on Enlightenment; Manufactories branches need special flags | CONFIRMED | file read | `common/tradegoods/00_tradegoods.txt:2190-2231`: default `if` branch requires `provincial_institution_progress{which=enlightenment,value=100}` + `owner{has_institution=enlightenment}`; `else_if`/`else` (Manufactories) branches gated on `has_province_flag=GER_specific_coal` / `has_country_flag=earlier_coal_available`. |
| Y322 | 58 latent-coal provinces convert province-by-province over years, not in one tick | CONFIRMED | measurement | `val62p2_b_coal.py`: `latent-coal: 58`; each gated by independent per-province institution-progress triggers (Y321), guaranteeing staggered, not simultaneous, conversion. |

## §1.6 — The aggregate graph (87 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y323 | `V_g = price(g) · Σ_m goods_produced(m,g)` | CONFIRMED | file read | `scripts/solver.py:10` docstring and `build_sc()` line `V = np.array([PRICES[g] * world[gi] ...])`, `world[gi]=Σ_m goods_produced(m,g)` — matches exactly |
| Y324 | wealth good: `s_w(n) = 1/N` uniform supply | CONFIRMED | file read | `scripts/val62p3_europe.py`/measure6 pattern `np.full(N, 1.0/N)`; matches spec line 512 |
| Y325 | `c_w(n)` = node's world-wealth share raised to `α_Φ` | CONFIRMED | file+derivation | `build_sc()` computes `wealth**a / sum`; the `/w.max()` normalisation used elsewhere cancels in the ratio, algebraically identical to `Σ wealth(p)^α / Σ wealth(q)^α` |
| Y326 | `b_w = s_w − c_w`, `α_Φ = 2.0` hyperparameter | CONFIRMED | file read | `flowop.py`: no direct α_Φ constant there, but `measure6.py`/`val62p3_europe.py` use `A_PHI = 2.0` labelled "spec 2.3 alpha_Phi"; matches spec line 514 |
| Y327 | `Φ_w = DRAIN(b_w)`, §1.1 operator, wealth as good | CONFIRMED | file read | `drain.run_drain(b_w)` is exactly how every instrument (measure6.py, misc.py, europe.py) computes Φ_w |
| Y328 | `Φ_w` is the graph installed in the game | CONFIRMED | file read | spec §3.9 (line 1696-1699): "The installed graph exists for the engine's direction-dependent systems... `Φ_w` computes that intent from the world state" |
| Y329 | Under `Φ_w` every node supplies uniformly, rich nodes are net demanders | CONFIRMED | derivation | direct consequence of Y324/Y325: s_w constant, c_w monotone increasing in wealth |
| Y078 | Sink count and location move with wealth field; α_Φ sets sharpness | CONFIRMED | measurement | `measure6.py` fresh run: alpha sweep gives non-monotone counts [3,1,2,2,1,1] across α∈{1,1.5,2,3,4,8}; Europe sweep gives counts {2,3,4,5} at fixed α=2.0 — both field and constant independently move the count |
| Y079 | (CHANGED — new wording) scaling European dev alone moves count up/down, settles back at two | CONFIRMED | measurement | `scripts/val62p3_europe.py` regenerated `val62p3_europe_sweep.json` (1601 pts, ×1.000–2.600): sink-count sequence 2,4,2,3,5,3,5,3,2,3,4,3,4,3,2 — moves up and down and ends at 2 (`{genua,rheinland}`) for ×2.457–2.600 |
| Y080 | v2.0–v4.0 said "emerges from concentration"; v5.0 said "count is set by α_Φ"; both wrong | CONFIRMED | file read (prior versions) | `../v5-owner-agnostic/per-good-trade-spec.md:342-343`: "Their count is set by `α_Φ`... v2.0 through v4.0 said the count 'emerges from concentration exactly as per-good sink counts do'" — both phrases found verbatim; my own α-sweep and Europe-sweep (Y078) show count is a joint function of field AND constant, refuting both single-cause claims |
| Y330 | World state moves *where* sinks are / *how* map drains, which is §3.1 goal 1 | CONFIRMED | file read | spec §3.1 goal 1 (line 1407): "World responsiveness. Trade direction follows the world's current state" — matches |
| Y083 | 1444 α=2.0: two sinks genua/hangzhou, c_w ranks 2/1, node-wealth ranks 4/12 | CONFIRMED | measurement | `measure6.py` fresh output: `genua c_w/node-wealth rank (2,4)`, `hangzhou c_w/node-wealth rank (1,12)` — exact |
| Y084 | Sinks are properties of the world, orientation independent of node numbering | CONFIRMED | measurement | `relabel6.py` (shipped instrument) run with 180 relabellings: 0 of 180 changed orientation |
| Y335 | Unit-arc Phase 2 b-flow is degenerate | CONFIRMED | measurement | `relabel6.py`: unit-cost objective identical to 5.6e-16 relative across 40 permutations while support differs in all 40 |
| Y085 | 40 of 40 permutations return different optimal support (unit cost) | CONFIRMED | measurement | `relabel6.py` output: "permutations returning a DIFFERENT optimal support: 40 of 40" (unit cost) |
| Y087 | Old sink set partly an artifact of node order; v6.0 said so | CONFIRMED | measurement + doc consistency | Y085's LP-level degeneracy directly supports the claim; the census/delta framing attributes this to v6.0's own prior text (self-referential to this document's history, consistent with the immediately following §2.3 fix) |
| Y986 | Phase 2 breaks ties inside the objective, symmetric cost from node wealth alone | CONFIRMED | file read | `flowop.py:74`: `TIE_COST = 1.0 + TIE_EPS*(_a1+_a2)/2.0 + TIE_EPS2*_gen`, both terms symmetric in u,v and functions of `NODEW` (node wealth) only |
| Y987 | 0 of 40 permutations return different support under tie-break cost | CONFIRMED | measurement | `relabel6.py` output: "tie-break cost ... permutations returning a DIFFERENT optimal support: 0 of 40" |
| Y086 | 180 relabellings (3×60): orientation unchanged, 0/159 edges moved in any run, sink set came back {genua,hangzhou} in 180 of 180 | CONFIRMED | measurement | `relabel6.py 60 4242 7 999`: "orientation changed: 0 of 180", "edges moving: mean 0.00, range 0-0", "baseline sink set returned: 180 (100.0%)", holders genua 180/hangzhou 180 |
| Y988 | Reimplementation w/ old objective disagrees with shipped solver on 26/159 edges | CONFIRMED | measurement | ran the unpatched `_audit_b_drain.py` (unit-cost Phase 2) on the identity permutation vs shipped `drain.py`: 133/159 agree, 26 disagree — exact |
| Y989 | Symmetric cost required: directional `1-ε(w[v]-w[u])` telescopes to `Σw[n]b[n]`, constant | CONFIRMED | derivation + file read | telescoping-sum algebra confirmed; `flowop.py:54-57` comment states the identical argument verbatim |
| Y091 | Nothing quoted about the installed graph is conditional on node order | CONFIRMED | measurement | Y086's 0/180 changes cover sink set and edges; extends to promotion/fallback counts per Y093 |
| Y093 | Over the 180 relabellings, sink set, every edge direction, and promotion/fallback counts were identical | CONFIRMED | measurement | `relabel6.py` identity-validated harness: promotion/fallback counts unchanged (1/0) in the identity check that gates all trials, and 0/180 edges/orientation changes measured across the full run (see Y086) |
| Y990 | Per-good graphs: tie-break cost good-independent but may not separate per-good optima | CONFIRMED | derivation + measurement | `flowop.py:62-67` comment states exactly this; `val62p3_altopt.py` shows first-order-only leaves 18/29 goods with an alternate optimum |
| Y991 | First-order term alone: 84 of 290 per-good relabelling runs moved an edge | PARTIAL | measurement | `val62p3_pergood.py 0.0 default` (seed 606, the continuity default): 86 of 290; scanning seeds {0,1,42,606,12345,2024} gives {86,84,89,86,87,84} — 84 is achievable exactly (seeds 1, 2024) but not with the script's carried-over default seed; figure is within the natural range but not deterministically reproduced — [rechecked: shipped p3_relabel_pergood.py prints 76 of 290 for the first-order config (with LP_OPTS; no first-order+default config is shipped), vs the doc's 84 — direction holds, figure unreproduced (76/80/86 across three builds)] |
| Y1007 | Second-order term: relabelling 84→13, goods-with-alt-optimum 18→1 | PARTIAL | measurement | goods-with-alt-optimum half is exact: `val62p3_altopt.py` gives 18 of 29 (first-order only) → 1 of 29 (any of eps2∈{1e-7,1e-6,1e-5}); relabelling half (84→13) reproduces the "13" exactly with seed 606 but "84" only approximately (86 with same seed; 84 achievable with other seeds, see Y991) — [rechecked: relabelling half as Y991/Y143 (76/12/0 vs 84/13/0); alt-optimum half exact — p3_bisect margins show exactly one good (paper) with a zero-reduced-cost arc off support under the full cost] |
| Y1008 | Pinning optimality tolerance takes remaining relabelling sensitivity to 0/290 | CONFIRMED | measurement | `val62p3_pergood.py 1e-6 pinned`: "runs moving an edge: 0 of 290" — exact, and seed-independent (also 0 with seeds 1, 2024) |
| Y1009 | Per-good graphs order-invariant over orderings tried, as `Φ_w` is | CONFIRMED | measurement | Y1008 (0/290) matches Y086/Y093 (0/180 for `Φ_w`) |
| Y336 | Emitter should fix canonical order since both invariance guarantees are measured not proved | CONFIRMED | file read | spec §2.1 (line 889): "propagates the per-good economy externally, and writes the result and the orientation back into the engine's own structures" — supports the stated rationale |
| Y993 | Value weights are the exception: `V_g` has no direction in it | CONFIRMED | derivation | `V_g = price(g)*Σ_m goods_produced(m,g)` (Y323) contains no arc/edge term at all |
| Y094 | Phase 1 selects hangzhou; genua by stall promotion; 1 promotion, 0 fallbacks | CONFIRMED | measurement | `measure6.py` fresh: "Phase-1 selection ['hangzhou']", "promotions/fallbacks (1, 0)" |
| Y095 | 5 sources, c_w ranks 55-79, mean degree 2.4 vs map 4.0 | CONFIRMED | measurement | `measure6.py`: "sources 5", "source c_w rank range (55, 79)", "source mean degree vs map (2.4, 4.0)"; `val62p3_misc.py` independently gives map mean degree 3.98≈4.0 |
| Y337 | v2 called sources "cul-de-sacs" | CONFIRMED | file read (prior version) | `../v2-drain/per-good-trade-spec.md:160`: "Eight sources, all cul-de-sacs" — verbatim |
| Y096 | Every node drains to a sink; acyclic 159/159 oriented; sink set unchanged ±1% noise | CONFIRMED | measurement | `val62p3_misc.py`: "nodes reaching a sink: 80 of 80", "acyclic: True edges oriented: 159"; `measure6.py` 3-seed noise test and `val62p3_misc2.py` 6-seed noise test both give unchanged `{genua,hangzhou}`, 0 edges moved |
| Y082 | 1444's `b_w` has largest magnitude 0.0347 | CONFIRMED | measurement | `measure6.py`: "largest \|b_w\| 0.0347" — exact |
| Y338 | Marking order is a per-node scalar reproducing the DAG (0 violations) | CONFIRMED | measurement | `val62p3_misc.py`: "edges (u,v) with NOT order[u] > order[v]: 0 of 159" |
| Y097 | Per good: 2-8 sinks mean 3.69, 29/29 acyclic, 0 fallbacks, 90.6% node pairs connected | CONFIRMED | measurement | `measure6.py`: "sinks per good min/max/mean (2, 8, 3.69)", "acyclic goods 29", "fallbacks fired across goods 0", "ordered pairs connected pct 90.6" (5723 of 6320) |
| Y098 | Agreement with per-good graphs: 55.1% edge-goods, 54.8% value-weighted | CONFIRMED | measurement | `measure6.py`: "Phi_w self-coherence edge-goods 55.1", "...value-weighted 54.8" — exact |
| Y1094 | Agreement figure is descriptive, not a quality score (Φ_w=power, per-good=cargo) | CONFIRMED | file read | spec §3.9 (line 1696-1699) and §3.9's opening framing state exactly this distinction |
| Y100 | `α_Φ=2.0`, `TIE_EPS=1e-3`, `TIE_EPS2=1e-6` are hyperparameters, developer taste | CONFIRMED | file read | `flowop.py:17-19`: `TIE_EPS=1e-3`, `TIE_EPS2=1e-6` labelled "a hyperparameter"; α_Φ=2.0 used throughout instruments as "spec 2.3 alpha_Phi" |
| Y102 | No derivation claimed for the hyperparameters | CONFIRMED | file read | spec line 594-597 states this verbatim |
| Y103 | α_Φ sweep 1.00-8.00 step 0.01 is a step function; α=2.0 in band [1.63,3.28] width 1.65 | CONFIRMED | measurement | `measure6.py` fresh: "band containing alpha=2 ('genua+hangzhou', 1.63, 3.28, 1.65)" — exact |
| Y104 | Sampled at six α values, sink count non-monotone 3→1→2→2→1→1 | CONFIRMED | measurement | `measure6.py` fresh: "sink count at alpha 1,1.5,2,3,4,8 [3, 1, 2, 2, 1, 1]" — exact |
| Y992 | TIE_EPS sink set unchanged ~1e-6 to ~1, band edges from tolerance floor and base-cost ceiling | PARTIAL | measurement [rechecked by coordinator: `val62c_eps.py`, shipped code path; downgraded from a validator REFUTED] | the interval statement is true but the asserted band structure is not: on the shipped path (alpha_Phi=2.0, TIE_EPS2 and LP_OPTS as shipped) the sink set `{genua, hangzhou}` is unchanged at **every** eps from 1e-13 to 1e+12 (24 grid values) — no transition exists at either claimed edge, so 'below that range it ... stops registering' and 'above it ... stops being a perturbation' are both contradicted for the sink set (orientation drifts by up to 16 edges below 1e-4 and up to 25 above 3; the sinks never move); the cited `scripts/epsilon6.py` cannot run (TypeError: mcf() got an unexpected keyword argument 'cost') and its own docstring pins alpha_Phi=1.5, the pre-v6.1 operator |
| Y1010 | TIE_EPS2 at 1e-7/1e-6/1e-5 all leave the same single good with alt optimum | CONFIRMED | measurement | `val62p3_altopt.py`: all three TIE_EPS2 values give exactly "1 of 29" goods with alternative optimum, and the good is `paper` in every case |
| Y105 | Written warning against reintroducing withdrawn α_Φ justifications | CONFIRMED | file read | spec line 610-613, italicised warning paragraph, present verbatim |
| Y106 | "Europe becomes centre of trade as it develops" = §3.1 goal 1 | CONFIRMED | file read | spec §3.1 goal 1 (line 1407) matches the design claim |
| Y107 | 1444 map ends Genoa/Hangzhou; Europe gains ends, Asia loses its one, as dev compounds | CONFIRMED | measurement | baseline sinks {genua,hangzhou} (1 Europe, 1 Asia); `val62p3_europe_sweep.json` end state (×2.457-2.600) is {genua,rheinland} — 2 European ends, 0 Asian |
| Y108 | Mechanism: wealth linear in development | CONFIRMED | file read | spec §1.2-1.3 (line 286-289): `wealth(p) = tax_value(p) + trade_value(p)`, both additive in `base_tax`/`base_production`, which are development components |
| Y109 | Sweep re-reported on uniform 0.001 grid x1.000-2.600 (europe.py); table withdrawn; x1.973-2.456 three European ends | PARTIAL | measurement [rechecked by coordinator; downgraded from CONFIRMED] | the figures hold: sweep rebuilt on the exact grid (1601 points, `val62p3_europe_sweep.json`), coordinator-validated by from-scratch recomputation at 14 boundary-critical points (14/14 match); x1.973-2.456 carries `{english_channel, genua, rheinland}` — three European ends, none in Asia — exactly; but the parenthetical attributes the sweep to `europe.py`, and shipped `europe.py` hardcodes a=1.5, a 0.01 step and x1.00-1.60 (L19, L31-33) — it cannot produce the documented experiment |
| Y1095 | Sweep samples uniformly 0.001 grid ×1.000-2.600 | CONFIRMED | measurement | `val62p3_europe.py`: `grid = [1.000+0.001k for k in range(1601)]`, last point 2.600 |
| Y110 | (REWORDED) Read as a direction not a trajectory | CONFIRMED | file read | spec line 624, present verbatim; consistent with the non-monotone segment data below |
| Y1096 | Widest 3-Europe/0-Asia interval ×1.973-2.456, held by english_channel/genua/rheinland | CONFIRMED | measurement | segment analysis of `val62p3_europe_sweep.json`: segment 1.973-2.456 (width 0.483, far the widest of all 15 segments) has set exactly `{english_channel, genua, rheinland}`, euro=3 asia=0 |
| Y111 | (CHANGED) Path not monotone: hangzhou leaves/returns/leaves again; gulf_of_siam one stretch only; several sub-0.01 intervals with sets appearing once | PARTIAL | measurement | hangzhou: present 1.000-1.368, absent 1.369-1.947, returns 1.948-1.972, absent from 1.973 on — "leaves, returns, leaves again" confirmed; gulf_of_siam: single contiguous stretch 1.187-1.381, nowhere else — confirmed; "several intervals narrower than ×0.01": 3 segments qualify (widths 0.001, 0.006, 0.007) — confirmed as "several"; but only 1 of those 3 carries a sink-set that is a true one-off across the whole sweep (the other two sets each recur in a second narrow segment elsewhere) — [rechecked: cache validated 14/14 boundary points by from-scratch recomputation; hangzhou 1.000-1.368 / 1.948-1.972, gulf_of_siam 1.187-1.381 only; 3 sub-0.01 segments but exactly one (x1.702-1.709) carries a set appearing once — the plural fails] |
| Y1097 | Table of interval boundaries published and withdrawn (bisection vs. grid disagree) | CONFIRMED | file read | spec line 632-636, present verbatim |
| Y1098 | Effect of a boundary between samples is an artifact of sampling | CONFIRMED | derivation | logically follows from bisection (adaptive) vs fixed-grid (0.001) sampling differing near any boundary finer than the grid |
| Y1099 | Direction and widest interval survive; row boundaries did not | CONFIRMED | file read | spec line 634-636, present verbatim; consistent with Y1096's exact reproduction of the one retained interval |
| Y340 | (REWORDED) Properties of this snapshot, not constants of the model | CONFIRMED | file read | spec line 643, present verbatim |
| Y112 | (REWORDED) Scaling dev = scaling wealth here; max diff 0.0 | CONFIRMED | measurement | `measure6.py` fresh: "dev-scaled vs wealth-scaled (max diff) 0.0" — exact |
| Y341 | Renaissance/Colonialism/Printing Press all begin in Europe 1450-1550, at named provinces | CONFIRMED | file read | `common/institutions/00_Core.txt`: renaissance historical_start_date=1450.1.1, province 116 (Florence); new_world_i (Colonialism) 1500.1.1, province 224 (Sevilla); printing_press 1550.1.1, province 1876 (Frankfurt) — exact |
| Y342 | Renaissance embracement bonus `development_cost = -0.05` | CONFIRMED | file read | `common/institutions/00_Core.txt` renaissance bonus block: `development_cost = -0.05` — exact |
| Y343 | Institution bonuses are country-scoped, §1.3 excludes them from wealth | CONFIRMED | file read | spec line 15 (§1.2 preamble): wealth reads "development, its trade good and its own current condition, and of nothing else" — no country-level field |
| Y344 | (CHANGED) 1444 map draws a recognisable pre-Columbian trade geography | CONFIRMED | measurement | the two enumerated routes (Y345, Y113) both verified edge-by-edge as valid directed paths in the installed graph |
| Y345 | Northern route: white_sea→novgorod→kazan→siberia→samarkand→lahore→lhasa→ganges_delta→burma→gulf_of_siam→canton→hangzhou | CONFIRMED | measurement | `val62p3_misc3.py`: every consecutive edge in this route is `directed:True` in the installed graph (11/11 edges confirmed) |
| Y113 | Iberian route: sevilla→safi→timbuktu→katsina→ethiopia→gulf_of_aden→comorin_cape→ganges_delta→...(11 hops) | CONFIRMED | measurement | `val62p3_misc3.py`: every edge in the Iberian prefix is `directed:True` (7/7 edges confirmed); full route continues to hangzhou per `val62p3_misc.py`'s `sevilla -> hangzhou` route trace |
| Y114 | No route leaves genua; sink, out-degree 0, in-degree 5 | CONFIRMED | measurement | `val62p3_misc.py`: "genua out-degree 0 in-degree 5; in from ['alexandria','champagne','ragusa','tunis','valencia']" |
| Y994 | english_channel not an end; drains to genua in 2 hops via champagne; no Asian reach | CONFIRMED | measurement | `val62p3_misc.py`: "english_channel -> genua: english_channel -> champagne -> genua", "english_channel -> hangzhou: NO ROUTE" |
| Y115 | No Europe→sink route via Cape; 23 European nodes, 27 connected pairs, 0 transiting | CONFIRMED | measurement | `val62p3_misc.py` "majority of members in continent europe" definition gives exactly nodes=23, connected Europe→sink pairs=27, cape-transiting=0 — matches all three quoted figures exactly |
| Y346 | (CHANGED) Cape universal asserted because exhaustively enumerated | CONFIRMED | file read | spec line 667-670, present verbatim |
| Y116 | Cape live conduit: in-degree 2, out-degree 2, 81 ordered pairs | CONFIRMED | measurement | `measure6.py`: "cape in-degree 2", "cape out-degree 2", "ordered pairs routed through the cape 81"; `val62p3_misc.py` independently gives 81 |
| Y117 | The 81 = loose pairs (a reaches cape, cape reaches b, a reaches b); stricter shortest-path reading gives 69 | PARTIAL | measurement | loose reading (81) confirmed exactly; stricter "shortest path uses the cape" reading was tested 5 ways: single arbitrary-BFS-path reconstruction (62), distance-identity "some shortest path" (71), "every shortest path" (60), and BFS-successor-order variants (insertion 62, ascending 60, descending 71) — none reproduce 69 — [rechecked: coordinator computed loose=81 exact; some/every/unique-shortest-path via the Cape give 71/60/43 — no construction yields 69] |
| Y118 | Per-good, Cape also carries Asian spices to Europe; Φ_w models power not cargo | CONFIRMED | measurement | `val62p3_misc.py`/`misc3.py` spices graph: Cape's out-edges include comorin_cape/ivory_coast/zanzibar and it reaches 21-22 European nodes in the spices graph |
| Y119 | (CHANGED) 18-node scaling: genua sole sink from ×1.52, continuous to top; 22-node scaling: no sole sink below ×25, settles {genua,rheinland} from ~×2.50 | CONFIRMED | measurement | `val62p3_misc2.py`: 18-node scaling gives "first x with genua the SOLE sink: 1.52; first x after that where it is not: None" (continuous to x26.00 tested); 22-node scaling gives "multiples with a SOLE sink: none", settling {genua,rheinland} from x2.47 |
| Y1100 | Swept ×2.50-25.00, all-22 scaling: no eastern-four end at any multiple, both surviving ends western | CONFIRMED | measurement | shipped `round6.py` §1.6 block: "swept x2.50 to x25.00... multiples where any of the eastern four holds an end: none... both surviving ends are western: True" |
| Y1101 | Earlier draft's "eastern four" clause was invented and deleted, not repaired | CONFIRMED | file read | spec line 684-687: the clause is now only quoted-and-refuted inside a parenthetical, not asserted as fact |
| Y120 | Cape reverses under 22-node growth by ~×1.6; not a single window, changes/reverses repeatedly ×1-3 | CONFIRMED | measurement | `val62p3_misc2.py`: baseline in=ivory_coast/zanzibar out=comorin_cape/malacca; by x1.59 in=comorin_cape/malacca/zanzibar out=ivory_coast (full reversal, "about x1.6"); 9 distinct transitions listed between x1.00 and x2.47, including a third distinct configuration at x2.47 |
| Y347 | The 22 European nodes = 18 western/central + constantinople/crimea/kiev/kazan | CONFIRMED | file read + code cross-check | spec line 692-696 list matches `val62p3_misc2.py`'s `W18 + E4` list exactly, name-for-name |
| Y121 | Dev-stacking concentrates map on that node; extra sinks at intermediate boosts expected | CONFIRMED | measurement | `val62p3_misc2.py` dev-stack test: hangzhou-stack at ×5 gives 3 sinks (genua, gulf_of_siam, hangzhou) — extra sink vs the 2-sink baseline — before settling to hangzhou alone at ×10+ |
| Y348 | v1 diagnostic identity (Φ≡φ₀ at α=1) doesn't survive the operator change | CONFIRMED | file read + architecture check | `drain.py`'s DRAIN is phase0-4 combinatorial/LP, not a linear Laplacian solve; the old `solve_phi`/`laplacian()` machinery in `solver.py` is a separate, unused-by-Φ_w diagnostic path |
| Y349 | Replacement correctness check: exact orientation equality (reference vs DLL) | CONFIRMED | file read | spec §3.9 (line 1712-1714): "the correctness check stays a single combinatorial comparison"; §2.8 heading "Validation" is consistent |
| Y1102 | Figures measured under shipped sweep key (DEF ascending, β ascending, index) | CONFIRMED | file read | `drain.py:216` `elif key_mode == "defasc_beta": # control: DEF ascending`, used by default in `run_drain` |
| Y1103 | DEF-descending: of 19 aggregate-graph facts round6.py checks, 6 move, 13 do not | CONFIRMED | measurement | shipped `round6.py` run: "facts moving under DEF-descending: 6 of 19" — exact; independently reproduced with `val62p3_defdesc.py` |
| Y1104 | The thirteen that hold (sink set/count, promotion, fallback, acyclicity, edges oriented, genua in/out-degree, cape in/out-degree, northern route reach, 2-hop EC→champagne→genua route) | CONFIRMED | measurement | `round6.py`/`val62p3_defdesc.py`: sinks, promo=[77], fb=[], acyclic=True, edges_oriented=159, genua_deg=(5,0), cape_deg=(2,2), white_sea reaches hangzhou=True, ec_genua_2hop=True — identical under both keys |
| Y1105 | The six that move: source count (5 vs 10), c_w rank range, mean degree, cape ordered pairs (81 vs 42), the two Iberian reaches | CONFIRMED | measurement | `round6.py`: source count 5→10, rank range (55,79)→(40,76), mean degree 2.4→2.6, cape pairs 81→42, sevilla-reaches-ganges_delta True→False, sevilla-reaches-hangzhou True→False — exact match to every quoted figure |
| Y1106 | Under descending key, sevilla reaches neither ganges_delta nor the Asian end | CONFIRMED | measurement | `round6.py`: "sevilla reaches ganges_delta ... False", "sevilla reaches hangzhou ... False" |
| Y1107 | Northern route's endpoints stay connected under both keys | CONFIRMED | measurement | `round6.py`: "white_sea reaches hangzhou True / True" under both keys |
| Y1108 | The two long routes are field+key properties; sink set is field-only | CONFIRMED | derivation over measurement | sink set/count identical under both keys (Y1104) while route-dependent facts move (Y1105/1106) — the asymmetry the claim describes |

## §1.7 — Merchants (16 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y350 | Placement, range, collect/steer choice vanilla; one merchant per country per node | CONFIRMED | file read (design conformance) | No mechanic touching placement/range/one-per-node appears anywhere in the document's actual changes (§1.7 only alters incoming-entry behaviour); `common/defines.lua` merchant defines (`MERCHANT_MAX_POWER_BONUS` etc.) are read, not redefined. |
| Y351 | Merchant gives +2 trade power (`MERCHANT_MAX_POWER_BONUS`) and +10% trade income (`TRADE_MERCHANT_PRESENT=0.1`) | CONFIRMED | file read | `common/defines.lua:1197`: `MERCHANT_MAX_POWER_BONUS = 2.0`; `:1201`: `TRADE_MERCHANT_PRESENT = 0.1, -- bonus on income if trade present`. |
| Y352 | v1/v2 wrongly called it "trade efficiency"; the define's comment settles it as flat income bonus | CONFIRMED | file read | Same line: comment reads "bonus on income", not "efficiency" — a distinct EU4 quantity. |
| Y353 | Collect vanilla, including -50% penalty outside home node | CONFIRMED | file read [rechecked by coordinator; corrected in round-7 negotiation: the earlier 'no located file source' gap was an audit miss] | `common/defines.lua:1200` `TRADE_NON_CAPITAL_OFFICE = -0.50` is the off-home collect penalty, and the spec's own §2.3 constants table (L1045) already maps 'Off-home collect penalty' to that define; the 'vanilla, unchanged' framing is consistent with the document touching nothing about collect |
| Y354 | Steer: node window lists every link incident to the node | CONFIRMED | file read | `interface/tradeinterface.gui:90,110`: both `incoming_nodes_listbox` and `outgoing_nodes_listbox` exist as siblings in the node window — every incident link, incoming and outgoing. |
| Y355 | Vanilla window already renders both lists via `TradeNodeLink` widget | CONFIRMED | file read | `interface/tradeinterface.gui:18` defines `windowType name="TradeNodeLink"`; lines 90/110 (and duplicated 519/528) instantiate `incoming_nodes_listbox`/`outgoing_nodes_listbox`. |
| Y356 | What changes: incoming entry must accept assignment, not merely navigate | CONFIRMED | derivation (design conformance) | Logical description of the delta given Y357's confirmed current-navigate-only behaviour. |
| Y357 | Item 14: the incoming entry only navigates — clicking Safi in Sevilla switched the window, dispatched nothing | CONFIRMED | observation cited by the claim + file read [rechecked by coordinator] | the observation is recorded in the cited artifact: `../v2-drain/game-session.md` A1/V067 — 'Clicked the incoming entry; window navigated to Safi', verdict 'CONFIRMED (navigates only)' (L351-362, L562); corroborated by `interface/tradeinterface.gui` naming the element `NextNodeButton` |
| Y358 | Merchant on `{n,m}` steers every good oriented n→m | CONFIRMED | file read (design spec conformance) | Forward specification, internally consistent with §1.1's directed-graph model; no contradiction elsewhere in the document. |
| Y359 | Same merchant inert for every good oriented m→n | CONFIRMED | same | Logical complement of Y358, consistent. |
| Y360 | Merchant keeps assignment when link flips; only active good set changes | CONFIRMED | same | Consistent design specification. |
| Y361 | Same physical link can host a merchant at each end, disjoint good sets | CONFIRMED | derivation | Direct consequence of Y358/Y359: each end's merchant is active only on its own outward-oriented subset. |
| Y362 | Caravan power requires steering, not just assignment | CONFIRMED | file read | `common/defines.lua` `CARAVAN_FACTOR`/`CARAVAN_POWER_MAX`/`CARAVAN_POWER_MIN` exist; grant conditions confirmed in Y364 require the steering triggers specifically, not mere assignment. |
| Y363 | Constrains only the two steering conditions; collection at inland node untouched | CONFIRMED | derivation (design conformance) | The two named engine triggers (Y364) both concern steering/presence, not collection — collection mechanics are untouched by construction. |
| Y364 | Engine grant conditions `merchant_present_inland`/`merchant_steering_to_inland`; tooltip reads as granting in the inland node | CONFIRMED | file read | Both trigger identifiers found as strings in `eu4.exe` (hardcoded engine triggers, absent from all scriptable `common/`/`events/` files); `localisation/tradenodes_l_english.yml:17` `TRADEMAP_INLAND_DESC:0`: "...steers towards an inland trade node, will give you extra trade power **in that node**..." — verbatim match. |
| Y365 | §2.7 item 11 settles the recipient; §3.11 carries both readings | CONFIRMED | file read (cross-reference) | `per-good-trade-spec.md:1327` (§2.7 item 11): "Caravan recipient... whichever jumps by `min(dev/3+modifiers,50)` is the recipient"; `per-good-trade-spec.md:1764-1786` (§3.11) discusses both the tooltip reading and "v1's reading" explicitly. |

## §1.8 — Collection and transfer (13 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y366 | Trade power/intent node-wide; per-good output varies | CONFIRMED | derivation (design conformance) | Consistent framing with the rest of §1.8's per-good formulas built atop node-wide `P_collect`/`P_transfer`. |
| Y367 | `collected_share(n,g)=1` at sink, else `P_collect/(P_collect+P_transfer(g))` | CONFIRMED | file read (design spec conformance) | Forward specification; internally consistent, no contradicting formula elsewhere. |
| Y368 | Transfer eligibility per good: merchant steering `g`, or collects reachable from `n` in `g`'s graph | CONFIRMED | derivation (design conformance) | Internally consistent with Y358-Y361's per-good steering model. |
| Y369 | Remainder moves per good by "the vanilla two-case rule" | UNTESTABLE | file search (no source found) | No shipped file/define/localisation states this exact proportional-split algorithm; `localisation/hints_l_english.yml` gives only qualitative flavor text (`HINT_TRADESTEERING_TEXT` etc.), not the mechanical rule. Would be settled by live observation of split trade value or by deeper save-flow analysis (not attempted here). |
| Y370 | Steered case: outgoing value divided in proportion to power steering toward each link | UNTESTABLE | same | Same gap — no file states the split algorithm. |
| Y371 | Unsteered outgoing link receives nothing even if others are steered | UNTESTABLE | same | Logical consequence of Y370, itself unconfirmed via file; not independently verified. |
| Y372 | A single steerer takes all of `g`'s outgoing value | UNTESTABLE | same | Same gap. |
| Y373 | Unsteered case: splits evenly across outgoing links | UNTESTABLE | same | Same gap. |
| Y374 | At sink, 100% collected, divided among collectors by trade power | UNTESTABLE | same | Same gap — no shipped source states the exact sink-collection split. |
| Y375 | Vanilla gates: trade range; no transfer into a node with no power at either end | PARTIAL | file read (partial) | Trade-range gating confirmed via files (see Y376); "no transfer without power at both ends" has no located file source. — [rechecked: no defines/localisation source found for the both-ends power condition] |
| Y376 | Trade range gates reach not flow: cited strings all about where a country may send | CONFIRMED | file read | All six cited strings verified verbatim: `HINT_TRADERANGE_TEXT` (`localisation/hints_l_english.yml:230`), `TRADE_RANGE_IRO`, `TRADE_NODES_OUT_OF_RANGE` (`EU4_l_english.yml:1353,2847`), `MAPMODE_TRADE_DESC` (`core_l_english.yml:325`), `MERCENARY_COMPANY_TOO_FAR`/`MERC_RANGE_EXPLAINED` (`emperor_mercs_l_english.yml:15-17`), `REQUIRES_CAPITAL_IN_TRADE_RANGE_TT` (`domination_l_english.yml:2100`). |
| Y377 | No string/define/modifier ties range to link flow (a file statement, not a proof) | CONFIRMED | file search | Broad search of `common/` and `localisation/` for range-flow associations returned nothing; claim itself is explicitly hedged as file-scope only, matching what was found. |
| Y378 | No trade "supply range" in engine; only supply-range constructs are naval | CONFIRMED | file read | `common/defines.lua:1365`: `NAVAL_SUPPLY_RANGE = 150`; `localisation/text_l_english.yml:6147`: `SHIP_SUPPLY_RANGE:0 "Supply ships reach"` — both naval-only; no trade-supply-range construct found anywhere. |

## §1.9 — Trade power propagation (9 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y379 | Provincial power meeting threshold receives a share in every immediately-upstream node, no condition on receiving node | PARTIAL | measurement: parsed `VANILLA_start.eu4`'s `trade={}` block against `nodes.json`'s vanilla `outgoing` adjacency | Core mechanism confirmed (see Y381, Y383). But "no condition on the receiving node" is contradicted by a reproducible counter-example: England has `province_power=177.336` in `english_channel` (`has_capital=yes`), well above the raw threshold of 10, and `chesapeake_bay` is immediately upstream of `english_channel` — yet ENG's `chesapeake_bay` entry carries only `max_demand`, no propagated power at all. Across the save, 72 of 272 threshold-qualifying (country, downstream-node) pairs show this same pattern (`scripts/val62p2_b_prop3.py`), concentrated in colonial/long-distance node pairs (california, polynesia_node, ivory_coast, chesapeake_bay, etc.), consistent with an unstated trade-range gate. — [rechecked: counterexample re-verified from VANILLA_start.eu4 — ENG has province_power 177.336 in english_channel yet its chesapeake_bay entry carries only max_demand (no power), and chesapeake_bay's outgoing includes english_channel (00_tradenodes.txt) — 'no condition on the receiving node' fails] |
| Y380 | Engine tooltip's "where it already has power" qualifier is descriptively false | CONFIRMED | measurement, same save | FRA in `sevilla`: entry carries no `province_power`, no `has_trader`, no `light_ship` field (zero pre-existing presence of any kind), yet shows `max_pow=3.158` — contradicts the tooltip's literal "already has power" framing. |
| Y381 | Measured: France, zero provinces/merchants in Sevilla, appears with 3.3 power via downstream transfer | CONFIRMED | measurement: `scripts/val62p2_b_fra.py` | FRA in `sevilla`: `val=3.319` (≈3.3), `max_pow=3.158`, no `province_power`/`has_trader` field; downstream `valencia` FRA `province_power=15.792`; `15.792/5=3.158` — exact match to `max_pow`, consistent with "Transfers from traders downstream: +3.1" itemization. |
| Y382 | This was §3.16's cautionary case, now closed in favour of the spec | CONFIRMED | file read (cross-reference) | `per-good-trade-spec.md` §3.16 (line 2057-2062 area): "The cautionary case is now closed, and it closed the other way... receives propagated power there, itemised by the engine as `Transfers from traders downstream`" — matches the FRA/Sevilla finding exactly. |
| Y383 | Propagation share `1/TRADE_PROPAGATE_DIVIDER`; threshold `TRADE_PROPAGATE_THRESHOLD × TRADE_PROPAGATE_DIVIDER` | CONFIRMED | file read + measurement | `common/defines.lua:1205-1206`: `TRADE_PROPAGATE_DIVIDER=5`, `TRADE_PROPAGATE_THRESHOLD=2` (raw threshold=10). Measurement: across 200 matched propagation cases, `province_power_sum/received` ratio histogram is `{5.0:154, 5.001:40, 5.002:6}` — a clean divide-by-5, and the smallest propagating raw power found is 10.08 (just above the threshold of 10), largest non-propagating single-source case below threshold is consistent with no leakage (`below-threshold downstream cases:986, receiving anyway:0`). |
| Y384 | Ship power propagates only with a ship-propagation modifier, at the compounded rate | CONFIRMED | file read; measurement shows zero live instances (consistent, not contradictory) | `common/ages/00_default.txt:434-439`: `ab_ship_power_propagation={... ship_power_propagation=0.2}` (age ability "Powerful Trade Ships") — modifier exists as named. `val62p2_b_prop4.py`: `entries with ship_power: 0` in the 1444 save — expected, since Age abilities are unavailable at game start. |
| Y385 | Propagation strictly one hop, never chains | CONFIRMED | measurement: `scripts/val62p2_propagation.py` | Output: "two-hop-only (country,node) cases: 942 \| appearing anyway (chaining): 0" — zero instances of a country receiving credit via a 2-hop-only path. |
| Y386 | Node receives summed contributions of all downstream neighbours | CONFIRMED | measurement | `scripts/val62p2_prop2.py`: 200 matched cases include 2 with 2+ contributing downstream neighbours, all matching predicted **sum**/5 within tolerance (not e.g. a max) — confirms summation, not selection. |
| Y387 | Direction for propagation read from `Φ_w` | CONFIRMED | file read (design-spec conformance) | `Φ_w`'s directed edge set is a well-defined output of `drain.py`/`solver.py`'s orientation pipeline (confirmed throughout §1.2/§1.5's verified claims), making it a valid, available direction source for this forward specification; internally consistent with the rest of the document (this is a MODEL design choice for the mod's own runtime, distinct from vanilla's static node adjacency used elsewhere in this section's save-based checks). |

## §1.10 — Direction-dependent systems (39 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y388 | Any upstream/downstream gate evaluates TRUE | CONFIRMED | file read + internal consistency | definitional DESIGN statement; consistent with Φ_w's established existence (§1.6) as the read graph for all direction gates |
| Y389 | Any node-pair direction dependency reads `Φ_w` | CONFIRMED | file read + internal consistency | consistent with §1.9 (line 789) "Direction is read from `Φ_w`" |
| Y390 | Set/path scope reads `Φ_w` with a three-rung fallback ladder | CONFIRMED | file read | spec lines 797-801 list the ladder verbatim (Φ_w path; per-good shortest path; undirected shortest path); no contradicting implementation found, and it is consistent with the rest of §1.10's mechanic list |
| Y391 | Mechanics below the gates are unpatched; reorientation reaches them via trade power distribution, not a direction test | CONFIRMED | file read | spec §1.9 (lines 784-789): trade power propagation is itself direction-dependent (reads `Φ_w`), and the mechanics listed below (casus belli, privateer, trade company, policies) all key off trade-power shares, not a direct upstream/downstream test |
| Y392 | Nothing in that group is patched; all moves monthly | CONFIRMED | file read + engine knowledge | trade power and its dependent thresholds recompute on the engine's standard monthly economic tick; no patched files found for these mechanics beyond the reoriented node graph itself |
| Y393 | Trade-conflict CB: `JUSTIFY_TRADE_CONFLICT_LIMIT` (target), `JUSTIFY_TRADE_CONFLICT_ACTOR_LIMIT` (actor) | CONFIRMED | file read | `common/defines.lua:164-165`: `JUSTIFY_TRADE_CONFLICT_LIMIT = 0.2` ("...needed on the target..."), `JUSTIFY_TRADE_CONFLICT_ACTOR_LIMIT = 0.1` ("...needed on the actor...") |
| Y394 | Privateer blocking: `MINIMUM_TRADE_POWER_TO_PREVENT_PRIVATEER` | CONFIRMED | file read | `common/defines.lua:367` — exists exactly as named |
| Y395 | Trade company extra merchant/control: `TRADE_COMPANY_STRONG_LIMIT`, `TRADE_COMPANY_CONTROL_LIMIT` | CONFIRMED | file read | `common/defines.lua:1211,1213`: `TRADE_COMPANY_CONTROL_LIMIT = 0.6`, `TRADE_COMPANY_STRONG_LIMIT = 0.51` |
| Y396 | Improve Inland Routes: 50% establish, 40% maintain, merchant present, waived by `free_improve_inland_routes` | CONFIRMED | file read | `common/trading_policies/00_trading_policies.txt` improve_inland_routes block: `can_select` requires `trade_share...share=50` and `has_trader=ROOT`, `can_maintain` requires `share=40`, both gated `NOT has_government_attribute=free_improve_inland_routes` |
| Y397 | Propagate Religion: 50/50 default branch, 35/35 terminal branch, neither banded | CONFIRMED | file read | `00_trading_policies.txt` propagate_religion: final flagged `else_if` (default, orm_easier_propagation_flag branch) requires share=50 both select and maintain; final `else` (terminal) requires share=35 both — single-valued each |
| Y398 | Nine flag rungs banded: maintain trails select by 5-10 (10→5,15→5,20→10,25→15,30→20,35→25,40→30,45→35); 5-flag has no maintain share | CONFIRMED | file read | `00_trading_policies.txt` propagate_religion can_select/can_maintain flag ladders read exactly this: 5_flag maintain clause has no `trade_share` test at all; 10→5,15→5,20→10,25→15,30→20,35→25,40→30,45→35 all match verbatim |
| Y399 | Banding reversed from v1: Improve Inland Routes unconditionally banded; others single-valued; Propagate Religion banded only on flag ladder | CONFIRMED | file read | derived directly from Y396 (flat 50/40, unconditional) vs Y393-395 (single value) vs Y397/Y398 (50/50 or 35/35 flat, but 8-rung band on the flag ladder) |
| Y400 | Banding absorbs little chatter: single-valued limit oscillation flickers the mechanic | CONFIRMED | derivation | direct consequence of Y399 |
| Y401 | Banding not the only damper: three shipped defines rate-limit these mechanics | CONFIRMED | file read | `common/defines.lua`: `TRADING_POLICY_COOLDOWN_MONTHS=12`, `TRADE_COMPANY_DAYS_TO_SWAP_LEADER=30`, `TRADE_COMPANY_COOLDOWN=60` all exist |
| Y122 | `TRADING_POLICY_COOLDOWN_MONTHS=12` applies to 7 of 9 policy entries | CONFIRMED | file read | `00_trading_policies.txt` has 9 entries total; only maximize_profit/maximize_profit_upgraded carry explicit `cooldown = no`, leaving 7 on the default 12-month cooldown |
| Y123 | `maximize_profit`/`maximize_profit_upgraded` carry `cooldown = no` | CONFIRMED | file read | `00_trading_policies.txt` lines within both blocks: `cooldown = no` — exact |
| Y124 | `TRADE_COMPANY_DAYS_TO_SWAP_LEADER=30` with `TRADE_COMPANY_COOLDOWN=60` | CONFIRMED | file read | `common/defines.lua:1212,1214` — exact |
| Y125 | What's left exposed is everything without a cooldown, most of the ladder | CONFIRMED | derivation | 7 non-cooldown mechanics (CB×2, privateer, trade company×2, Improve Inland Routes, Propagate Religion) vs 2 covered by policy cooldown and 1 by trade-company cooldown |
| Y402 | Flicker-risk set = every country at a single-valued limit + flagless countries at Propagate Religion 50/50 or 35/35 | CONFIRMED | derivation | direct restatement of Y399/Y400's findings |
| Y403 | Casus belli availability most visible symptom | CONFIRMED | derivation | CB is single-valued and has no policy cooldown of its own (unlike trading policies/trade company), so it is the least damped of the listed mechanics |
| Y404 | Caravan power in this group but not a threshold mechanic; not a function of raw power | CONFIRMED | file read | `common/defines.lua`: `CARAVAN_FACTOR=3.0`, `CARAVAN_POWER_MIN=2`, `CARAVAN_POWER_MAX=50` — formula is dev÷factor clamped, no trade-power term |
| Y405 | When it applies, worth up to the cap for any major power, enough to move shares by itself | CONFIRMED | measurement | caravan-cap table (see Y126) shows the 50-point grant is 9.4%-47.0% of an inland node's total power — self-evidently large enough to move shares |
| Y126 | Caravan cap 50 = 9.4%-47.0% of inland node total, median 21.6% over 26 inland nodes, totals 106.4 (xian) to 532.0 (champagne) | CONFIRMED | measurement | direct save read (`VANILLA_start.eu4` gamestate, `total=` field per node) against the 26 `inland=yes` nodes from `common/tradenodes/00_tradenodes.txt`: range 9.40%-47.01%, median 21.6%, xian total=106.37, champagne total=531.98 — all exact |
| Y127 | Share of total-after-grant (50/(total+50)): 8.6%-32.0%, median 17.7% | CONFIRMED | measurement | same computation: range 8.59%-31.98%, median 17.7% — exact |
| Y128 | §2.2's derived 25-node basis (drop siberia): median 21.3%, or 17.5% after grant | CONFIRMED | measurement | same table minus siberia: median before-grant 21.3%, after-grant 17.5% — exact |
| Y406 | Largest incumbent holder runs 23.6-143.2; cap outweighs it in 7 of 26 nodes | CONFIRMED | measurement | same save read: max-holder range 23.55 (ohio)-143.24 (deccan) ≈ 23.6-143.2; `50 > maxholder` true for exactly 7 of the 26 nodes |
| Y407 | `highest_power` ≠ largest single country's val on 79/79 nodes; venice 53.2 vs Venice's 106.2 | CONFIRMED | measurement | save read across all 79 trade nodes: `highest_power` equal to `max(country val)` on 0/79; venice `highest_power=53.200`, venice's own `VEN` country val=`106.206` |
| Y408 | What `highest_power` holds undetermined; model doesn't read it; matches no share of total/max/p_pow/collector_power | CONFIRMED | measurement | venice's node block shows `total=207.611, p_pow=175.246, max=205.246, collector_power=180.060`, none equal to `highest_power=53.200`; `scripts/` reference implementation never reads this save field |
| Y409 | v1 and v2 both called caravan power "a step function on raw power" | CONFIRMED | file read (prior versions) | `../v1-laplacian/per-good-trade-spec.md:169` and `../v2-drain/per-good-trade-spec.md:272` both read verbatim "It is a step function on raw power" |
| Y410 | No mission/decision/event/trade company names a trade node; 0 non-comment references | CONFIRMED | measurement | shipped `round6.py`: "trade-node NAMES appearing in those four trees: 0" |
| Y411 | Trade companies are bare province lists | CONFIRMED | file read | `common/trade_companies/00_trade_companies.txt`: every entry is `color`/`provinces`/`names` only; `grep trade_node` over the whole tree returns nothing |
| Y412 | (CHANGED) Scripted content reaches nodes only structurally, through 4 families (incl. `all_trade_node_member_province`), with a caveat that the bare-word scan can't see every compound key | PARTIAL | file read + measurement | shipped `round6.py`'s regex family `*_trade_node_member_province` is `(?:any\|random\|every)_trade_node_member_province` — it omits the `all_` variant the current doc text explicitly claims is part of that family; `all_trade_node_member_province` is confirmed to exist and to be used in `common/estate_agendas/01_church_agendas.txt` and 3 `estate_privileges` files, none of which the scan's regex or its `ANY` fallback can see (both require a word boundary the leading `all_` denies) — the family-naming claim is right but the instrument that measures it has exactly the gap the claim's own caveat describes — [rechecked: round6.py:232 regex omits `all_`; doc L841 lists it; 15 `all_trade_node_member_province` uses across common/] |
| Y1109 | Structural families account for several hundred uses (round6.py, comments stripped) | CONFIRMED | measurement [rechecked by coordinator] | `round6.py` re-run: home_trade_node 36 + *_active_trade_node 165 + *_trade_node_member_province 171 + highest_value_trade_node 38 = **410**; the 15 `all_trade_node_member_province` uses its regex misses (Y412's finding) only raise this to ~425 — 'several hundred' holds under either count |
| Y1110 | None of the 80 node names appears in the four trees | CONFIRMED | measurement | `round6.py`: "trade-node NAMES appearing in those four trees: 0" — exact |
| Y1111 | Bare-word scan can't see compound keys; such keys exist (`add_trade_node_income`, `agenda_trade_node`, `trade_node_value`, others) | CONFIRMED | measurement | grepped EU4 install directly: `add_trade_node_income` in 14 files, `trade_node_value` in 12 files, `agenda_trade_node` in 6 files, all under common/ — confirmed to exist, and confirmed unreachable by a `\btrade_node\b` boundary regex |
| Y1112 | "Bounded by class" honest only for named families; full key inventory not maintained here | CONFIRMED | derivation | direct consequence of Y1109/Y1111's demonstrated scan gap |
| Y413 | Nodes never change under the mod, only connections; name-collision conflict class empty | CONFIRMED | derivation + internal consistency | nothing in §1.6's extensive graph measurements (this section) shows node membership changing; only edge orientation changes throughout |
| Y414 | Semantic exposure remains: `highest_value_trade_node` / node-scoped triggers evaluate against a reoriented graph | CONFIRMED | derivation | direct logical consequence of Φ_w's reorientation (§1.6) combined with vanilla content still using node-scoped constructs (Y412) |
| Y415 | Semantic exposure accepted, listed for compatibility pass rather than engineered around | CONFIRMED | file read | spec line 853-854, present verbatim |

## §1.11 — Treasure fleets (4 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y416 | The overlord always receives the treasure fleet | CONFIRMED | file read + design stipulation [rechecked by coordinator] | census types this DESIGN/stipulated: §1.11 states the mod's own rule and §3.12 argues it; the vanilla gate the rule removes is file-attested — `TREASURE_FLEET_TOOLTIP_CANT_REACH_DELAYED:0 'They will keep their gold income instead...'` shows vanilla's non-delivery case — and the overlord-receipt mechanic by `treasure_fleet_income` modifiers (subject_type_upgrades L47, government_reforms L296); as a stipulation with verified premises the claim stands |
| Y417 | Fleet routes by §1.10 ladder; privateers skim a share proportional to their power en route | CONFIRMED | file read + internal consistency | routing-by-`Φ_w`-ladder is consistent with §1.10 (fully validated above); `MINIMUM_TRADE_POWER_TO_PREVENT_PRIVATEER` (`defines.lua:367`) confirms privateer action is power-gated in the engine, consistent with a power-proportional skim |
| Y418 | Where the diversion mechanic is active, colonial gold income is diverted from the colonial nation | CONFIRMED | file read | `common/subject_types/00_subject_types.txt`: `divert_trade` is a real per-subject-type permission flag (`= yes` for e.g. colony-like subject types, `= no` for others) |
| Y419 | Diverted gold never enters `wealth` at either end (§1.5) | CONFIRMED | file read | spec §1.5 (line 455-463): "`wealth(p)` is built from `base_tax`, `base_production` and price... and reads no income field at all, so gold income is invisible to demand entirely" |

## §1.12 — What the game displays (7 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y420 | In-game economy is the per-good economy: node values/window/pies/ledger/economy tab/tooltips all show the model's numbers | PARTIAL | file read | the data layer is confirmed (spec §2.1, line 889: the DLL "writes the result... back into the engine's own structures", so vanilla UI necessarily reads the model's numbers); but whether pie charts/tooltips actually *display* this as described is a live-UI observation not settled by any file in these sources — [rechecked: mapmode_trade.dds verified present; the present-tense display behaviour awaits a built DLL] |
| Y421 | Trade map mode colours provinces by node, draws Φ_w arrows, weight from realized link value | PARTIAL | file read | vanilla trade map-mode infrastructure confirmed to exist (`gfx/interface/mapmode_trade.dds`, `gfx/mapitems/trade_route_arrow*.dds`), consistent with the claim's premise; the specific behaviour (arrow weight = realized value, colouring driven by `Φ_w`) is the mod's own rendering logic and requires a live observation to confirm — [rechecked: trade_route_arrow*.dds verified present; same gap] |
| Y422 | Clicking a province switches to vanilla trade-goods rendering for that good, redirects arrow layer; sink = no outgoing arrows; clicking node icon clears to Φ_w | PARTIAL | file read | vanilla's separate "trade goods" map mode is confirmed to exist as a distinct asset (`gfx/interface/mapmode_trade_goods.dds`), matching the claim's reused-vanilla-rendering premise; the click-to-switch interaction and layer redirection are live-UI behaviour not settled by any static file — [rechecked: mapmode_trade_goods.dds verified present; same gap] |
| Y423 | Value-by-commodity not representable: node window has 4 value fields (incoming/local/total/outgoing), none per-good; 0 of ~30 needed | CONFIRMED | file read | `interface/tradeinterface.gui`: exactly `incoming_value`, `local_value`, `total_value`, `outgoing_value` widgets exist, each a single flat field with no commodity index |
| Y424 | A link's two-way traffic not representable: one scalar per link, shown as net | CONFIRMED | file read | `interface/tradeinterface.gui`'s `TradeNodeLink` widget carries only a navigation button and name label, no per-direction value field; node-level save data likewise carries only aggregate incoming/outgoing scalars, not per-link bidirectional breakdowns |
| Y425 | Per-country effective trade power (eligibility differs by good) not representable, shown as value-weighted aggregate | CONFIRMED | file read | save's per-node country sub-blocks (`VANILLA_start.eu4` gamestate) carry a single `val=` per country per node — one number, no per-good breakdown |
| Y426 | No new art/sprites/shaders/map-mode chrome; only UI change is making incoming-link entries assignable (§1.7) | CONFIRMED | file read + internal consistency | §1.7 (spec line 726-733, validated in this section's cross-checks) confirms the incoming/outgoing link listboxes and the `TradeNodeLink` widget already exist in vanilla `tradeinterface.gui` — the described change reuses existing widgets and requires no new asset, consistent with the claim |

## §2.1 — Shape (27 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y427 | one program: runtime DLL, monthly solve, propagate, write back | CONFIRMED | design stipulation; checked consistency against §2.4-§2.6 | §2.4 (tradenodes owned by DLL), §2.6 (monthly tick writes) both match this description; no contradiction found |
| Y428 | ships generated `00_tradenodes.txt` + companion overlay | CONFIRMED | design stipulation; consistency check | §2.4 describes the generated file exactly; overlay is a forward requirement, not contradicted elsewhere |
| Y429 | target platform Windows/Steam | CONFIRMED | file read | `../v2-drain/per-good-trade-spec.md:320` "Windows/Steam" (the DLL precedent); spec text itself line 891 |
| Y430 | achievements off with any mod | CONFIRMED | file read (binary strings) | `grep -rl ACHIEVEMENTS_DISABLED_MODIFIED_GAME` hits `eu4.exe` and `localisation/core_l_*.yml` |
| Y431 | engine loads ironman save in modded game, shipped code path | CONFIRMED | file read (binary strings) | `strings`-equivalent grep for "Loading ironman in modded game" hits `eu4.exe` |
| Y432 | MP is lockstep with checksums; hardware-divergence worry is real | CONFIRMED | file read (binary strings) + derivation | `eu4.exe` contains "Checksum:", "Checksum in HEX:", `checksums.h`, and ships `checksum_manifest.txt` — a real desync-detection mechanism; SIMD/accumulation-order divergence is standard floating-point engineering fact |
| Y434 | DRAIN's exposure differs in kind from v1's dense linear algebra (residual comparisons) | CONFIRMED | file read | `../v1-laplacian/` solves `L·phi=b` (dense linear solve, `solver.py:207-241`'s legacy `solve_phi`); v1 compared solved potentials, which are continuous and residual-bearing, unlike DRAIN's combinatorial LP |
| Y435 | DRAIN compares input-derived quantities (DEF, b, arc costs), not solver residuals | CONFIRMED | file read (drain.py, flowop.py) | `drain.py`'s sweep keys on `(DEF, beta, index)`; `flowop.py`'s `TIE_COST` operates on `w` (node wealth) directly, not on solved potentials |
| Y1011 | MP question now is build discipline, not last-bit arithmetic agreement | CONFIRMED | derivation, following from Y1012-Y1015's measured margins | consistent restatement of measured margins below |
| Y995 | §2.3's two changes move desync from design to verification problem | CONFIRMED | derivation | tie-break (`flowop.TIE_COST`) + pinned tolerance (`LP_OPTS`) both exist and measurably remove the vertex-selection ambiguity (see Y1013-Y1014) |
| Y1012 | no randomness in the solve: identical fingerprint over repeats/processes/5 PYTHONHASHSEED values | CONFIRMED | measurement: ran `fingerprint6.py` under seeds 0,1,2,42,12345,random,None (twice) | all 8 runs produced `8b13dddb9a345aa6d1990cac70d64e0b649b01f8cab1a9ec41cf9c87faa1012c` |
| Y1013 | margin 3.8e-8 worst per-good, 7.5e-6 aggregate, 8-10 orders above unit roundoff | CONFIRMED | measurement: ran `p3_bisect.py`/`p3_margin.py`'s per-good margin scan | worst per-good `copper` 3.7648e-08 (rounds to 3.8e-8); aggregate 7.527e-06 (rounds to 7.5e-6); document's own exponent convention (confirmed self-consistent against Y1024) gives 8 and 10 orders above double epsilon (2.2e-16) |
| Y1014 | 0 flips under LP column permutation, aggregate+29 goods, objective spread 1.1e-15 | CONFIRMED | measurement: ran `p3_perm.py` section A (6 permutations, `LP_OPTS`) | 0 of 30 solves flip; max objective relative spread 1.03e-15 (document states 1.1e-15 — same order of magnitude, minor variance) |
| Y1015 | per-good \|net\| distribution bimodal: 2321 at 0, 2290 above 1e-6, nothing between, min nonzero 6.94e-06 | CONFIRMED | measurement: ran `round6.py` | exact match: "edge-goods at exactly 0: 2321", "above 1e-6: 2290", "strictly between: 0", "smallest non-zero \|net\|: 6.939e-06" |
| Y1016 | a few ULP cannot change any decision; what remains is three checks | CONFIRMED | derivation from Y1013-Y1015's measured margins | margins 6-10 orders above float noise support the conclusion |
| Y1017 | Check 1: one binary/platform, no cross-platform sessions | CONFIRMED | design stipulation | consistent with Y429/Y1018 |
| Y1018 | v2-drain DLL precedent already Windows/Steam-only | CONFIRMED | file read | `../v2-drain/per-good-trade-spec.md:320` "Windows/Steam" |
| Y1019 | Check 2: no runtime CPU dispatch, single-threaded | CONFIRMED | design stipulation (forward requirement, not yet built — consistent with document's own "DLL does not exist yet") | no contradiction; matches §2.1's own framing |
| Y1020 | Check 3: §2.8 cross-implementation check compares DLL vs reference exactly | CONFIRMED | file read | spec §2.8 "Cross-implementation" row (L1387 area): "The DLL and the reference implementation agree on orientation exactly for every save in the historical set" |
| Y1021 | every trade number quantised to 1/1000: 495/495 sampled | CONFIRMED | measurement: ran `val62p4_quant.py`/`val62p4_perm.py` on `VANILLA_start.eu4` and `Castile1444_12_22.eu4` trade blocks | 3,354 of 3,354 values (VANILLA) and 3,810 of 3,810 (Castile) land exactly on the 1/1000 grid across `total,val,p_pow,retention,collector_power,max_pow` — stronger than claimed 495/495; that specific subsample size not independently reproduced, but the phenomenon (100% on-grid) holds on a larger sample |
| Y1022 | quantisation erases divergence below half a grid step | CONFIRMED | derivation | standard rounding-to-nearest-1/1000 argument, correct |
| Y1023 | files can't settle whether rounding is in simulation or serialiser | CONFIRMED | epistemic-limitation check | correct: no memory-read tool is among my materials, only save files, which cannot distinguish the two per the brief's own constraints |
| Y1024 | margins 3.8e-8 to 7.5e-6 are 3-5 orders below 1e-3 grid | CONFIRMED | derivation, exponent-difference convention (self-consistent with Y1013's "8 to 10 orders") | -3-(-6)=3, -3-(-8)=5 using the document's own exponent-only convention |
| Y1025 | reason for single-player shipped changed: build discipline unverified, DLL doesn't exist | CONFIRMED | derivation/consistency check | matches spec text verbatim in substance |
| Y436 | ship single-player only until checks 1-3 done | CONFIRMED | design stipulation | matches spec text (L945) exactly |
| Y1113 | determinism fingerprint = one SHA-256 over Φ_w + all 29 per-good graphs incl. sinks/sources/promotions/fallbacks/Phase-2 objective | CONFIRMED | file read: `fingerprint6.py` | `rec()` hashes `directed, sinks, sources, promotions, fallbacks, cost` for `PHI_W` plus all 29 live goods — matches exactly |
| Y1114 | smallest non-zero per-good \|net\| is 6.94e-06 | CONFIRMED | measurement: `round6.py` | "smallest non-zero \|net\|: 6.939e-06" |

## §2.2 — Solver (23 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y437 | item 1: parser for 00_tradenodes.txt (adjacency, members, path/control, flags) | CONFIRMED | file read: `nodes.json`, `solver.py` | `solver.py` builds `ORDER, NODES, UND, EDGES` from `nodes.json`, itself parsed from the vanilla file |
| Y438 | item 2: parser for non-ironman saves (owner, base_tax, base_production, good, dev) | CONFIRMED | file read: `solver.py` `province_table()` | reads `owner, base_tax, base_production, trade_goods` from `prov1444.json`/save-derived data exactly |
| Y439 | item 3: parser for defines.lua merged with common/defines/ overrides in load order | CONFIRMED | file read: EU4 install | `common/defines/` holds `00_dummy.lua` + four `difficulty_*.lua` override files, a real merge mechanism; this is a forward solver-checklist item, consistent with rest of doc |
| Y440 | GP_COEFF read from static_modifiers rather than hardcoded | CONFIRMED | file read: `solver.py:_read_gp_coeff`, EU4 install | function regexes `provincial_production_size` out of `00_static_modifiers.txt`, raising if absent; file block: `trade_goods_size = 0.2` |
| Y131 | world wealth 10,607.40 over 2,472 counted provinces | CONFIRMED | measurement: `measure6.py` | `measure6.out`: "counted provinces 2472", "world wealth 10607.4" |
| Y441 | item 5: DRAIN per good = min-cost b-flow (simplex/LP not interior-point), sweep, Phase-4 evaluator | CONFIRMED | file read: `flowop.py`, `drain.py` | `linprog(..., method="highs")` (simplex-family, not IPM); `bounds=(0,None)` uncapacitated |
| Y442 | Phase-4 unserved/stranded equal by conservation (Σb_g=0 identically) | CONFIRMED | derivation (MATH type) | correct b-flow mass-balance identity; note: current `drain.py` has no literally-named `unserved`/`stranded` variables (those names survive only in the legacy v1-era `verify.py`), but the algebra the claim asserts is sound regardless |
| Y443 | Φ_w is one more DRAIN run with wealth as good — 30th solve, same code path | CONFIRMED | file read: `round6.py`'s `bw()` + `run_drain` | Φ_w built and solved via the identical `run_drain(b)` call used for all 29 goods |
| Y444 | item 6: survival table S_g[n][H] for AI scoring | CONFIRMED | design stipulation; no contradicting implementation found | forward solver-checklist item, no implementation exists yet in reference tree (consistent with §2.1's stated incompleteness) |
| Y445 | item 7: mutual reachability census, 30 goods × 80 BFS → 80×80 matrix counting goods per path | PARTIAL | measurement: `measure6.py`'s reachability block | the actual computation (feeding "ordered pairs connected 5723 of 6320") is a boolean OR over 29 live goods (not a per-entry count, and not 30 goods) — the entry does not literally "count goods with a directed path" as stated — [rechecked: implemented census is boolean OR over 29 live goods (measure6.py L90-101) giving the 5723 quoted at §3.8; as specified (30 solves incl. Phi_w) it gives 5758, and as a count its max entry is 26 — description and instrument disagree on both axes] |
| Y446 | item 8: synthetic-shock harness — edit parsed data, re-solve | CONFIRMED | file read: `round6.py`'s "razing a Chinese node" block | zeroes `hangzhou`/`beijing` node wealth and re-runs DRAIN, functionally matching the description |
| Y447 | cost per good = one uncapacitated MCF on 80 nodes/318 arcs + O(V+E) sweep | CONFIRMED | measurement: `N`, `EDGES_UND`, `ARCS` lengths | N=80, edges=159, arcs=318 (=159×2); `bounds=(0,None)` confirms uncapacitated |
| Y133 | repeated 12-run experiments don't reproduce closely enough for a finer figure | CONFIRMED | measurement: own 3×29-good timing run | own quick run gave totals 0.098-0.117s, itself outside v5.0's claimed 0.17-0.21s window, supporting the non-reproducibility finding |
| Y134 | v5.0's "0.17-0.21s" not reproducible: 1, then 0, then 0 runs land inside it | CONFIRMED | consistency with own re-measurement | own totals (0.098-0.117s) also fall outside that window every time, consistent with the claim |
| Y448 | "milliseconds each" holds already with a generic LP; no specific projection for native network simplex | CONFIRMED | measurement/derivation, consistent with own timing (3.4-4.0 ms/good average) | own run: 3.4-4.0 ms/good, well under "tens of milliseconds" |
| Y449 | two implementations of one spec: reference solver + DLL (items 4-7 in host language) | CONFIRMED | design stipulation; consistency w/ §2.8 | §2.2 text describes this exactly; DLL doesn't exist yet, consistent with §2.1 |
| Y450 | must agree on orientation exactly, no tolerance band, replaces v1's identity check | CONFIRMED | file read: §2.8 "Cross-implementation" row | matches almost verbatim |
| Y451 | parsers/harness stay reference-only; DLL never reads a save | CONFIRMED | design stipulation, consistent | no contradiction; DLL reads live memory per §2.2's own text |
| Y452 | inland is derived (no coastal province among members), not trusted from flag | CONFIRMED | file read: `nodes.json`, `coastal.json` (via `val62p4_inland.py`) | derivation logic reproduced exactly |
| Y453 | flag/derivation disagree at exactly one node, siberia (1781/1782 Arctic-coast); 25 vs 26 | CONFIRMED | measurement: `val62p4_inland.py` | "flag inland count: 26 derived inland count: 25", "flag minus derived: ['siberia']", "1781 coastal: True 1782 coastal: True" |
| Y129 | wealth = TAX_COEFF·base_tax + GP_COEFF·base_production·(1+goods mods)·price, no tax modifier | CONFIRMED | file read: `solver.py:province_table` | `tax=TAX_COEFF*base_tax*(1+tmod)` with `tmod=0.0` always (comment: "tax term takes NO province-state modifier"); `gp=GOODS_PRODUCED_FACTOR*base_production*(1+gmod)` |
| Y130 | 4 modifiers reach goods_produced; at 1444 only devastation live (11 provinces); unrest defined, not read | CONFIRMED | file read: `solver.py` + EU4 static_modifiers file | `STATE_GOODS_MOD` has exactly 4 keys (devastation/prosperity/occupied/under_siege); `LIVE_STATE_MODS=('devastation',)`; measure6.out "devastated provinces counted 11"; game file's `unrest={...}` block exists (line 487) with `local_tax_modifier=-0.02` but `STATE_TAX_MOD={}` (empty) — defined and not read |
| Y132 | ~0.1s for 29 goods, ms not tens of ms, "single-digit" deliberately withdrawn | CONFIRMED | measurement: own timing run | 0.098-0.117s total (order 0.1s), 3.4-4.0 ms/good average, consistent with stated withdrawal of "single-digit" given documented replicate variance |

## §2.2a — What map this is for (17 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y454 | v2 called target "map-agnostic" while proving properties only on the measured map | CONFIRMED | file read: `../v2-drain/per-good-trade-spec.md` | line 5: "Target: EU4 (final patch, 1.37.5 Inca), extended timeline compatible, **map-agnostic**"; v2's properties are all measured on vanilla 1444 only |
| Y455 | Premise 1: reachability = LP feasibility, feasible because Σb_g(n)=0 identically | CONFIRMED | derivation | standard b-flow feasibility fact on a connected graph |
| Y456 | on >1 component, global balance insufficient; cross-component imbalance is infeasible outright | CONFIRMED | measurement: `toys.py` T5 | "LP success: False \| The problem is infeasible. (HiGHS Status 8...)" on a disconnected per-component-imbalanced graph |
| Y457 | vanilla 1444 is one component | CONFIRMED | measurement: `solver.py` `COMPS` | `len(COMPS)==1`, containing all 80 nodes |
| Y458 | solver must compute components once at load; single component proceeds | CONFIRMED | design stipulation (forward requirement) | consistent with document's stated incompleteness elsewhere (§2.1); current `drain.py` has no component-aware logic for DRAIN specifically (only the legacy v1 Laplacian path does), but this is stated as a requirement, not a built fact |
| Y459 | solver must not silently hand an infeasible program to the LP | CONFIRMED | design stipulation | same as Y458; not yet enforced in `drain.py` but stated as a requirement |
| Y460 | v1 had per-component renormalisation; v2 dropped it without replacement; v3 restores it | CONFIRMED | file read: `../v1-laplacian/per-good-trade-spec.md`, `../v2-drain/per-good-trade-spec.md` | v1 line 31: "Solve per connected component, renormalizing `s` and `c` within each so they balance. Skip isolated nodes."; v2 spec has zero mentions of "component" |
| Y461 | Premise 2: Phase 0 is a no-op, or map-dependent properties read as measurements | CONFIRMED | design/model framing, consistent | matches §1.1's structure; vanilla confirmed Phase-0-inert (see Y462/props6.py) |
| Y462 | where Phase 0 acts, three properties weaken and spec says so | CONFIRMED | file read: table at spec L1019-1024 | matches the table's own three rows exactly |
| Y463 | Global DAG still proved where Phase 0 acts (pendant edges are bridges, cannot close a cycle) | CONFIRMED | measurement: `toys.py` T1 (pendant net-importer, V029/V126) | directed graph `[('A','B'),('A','D'),('B','C'),('D','B')]` is acyclic |
| Y464 | sink-set equality measured exact 29/29 on 2-core, fails where Phase 0 acts | CONFIRMED | measurement: `props6.py` (2-core: "sink-set identity holds on 29 of 29 goods; failures []") + `toys.py` T1 (Phase-0-active: actual sinks `['C']` != formula set `['B']`, EQUAL: False) | both halves independently confirmed |
| Y135 | free-edge determinism unaffected by Phase 0; index-independence not (key reads post-fold β) | CONFIRMED | file read: `drain.py` sweep key construction + table at spec L1024 | key is `(DEF, beta, index)` computed on peeled/folded balances, matching the claimed mechanism |
| Y466 | T2 (selected flow-terminal loses sinkhood to free edge) and T3 (fallback promotion) both break equality inside the 2-core | CONFIRMED | measurement: `toys.py` T2, T3 | T2: actual sinks `['u2']` vs formula `['u1','u2']`, EQUAL False; T3: actual sink `['A']`, formula set empty, sink reached only via FALLBACK |
| Y467 | stated target: connected maps, min degree ≥2 → every §1.1 property proved/measured | CONFIRMED | design stipulation, consistent w/ vanilla's measured min-degree 2 (`props6.py`) | "min degree 2 max 8 mean 3.98" |
| Y468 | on connected map with pendants, algorithm still runs, still acyclic/fully-oriented/demand-serving | CONFIRMED | measurement: `toys.py` T1 | T1 ran to completion, produced a fully-oriented acyclic directed graph |
| Y469 | on disconnected map, solver must renormalise per component or refuse | CONFIRMED | design stipulation; T5 confirms the underlying infeasibility premise | `toys.py` T5 shows the current (un-renormalised) LP is infeasible on a disconnected imbalanced graph, consistent with the stated requirement not yet being built |
| Y465 | marking order reproduces DAG on 2-core, fails where Phase 0 acts (pendants have no marking order) | CONFIRMED | derivation, consistent w/ table at L1023 | pendant edges are peeled before the core sweep runs, so no marking-order comparison applies to them |

## §2.3 — Constants (44 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y470 | constants read at runtime, never hardcoded | CONFIRMED | file read: `solver.py` | `PRICES` and `GOODS_PRODUCED_FACTOR` both loaded from install files at import time |
| Y471 | nine runtime-read uses map to named defines | CONFIRMED | file read: `defines.lua` | all nine (`TRADE_PROPAGATE_DIVIDER=5`, `TRADE_PROPAGATE_THRESHOLD=2`, `TRADE_NON_CAPITAL_OFFICE=-0.50`, `TRADE_POWER_HOME_BONUS=0.1`, `MERCHANT_MAX_POWER_BONUS=2.0`, `TRADE_MERCHANT_PRESENT=0.1`, `TRADE_ADDED_VALUE_MODIFER=0.05`, `CARAVAN_FACTOR=3.0`/`CARAVAN_POWER_MAX=50`/`CARAVAN_POWER_MIN=2`, `PS_MOVE_TRADE_PORT=200`) found exactly |
| Y472 | TRADE_MERCHANT_PRESENT is a bonus on income, not trade efficiency | CONFIRMED | file read: `defines.lua:1201` | `TRADE_MERCHANT_PRESENT = 0.1, -- bonus on income if trade present` |
| Y136 | the two coefficients differ in kind: GP_COEFF read, TAX_COEFF not | CONFIRMED | file read: `solver.py` | `GOODS_PRODUCED_FACTOR = _read_gp_coeff()` (file read) vs `TAX_COEFF = 1.0` (literal) |
| Y137 | v3.0-v5.0 said neither coefficient in a file, shipped a sweep that walked past the block holding one | PARTIAL | file read + instrument read [rechecked by coordinator] | text half confirmed in all three versions (v3 L544-545, v4 L617, v5 L717: 'hardcoded in the binary — defines.lua and common/defines/ were searched and contain neither'); the sweep survives and confirms the mechanism: `../v4-owner-agnostic/scripts/audit_modifiers.py` loads `common/static_modifiers` definitions (L21) with `trade_goods_size` among WEALTH_KEYS (L30) yet reports only history-applied modifiers, so `provincial_production_size` (GP_COEFF's block) never surfaced — 'walked past the block' confirmed; but only v4's tree ships the sweep (v3 has no scripts dir; v5's 61 scripts include no modifier sweep), so 'v3.0 through v5.0 ... shipped' overstates |
| Y473 | GP_COEFF=0.2, measured on 4 provinces/4 dev levels (Caceres/Girona/Garnatah/Barcelona) | CONFIRMED | measurement: `val62p4_garn.py` on `VANILLA_start.eu4` | base_production values 2,3,4,5 confirmed for Caceres(1747)/Girona(212)/Garnatah(223)/Barcelona(213) exactly, matching arithmetic 0.2×dev = 0.40/0.60/0.80/1.00 |
| Y474 | TAX_COEFF=1.0, measured on 2 provinces/2 dev levels (Garnatah/Caceres) | CONFIRMED | measurement: `val62p4_garn.py` | Garnatah base_tax=6, Caceres base_tax=2, matching arithmetic 1.0×base_tax = 6.00/2.00 |
| Y475 | both read off base tooltip lines, no owner term; Garnatah also has local_autonomy=0 | CONFIRMED | measurement: careful brace-matched block extraction of province 223 from `VANILLA_start.eu4` | `local_autonomy` does not appear anywhere in Garnatah's true 3,291-char block (absent field defaults to 0); note: an earlier fixed-offset scratch script (`val62p4_quant.py`) misread "2.500" here by overrunning the block boundary into province 224 (Sevilla) — re-verified with proper brace matching |
| Y476 | neither coefficient read off a province window (owner modifiers, some randomised) | CONFIRMED | design/methodological stipulation, consistent with §1.3's randomised-owner-modifier discussion | no contradiction found |
| Y477 | prices from common/prices/00_prices.txt at runtime, never hardcoded | CONFIRMED | file read: `solver.py`, EU4 install | `PRICES` loaded via `pdx.load(...00_prices.txt...)`; file exists |
| Y478 | design constants: excluded-goods (gold default), P₀=2.0, α_Φ=2.0, TIE_EPS=1e-3/TIE_EPS2=1e-6, r=0, tolerance 1e-11 | CONFIRMED | file read: `solver.py` (`EXCLUDED={"gold","unknown"}`), `flowop.py` (`TIE_EPS=1e-3`,`TIE_EPS2=1e-6`,`ZERO_TOL=1e-11`), `drain.py` (`dilate_r=0` default), `round6.py`/`measure6.py` (`A_PHI=2.0`, `ALPHA=...PRICES[g]/2.0...`) | all values match exactly |
| Y479 | α_Φ/TIE_EPS/TIE_EPS2 are hyperparameters, developer taste, no justification offered | CONFIRMED | file read: spec text L1083-1089, self-referential | document explicitly withdraws all prior derivations for α_Φ |
| Y339 | changing hyperparameters is a design decision; §1.6 records sensitivity | CONFIRMED | measurement: `measure6.py`'s α-sweep rows | "sink count at alpha 1,1.5,2,3,4,8: [3, 1, 2, 2, 1, 1]", European-scaling rows, wealth-noise rows all present |
| Y996 | TIE_EPS/TIE_EPS2 set the Phase-2 objective per the printed formula | CONFIRMED | file read: `flowop.py` | `TIE_COST = 1.0 + TIE_EPS*(_a1+_a2)/2.0 + TIE_EPS2*_gen` where `_gen=frac(min·max·7919)` — matches the printed formula exactly |
| Y1026 | two cost terms differ; only the first-order term means anything | CONFIRMED | derivation, consistent with design framing | matches spec text |
| Y1027 | second-order term is tie-breaking only, arbitrary form | CONFIRMED | derivation | consistent |
| Y998 | single cost vector doesn't guarantee uniqueness; depends on b too | CONFIRMED | derivation | standard LP-degeneracy fact: reduced-cost zero depends on both cost and constraint/RHS structure |
| Y1028 | zero-reduced-cost arcs: aggregate 40 (unit)→0 (first-order); per-good 41 across 18/29 goods | CONFIRMED | measurement: ran `p3_margin.py` | exact match: "unit aggregate 40 ... first aggregate 0 ... per-good total 41 on 18 of 29 goods" |
| Y1029 | adding second-order term → 1 arc on 1 good | CONFIRMED | measurement: `p3_margin.py` | "full ... per-good total 1 on 1 of 29 goods \| goods still carrying one: {'paper': 1}" |
| Y1030 | solver's optimality tolerance is a correctness requirement, not a performance knob | CONFIRMED | measurement: `round6.py` HiGHS-floor test | a rejected `1e-11` silently reverts to the 1e-7 default with `success=True`, "the failure is silent unless stderr is read" — exactly the correctness risk claimed |
| Y1053 | scipy `linprog(method="highs")` docs default dual/primal tolerance = 1e-07 (scipy 1.18.0) | CONFIRMED | measurement: read `scipy.optimize._linprog_highs._linprog_highs`'s docstring; checked `scipy.__version__` | installed scipy is exactly 1.18.0; docstring: "Dual feasibility tolerance. Default is 1e-07." / "Primal feasibility tolerance. Default is 1e-07." |
| Y1031 | margin as low as 3.8e-8 sits inside the 1e-7 default tolerance | CONFIRMED | measurement: `p3_bisect.py`/`p3_margin.py` | worst per-good margin (`copper`) 3.7648e-08 < 1e-7 default |
| Y1032 | 6 permutations: copper/paper differ on 12/8 edge-slots, objectives differ 7.7e-10 rel | PARTIAL | measurement: ran `p3_perm.py` section B (default tolerance) | paper: sum of flips over 6 perms = 8 (exact match), max spread 7.66e-10 ≈ claimed 7.7e-10 (exact match); copper: flips `[0,0,2,0,0,2]`, sum=4/max=2, not 12 — reproducibly (re-ran twice, identical, deterministic with the fixed seed) — [rechecked: coordinator re-ran p3_perm.py section B — copper flips [0,0,2,0,0,2] (sum 4, not 12), paper [2,2,2,0,2,0] (sum 8, exact), max spread 7.66e-10 (exact)] |
| Y1054 | mechanism confirmed by bisecting the tolerance against copper | CONFIRMED | measurement: `p3_bisect.py` | tolerance sweep shows unset≡1e-7, both giving nonzero flips, 1e-8/1e-9/1e-10 giving zero, across all 3 tested seeds |
| Y1055 | unset and 1e-7 give the same 8 flips over 4 permutations | PARTIAL | measurement: `p3_bisect.py`, 3 seeds | unset≡1e-7 confirmed exactly (identical pair for all 3 seeds); specific count of 8 reproduces only for one seed (7): results were (2,4),(8,10),(6,8) for seeds (20260821,7,4242) — the "8" figure is seed-dependent, not the default seed's own value (2) — [rechecked: p3_bisect.py re-run — unset == 1e-7 exactly for all three seeds; the flip count over 4 perms is 2/8/6 by seed, so the quoted 8 is one seed's value; 1e-8 gives 0 for all seeds (confirmed)] |
| Y1056 | 1e-8 already gives 0 flips, first value below copper's 3.765e-8 margin | CONFIRMED | measurement: `p3_bisect.py` | tol 1e-08 → (0,0) for all 3 seeds; 1e-8 < 3.765e-08 margin, and 1e-7 (next value up) still gives nonzero flips |
| Y1057 | flips appear exactly when tolerance exceeds the margin | CONFIRMED | derivation from Y1054-Y1056's measurements | consistent with the bisection data |
| Y1033 | flowop.LP_OPTS ships 1e-10, HiGHS's floor, spread 1.1e-15 | CONFIRMED | file read: `flowop.py`; measurement: `round6.py` (1e-11 rejected) | `LP_OPTS={"dual_feasibility_tolerance":1e-10,"primal_feasibility_tolerance":1e-10}`; 1e-11 explicitly rejected with "Invalid option value"; spread measured 1.03e-15/8.43e-16 in my reproduction (order-of-magnitude match to claimed 1.1e-15) |
| Y1034 | no figure moved when pinned tolerance went in; shipped order already reached true optimum | CONFIRMED | derivation from Y1032/Y1054 data | only copper/paper are order-dependent, and only under permuted column order at default tolerance — the identity/shipped order's own result is unaffected by the tolerance choice |
| Y1035 | 2nd-order term costs 0.1-0.2 self-coherence points; sinks 2-8/mean 3.69; all 29 acyclic; Φ_w sinks unchanged; ±1% noise 0 edges moved on 6 seeds | PARTIAL | measurement: custom script comparing self-coherence under first-order-only vs shipped (full) cost, correcting for `drain.TIE_COST` binding; `measure6.out`; wealth-noise rows | sinks-per-good (2/8/3.69), all-29-acyclic, and Φ_w sinks unchanged under ±1% noise (3 seeds shown, all `['genua','hangzhou']`) all confirmed exactly; self-coherence delta measured smaller than claimed: edge-goods 55.17%→55.13% (Δ0.04pt), value-weighted 54.88%→54.79% (Δ0.09pt), vs claimed 0.1-0.2 points — [rechecked: coordinator's independent construction (val62c_y1035.py) gives deltas 0.043 edge-goods / 0.098 value-weighted — only the value-weighted metric touches the claimed 0.1-0.2; sinks/spg/acyclicity identical in both worlds] |
| Y1036 | 2nd-order term replaces an arbitrary+order-dependent tiebreak with an arbitrary-but-invariant one | CONFIRMED | derivation, consistent w/ Y1014/Y495/Y141's measured order-invariance | matches spec framing |
| Y1037 | normalisation is load-bearing for 2nd-order term, wasn't for 1st-order alone | CONFIRMED | measurement: `p3_norm2.py` | real per-good/aggregate differences appear across normalisations under the full (2nd-order) cost, unlike the linear 1st-order-only case |
| Y997 | every DRAIN solve uses TIE_COST; unit costs kept only for FLOW/TREE ops and verify.py checks | CONFIRMED | file read: `flowop.py:mincost_flow` | docstring: "cost=None means unit arc costs. The per-good operators in this file and in verify.py use unit costs; DRAIN's Phase 2 passes TIE_COST." |
| Y483 | DLC state is a third input axis (treasure fleet/caravan power DLC-conditional) | CONFIRMED | design stipulation, consistent with §3.11/§3.12's caravan/treasure-fleet discussion | no contradiction found; standard EU4 DLC architecture (Wealth of Nations/Conquest of Paradise) |
| Y1039 | normalisation choice is a third arbitrary decision, recorded not defended | CONFIRMED | file read: spec text L1174-1175 | "The choice is recorded rather than defended: min-max is what the implementation uses..." matches near-verbatim |
| Y480 | DRAIN's knobs at defaults: r=0, tolerance 1e-11 (ρ withdrawn, never shipped) | CONFIRMED | file read: `drain.py:phase1(core, beta, dilate_r=0)` signature; §3.13 text | no `ρ` parameter in shipped Phase 1; §3.13 confirms ρ=0.5 belongs only to "its own Phase 1" of the (unshipped) calibration option |
| Y481 | zero-flow tolerance is absolute, not relative, couples to scale of b | CONFIRMED | file read: §3.13 text | "The tolerance is absolute, and v2.1 was wrong to file it as purely numerical" |
| Y482 | calibration option (§3.13) replaces Phase 1, moves the tolerance, removes α's clamp; baseline doesn't use it | CONFIRMED | file read: §3.13 text | "a demand-mass quantile ρ=0.5 in its own Phase 1", "a twig tolerance of 3e-4" (replaces 1e-11), "α unclamped at exponent 2 (cloves reaches α=16)"; "The baseline does not adopt it" |
| Y999 | structured term (absdiff) tried/rejected: both costs make all 159 edge costs distinct; structured leaves 11/29 goods w/ alternative optimum vs shipped's 1 | CONFIRMED | measurement: `p3_margin.py` | "full 159 distinct...absdiff 159 distinct" (both 0 duplicates); "absdiff...goods still carrying one: {chinaware, cloth, cocoa, copper, cotton, gems, glass, paper, spices, tropical_wood, wine}" = 11 goods exactly |
| Y1038 | normalisation sweep: world-total moves aggregate 7/159 edges; 13/29 per-good graphs move under ≥1 candidate | CONFIRMED | measurement: ran `p3_norm2.py` | exact match: "total 7/159" agg edges; "union over max/mean/total/totalxN, vs min-max: 13 of 29" |
| Y1115 | \|w[u]-w[v]\| telescopes along a path; frac(lo·hi·7919) has no such structure | CONFIRMED | derivation | correct: a telescoping sum of absolute wealth differences along any path collapses toward endpoint-dependent behaviour, unlike the multiplicative-fractional term |
| Y1116 | w/mean and N·w/sum are algebraically the same vector; on this field so are min-max and w/max | CONFIRMED | measurement: `p3_norm2.py` | mean and totalxN produce identical downstream results (0/159 agg, identical 9-good sets, both at LP_OPTS and default tol) though the script's strict bit-identity check reports `False` (floating-point computation-order noise, not an algebraic difference); min-max vs max: 0/159, 0/29, 0/29 differences confirms exact equivalence (minimum wealth is exactly 0) |
| Y1117 | cape_of_good_hope holds no counted province: 20 members = 1 sea zone + 19 land, none owned at 1444 | CONFIRMED | measurement: `val62p4_cape.py` | "cape_of_good_hope members: 20"; "sea zones among members: [1460]"; "land provinces: 19"; "land members OWNED at 1444: 0 []"; model node wealth 0.0, global min |
| Y1118 | re-measuring probe must inherit LP_OPTS: unpinned w/mean gives 5 goods, pinned gives 9, 5 ⊂ 9 | CONFIRMED | measurement: `round6.py`, `p3_norm2.py` | pinned: 9 goods `{copper,cotton,fish,gems,glass,grain,paper,wine,wool}`; default: 5 goods `{copper,fish,gems,grain,paper}` — a strict subset |

## §2.4 — The tradenodes file (28 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y484 | generated once from campaign start's Φ_w, owned by DLL in memory, no per-session regen | CONFIRMED | design stipulation, consistent with rest of §2.4-§2.6 | no contradiction |
| Y485 | engine validates (not sorts) declaration order, logs once per violation, tolerates violations | CONFIRMED | file read: `../v2-drain/game-session.md` (B2) | 159/159 backward links logged exactly once each; game loaded and ran to Dec 22 1444 without incident |
| Y486 | 159/159 backwards file logs 159 errors, loads/plays normally, total/retention unchanged, power fields within run-to-run variance | CONFIRMED | file read: `../v2-drain/game-session.md` (B2, Observation 3) | `total` 1/79 differ max 0.012% (identical to the noise-only control); `retention` 0/80 differ in both; `current`/`local_value`/`outgoing` differ by a similar magnitude in noise vs test |
| Y487 | 2-node cycle: STACK_OVERFLOW at 0x00007FF6DDE6A8B4, 1002 frames, three launches, controls fine, both from game-session.md | PARTIAL | artifact read + observation cited by the claim [rechecked by coordinator] | the observation itself is artifact-true: three dumps on disk (2026-08-20 13:42:50 / 13:46:17 / 16:56:21) with identical address, 1002 frames each, cycle mod loaded — so 'reproduced on three launches' is correct, and 'the dump records no per-frame addresses' verified (frames read '(function-name not available) (+ 0)'); but the sentence cites *both* observations to `../v2-drain/game-session.md`, which records **two** launches ('reproduced on two independent launches', 'stack overflow, twice', L71-72) — the third dump post-dates the cited record, so the citation, not the count, is wrong |
| Y488 | crash dump records no per-frame addresses | CONFIRMED | file read: `../v2-drain/game-session.md` | trace shows "1 … 1002 eu4.exe (function-name not available)" — no per-frame addresses given |
| Y489 | acyclicity is a hard correctness requirement, established by observation | CONFIRMED | file read: `../v2-drain/game-session.md` (B1+B3 conclusion) | "Combined with B1, the constraint on the generator is exactly one thing — the emitted graph must be acyclic" |
| Y490 | reversed link honoured completely: Valencia moves sides, Sevilla becomes end node, merchant switches steer→collect, two countries vanish | CONFIRMED | file read: `../v2-drain/game-session.md` (B3) | "zero non-descriptor lines"; Valencia moved incoming; Sevilla "five incoming links, zero outgoing"; Castile's merchant switched to "collecting"; Aragon(14.2)/France(3.3) "disappear from the node entirely" |
| Y491 | (restates Y490's specific findings) | CONFIRMED | same as Y490 | same evidence |
| Y492 | every provincial power figure unchanged in that test | CONFIRMED | file read: `../v2-drain/game-session.md` (B3) | "every *provincial* power figure stayed identical (Castile 129.9, Portugal 88.5, Morocco 23.4, Granada 11.6)" |
| Y493 | that test is the mod's core premise verified end to end | CONFIRMED | file read: `../v2-drain/game-session.md` (B3) | "This is the mod's core premise demonstrated end to end" — near-verbatim match |
| Y494 | item 1: emit in decreasing Φ_w marking order, matches engine convention, vanilla file 0/159 violations | CONFIRMED | measurement: `val62p4_tn.py` | vanilla: "order-violations=0" of 159 links |
| Y138 | canonical order still required but no longer decides the installed map | CONFIRMED | derivation, consistent with Y139-Y143's measured order-invariance | matches spec narrative |
| Y495 | Phase 2 min-cost b-flow degenerate under unit costs: many distinct equal-cost supports | CONFIRMED | measurement: `relabel6.py` | "unit cost (former)...permutations returning a DIFFERENT optimal support: 40 of 40" |
| Y139 | 40 of 40 permutations return a different optimal support (unit cost) | CONFIRMED | measurement: ran `relabel6.py 60 4242 7 999` | exact match: "unit cost (former)...40 of 40" |
| Y140 | those permutations reach an objective identical to within a few ULP | CONFIRMED | measurement: `relabel6.py` | "unit cost (former) objective 0.789420000928 max rel deviation 5.626e-16" (≈2.5 ULP) |
| Y147 | §2.3 now breaks those ties inside the objective | CONFIRMED | file read: `flowop.py` `TIE_COST` | consistent with Y996 |
| Y141 | 0/40 under tie-break cost; 180 relabellings → 0/159 edges; instrument aborts on old objective (26/159 disagreement) | CONFIRMED | measurement: ran `relabel6.py 60 4242 7 999` (180 total) + custom script comparing unpatched (unit-cost) reimplementation vs shipped support on identity | tie-break: "0 of 40"; pooled 180: "orientation changed: 0 of 180", "edges moving: mean 0.00, range 0-0"; unpatched-vs-shipped comparison: 26 of 159 undirected edges disagree exactly (52 directed mismatches / 2) |
| Y143 | 2nd-order term takes per-good sensitivity 84/290→13; pinning tolerance takes it to 0/290 | PARTIAL | measurement: ran `val62p4_relab.py` and cross-validated with `p3_relabel_pergood.py` (same seed 20260821, both give identical results) | reproduced: first-order-only(default tol) 80/290 (doc: 84), full-cost(default tol) 12/290 on 2 goods (doc: 13), full-cost+LP_OPTS 0/290 (doc: 0, exact match) — final figure confirmed exactly, intermediate figures off by small but deterministic/reproducible amounts — [rechecked: shipped p3_relabel_pergood.py prints 76 (first-order+LP_OPTS) / 12 (full+default tol) / 0 (full+LP_OPTS) — final figure exact, 84 and 13 reproduced by no construction (76/80/86 and 12 across four independent builds)] |
| Y146 | counts HiGHS-specific in detail, not in kind: any simplex returns a vertex of a degenerate face | CONFIRMED | derivation | standard LP theory: any simplex-family solver returns a basic feasible (vertex) solution |
| Y144 | v6.0's 580/580 sweep script measures unit-cost objective, superseded by 13-of-290 | CONFIRMED | file read: `../v5-owner-agnostic/scripts/_audit_b_1444perm.py` and its dependency `_audit_b_drain.py` | script exists; `_audit_b_drain.py` line 58: `linprog(c=np.ones(len(arcs)), ...)` — confirmed unit cost |
| Y148 | priority key ties in more places than §1.1 documents; none fire on 1444 (0 exact ties of all three kinds) | CONFIRMED | measurement: ran `props6.py` | "exact (DEF,b) key collisions: 0", "Phase-1 within-cluster argmin ties: 0 ; top-k cut ties: 0" — all three zero counts match exactly |
| Y149 | end-flag list is a function of the world, not node order: {genua,hangzhou} in all 180 relabellings | CONFIRMED | measurement: `relabel6.py` (180 relabellings) | "sink-count distribution: {2: 180}"; "most frequent end holders: [('genua', 180), ('hangzhou', 180)]" — both nodes present in all 180 runs, sink count always 2 |
| Y150 | 1444 has two end nodes (genua, hangzhou) vs vanilla's three | CONFIRMED | measurement: `measure6.py` + `val62p4_tn.py` | measure6.out: "Phi_w sinks ['genua', 'hangzhou']"; vanilla file's `end=yes` nodes: `['genua', 'venice', 'english_channel']` (three) |
| Y498 | end count not fixed, follows wealth field and α_Φ, emitter reads it from the solve | CONFIRMED | consistency with measure6.out's α-sweep ("sink count at alpha 1,1.5,2,3,4,8: [3,1,2,2,1,1]") | count genuinely varies with α_Φ |
| Y499 | item 3: link reversal = move outgoing block, reverse path list, reverse control pairs | CONFIRMED | file read: `../v2-drain/game-session.md` (B1 setup) | shows exact worked example: `path = 4546 223 222 221 1295` → reversed `1295 221 222 223 4546`; `control` pairs likewise reversed |
| Y500 | item 4: location/members/inland=yes/ai_will_propagate_through_trade/unrecognized keys round-trip byte-faithfully | CONFIRMED | file read: `../v2-drain/game-session.md` | "Verified after writing: 80 blocks, 160 links, the 2-cycle present, everything else byte-faithful" |
| Y496 | node window renders incoming/outgoing link lists in file declaration order | CONFIRMED | file read: `../v2-drain/game-session.md` (B2, Observation 4) | "the node window's link lists are rendered in file declaration order, so an emitter that reorders nodes will reorder what the player sees" — near-verbatim match; vanilla lists "Tunis, Safi", reversed file lists "Safi, Tunis" |
| Y497 | item 2: end=yes on every Φ_w sink, stripped from any former end node gaining outgoing links | CONFIRMED | design stipulation, consistent with measured end-set behaviour (Y149/Y150/Y498) | no contradiction |

**Continuity note:** this fragment extends prior scratch work under `scripts/val62p4_*.py` (all read, re-run, and — where a bug was found — corrected in place rather than trusted; see Y475's note on `val62p4_quant.py`'s block-overrun bug, resolved with a fresh brace-matched read). One new instrument was added: `scripts/val62p4_selfcoh.py` (Y1035's self-coherence delta, correcting for `drain.py`'s `from flowop import TIE_COST` name-binding, which an earlier draft of the same idea in this session had missed). `scripts/val62p4_oldobj_diff.py` was added inline to settle Y141's parenthetical (26-of-159 disagreement).

## §2.5 — Runtime attachment (4 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y501 | Attachment uses pattern scanning/hooking, following the "EU4dll precedent" | UNTESTABLE | searched the whole `per-good-trade` tree and the EU4 install for a copy of the EU4dll project | No copy of EU4dll exists anywhere on disk (only the phrase itself, verbatim, across v1-v6 spec versions). Its actual scanning/hooking method and scope cannot be checked; only `eu4.exe` itself (38,462,504 bytes, matches the v1 audit's recorded size) is available as a target |
| Y502 | Mod ships a runtime-patching DLL, not a modified executable | CONFIRMED | consistency check: grepped `per-good-trade-spec.md` for every "the DLL" reference | Every other mention of the runtime component ("owned by the DLL in memory" L1186, "the DLL never reads a save" L989, "the DLL that would prove it does not exist yet" L947) is consistent with a DLL, never a modified executable |
| Y503 | The binary is frozen, so offsets found stay found | CONFIRMED | file metadata check (no execution): `Get-Item eu4.exe .Length` | `eu4.exe` is a static 38,462,504-byte file, matching the size recorded in the v1-laplacian audit (`38,462,504-byte PE at version v1.37.5.0`) — a fixed installed binary that does not change between sessions |
| Y504 | §1.10's nation-pair gates are hooked and returned true at the call site, not via a shared predicate | CONFIRMED | consistency check against §1.10 | §1.10 (L793): "Any mechanism gated on one nation being upstream or downstream of another evaluates TRUE" — Y504 restates this as an implementation mechanism (hardcode true rather than compute a predicate); no contradiction anywhere in the document |

## §2.6 — Writing to the engine (10 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y505 | Monthly trade tick runs in three passes (power/modifiers; propagation; value) | UNTESTABLE | checked §2.7/§3.13 for probe status; checked v1 spec for the claim's origin | §3.13 (L1815): "only pass caching, pass-2 content, write windows... truly need the debugger" — still listed as open in §2.9 (twelve items 1-11 and 16 still open). No debugger session exists in our materials; RUNNING GAME excluded by the brief |
| Y506 | Written each tick: node trade value = Σ_g value_g(n) | CONFIRMED | derivation: cross-checked against §1.8's node-wide/per-good framework and §3.10's income identity, which use the same `value_g(n)` term consistently (L1752) | No contradiction; a per-good sum aggregating to a node total is the natural consequence of the 29-separate-graphs design used throughout |
| Y507 | Written each tick: node collectible pool = Σ_g value_g(n)·collected_share(n,g) | CONFIRMED | derivation, same cross-check as Y506 | Identical term used identically at L1278 and L1752 (§3.10's derivation) |
| Y508 | Written each tick: per-link value = net realized flow summed over goods, in the installed Φ_w direction | CONFIRMED | consistency check against §1.6/§3.9 (Φ_w as the one installed graph) | Matches the document's single-installed-graph design throughout; also consistent with §2.8's measured Φ_w-vs-realized sign disagreement (54.8%), which presupposes per-link values are written in the Φ_w direction and can therefore be negative |
| Y509 | Country trade income is derived by the engine from the written fields, unless stored | UNTESTABLE | no built DLL, no decompiled engine income-calculation code in our materials | This is an assumption about vanilla engine internals underlying the whole §2.6/§3.10 design; nothing in the game files, saves, or scripts observes the engine's own country-income computation directly |
| Y510 | `collect_pool` is per-good on the inside via `collected_share(n,g)`/`P_transfer(g)` (§1.8, commodity-specific) | CONFIRMED | derivation: read §1.8 (L753-759) and §3.10 (L1749-1756) | §1.8: "Trade power and collect/transfer intent are node-wide. What varies per good is what they produce," and "Transfer eligibility is per good" — supports the premise exactly as stated |
| Y511 | `powershare_C` is good-independent (merchant-or-home property), so it factors out of the per-good sum | CONFIRMED | derivation: same §1.8/§3.10 read; algebra checked step by step | §3.10 (L1756): "Every term that feeds a collector's power at a node is node-wide... so none of them can reintroduce a `g`" — the factoring step is algebraically valid given §1.8's stated premises |
| Y512 | Two deadlines: display immediately after the value pass; AI reads during the month | UNTESTABLE | checked §3.13's probe-status list | Part of the still-open pass-caching/write-window probes (§3.13, §2.9); requires debugger observation not in our materials |
| Y513 | Payment bounded by month boundary; treasury reconciles against previous month's income | UNTESTABLE | same as Y512 | Same open-probe status; no primary source available to settle engine reconciliation timing |
| Y514 | Per-link values are written net, which can be negative when realized flow opposes the drawn arrow | CONFIRMED | consistency + cross-reference to §2.8's measured sign-disagreement figures | §2.8/§1.6: Φ_w agrees with per-good graphs on only 54.8% of edge-goods (value-weighted) — realized flow genuinely opposes the installed direction on a large minority of edges, which is exactly the condition under which a net-written value would be negative |

## §2.7 — Probes (26 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y515 | Items 1-10 are v1's probe set, "settled with a debugger... in one session," though several are observable without one | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md` L284; cross-check against `../v2-drain/game-session.md` | v1 §2.7 opens verbatim "Settled with a debugger on a vanilla install, in one session." `game-session.md` (the claim audit's own session) settled probes 13-15 and the link-reversal/declaration-order questions using screen capture and save-file parsing — explicitly *not* a debugger — directly supporting "several are observable without one" |
| Y516 | Items 12-15 are done: run against 1.37.5 in `game-session.md`, folded into §1.9, §2.4, §3.6 | PARTIAL | file read: `game-session.md`; cross-checked §1.9 (L780-787), §2.4 (L1188-1197), §3.6 (L1619-1636) | Items 13-15 were run and are folded into exactly those three sections (verified: France-power example in §1.9, cyclic-crash/declaration-order text in §2.4, cyclic-crash text in §3.6). But item 12 was *not* run — the document's own next sentence (Y517) says it was dropped rather than run, so "they were run" overstates for item 12 — [rechecked: L1296-1298 re-read — 'Items 12-15 are done. They were run ...' followed by 'Item 12 was dropped rather than run'; the folded-into sections verified] |
| Y517 | Item 12 dropped rather than run: under owner-agnostic wealth the per-province production-income field is unread | CONFIRMED | derivation: read §1.3 (L253-282) | §1.3's wealth formula reads only `base_tax`, `base_production`, trade good and province-state modifiers — it never reads a precomputed production-income field; consistent with probe item 9's own framing of that field as a distinct, separately-checkable vanilla concept |
| Y518 | Probe 13 (cyclic node file): engine does not tolerate a cycle, `EXCEPTION_STACK_OVERFLOW`, 1002 frames, twice | CONFIRMED | file read: `game-session.md` §B1 | "`EXCEPTION_STACK_OVERFLOW` (C00000FD), reproduced on two independent launches... 1002 recorded frames, all at one identical return address" — matches exactly |
| Y519 | Probe 14: incoming-link entry only navigates; clicking Safi switched the window, dispatched nothing | CONFIRMED | file read: `game-session.md` §A1 | "The incoming entry is a pure navigation control" — clicking navigated Sevilla→Safi, no merchant dispatched, matches verbatim |
| Y520 | Probe 15: "where it already has power" is not a precondition; France 0 provinces/0 merchants, 3.3 power in Sevilla | CONFIRMED | file read: `game-session.md` §A2 | France: "0.0" provinces and merchant columns, 3.3 total power, itemised "Transfers from traders downstream: +3.1" — matches exactly |
| Y151 | §1.9's "every immediately upstream node" is consistent with probe 15's reading | CONFIRMED | file read: `game-session.md` §A2 conclusion; §1.9 L781 | game-session.md: "§1.9 is correct as written and needs no change... with no condition on the receiving node" — matches §1.9's actual wording exactly |
| Y521 | §2.4 item 3 link-reversal check: done and passed, hand-flipped link loaded with zero errors and rebuilt the economy | CONFIRMED | file read: `game-session.md` §B3 | "`logs/error.log`: zero non-descriptor lines"; Valencia moved outgoing→incoming, Sevilla's merchant switched steering→collecting, power shares recomputed — matches |
| Y522 | Declaration-order question settled: engine validates order, logs one error per violation, tolerates violations | CONFIRMED | file read: `game-session.md` §B2 | "exactly 159 lines, one per backwards link"; game loaded, ran to Dec 1444 without incident — matches exactly |
| Y523 | Probe 1: pass caching — does flipping a link crash/stale/rebuild per pass | UNTESTABLE | checked §3.13's probe-status list | Explicitly still debugger-only and open (§2.9's "twelve items still open (1-11 and 16)") |
| Y524 | Probe 2: what imposes pass 2's ordering, given propagation is one hop | UNTESTABLE | same | Same open-probe status |
| Y525 | Probe 3: write windows relative to the value pass | UNTESTABLE | same | Same open-probe status |
| Y526 | Probe 4: negative link values — write one, observe arrow rendering/protect-trade | UNTESTABLE | same | No live game observation available; still open |
| Y527 | Probe 5: merchant storage — flip a link hosting a steering merchant | UNTESTABLE | same | Still open; B3's flip test used an *unassigned* corridor, not a steering merchant, so it does not settle this |
| Y528 | Probe 6: caravan, twice — incoming-in-Φ_w link and a no-goods link | UNTESTABLE | same | Still open |
| Y529 | Probe 7: is arrow render state separate from the economic link | UNTESTABLE | same | Still open |
| Y530 | Probe 8: `TRADE_PROPAGATE_THRESHOLD` semantics — does the raw requirement double | UNTESTABLE | same | Still open; §3.13 files it "Derived — probably right, cheaply falsifiable," i.e. not yet run |
| Y531 | Probe 9: diverted colonial gold in the per-province production-income field | UNTESTABLE | same | Still open |
| Y532 | Probe 10: caller enumeration — disassemble every "is X downstream of Y" call site | UNTESTABLE | same, distinct from Y533's partial static-analysis result | The *full* disassembly/enumeration task is unstarted; only a partial static string-table scan (Y533) exists |
| Y533 | Static string-table analysis already yields three named call sites (DIPLO_SELLPROV_NOT_UPSTREAM, TREASURE_FLEET_TOOLTIP_CANT_REACH, TRADE_POWER_UPSTREAM); no colonisation refusal string | CONFIRMED | file read: grepped EU4 `localisation/*.yml` for all three identifiers, and for colonization+upstream/downstream text | All three found verbatim: `eldorado_l_english.yml:1074`, `tmm_l_english.yml:1311`, `tradenodes_l_english.yml:142` — text of each confirms "compare trade capitals" reading. No colonization-refusal string combining "colon"+"upstream/downstream" found in any localisation file |
| Y534 | Probe 11: caravan recipient — merchant in coastal node steering to inland one | UNTESTABLE | same open-probe status | Still open (item 11 of the twelve) |
| Y535 | Engine tooltip and identifier `merchant_steering_to_inland` both read as the inland node; if so §3.11 inverts | CONFIRMED | file read [rechecked by coordinator] | the sentence asserts how the two artifacts *read*, with the runtime consequence explicitly conditional ('if that holds'); both verified: `eu4.exe` carries `merchant_steering_to_inland` verbatim, and localisation ships `MERCHANT_STEERING_TO_INLAND:0 'Merchant steering towards inland'` and `MERCHANT_PRESENT_INLAND:0 'Merchant present inland'` — both name the inland side; the antecedent's runtime truth is exactly open probe 11, which the document leaves open |
| Y1040 | Probe 16: is EU4's 1/1000 quantisation in the simulation or the serialiser | UNTESTABLE | checked §2.9's still-open list; own re-run of a similar grid check on 4 saves | §2.9 lists item 16 among the still-open twelve. `scripts/val62p5_g.py` confirms every trade field on all 4 available saves sits exactly on the 1/1000 grid (3354-3810 values, 0 off-grid per save) but this cannot distinguish simulation-side vs serialiser-side rounding without a live memory read |
| Y1041 | If rounding is in the simulation it defends lockstep MP; if only in the writer it says nothing about determinism | CONFIRMED | derivation: conditional-logic check | Both branches of the if/then are sound engineering claims independent of which antecedent holds; the sentence asserts the conditional, not the antecedent |
| Y1042 | Probe 16 settles what §2.1 may claim about the engine's defence, and whether the mod should round at its write boundary | UNTESTABLE | checked probe 16's status | Describes what an unexecuted probe would determine; nothing to check until it runs |
| Y536 | All writes land atomically at the tick hook with the sim paused | UNTESTABLE | checked §3.13's probe-status list | Requires debugger observation of the write hook; not settled by any file, save, or the game-session.md probes actually run |

## §2.8 — Validation (49 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y537 | Spice/cloves 1444: "source in Indonesia, and both source there alone" — spices from the_moluccas+kongo, cloves from the_moluccas only | PARTIAL | measurement: `scripts/val62p5_a.py`/`b.py`, graph in-degree-0 nodes per good | Graph-source lists match exactly (spices: `['kongo','the_moluccas']`; cloves: `['the_moluccas']`), and sink figures (Genoa/Brazil ranks 1/73; Genoa/Kongo/Brazil ranks 2/55/72) match exactly. But `kongo` is not in Indonesia (it is Central Africa) — the "source in Indonesia, and both source there alone" framing is inconsistent with its own very next clause naming kongo as a second spices source — [rechecked: coordinator re-derived graph-sources (spices {kongo, the_moluccas}; cloves {the_moluccas}) and sinks — figures exact; but kongo is Central Africa, contradicting 'Source in Indonesia, and both source there alone' in the same sentence, and spices is *produced* in 18 nodes, so 'source' can only mean graph-sources] |
| Y1043 | v6.0 listed Australia, Venice, Deccan among termini; none holds either sink on this field | CONFIRMED | measurement + file read [rechecked by coordinator] | field fact re-derived: spices sinks `{brazil, genua}`, cloves sinks `{brazil, genua, kongo}` — australia/venice/deccan hold none (coordinator per-good DRAIN run); the v6.0 attribution is verifiable from the project's change record: `changes-v6.md` L395-396 ('It listed Australia, Venice and Deccan among the termini') with the superseded row quoted at L5938 |
| Y540 | Malacca↔Cape post-1500: spice routes Malacca→Cape→Europe | CONFIRMED | measurement: `val62p5_a.py`, flow-into-cape share | 24.09% of world spice supply already routes through the Cape on the static 1444 field, confirming the routing mechanism exists independent of any date gate |
| Y541 | Malacca↔Cape pre-1500: withheld by range and power-at-both-ends gate, not by direction | CONFIRMED | derivation, cross-checked against §1.10's power-at-both-ends framing | Consistent with the measured fact that the edge orientation/flow already exists on the static field (Y540); realization is gated by historical range/power presence, not by the model's direction assignment |
| Y542 | 1000 AD start puts sinks in the Muslim world and Song China, "with no era data" | UNTESTABLE | checked `solver.py`'s province source | `solver.py` reads only `prov1444.json` (1444.11.11 data); the document's own qualifier ("no era data") concedes this is unverifiable with available materials |
| Y155 | hangzhou, not beijing, is China's wealth pole: 226.7 vs 143.0, ranks 12/39 of 79 | CONFIRMED | measurement: `val62p5_a.py`, node-wealth ranking | Output: "hangzhou wealth 226.7 rank 12 of 79 / beijing wealth 143.0 rank 39 of 79" — exact match; also matches `measure6.out`'s "hangzhou c_w/node-wealth rank (1,12)" |
| Y156 | Zeroing beijing also moves the map — 8 flips | CONFIRMED | measurement: `round6.py` | "zeroing beijing -> sinks ['genua','hangzhou'] 8 of 159 edges flip" — exact match |
| Y153 | On the razed field, 40 of 40 relabellings return {genua, gulf_of_siam}, hangzhou an end in none | CONFIRMED | measurement: `scripts/p3_razed_relabel.py` (pre-existing instrument, re-run) | "razed hangzhou 40 relabellings: baseline sink set returned 40/40 \| hangzhou an end in 0 \| sets {('genua','gulf_of_siam'): 40}" — exact match |
| Y544 | Ming loses Mandate: nothing moves that day, since §1.3 reads no owner property | CONFIRMED | derivation: §1.3 (L253-282) | §1.3's wealth formula reads development, trade good, province-state modifiers only — no institutional/owner flag such as the Mandate |
| Y545 | This row is the owner-agnosticism check, not a responsiveness check | CONFIRMED | consistency with §1.3's owner-agnosticism theme | Directly consistent framing |
| Y546 | Major war in China shifts corridors for the duration, reverts as devastation heals | CONFIRMED | derivation: §1.2 (devastation as a live input) + §3.6 (no-hysteresis, pure function of current state) + analogy to the razed-hangzhou test | §1.2 lists devastation as a live province-state modifier; §3.6 establishes the model is a pure function with no hysteresis, so a reverted input necessarily reverts the output; the razed-hangzhou test (Y153/Y154) is a concrete instance of exactly this mechanism |
| Y547 | Many poor provinces vs few rich: luxury demand to the rich node, bulk to the many-province node | CONFIRMED | derivation: direct restatement of §1.4's regime split (L443-451) | §1.4: "α>1... Luxuries concentrate on individually rich provinces" / "α<1... Bulk goods spread toward populous regions" |
| Y548 | Price crash: α falls below 1, regional sinks reappear | CONFIRMED | derivation + measurement: α-sweep on spices (ad hoc script) | §1.4's α(g) formula is monotone increasing in price, so a crash necessarily lowers α; at α=0.5 (below spices' baseline 1.5), spices sinks became `['baltic_sea','saxony']` instead of `{genua,brazil}` — geographically incidental "regional" sinks reappearing, matching the claim |
| Y549 | Caribbean 1650: sugar production income makes it a sink for cloth, tools, wine | UNTESTABLE | checked `solver.py`'s province source | Only 1444 province data is available (`prov1444.json`); no 1650 province/history state exists in our materials |
| Y550 | Kilwa 1000: ivory income makes it a sink for Indian textiles | UNTESTABLE | same | No 1000 AD province data available; consistent with Y542's own "no era data" admission |
| Y551 | A consuming leaf terminates the DAG of every good it consumes but does not produce | CONFIRMED | derivation: read `drain.py`'s `compile_dirs` peeled-edge rule; demonstrated via `toys.py`'s T1 | `compile_dirs`: a peeled (degree-1) node's single edge is oriented `(u,v)` [into the leaf] iff its absorbed balance `bv<0`, which by construction makes a net-importing leaf a sink; T1 demonstrates this exactly. (1444's own map has zero literal degree-1 nodes, so this is a construction-level guarantee, not a 1444 measurement) |
| Y552 | An inert merchant's goods take the even split as if the node were empty; node-wide bonuses still apply | CONFIRMED | cross-reference: §1.7 (L722) and §3.10 (L1739) | §1.7: merchant's +2 power/+10% income bonus applies "node-wide, regardless of what it is doing"; §3.10 references an even split as vanilla's default un-steered behaviour |
| Y553 | A node sinking spice but not cloth collects spice fully, cloth at the ratio with remainder pushed | CONFIRMED | derivation: direct application of §1.8's `collected_share(n,g)` formula (L755-756) | `collected_share=1` if sink for `g`, else `P_collect/(P_collect+P_transfer(g))` — applied independently per good, exactly as the claim describes |
| Y554 | A near-balanced link may flip monthly, carries near-zero either way, assignments survive | CONFIRMED | cross-reference: §3.6 (L1631-1634), already measured there | "zero support-membership changes moved more than 1e-6 of flow" under demand nudges; "merchant assignments are to links, so they survive flips untouched" |
| Y555 | Two-way Atlantic corridor: merchants at both ends on disjoint good sets, neither blocking the other | CONFIRMED | measurement: ad hoc script over all 29 per-good `run_drain` results | All 159 of 159 undirected edges carry flow in *both* directions across different goods, including every edge touching `english_channel` (e.g. ivory_coast↔EC: 21 goods one way, 8 the other) |
| Y556 | Economy tab vs overlay: matches to the ducat; self-consistency check, not vs stock EU4 | CONFIRMED | consistency check against `game-session.md`'s own recommendation | game-session.md explicitly recommends exactly this framing after finding vanilla values non-reproducible run to run |
| Y557 | Stock trade values not reproducible run to run: 49/80 nodes differ by up to 8.96% | CONFIRMED | measurement: `scripts/val62p5_e.py` on VANILLA_start.eu4 vs VANILLA2_start.eu4 | "nodes differing on ANY: 49 of 80; overall max 8.959%" — matches "49 of 80... up to 8.96%" exactly |
| Y558 | AI merchant placement randomised; power-dependent fields inherit it; retention 80/80, total 78/79 (zambezi 0.012%) | CONFIRMED | same measurement | "retention 0 of 80 differ... total 1 of 79 differ, max 0.012% (zambezi)" — exact match |
| Y559 | Any comparison against unmodded numbers needs a tolerance and a null run | CONFIRMED | cross-reference: `game-session.md`'s own explicit conclusion | Verbatim conclusion of the B2 investigation |
| Y560 | Reachability: 100% of demand reachable, asserted every tick, zero orphan sinks | CONFIRMED | measurement: `scripts/final.py` PART A | "V032 reach==100%: 29/29 \| orphan sinks total: 0" |
| Y561 | Conservation: Phase-4 Σunserved == Σstranded to machine precision, every good every tick | CONFIRMED | measurement: `val62p5_f.py` + `final.py` | "max \|sum unserved - sum stranded\| over 29 goods: 0 (eps=2.22e-16)"; `final.py`: "V089 unserved==stranded (<1e-9): 29/29" |
| Y562 | Determinism: re-running a tick reproduces the orientation bit-for-bit; one fingerprint across seeds | CONFIRMED | measurement: `scripts/fingerprint6.py` re-run under PYTHONHASHSEED 0,1,2,42,12345,random and unset | All 7 runs produced the identical hash `8b13dddb9a345aa6d1990cac70d64e0b649b01f8cab1a9ec41cf9c87faa1012c` |
| Y1044 | New row: LP configured tighter than the tie-break margin — `flowop.LP_OPTS` sets both tolerances 1e-10 | CONFIRMED | file read: `flowop.py` L85 | `LP_OPTS = {"dual_feasibility_tolerance": 1e-10, "primal_feasibility_tolerance": 1e-10}` — exact match |
| Y563 | Acyclicity asserted on every per-good graph, on Φ_w, and on the emitted file's declaration order | CONFIRMED | measurement: `final.py`/`val62p5_a.py` | "V028 acyclic: 29/29"; Φ_w "acyclic: True" (`val62p5_a.py`) |
| Y564 | Sink-set 2-core containment is a hard assertion every tick, unconditional | CONFIRMED | measurement: `val62p5_f.py` | "core sinks outside {selected} U {promoted} U {fallbacks}: 0" over all 29 goods |
| Y565 | Asserting containment in {selected}∪{promoted} alone would halt on T3 (correct behaviour) | CONFIRMED | measurement: `toys.py` T3 | "sink inside {selected} U {promoted}: False inside U {fallbacks}: True" — exactly the halt-on-correct-behaviour case described |
| Y566 | Sink-set equality is monitored, not asserted: exact on 1444, 29/29 goods, zero fallbacks | CONFIRMED | measurement: ad hoc equality script | "equality {selected cap flow-terminal} U promoted == actual sinks: 29/29; goods using fallback: 0" |
| Y567 | Where Phase 0 acts, equality doesn't apply; the check is the Phase-4 orientation rule; T1 is expected output | CONFIRMED | measurement: `toys.py` T1 | Matches T1's construction and stated expected mismatch exactly |
| Y568 | Colonization check: observer run to 1600, roughly vanilla pace | UNTESTABLE | checked available saves | Only 1444-dated saves (plus one Dec-1444 save) exist in our materials; no 1600 observation possible without the live game |
| Y569 | AI convergence check: greedy assignment settles with damping rather than oscillating | UNTESTABLE | same | Requires live multi-tick observation not available |
| Y570 | Latent-good check: repricing 45 owned latent-coal provinces flips 16 of 159 Φ_w edges | CONFIRMED | measurement: `measure6.py`/`measure6.out` | "latent-coal owned and counted 45... coal activation edge flips 16" — exact match |
| Y571 | Cross-implementation check: DLL and reference implementation agree on orientation exactly | UNTESTABLE | checked for a built DLL | No DLL exists in our materials ("the DLL that would prove it does not exist yet," L947); nothing to compare |
| Y572 | Φ_w-vs-realized sign disagreement measured, not asserted: 55.1% edge-goods, 54.8% value-weighted | CONFIRMED | measurement: `val62p5_f.py` | "edge-good agreement 55.1%; value-weighted 54.8%" — exact match; also matches `measure6.out` |
| Y573 | Flip behaviour measured per decade, peace vs war, reversion as occupation lifts | UNTESTABLE | checked for any such measurement | This is a stated future-measurement plan ("Measured, not asserted") — no such data exists in our materials |
| Y574 | Propagated-share change per node measured on each flip, alongside power/in-degree covariance | UNTESTABLE | same | Future-measurement plan, no data collected |
| Y575 | Total propagated power is not the quantity to watch: Σindeg=\|E\| is invariant under reorientation | CONFIRMED | derivation: trivial graph-theoretic fact | Reorientation relabels a fixed 159-edge set; it cannot change edge count, so the sum of in-degrees over all nodes is definitionally |E| regardless of orientation |
| Y576 | Income balance measured on two metrics: total world income and distribution across great powers | CONFIRMED | consistency check against §2.9 | §2.9 (L1399): "gate income balance on both metrics" — matches the "two metrics" framing |
| Y538 | Under §3.13 calibration, spices sinks at doab/genua; cloves moves to beijing; no Chinese spices sink | CONFIRMED | measurement: re-ran `scripts/final.py` PART B (the calibration instrument) | "V107 spices sinks under calibration: ['doab','genua']"; "V107 China nodes among calibration spices sinks: []"; "V179 cloves alpha=16 sinks: ['beijing']" — all exact matches |
| Y152 | Sinks are {selected∩flow-terminal}∪promoted, 2-8 per good; top-8 demanders sink at 19.4% (45/232), bottom-8 at 7.3% (17/232) | CONFIRMED | measurement: `round6.py` | "sinks among each good's top-8 demanders: 45 of 232 = 19.4%" / "bottom-8: 17 of 232 = 7.3%" — exact match |
| Y154 | Zeroing hangzhou moves Φ_w sinks {genua,hangzhou}→{genua,gulf_of_siam}, 32 of 159 edges flip | CONFIRMED | measurement: `round6.py` | "zeroing hangzhou -> sinks ['genua','gulf_of_siam'] 32 of 159 edges flip" — exact match (current document already carries the corrected 32, not the superseded 30) |
| Y543 | v4.0 (not v2-v4.0) said zeroing beijing "moves nothing"; it does — asymmetry is which node keeps its end | CONFIRMED | file read: `v2-drain/`, `v3-owner-agnostic/`, `v4-owner-agnostic/`, `v5-owner-agnostic/` spec files | v4-owner-agnostic L754 literally says "Zeroing beijing... moves nothing"; v2 and v3 instead zeroed *beijing* as their primary razed-China test and reported it *relocates* the sink — they never said "moves nothing," confirming the narrowed attribution is correct where the old broader "v2 through v4.0" wording was not |
| Y1045 | Configuration can regress silently on solver upgrade; three-branch reduced-cost classification (halt/report/halt) | CONFIRMED | measurement: `round6.py` §"2.3/3.13 what HiGHS does..." | Column-permutation test: default/unset tolerance moves 8 edge-slots on `copper`, 1e-8/1e-10 move 0 — demonstrating the silent-regression mechanism the three-branch check exists to catch |
| Y1119 | Calibration figures moved when §2.3's tie-break cost reached the calibration's Phase 2, previously reading a different vertex | CONFIRMED | file read: `scripts/final.py` PART B, code + comment | Comment: "the calibration read a vertex the solver happened to return rather than the one DRAIN installs"; code now calls `mincost_flow(..., cost=TIE_COST)` where the comment marks it as "the last surviving unit-cost Phase-2 call" |
| Y1120 | `paper` is in the genuine-tie state: zero reduced cost on an arc carrying no flow in any optimum | CONFIRMED | measurement: `scripts/val62p5_a.py`/`b.py` | One off-support zero-reduced-cost arc found (cape_of_good_hope→malacca); maximizing its flow subject to staying at the optimal objective gives ~1e-12 (numerically zero, confirming it is a genuine tie, not an exploitable alternative optimum) |

## §2.9 — Build order (8 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y577 | The build is not phases but two tracks run in parallel | CONFIRMED | consistency check | The following text literally implements this: "Solver track —..." / "Memory track —..." |
| Y579 | Then the b-flow and sweep with their per-tick assertions (reachability, conservation, acyclicity...) | CONFIRMED | cross-reference to §2.8's assertion list | All named assertions (reachability, conservation, acyclicity, determinism, 2-core containment) appear as rows in §2.8's table |
| Y581 | Then: write §1.10's classified call-site list, gate income balance on both metrics, decide negative-link display policy | CONFIRMED | cross-reference to §1.10 item 10 (still incomplete) and §2.8's income-balance metrics | Consistent with probe 10's unfinished status and Y576's two metrics |
| Y578 | Solver track starts with the defines parser: every define the model reads is a runtime read, except TAX_COEFF | CONFIRMED | file read: §2.3 (L1037-1060) | §2.3's table lists 9 runtime-read defines plus GP_COEFF (shipped file value, read at runtime); TAX_COEFF explicitly "is not in any file that has been found... remains a measured constant" — exact match |
| Y580 | Memory track is the §2.7 probe session: the twelve items still open there (1-11 and 16) on one trace | CONFIRMED | count check against §2.7's current probe list | Items 1-11 (pass caching through caravan recipient) + item 16 (quantisation) = 12 items, all confirmed still open (13-15 struck, 12 dropped) — matches exactly |
| Y1121 | Each per-tick assertion paired with a negative fixture that makes it fail | UNTESTABLE | checked for the actual per-tick assertion implementation | No built solver/DLL with per-tick assertions exists yet; `redtest6.py` (Y1123) is explicitly only "the reference-side version" of this requirement, i.e. an analogue for the spec-audit harness, not the thing itself |
| Y1122 | Four of the round-5 audit's defects were checks that could not fail | CONFIRMED | file read: `redtest6.py` docstring; cross-checked against `validation-v6-round5-part2.md` | `redtest6.py`'s own docstring: "Four defects found in the round-5 audit shared one signature" and implements exactly 4 fixtures; round-5's own finding "A1 — epsilon6.py: the gate cannot fail by construction" (L410) matches fixture 1 directly |
| Y1123 | `scripts/redtest6.py` is the reference-side version of the negative-fixture requirement | CONFIRMED | measurement: re-ran `redtest6.py` in full | "RESULT: 6 fixtures, 0 did not produce the required red" — the harness runs and does exactly what the claim describes |

## §3.1 — Goals (7 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y582 | Goal 1, world responsiveness: direction follows current state; razing hangzhou moves the sink | CONFIRMED | cross-reference to the already-measured razed-hangzhou test (Y154) | Zeroing hangzhou's wealth measurably moves the Φ_w sink set (32/159 edges flip) — direct demonstration |
| Y583 | Goal 2, realism: China is a silk source and spice sink at once — impossible under one graph | CONFIRMED | measurement: ad hoc per-node source/sink check across all 29 goods | `hangzhou` is both a raw silk *source* (S>0) and a sink for 8 other goods simultaneously (cocoa, copper, cotton, dyes, gems, glass, sugar, tobacco) — demonstrates the underlying source/sink co-location mechanism the Goal illustrates |
| Y584 | Goal 3, preserve feedback loop: sinks accumulate, fund development, reinforce | CONFIRMED | cross-reference to §1.8's `collected_share=1` at sinks | Full local collection at a sink (nothing pushed further) preserves the accumulation channel the goal describes |
| Y585 | Goal 4, return flows: export regions historically imported manufactures; vanilla cannot express this | CONFIRMED | derivation: §1.3's wealth formula creates cross-good demand from production income | wealth(p) feeds `c(n,g)` for every good g, not only the good produced — a sugar island's income genuinely creates cloth/tools demand; vanilla has no per-good demand concept at all to compare against |
| Y586 | Goal 5, route-aware direction: reflects reachability, not which neighbour is richer | CONFIRMED | cross-reference to §3.2's monotonicity critique and DRAIN's flow-based design | Directly matches the "Local comparison is monotone" argument and DRAIN's LP-feasibility approach, both already established in §3.2 |
| Y587 | Goal 6: zero authored data | CONFIRMED | broad consistency check across §1.x-§2.x | No hardcoded trade direction or value found anywhere; every figure is formula-derived from game files or defines |
| Y588 | Goal 7: the game's own numbers are the model's numbers | CONFIRMED | cross-reference to §2.6/§3.10 | §3.10: "the engine's own vanilla collection math reproduces every country's per-good income exactly" |

## §3.2 — Why a flow and a drainage sweep (38 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y589 | Two orientation families fail before DRAIN: first by theorem, second by exact rule whose consequence is measured; v2 called both theorems | CONFIRMED | file read: `v2-drain/per-good-trade-spec.md` L514 | "Two families of orientation fail before this one, and both failures are theorems, not taste" — v2 verbatim, confirming the overstatement |
| Y590 | Local comparison is monotone; Malacca→Cape→Europe requires a dip through a low-value intermediary | CONFIRMED | derivation + cross-reference to Cape's measured s=c=0 | A locally-monotone orientation cannot dip; the Cape (s=c=0, confirmed via ad hoc script) sits between richer neighbours, exactly the dip the route requires |
| Y591 | Monotonicity killed v1's rank strawman and the s-c operator: "one sixth" of world demand unreachable, Genoa crowned an unreachable cloves sink | PARTIAL | measurement: `val62p5_f.py` + follow-up scripts | Genoa-as-unreachable-cloves-sink confirmed exactly ("genua is a sink: True; genua reachable from supply: False"). But "one sixth" (16.67%) does not reproduce: unweighted per-good mean unreachable demand is 17.07%, value-weighted is 7.7%, cloves-specific is 67.6% — no natural construction gives exactly 1/6 — [rechecked: coordinator re-ran the instrument — s-c operator unreachable demand 17.07% unweighted (7.7% value-weighted), genua a cloves sink unreachable from supply (both exact); 'one sixth' (16.67%) matches no construction, nearest is the unweighted 17.07%] |
| Y592 | Merchants cannot repair a wrong orientation — select among existing arrows, cannot reverse one | CONFIRMED | cross-reference to `game-session.md` §B3 | When Sevilla's outgoing link vanished, Castile's merchant switched steering→collecting rather than somehow retaining an outgoing option — merchants react to, but cannot alter, the graph |
| Y593 | v1's Laplacian sink rule is exactly (c-s)/deg > mean(nbr φ) - min(nbr φ), verified on every (good,node) pair | CONFIRMED | measurement: `val62p5_d.py` | "sink rule matches actual sinkhood on 2320 of 2320 (good,node) pairs; mismatches: []" — exact |
| Y594 | Supply sparse where demand dense: spices in 18/80 nodes, cloves in 1; every owned-province node carries demand | CONFIRMED | measurement: `val62p5_a.py`/`f.py` | "spices produced in 18 nodes... cloves produced in 1 node"; "nodes with owned provinces: 79... with c>0 for spices: 79" — exact match |
| Y595 | v1 Laplacian: highest-demand node never a spices sink; a zero-demand node outranked Genoa/Beijing; deleting demand variation left the sink unmoved | CONFIRMED | measurement: `val62p5_f.py` | "highest-demand node = genua (a sink: False)"; zero-demand node `cape_of_good_hope` has φ=0.0618, exceeding genua's -0.0583 and beijing's 0.0348; uniform-c spices sinks still include `saxony` (the unmoved v1 baseline sink) |
| Y596 | v1/v2 quantified asymmetry as "supply contrast 10⁷ vs demand contrast 10²-10³"; ratio was max(s)/ε-floor, removed by §1.2 | CONFIRMED | file read: `v1-laplacian/diagnosis.md` L327; §1.2 (L240-248); measurement `val62p5_f.py` | diagnosis.md verbatim: "supply contrast is 2.5×10⁷ and demand contrast is ~10²-10³"; measured eps-floor ratio (2.52e7/8e7) matches the 10⁷ order; §1.2: "No regularizer... DRAIN does not need it" |
| Y157 | Ratio metric cannot see sparsity: spices in 18/80 nodes, cloves in 1; (c-s)/deg dominated by where supply exists | CONFIRMED | measurement: `val62p5_a.py` | Matches Y594's figures exactly; logical argument follows directly from the sink-rule identity (Y593) |
| Y158 | On the contrast metric itself, demand is the wider side, not supply | CONFIRMED | measurement: `val62p5_f.py` | "supply contrast over producing nodes: 1..97" vs "demand contrast: 211..15010" — even the *minimum* per-good demand contrast (211) exceeds the *maximum* per-good supply contrast (97) |
| Y597 | No parameter fixes it: α strong enough to matter destroys §1.4's regime split | CONFIRMED | measurement: ad hoc α-sweep on spices, v1 Laplacian sinks | Within the legitimate clamp [0.2,3.0], the sink never reaches a Chinese node (α=3.0 gives `rheinland`); reaching `hangzhou` requires α≈5-8, far outside §1.4's stated clamp range |
| Y159 | Better wealth inputs move Genoa to a co-sink at ~×1.7 | CONFIRMED | measurement: `val62p5_c.py` | "genua minimal wealth multiple to become a spices sink: 1.724" — matches "roughly ×1.7" exactly |
| Y160 | Moving the sink to a Chinese node needs 3.6-4.8×: beijing 3.63, hangzhou 4.13, xian 4.61, canton 4.78 | CONFIRMED | measurement: `val62p5_c.py` | "beijing 3.626 / hangzhou 4.125 / xian 4.606 / canton 4.775" — exact match to quoted precision |
| Y161 | These are wealth multiples, not demand multiples — demand (wealth^α, α=1.5 for spices) makes the effective factor larger | CONFIRMED | derivation: k^α > k for α=1.5>1 | spices' α = min(3,(price/2))=1.5 (price=3.0); at k=1.724, k^1.5≈2.26 > 1.724, confirming the demand-side factor is larger |
| Y162 | Four named Chinese nodes are not the cheapest: girin needs 3.89×, yumen 4.49×, both inside the range | CONFIRMED | measurement: `val62p5_h.py`/`i.py` | girin 3.888, yumen 4.493 — exact match; both slot between the named nodes in the full ranking (beijing<girin<hangzhou<yumen<xian<canton), so the four named are not collectively the 4 cheapest |
| Y598 | v2 wrote "1.7× where 4-5× is needed," compressing two quantities into one comparison | CONFIRMED | file read: `v2-drain/per-good-trade-spec.md` L533 | "better wealth inputs move the threshold by 1.7× where 4-5× is needed" — verbatim match |
| Y599 | Conservation lesson: node-balance-imposing operators serve 100% of demand as a theorem; others strand it | CONFIRMED | derivation: standard flow-feasibility fact | An operator enforcing exact node balance guarantees full service by construction (given feasibility); non-balance heuristics (rank, seeded basins) carry no such guarantee |
| Y600 | DRAIN takes conservation from the b-flow; reachability is LP feasibility, not aspiration | CONFIRMED | cross-reference to measured 100% reachability (Y560/Y611) | Directly demonstrated: 29/29 goods, 100.0% reachability, zero orphan sinks |
| Y601 | v1 did state aggregate acyclicity as C061; its ε-machinery decided dead-branch direction | CONFIRMED | file read: `v1-laplacian/claims.md` L210; spec L477 | C061: "`Φ` is a potential, so orienting edges by it is acyclic" — verbatim; "ε magnitude: small enough to be invisible... large enough to decide dead branches against floating-point noise" — verbatim |
| Y163 | Sink placement is a measurement on one input: on 1444, final sinks = {selected∩flow-terminal}∪promoted, exact 29/29 | CONFIRMED | measurement: ad hoc equality script | "equality... == actual sinks: 29/29; goods using fallback: 0" |
| Y164 | v5.0's two rescue conditions (Phase 0 no-op, no fallback) are necessary but not sufficient: T2 satisfies both, still breaks equality | CONFIRMED | measurement: `toys.py` T2 | T2's output shows `promoted=[]` (no fallback) and is core-only (Phase 0 no-op by construction) yet "EQUAL: False" |
| Y602 | T1, pendant importer: triangle A(+5),B(-3),D(0) with leaf C(-2); actual sinks {C}, formula {B} | CONFIRMED | measurement: `toys.py` T1 | "directed: [('A','B'),('A','D'),('B','C'),('D','B')]... actual sinks = ['C'] \| formula set = ['B'] \| EQUAL: False" — exact match |
| Y603 | T2, free-edge race: 5-cycle+chord; actual {u2}, formula {u1,u2} | CONFIRMED | measurement: `toys.py` T2 | "actual sinks = ['u2'] \| formula set = ['u1','u2'] \| EQUAL: False" — exact match |
| Y604 | T3, fallback branch: triangle with b=0 everywhere, wealth 3/2/1; actual {A}, formula empty | CONFIRMED | measurement: `toys.py` T3 | "actual sinks = ['A'] \| formula set = [] \| EQUAL: False \| FALLBACK promoted: ['A']" — exact match |
| Y605 | What survives unconditionally: every 2-core node not selected/promoted/fallback-promoted gets an out-arc | CONFIRMED | derivation (sweep's `ready()`/termination logic) + measurement (0 violations, 29/29 goods) | Structural guarantee of the sweep's construction, confirmed with zero exceptions across all 29 goods (`val62p5_f.py`) |
| Y606 | Pendant net-importers are the only sinks outside that set | CONFIRMED | derivation: `compile_dirs`'s peeled-edge rule | An exporting pendant (bv≥0) always gets an outgoing edge; only a net-importing pendant (bv<0) becomes a sink — the only category of sink possible outside the 2-core containment guarantee |
| Y607 | §2.8 therefore carries two runtime checks, not one weakened one | CONFIRMED | cross-reference to §2.8's actual two rows ("Sink set, 2-core" and monitored equality) | Matches exactly what §2.8's table implements |
| Y608 | On pendant edges the Phase-4 orientation rule is the check; T1 is expected output | CONFIRMED | measurement: `toys.py` T1 | T1's result is exactly the described expected (non-violation) output |
| Y609 | Written as a single assertion with an escape clause, all three counterexamples vanish; against the narrower set T3 would halt on correct behaviour | CONFIRMED | measurement: `toys.py` T3 | T3's sink is outside {selected}∪{promoted} but inside ∪{fallbacks} — a narrower assertion would misfire on this legitimate case |
| Y610 | Free-edge direction is marking order under (DEF asc, b asc, index); zero exact ties on 1444, 29/29 goods | CONFIRMED | measurement: ad hoc tie-count script | "total free edges across 29 goods: 2321, exact (DEF,beta) ties: 0" |
| Y611 | Reachability: orientation contains the LP certificate; measured 100.0%, 29/29 goods, zero orphan sinks | CONFIRMED | measurement: `final.py` | "V032 reach==100%: 29/29 \| orphan sinks total: 0" |
| Y612 | Aggregate acyclicity: Φ_w is itself a DRAIN orientation, acyclic by the same marking-order argument | CONFIRMED | measurement: `val62p5_a.py` | "phi_w: core 80 of 80 \| every arc later->earlier marked: True \| acyclic: True" |
| Y613 | Φ_w's marking order is a per-node scalar reproducing the DAG, for any potential-needing consumer | CONFIRMED | measurement: `val62p5_a.py` | Same output: order-consistency confirmed for Φ_w and separately for 29/29 per-good graphs |
| Y615 | Corridor runs through the Cape: Malacca reaches the Channel in 3 hops through Cape vs 7 via Alexandria; 24% of world spice supply | CONFIRMED | measurement: `val62p5_a.py` (BFS + flow share) | "via cape: 1+2=3... via alexandria: 4+3=7"; "spice flow INTO cape: 0.2409" — both exact matches |
| Y616 | Peripheral termini still exist; value only arrives where someone holds power at both ends | CONFIRMED | cross-reference to §1.8/§1.9's collection/propagation framework | Consistent with the collected_share/P_transfer mechanism, which requires power presence for collection or transfer at either end |
| Y614 | Conduits still work: a node with s=c=0 (the 1444 Cape) carries flow through | CONFIRMED | measurement: ad hoc script | Cape's summed s and c are both exactly 0.0 across all 29 goods; in- and out-degree nonzero for all 29 goods |
| Y1124 | Degree is the weaker evidence for conduit function; an oriented edge is not a routed unit | CONFIRMED | derivation, consistent with the flow-vs-degree distinction already established | Directly follows from the LP-flow vs orientation-only distinction used throughout §2.1/§3.2 |
| Y1125 | On the certificate flow, Cape has both incoming and outgoing flow on 28/29 goods; exception is paper (routes none) | CONFIRMED | measurement: ad hoc script over `run_drain`'s flow_arc/net for all 29 goods | "goods with BOTH incoming and outgoing FLOW at cape: 28/29... without both: [('paper', 0, 0)]" — exact match |

## §3.3 — Why wealth, and why per province (18 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y617 | Demand is purchasing power = what the place is worth per year (§1.3) | CONFIRMED | file read | §1.3 L258: "what the land is worth per year, before anyone's government touches it"; §1.3 L255 "Assembled per province" |
| Y618 | Wealth captures return flows free: sugar island's production carried by its good not development | CONFIRMED | derivation check | §1.3 L286-289 formula: `trade_value(p)=goods_produced(p)·price(good(p))`, `wealth=tax_value+trade_value` — a good's price, not development, sets the trade term |
| Y619 | Return-flow effect modest: sugar 3.0/cocoa 4.0/coffee 3.0 are 1.2-1.6x grain 2.5; cloves 8.0/coal 10.0 largest ratios | CONFIRMED | file read | `common/prices/00_prices.txt`: grain=2.5, sugar=3, coffee=3, cocoa=4, cloves=8, coal=10 (sugar/grain=1.2, cocoa/grain=1.6, cloves/grain=3.2, coal/grain=4.0) |
| Y620 | v1 and v2 said "negligible development but large production income" | CONFIRMED | file read | `v1-laplacian/per-good-trade-spec.md` L371 and `v2-drain/per-good-trade-spec.md` L564, verbatim identical phrase |
| Y621 | No colonial-nation dependency, no timeline restriction, no owner dependency | CONFIRMED | file read | solver.py wealth path has no colony/date/owner term; v3-owner-agnostic L817 first added "no owner dependency" to the inherited v1/v2 sentence |
| Y622 | Wealth chosen for the place not the owner: autonomy/ideas/reforms/estates/tech excluded | CONFIRMED | file read | scripts/solver.py: wealth built only from `base_tax`, `base_production`, price and `STATE_GOODS_MOD`; no autonomy/idea/reform/estate/tech term anywhere in the wealth path; §1.3 L259-260 states the same exclusions |
| Y623 | What remains still moves: development, goods, prices, and place-condition modifiers | CONFIRMED | file read | §1.3 table L340-346: devastation/prosperity/under_siege/occupied all carry `trade_goods_size_modifier` and reach `goods_produced` |
| Y624 | Besieged province genuinely produces less — economics not noise, ties to Goal 1 | CONFIRMED | file read | `under_siege`: `trade_goods_size_modifier=-0.25` (§1.3 L345); §3.1 Goal 1 (L1407) "Trade direction follows the world's current state" |
| Y625 | Model removes ownership-volatility: wealth doesn't change on conquest | CONFIRMED | derivation check | solver.py's wealth formula reads no owner field; §1.3 L262 "a province's wealth does not change when it is conquered" |
| Y626 | Plan around the world not the graph: map legible, not unchanging | CONFIRMED | derivation check | consistent with the conquest-invariance just confirmed (Y625) |
| Y627 | Trade income excluded for circularity not speed | CONFIRMED | derivation check | including trade income would close a demand→orientation→flow→demand loop; solver.py's wealth has no trade-income term |
| Y628 | Loop closes the long way: trade funds development, development raises tax/production | CONFIRMED | derivation check | consistent with §1.3's wealth-from-development formula; identical clause carried unchanged from v1 (`v1-laplacian` L375) |
| Y165 | `cape_of_good_hope` members=20 but 1460 is a sea zone in `sea_starts` | CONFIRMED | measurement (`val62p6_nodes.py`) | `map/default.map` `sea_starts` contains 1460: True; `cape_of_good_hope` members=20, land=19 |
| Y629 | Node sizes run 19 (cape) to 77 (girin) land provinces, 4x spread, no structural rule | CONFIRMED | measurement (`val62p6_nodes.py`) | smallest cape_of_good_hope=19, largest girin=77, spread 19..77 = 4.053x |
| Y630 | Node-aggregate wealth^power rewards node size | CONFIRMED | derivation check | `(k·w)^α > k·w^α` for α>1, k>1 (convexity/power-mean inequality) |
| Y631 | Distortion measured against per-province form: node-level overweights k-province node by k^(α−1) | CONFIRMED | derivation check | `(k·w)^α / (k·w^α) = k^(α-1)`, exact algebra |
| Y632 | v2 said 77-province node beats 19-province node of equal total wealth by 2x; at equal totals node-level form ties | CONFIRMED | file read + derivation | `v2-drain/per-good-trade-spec.md` L573-574 verbatim "beats a 19-province node of equal total wealth by 2×"; node-level form `(Σw)^α` depends only on the total, so equal totals give equal values regardless of k |
| Y633 | Exponent inside sum concentrates demand where individual provinces are rich; at α=1 forms coincide exactly | CONFIRMED | derivation check | `Σ w_i^1 = (Σ w_i)^1` identically |

## §3.4 — Why supply is pre-modifier (5 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y634 | Production efficiency doesn't conjure more cloves, just more ducats from the same cloves | CONFIRMED | file read | §1.2 L246 "`goods_produced` is a physical quantity — pre-production-efficiency, pre-autonomy" |
| Y635 | Owner effects don't belong in demand either: v1/v2 excluded from supply then reintroduced via wealth | CONFIRMED | file read | `v1-laplacian/per-good-trade-spec.md` L54,383: `wealth(p)=tax_income(p)+production_income(p)`, and "production income is defined by [production efficiency and local autonomy]" — the very owner terms v1/v2's supply excluded |
| Y636 | Supply and demand both properties of the place; supply-side argument applies unchanged | CONFIRMED | derivation check | §1.2 and §1.3 both define their quantities from `base_production`/`base_tax`/condition only, with no owner term |
| Y637 | Aggregate uses trade value not production income: trade value unaffected by efficiency/autonomy | CONFIRMED | file read | §1.6 L510 `V_g = price(g)·Σ_m goods_produced(m,g)`, and `goods_produced` is pre-efficiency/pre-autonomy per §1.2 |
| Y166 | v1: substituting production income broke α=1 identity, orientation collapsed to well under half the map | CONFIRMED | file read (v1's own audit) | `v1-laplacian/validation.md` C424: "Orientation agreement collapses from 159/159 to 68/159" (42.8%, well under half) |

## §3.5 — Why alpha is anchored absolutely (19 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y638 | Anchoring at 2 ducats (not price median) means concentration moves only with the good's own price | CONFIRMED | derivation check | `α(g)` formula is a function of `PRICES[g]` alone (scripts/measure6.py L62); a median anchor would make it a function of all prices |
| Y639 | Under a median anchor a good could concentrate because an unrelated good got expensive | CONFIRMED | derivation check | direct consequence of Y638's mechanism |
| Y640 | At vanilla base prices nothing sits below the 2.0 anchor; minimum tradeable base price is exactly 2.0 | CONFIRMED | file read | `common/prices/00_prices.txt`: min base_price = 2.0, at fur/naval_supplies/slaves/tea/tropical_wood/livestock — exactly the 6 named |
| Y641 | Grain is 2.5, not the 1.25 v1 recorded; both v1 figures were price/P0 misread as prices | CONFIRMED | file read | grain base_price=2.5 (00_prices.txt); `v1-laplacian/per-good-trade-spec.md` L389 literally says "grain lands near 1.25 and livestock near 1.00" = grain/P0=1.25, livestock/P0=1.00 |
| Y642 | Sublinear regime entered only via a price event; shipped data answers how often | CONFIRMED | measurement (`val62p6_partition.py`) | partition 13/2/4/11 reproduced exactly (see Y168-176 below) |
| Y167 | `change_price` values are fractions of base price not ducats; tutorial save settles it (paper 4.375/3.5, gems 5.000/4.0) | CONFIRMED | measurement (save read) | `tutorial/eu4_tutorial_chapter10.eu4` (plaintext EU4txt): `paper` `current_price=4.375`, base_price 3.5 in 00_prices.txt (×1.25); `gems` `current_price=5.000`, base_price 4.0 (×1.25) |
| Y168 | 161 textual `change_price` blocks: 93 events/, 14 missions/, 1 common/, 53 history/ (13 negative, all in HAB-Austria) | CONFIRMED | measurement (`val62p6_census.py`, `val62p6_partition.py`) | total 161; by tree {'events':93,'missions':14,'common':1,'history':53}; history negatives (13) all in `history/countries/HAB - Austria.txt` |
| Y169 | 10 of 161 never execute: 4 in `effect_tooltip`, 3 in `effect=""` of `country_event_with_effect_insight`, 3 in `tooltip={}`, so 151 executable | CONFIRMED | measurement (`val62p6_census.py`) | quoted: 4×`effect_tooltip` (DOM_Britain L919, KoK_Persia L3384/3390/3396), 3×`effect` under `country_event_with_effect_insight` (KoK_Byzantine, KoK_Yemen, WOC_Italian); bare-in-tooltip: 3 (flavorMAL L1736, WOC_Hisn_Kayfa L1448/1459); 10 total, 161−10=151 |
| Y170 | 6 of 7 quoted blocks duplicate an events/ block; the 7th (fur/ENGLISH_FUR_TRADE) is never set by any event | CONFIRMED | measurement (`val62p6_dupcheck.py`) | 6 of 7 quoted (good,key,value) triples match an events/ bare block exactly; `ENGLISH_FUR_TRADE` (DOM_Britain_Missions.txt) appears in no other file across events/missions/decisions/common/history |
| Y171 | v4.0 said 154 (dropped the 7 quoted), v5.0 said 161 (counted them); both wrong about which is executable | UNTESTABLE | — | requires reading v4.0's/v5.0's own census scripts/output, which are not present in this base directory's read-only prior-version dirs (v4/v5 dirs hold only spec+validation text, no census script producing 154); cannot independently confirm the attributed error without that instrument |
| Y172 | v5.0 claimed the scan was "guarded by a per-file count assertion"; no such assertion existed | UNTESTABLE | — | same gap as Y171: v5.0's toolchain is not present in `../v5-owner-agnostic/` (only spec+validation.md, no scripts dir with the named guard to inspect) |
| Y173 | `verify6.py` checks the census only by matching a printed total, not by reconciling per file | CONFIRMED | file read | scripts/verify6.py's census check compares only an aggregate count against `measure6.out`'s total, no per-file reconciliation logic present |
| Y174 | `pdx.py` tokenises a quoted string as one opaque unit, so a `change_price` inside a tooltip string is invisible to it | CONFIRMED | file read | scripts/pdx.py treats `"..."` as a single token; confirmed operationally since `measure6.py`'s walker (which uses pdx.py) reports totals consistent with missing the 7 quoted blocks unless specially handled |
| Y643 | `wool`'s largest single negative: history's `NEW_DRAPERIES` −0.25 vs events' −0.20 for the same key | CONFIRMED | measurement (`val62p6_partition.py`) + file read | `history/countries/HAB - Austria.txt` `NEW_DRAPERIES`=-0.25 on wool; `events/PriceChanges.txt` `NEW_DRAPERIES`=-0.20 on wool |
| Y175 | 1.875 is the single-key floor not campaign figure: same `1540.1.1` block also applies `COTTON_IMPORTS=-0.10`; sums to 1.625 or compounds to 1.6875, unsettled | CONFIRMED | file read + measurement | `HAB - Austria.txt` L290-315: one `1540.1.1` block carries both `COTTON_IMPORTS -0.10` and `NEW_DRAPERIES -0.25` on wool; 2.5·(1-0.35)=1.625, 2.5·0.75·0.90=1.6875 |
| Y176 | Partition needs history value; events alone give 12/3/4/11 not 13/2/4/11 | CONFIRMED | measurement (`val62p6_partition.py`) | "events/ ONLY -> below/exact/above/none = 12/3/4/11" (wool moves to 'exact' using only the -0.20 events key) vs all-five-trees 13/2/4/11 |
| Y644 | v2's 13 was right; v3.0 reached 12 by parsing four of the five trees | CONFIRMED | file read | `v3-owner-agnostic/per-good-trade-spec.md` L858-864: "12 of 30 goods... (v2 said 13...) All 101 change_price blocks in events/, decisions/, missions/ and common/ were parsed; history/ contributes only positive entries" (four trees, history/ wrongly assumed all-positive) |
| Y645 | Point of the regime: without it a crash could only fail to concentrate, never spread | CONFIRMED | derivation check | consequence of sublinear regime only activating below anchor (Y642) |
| Y646 | α deliberately mild: production geography differentiates goods, α expresses concentration only | CONFIRMED | derivation check | consistent design statement, matches §2.3's "α_Φ... hyperparameter... developer taste" framing applied to per-good α |

## §3.6 — Why no hysteresis, and why there is no epsilon (24 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y647 | A margin on orientation is a correctness bug: splicing two acyclic orientations need not be acyclic | CONFIRMED | derivation check | standard graph-theory fact: mixing edge orientations from two different (even both acyclic) solves can create a cycle |
| Y648 | v1 test: tol=1e-3, values {0,0.0006,0.0012} turned acyclic prior into A→B→C→A | CONFIRMED | file read (v1's own test) | `v1-laplacian/validation.md` L4739-4749, C446 confirmed: "A->B->C->A" reproduced exactly at tol=1e-3 on those values |
| Y649 | Node-file format represents cycles fine — a list of named directed links, no acyclicity constraint | CONFIRMED | file read | `common/tradenodes/00_tradenodes.txt` node blocks are simple `name={...}` outgoing-link lists with no schema-level cycle check |
| Y650 | What matters is the engine's behaviour on a cyclic file, now measured not assumed | CONFIRMED | file read | §2.7 item 13 "settled, and it reverses the hedge" — v2's own text (`v2-drain` L614) said this was still "unverified"; now resolved |
| Y651 | Two-node cycle: STACK_OVERFLOW at one address, 1002 frames, three launches; vanilla and reversed file load | CONFIRMED | artifact read [rechecked by coordinator] | three engine crash dumps exist: crashes/eu4_20260820_{134250,134617,165621}, each `Unhandled Exception C00000FD (EXCEPTION_STACK_OVERFLOW) at 0x00007FF6DDE6A8B4`, exactly 1002 `eu4.exe` frames, meta.yml `Mods: mod/pgt_cycle.mod`; pgt_cycle's only diff vs vanilla adds `valencia -> sevilla`, closing a 2-cycle with vanilla's `sevilla -> valencia` (00_tradenodes.txt L1980/L2039); the two no-crash controls (vanilla; 159 backwards links, error.log exactly 159 lines) recorded in `../v2-drain/game-session.md` L99-152 |
| Y652 | Engine walks the node graph recursively; a cycle never terminates | PARTIAL | artifact read + derivation check [rechecked by coordinator] | recursion and nontermination on the tested cycle are proven by the dumps (1002 same-module frames, single exception address, three launches) and the differential controls pin cyclicity as the cause (vanilla and 159-backwards both load, game-session.md L104-108); but the routine's identity (a node-graph walk) and the universal over all cycles rest on inference from one two-node construction — disassembly or further cycle shapes would settle |
| Y653 | Acyclicity enforced because engine provably cannot survive its absence, not (as v2) unprovability | PARTIAL | file read + artifact read [rechecked by coordinator] | v2's contrast wording confirmed verbatim (`../v2-drain/per-good-trade-spec.md` L613-615: 'acyclicity is enforced because we cannot prove the engine tolerates its absence'); the crash basis is real and artifact-verified (see Y651), but **provably** turns one reproduced two-node construction into a universal proof — what exists is a measurement of one cycle class, not a proof over cyclic inputs |
| Y654 | Nothing needs to stop churn: an alternating-support link carries near-nothing, merchants survive flips | CONFIRMED | measurement (`val62p6_nudge.py`) | reproduced independently: 174 trials (29 goods x 6 nudges), 5 raw support-membership crossings, largest flow on any changed edge = 2.2e-9 |
| Y655 | "Carries near-nothing" is measured not derived; v1's continuity argument doesn't port to a discrete LP support | CONFIRMED | derivation check | an LP support is a discrete selection (which basic variables are nonzero), not continuous in the data, so a continuity/near-flat-potential argument does not transfer to it |
| Y656 | Measured 1444: 29 goods x 6 random 1e-9 nudges, zero support changes moved >1e-6 flow; six-seed ±1% wealth noise moved no edge | CONFIRMED | measurement (`val62p6_nudge.py`, `val62p6_wealthnoise6.py`) | nudge test: 174 trials, largest flow on a changed edge = 2.2e-9 (<1e-6); six-seed wealth-noise test (seeds 9000-9005, same construction as measure6.py): 0 edges differ from base in all 6 seeds |
| Y657 | At exactly degenerate inputs (two equal-hop corridors) the map is discontinuous in principle | CONFIRMED | derivation check | standard LP fact: at a degenerate/tied optimum the argmin correspondence is not single-valued, hence discontinuous as a function of input |
| Y1046 | With both cost terms + tolerance pinned, optimum unique on aggregate and all 29 per-good solves, margin 3.8e-8 at worst vs 2e-16 double noise | CONFIRMED | measurement (`round6.py`, re-run fresh) | per-good floor: copper 3.765e-08 (rounds to 3.8e-8); §2.3 L1139 confirms `flowop.LP_OPTS` ships 1e-10, well under this margin; double-precision epsilon ≈2.22e-16 |
| Y1126 | Uniqueness margin is not a constant of the design; worth knowing how much is a gift of the chosen α_Φ | CONFIRMED | derivation check | direct framing consequence of Y1127's measurement (margin changes 60x with α_Φ) |
| Y1127 | Aggregate margin 7.53e-06 at α_Φ=2.0, 1.267e-07 at 1.5 — factor of sixty for a §1.6-taste change | CONFIRMED | measurement (`round6.py`, re-run fresh) | fresh output: "aggregate margin at alpha_Phi = 2.0 : 7.527e-06" / "at alpha_Phi = 1.5 : 1.267e-07"; ratio = 59.4 ≈ sixty |
| Y1128 | Per good, 2 of 29 solves (copper 3.765e-08, paper 8.92e-08) sit inside HiGHS's 1e-7 default; 27 above it | CONFIRMED | measurement (`round6.py`, re-run fresh) | fresh output: "per-good floor 3.765e-08 (copper); 2 of 29 at or below HiGHS's 1e-7 default: copper 3.765e-08, paper 8.915e-08" |
| Y1129 | `round6.py` reports the margin as the smallest positive reduced cost on an arc outside the support | CONFIRMED | file read | scripts/round6.py's uniqueness-margin block computes exactly this quantity (smallest positive reduced cost among non-support arcs) |
| Y1130 | Pinning the tolerance is load-bearing at these values, not precautionary; re-measure on any α_Φ/wealth-field change | CONFIRMED | derivation check | direct consequence of Y1127-Y1128: the margin (3.8e-8 to 8.9e-8 for the tightest goods) sits inside HiGHS's untouched 1e-7 default, so an unpinned solver could return a wrong optimum there |
| Y1047 | Discontinuity remains a property of the program: an input with exactly tied routing costs still has no unique answer | CONFIRMED | derivation check | tautological LP fact — genuine cost ties are unresolvable by any fixed tie-break |
| Y658 | v1's ε deleted because the problem (comparing near-equal solved potentials) no longer exists | CONFIRMED | file read | §1.2 L248-251 "No regularizer... DRAIN does not need it: free edges are oriented combinatorially... not by comparing near-equal solved potentials" |
| Y659 | DRAIN's free edges oriented combinatorially: priority key (DEF,b,index) computed from input data over LP support structure | CONFIRMED | file read | §1.1 L147-148 "pop by the deterministic priority key (DEF ascending, b ascending, index)" |
| Y660 | Measured count of exact (DEF,b) key ties on 1444 data is zero; LP deterministic (six identical solves, one orientation) | CONFIRMED | measurement (`props6.py`, re-run fresh) | "core nodes summed over goods: 2320 ; exact (DEF,b) key collisions: 0"; §1.1 L233-236 states six identical single-process solves return one orientation |
| Y661 | Determinism asserted per tick rather than approximated by a nudge | CONFIRMED | derivation check | consistent with §2.8's per-tick determinism assertion and the deletion of v1's ε |
| Y662 | What replaces the ε-magnitude question in §3.13 is the cross-machine question, narrowed by §2.1 | CONFIRMED | file read | §1.1 L237-238 "Across machines it is the open question of §3.13"; §2.1 discusses build discipline for cross-machine reproducibility |
| Y1048 | LP need not pivot identically, only reach the same optimum, which the tie-break's margin makes robust to a few ULPs | CONFIRMED | file read | §2.1/§3.6 text: "the LP does not need to *pivot* identically, only to reach the same optimum, which the tie-break's margin makes robust to a few units in the last place" |

## §3.7 — Why eligibility is per good (5 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y663 | Vanilla rule: effective trade power counts only collectors/downstream-transferrers, not upstream-only capital | CONFIRMED | file read | localisation `HINT_TRADESTEERING_TEXT`: "You can never steer trade upstream"; `TRADE_POWER_UPSTREAM` string exists (§2.7 item 10); consistent with §1.8/§1.9's collect/transfer/propagation design |
| Y664 | Per-good model: "downstream" is per good — home downstream for cloth, upstream for spice, power counts for one not other | CONFIRMED | file read | §1.8 L759 "Transfer eligibility is per good... only if it has a merchant steering g at n, or it collects at some node reachable from n in g's graph" |
| Y665 | Per-good eligibility returns true for some good at every node, so no nation is globally inert, without inflating power everywhere | CONFIRMED | derivation check | direct reading of §1.8's per-good eligibility test combined with DRAIN's 100% per-good demand reachability (§1.1 bullet, confirmed at 29/29 goods) |
| Y666 | Forcing eligibility true for all goods = "direction doesn't exist", not "everyone up/downstream" | CONFIRMED | derivation check | logical restatement: making the per-good test unconditionally true collapses the per-good distinction entirely, inflating transfer power on every link |
| Y667 | 'Any non-collector with power is transferring' is the loose community summary and is wrong | PARTIAL | measurement [rechecked by coordinator: save trade-ledger scan] | the engine's own ledger distinguishes transferring from inert non-collectors (Castile1444_12_22.eu4): at `sevilla`, `pull_power` 17.642 = FRA 3.319 + ARA 14.323 exactly; at `venice` (an end node) 15.199 of non-collector power sits in `total` with **no** `pull_power` field; `bordeaux`/`lubeck`/`north_sea`/`white_sea`/`rheinland` show non-collector power partially excluded from `pull_power` (e.g. lubeck 78.372 vs 66.632) — so the quoted statement is wrong: that half CONFIRMED from engine arithmetic; the 'loose community summary' attribution is unverifiable within the materials (community sources excluded) |

## §3.8 — Why gates evaluate true (14 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y668 | Vanilla gates assume one global up/downstream relation per nation pair | CONFIRMED | derivation check | consistent with §1.10's description of the single-graph gate mechanics being replaced |
| Y669 | Every province is upstream for some good (a region receiving your cloth ships you furs) | CONFIRMED | derivation check | direct consequence of a 29-good, bidirectionally-varied trade system on a connected map; illustrative not a measured figure |
| Y670 | No fact of the matter for the gate to test; honest fix is to stop consulting it | CONFIRMED | derivation check | follows from Y668/Y669: a boolean with no single ground truth should not be tested at all |
| Y671 | Node-pair dependencies (propagation) differ and keep reading `Φ_w` | CONFIRMED | file read | §1.9 L789 "Direction is read from `Φ_w`" |
| Y672 | Distinction (gate vs node-pair dependency) easy to miss, expensive to get wrong | CONFIRMED | derivation check | setting propagation's node-pair test unconditionally true (as with nation-pair gates) would multiply trade power across the map — a materially different failure mode |
| Y673 | Propagate Religion is node-local: establishes a centre of conversion in the node's own province | CONFIRMED | file read | `common/trading_policies/00_trading_policies.txt` `propagate_religion`: `unique = yes # Only one country can select this in a certain node`, `center_of_reformation = yes` |
| Y674 | Shipped file gates it on trade share AND trade-company region AND merchant present AND religion-group/flag disjunction AND `dominant_religion`, unique=yes per node | CONFIRMED | file read | same file: `can_select` has `OR={religion_group=muslim / zoroastrian_group / dharmic+mission / custom_trigger flags}`, `if... dominant_religion=ROOT`, graduated `trade_share` thresholds gated on `is_node_in_trade_company_region=yes` and `has_trader=ROOT` |
| Y675 | No trading policy anywhere in `00_trading_policies.txt` tests upstream/downstream | CONFIRMED | file read | full-file grep for "upstream"/"downstream" in `00_trading_policies.txt`: zero matches |
| Y676 | 3 of 5 trading policies have no trade-share threshold at all (merchant-present only) | CONFIRMED | file read | `maximize_profit`, `hostile_trading`, `establish_communities`: `can_select` requires only `FROM={has_trader=ROOT}`; `improve_inland_routes` and `propagate_religion` both carry trade-share thresholds |
| Y677 | Written down because the deferred artifact (probe 10) doesn't exist yet | CONFIRMED | file read | §2.7 probe 10 "Caller enumeration... Produce the list as a written artifact" is listed as not yet done |
| Y678 | Scopes read `Φ_w` not any-good reachability: gate is boolean, scope is a set/path | CONFIRMED | file read | §1.10 L797-801 fallback ladder starts with "Φ_w path" |
| Y679 | `Φ_w` is the graph the engine already walks, so scope call sites left alone, collapsing shared-predicate risk | CONFIRMED | derivation check | consistent with §1.9's "Direction is read from `Φ_w`" and §3.9's framing of `Φ_w` as the one installed graph |
| Y680 | Reading `Φ_w` for scopes is legible (one map predicts fleets) and balanced (any-good would be an enormous buff) | CONFIRMED | derivation check | consistent with the measured 90.6% any-good connectivity (Y681) vs a bounded single-graph reachability |
| Y681 | Any-good connectivity 1444/DRAIN = 90.6% (5,723/6,320); v2's 98.8% was v1's unre-measured Laplacian figure | CONFIRMED | measurement (`measure6.py`, re-run fresh) + file read | fresh run: "ordered pairs connected 5723 of 6320 / pct 90.6"; `v1-laplacian/validation.md` L2292/5251: "6245 / 6320" = 98.8% (v1's Laplacian figure); `v2-drain/per-good-trade-spec.md` L678 quotes "98.8%" verbatim |

## §3.9 — Why Phi_w is the installed graph (22 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y682 | Installed graph exists for direction-dependent systems (propagation, fleets, scopes), which model power not logistics | CONFIRMED | file read | §1.9/§1.10 both read `Φ_w` for propagation/scopes; §3.9's own framing (L1698-1699) is consistent with them |
| Y683 | Vanilla's authored arrows: empires pointing at biggest cities/richest areas, 3 authored ends (genua, venice, english_channel) | CONFIRMED | measurement (`val62p6_nodes.py`) | "authored ends (no outgoing): ['english_channel', 'genua', 'venice']" — exact match, from `00_tradenodes.txt` |
| Y177 | `english_channel` richest at 316.6, not a sink — drains to `genua`, 4th at 296.0 | CONFIRMED | measurement (`val62p6_field.py`, `round6.py`) | node wealth: english_channel=316.6 (rank1), genua=296.0 (rank4); Φ_w sinks=[hangzhou, genua]; english_channel routes via champagne to genua (2 hops, per §1.6 text) |
| Y685 | Wealth pulls but wealthiest node not automatically an end; what makes an end is where flow terminates | CONFIRMED | derivation check | direct consequence of Y177's measurement plus the flow-identity argument (Y1131-1134 below) |
| Y686 | `Φ_w` reuses §1.1 operator unchanged: one implementation, guarantees, single combinatorial correctness check | CONFIRMED | file read + code check | §1.6 L516 `Φ_w = DRAIN(b_w) # the §1.1 operator`; scripts/val62p6_field.py calls the identical `drain.run_drain` used for per-good solves |
| Y687 | (CHANGED) Two aggregates tested and rejected before the third (`Φ_w`) was adopted | CONFIRMED | file read | current doc L1721 bullet list: net-flow aggregate (rejected, cyclic) and marking-order aggregate (rejected, superseded), then `Φ_w` adopted — matches "two...before the third" exactly |
| Y688 | Value-weighted net flow `Σ_g V_g·net_g` is a flow, flows circulate, measurably contains cycles — cannot be installed | CONFIRMED | measurement (`val62p6_nodes.py`) | independent reimplementation: "directed cycles found: 28", e.g. `carribean_trade -> amazonas_node -> cuiaba -> lima -> polynesia_node -> california -> rio_grande -> mississippi_river -> carribean_trade` |
| Y689 | (CHANGED) Marking-order aggregate `Φ_ord` is acyclic for free only (self-coherence comparison no longer made) | CONFIRMED | derivation check + file read | current doc L1725 bullet states only "acyclic for free... needs no measurement", with no self-coherence comparison anywhere nearby; a per-node scalar comparison is acyclic by construction (same argument as `Φ_w`'s own marking-order-reproduces-DAG property, §1.6 L582) |
| Y690 | `Φ_w` adopted for one operator/guarantee-set/ends-that-move-with-world | CONFIRMED | file read | doc L1728-1730 near-verbatim match |
| Y178 | (CHANGED) `Φ_ord`'s ends are a function of Phase-3 queue-pop order, a design choice inside the operator, not a world fact; relabelling evidence dropped | CONFIRMED | file read + derivation | doc L1725-1727: "its ends are a function of the order Phase 3 pops its ready queue, which is a design choice inside the operator... follows from the definition and needs no measurement" — matches Phase 3's Kahn-style sweep description in §1.1, and no relabelling figure appears near this bullet |
| Y180 | (CHANGED) No figure quoted for the marking-order rejection; follows from definition | CONFIRMED | file read | doc L1727 "That follows from the definition and needs no measurement"; §3.15 L1966-1968 parallel entry: "The rejection is structural, so no figure is kept for it" |
| Y181 | v2.1-v4.0's "two vanilla-like ends at 1444" justification withdrawn, must not be revived | CONFIRMED | file read | `v2-drain` L685 and `v3-owner-agnostic` L963, `v4-owner-agnostic` L1053 all carry "two vanilla-like ends at 1444 that move with the world" verbatim; current doc L1731-1736 explicitly withdraws this reasoning |
| Y182 | (CHANGED) What `Φ_w` buys: one operator, one guarantee-set, ends that sit where the wealth is | CONFIRMED | file read | doc L1735-1736 verbatim: "What it buys is one operator, one set of guarantees, and ends that sit where the wealth is" |
| Y691 | A difference in `Φ_w` across a link is not the net value crossing it | CONFIRMED | file read | doc L1738 verbatim |
| Y692 | Realized movement follows vanilla propagation (even split / winner-take-all); can run opposite to `Φ_w` | CONFIRMED | file read | §1.8 L763-765: unsteered links split evenly, a single steerer "takes all... down its link, however little power it holds" — matches the dilution/winner-take-all mechanism described |
| Y693 | Disagreement rate measured not assumed; negative-link display policy deferred to data | CONFIRMED | file read | §1.6 L588 "Agreement with the per-good graphs is 55.1%... 54.8% value-weighted" is a measured figure, not an assumption |
| Y694 | Link values are realized flows; conservation holds by construction | CONFIRMED | file read | §2.6 table: "Per-link value \| net Σ_g realized flow, in the installed Φ_w direction" |
| Y1131 | A node can draw more edges in than it sends and still be a thoroughfare; the separating quantity is not degree | CONFIRMED | derivation check | direct consequence of the flow-conservation identity (Y1132) |
| Y1132 | Separating quantity is the flow identity `flow_in(n) − flow_out(n) = −b_w(n)` | CONFIRMED | derivation check | this is exactly the LP's node-balance constraint that Phase 2 enforces (§1.1 "Reachability is a feasibility theorem" bullet: "the LP imposes node balance") |
| Y1133 | Identity holds on all 36 net demanders and all 80 nodes, max residual 5.2e-17 | CONFIRMED | measurement (`round6.py`, re-run fresh) | fresh output: "identity holds on 80 of 80 nodes; max residual 5.2e-17... net demanders (-b_w > 0): 36 ; identity holds on 36 of them" |
| Y1134 | Every net demander absorbs exactly its own deficit and passes the rest on; an end passes none on | CONFIRMED | derivation check | direct restatement of the balance identity: at an end, flow_out=0, so flow_in = -b_w exactly (absorbs its whole deficit) |
| Y1135 | 18 of 80 nodes have out-degree>0 but zero outgoing flow | CONFIRMED | measurement (`round6.py`, re-run fresh) | fresh output: "nodes with out-degree > 0 but ZERO outgoing flow: 18 of 80" |

## §3.10 — Why the engine's economy is overwritten (19 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y695 | Paying correctly but displaying wrong is strictly weaker: node values/pie charts/ledger describe an economy nobody plays; AI/trade-league/peace/income-event systems read those figures | CONFIRMED | file read | `common/defines.lua` has `LIGHT_SHIP_MAINT_FACTOR`; trade-league mechanics present across multiple `common/` files; income-threshold events exist (`center_of_revolution_events.txt` etc.); consistent with v1's own validation.md finding of AI defines keyed on node trade value |
| Y696 | Engine's data model sufficient at node level for a narrower reason: `collect_pool` is per-good inside, but `powershare_C` (share among collectors) has no good dependence | CONFIRMED | file read | §2.6 L1282 near-verbatim: "collect_pool is itself per good on the inside... What factors out is the *other* term: powershare_C is a country's share among collectors, and whether a country collects is a merchant-or-home property with no good dependence at all" |
| Y697 | `powershare_C` factors out because collecting is merchant-or-home, no good dependence | CONFIRMED | file read | same §2.6 passage |
| Y698 | `income_C(n) = Σ_g value_g·collected_share·powershare_C = powershare_C·collect_pool` | CONFIRMED | derivation check | algebraic factoring of a good-independent scalar out of a sum, exact |
| Y699 | This is an identity not a measurement: `powershare_C` carries no g | CONFIRMED | derivation check | direct restatement of Y698's algebra |
| Y700 | Every term feeding a collector's power at a node is node-wide (merchant bonus, off-home penalty, propagation off one graph, caravan grant) | CONFIRMED | file read | `TRADE_POWER_HOME_BONUS`/`TRADE_NON_CAPITAL_OFFICE` (§2.3 table) are country/node-level, not good-keyed; `Φ_w` propagation is a single graph (§1.9); caravan grant keys on `dev/3` (§2.7 item 11), not on good |
| Y701 | One scalar per node reproduces every country's income exactly | CONFIRMED | derivation + measurement (`audit_f4.py`) | algebraic consequence of Y698; audit_f4.py "single graph worst relative error" ~1e-16 to 0 across 5 sample nodes, confirming the doubles-arithmetic reproduction |
| Y702 | v1-v4.0 quoted "agreement to 5.7e-14"/"1.4e-14"; both floating-point residuals of an exact identity, different constructions | PARTIAL | file read (all 5 prior spec files) | v1/v2/v3.0 confirmed verbatim ("5.7e-14"..."1.4e-14"); but v4.0 itself does **not** quote these — `v4-owner-agnostic/per-good-trade-spec.md` L1074-1076 measures **1.3e-16** on `gulf_of_siam` and explicitly attributes "5.7e-14"/"1.4e-14" only to **"v1 through v3.0"**, not itself. The correct range is v1-v3.0, not v1-v4.0 — [rechecked by coordinator: same five files re-read — v1 L438/440, v2 L706/708, v3 L984/986 quote 5.7e-14/1.4e-14; v4 L1074 quotes 1.3e-16 and attributes the old figures to 'v1 through v3.0'; finding reproduced] |
| Y184 | Propagation kept on single graph; reason is not the one v1-v6.0's first draft gave | CONFIRMED | file read | `changes-v6.md` L2896 carries this exact clause ("the reason is not the one v1 through v6.0's own first draft gave"), and L1385 shows an earlier v6.0 draft's different reasoning being superseded |
| Y186 | `gulf_of_siam`'s 29 goods leave it by 7 distinct downstream sets | CONFIRMED | measurement (own script, `solver.build_sc`+`drain.run_drain`) | direct one-hop successor sets of gulf_of_siam across 29 live goods: 8 total patterns, 1 of which is the empty set (9 goods for which gulf_of_siam is itself a sink, i.e. nothing "leaves" it); the 7 non-empty patterns match "leave it by seven distinct downstream sets" exactly. `changes-v6.md` shows the figure was revised from an earlier draft's "eight" (L1385, which counted the empty case) to "seven" (L1391 onward, excluding it) — confirming the intended convention |
| Y187 | Per-good propagation doesn't break the identity: `ps̄_C` (value-weighted per-good share) makes `collect_pool·ps̄_C=income_C` hold, `Σ_C ps̄_C=1` | CONFIRMED | derivation check | algebra: `Σ_C ps̄_C = Σ_g v_g·cs_g·Σ_C ps_C(g) / Σ_g v_g·cs_g = 1` since `Σ_C ps_C(g)=1` per good; `collect_pool·ps̄_C = Σ_g v_g·cs_g·ps_C(g) = income_C(n)` exactly |
| Y188 | Both inputs to `ps̄_C` exist per good at write time; §2.6 sums them into `collect_pool` | CONFIRMED | file read | §2.6 table: "Node collectible pool \| Σ_g value_g(n)·collected_share(n,g)" — exactly the two per-good inputs `v_g`, `cs_g` |
| Y189 | Real cost: `ps̄_C` not derivable from trade power alone — installing it means a fictitious per-node power | CONFIRMED | file read | doc L1760 near-verbatim; consistent with §2.3's constants being genuine engine-read trade-power defines, none of which is value-weighted |
| Y190 | This is a claim about what the engine exposes, not a magnitude | CONFIRMED | file read | doc L1760, direct restatement |
| Y191 | v1-v3.0's "5.96 ducats on ~250" (no such node); v4.0 deleted it (harness asserted); v4.0:0.41%; v5.0:"redistributive, single-digit%"; v6.0 first draft:"at most 0.1%" | CONFIRMED | file read (all 5 prior specs + v4 harness) | v1/v2/v3.0 verbatim "off by 5.96 ducats on a node paying ~250"; `v4-owner-agnostic/scripts/validate_v4.py` L452: `hasnt("3.10","the 5.96-ducat figure","off by 5.96 ducats on a node paying ~250")` (harness assertion of deletion); v4.0 "0.41%" confirmed; v5.0 "redistributive and single-digit percent, with the sign varying by collector" confirmed verbatim; `changes-v6.md` L1391 confirms an early v6.0 draft literally states "the error is... at most 0.1% at every node measured" |
| Y192 | No figure of the author's own quoted; identity holds, objection structural | CONFIRMED | file read | doc L1760 "No figure of my own is quoted here, because the identity holds and the objection is structural" — verbatim, and no numeric figure appears in that clause of the current document |
| Y703 | Only the decomposition by good exceeds what the engine can hold | CONFIRMED | derivation check | direct consequence of Y698-Y701: the scalar total is exactly reproducible, only a per-good breakdown is not natively stored by the engine |
| Y185 | (CHANGED) Reading the one installed graph leaves the propagated term good-independent, identity holds by construction (doubles-residual clause deleted) | CONFIRMED | file read | current doc L1758 reads exactly "so the identity holds by construction." with no doubles-residual clause — matches the CHANGED row's new wording; the algebraic content (Y698-Y699) independently confirms the identity |
| Y1136 | No residual quoted for the doubles check: it measures arithmetic not design; every version quoted a different number from a different construction | CONFIRMED | file read + measurement (`audit_f4.py`) | doc L1756 verbatim; audit_f4.py's own "single graph worst relative error" varies by node (0 to ~2.6e-16) across the 5 sampled nodes, and differs from every prior version's figure (5.7e-14 / 1.3e-16 / 0-3.7e-16), confirming "a different number from a different construction" |

## §3.11 — Why caravan power needs a condition added (12 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y704 | Vanilla steering is outgoing-only, per engine hint | CONFIRMED | file read: localisation | `localisation/hints_l_english.yml:234` `HINT_TRADESTEERING_TEXT`: "You can never steer trade upstream or past your Main Trade City" — exact match |
| Y705 | Node window already lists incoming links as clickable entries | CONFIRMED | file read: interface/tradeinterface.gui | `incoming_nodes_listbox`/`outgoing_nodes_listbox` (OverlappingElementsBoxType) populated by the `TradeNodeLink` windowType (line 17), which contains `guiButtonType name="NextNodeButton"` (line 27) — a real clickable button, not inert text |
| Y706 | Only-outgoing-steerable ⇒ "assigned"="steering" in vanilla | CONFIRMED | derivation | Follows from Y704 (upstream forbidden) + `TradeNodeLink`'s sole interactive element being the navigate button (no assign control) — nothing in vanilla lets an incoming entry take an assignment |
| Y707 | §1.7 makes incoming entries assignable, splits the two | CONFIRMED | file read: spec §1.7 | Spec lines 726-732: "what changes is what an incoming entry does — it must accept a merchant assignment rather than merely navigate" |
| Y708 | Caravan grant fires on `merchant_present_inland`/`merchant_steering_to_inland`, nothing else | PARTIAL | file read: `eu4.exe` string table | Both trigger strings exist verbatim in `eu4.exe`; no third "value moved" condition string found near them (`strings eu4.exe \| grep inland` — only the two, plus unrelated COT/GALLEY tokens). Trigger-name existence is confirmed; "nothing checking whether value moves" is an absence-of-a-string inference, not a positive proof of the engine's internal logic — [rechecked: both trigger strings verified in eu4.exe by coordinator grep; 'nothing else' remains an absence-of-string inference] |
| Y709 | Caravan tooltip reads as granting the bonus in the inland node | CONFIRMED | file read: localisation | `localisation/tradenodes_l_english.yml:17` `TRADEMAP_INLAND_DESC`: "...will give you extra trade power **in that node**..." — exact match |
| Y710 | §2.7 item 11 settles recipient; exposure surface is the two named readings | CONFIRMED | file read: spec §3.11/§2.7 | Spec lines 1776-1779 and item 11 (line 1327) match verbatim: "whichever jumps by min(dev/3+modifiers,50) is the recipient" and the inland-node-vs-adjacent-node hedge |
| Y711 | §1.7's added condition is the right guard under both readings | CONFIRMED | derivation | The "must steer something" filter is orthogonal to which exposure surface is correct — it excludes bare-assignment triggering under either reading |
| Y712 | Caravan power = dev/3 + policy/idea mods, clamped [2,50] | CONFIRMED | file read: `common/defines.lua` | `CARAVAN_FACTOR = 3.0`, `CARAVAN_POWER_MIN = 2`, `CARAVAN_POWER_MAX = 50` (lines 1220-1222); `caravan_power`/`caravan_trade_power` modifier keys found in `common/ideas/*.txt` and `common/policies/00_dip.txt` |
| Y713 | 19 countries at cap from 1444 dev; BUR/KOR/TIM/POR 2-10% short | CONFIRMED | measurement: `scripts/val62p7_b_dev.py` (parses `history/provinces` to 1444.11.11) | 19 countries dev≥150 (cap); next four: BUR 2.00%, KOR 3.33%, TIM 5.33%, POR 9.33% short — all inside stated 2-10% band. Reproduced independently in `final.py` PART D with identical figures |
| Y714 | Caravan power does not scale with node presence at all | CONFIRMED | file read + derivation | The defines-based formula (Y712) contains no node/power term; §1.10 states outright "It is not a function of raw trade power at all" |
| Y715 | Requiring steering restores vanilla; bare-assignment grant is the deviation | CONFIRMED | derivation | Follows directly from Y706 (vanilla coincidence of assigned/steering) plus §1.7's new decoupling |

## §3.12 — Why treasure fleets are always granted (7 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y716 | Argument is §3.8 consistency: gate compares two capitals with no single truth value | CONFIRMED | file read: spec §3.8 | Spec line 1678: "Every province is upstream for some good... There is no fact of the matter for the gate to test" |
| Y717 | v1 claimed a stronger bistability argument | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:454` | "the gate is bistable. Denial is not neutral... Granting diverts that income, lowering the node's wealth... Both states self-reinforce" — matches claim verbatim |
| Y718 | Bistability deleted: gold income never enters `wealth` | CONFIRMED | file read: spec §1.5 + `common/prices/00_prices.txt` | §1.5: "wealth(p) is built from base_tax, base_production and price... reads no income field at all"; `gold = { base_price = 0, goldtype = yes }` (prices file line 45-47) confirms gold is inert in trade value too |
| Y719 | Engine's denial branch: "They will keep their gold income instead" | CONFIRMED | file read: localisation | `localisation/eldorado_l_english.yml:1075` `TREASURE_FLEET_TOOLTIP_CANT_REACH_DELAYED` — exact string match |
| Y720 | Slow second-order version survives (kept gold → dev → wealth, years later) | CONFIRMED | file read + derivation | Matches spec lines 1796-1798 exactly; consistent with the "recoverable" escape v1's own audit identified (`v1-laplacian/validation.md` C549 blast-radius note) |
| Y721 | Inflation scales with money received relative to economy size | PARTIAL | file read: `common/defines.lua` | `GOLD_INFLATION=0.5`, `TREASURE_FLEET_INFLATION=0.5` confirm gold receipt causes inflation, but these are flat rate constants — no file states the "relative to economy size" normalisation explicitly; that precise mechanism is engine-internal arithmetic not found in any shipped file — [rechecked: defines re-read — GOLD_INFLATION/TREASURE_FLEET_INFLATION are flat 0.5; INFLATION_FROM_PEACE_GOLD 0.02 'per month of income' shows an income-relative normalisation exists for peace gold, but no file states it for treasure fleets] |
| Y722 | Route rule is a balance dial; privateers skim per node passed | CONFIRMED | file read: `common/defines.lua` | `PIRATES_TRADE_POWER_FACTOR=1.5`, `PIRATES_MONOPOLY_BONUS=1` confirm privateering is a real per-node mechanic; matches spec line 1800 verbatim |

## §3.13 — Open questions (35 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y723 | Prose-sourced questions distrusted, nothing built on them | CONFIRMED | file read: spec heading | Matches heading "Prose-sourced — distrust, build nothing on them" (line 1804) |
| Y724 | Colonization gate rests on a contradicted forum report; no-gate behaviour argument | UNTESTABLE | gap identified | Own census provenance: "unsourced beyond a contradicted forum report, as the document itself states." No forum access; the AI-colonization-scoring prediction requires a live game observation not among our materials |
| Y725 | Only direction-refusal strings belong to sell-province/treasure fleets, none to colonisation | CONFIRMED | measurement: `eu4.exe` string table | Found exactly `DIPLO_SELLPROV_NOT_UPSTREAM`, `TRADE_POWER_UPSTREAM(_DESC)`, `TREASURE_FLEET_TOOLTIP_CANT_REACH(_DELAYED)`; broad search for colonization+direction strings returns nothing |
| Y726 | Caller enumeration must return "no gate exists" as a successful result | CONFIRMED | file read: spec item 10 | Stipulation consistent with item 10's design (line 1326) — a valid enumeration outcome, not an error case |
| Y727 | Derived questions probably right, cheaply falsifiable | CONFIRMED | file read: spec heading | Matches heading (line 1808) |
| Y728 | `TRADE_PROPAGATE_THRESHOLD` file value and "documented raw requirement" differ by exactly the divider | UNTESTABLE | file read: `common/defines.lua` + gap | `TRADE_PROPAGATE_THRESHOLD=2`, `TRADE_PROPAGATE_DIVIDER=5` confirmed, product=10; but no game file/localisation states a raw-power propagation threshold anywhere (checked hint/text/tradenodes localisation) — "the documented raw requirement" is not locatable in any available source, so the "differ by exactly" comparison cannot be independently checked |
| Y729 | Pass 2 ordering: eligibility-resolution backward-reachability is the only named candidate | CONFIRMED | derivation / doc search | No alternative mechanism is proposed anywhere else in the document; internally consistent argument-from-exhaustion |
| Y730 | Debugger-only list shorter than v1 believed | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:282-296` | v1's §2.7 marks ALL 10 listed items "Settled with a debugger... in one session"; v6.2 keeps only items 1,2,3,10 (pass caching, pass-2 content, write windows, caller enumeration) as debugger-only — a strictly shorter set |
| Y731 | Items 11-15 need save/tooltip/file-edit; propagation-threshold and one-hop are node-window reads | CONFIRMED | file read: spec §2.7 items list | Item 8 = file-edit test, item 9 = save-assertable, items 11/14/15 = tooltip/click reads, item 13 = file edit — matches the classification |
| Y732 | Three cheap probes (caravan recipient, cyclic file, incoming-link button) change what this spec says | PARTIAL | file read: spec §2.7 items-done report | Item 13 (cyclic file) did change the spec ("reverses the hedge"); item 11 (caravan recipient) is unrun, so nothing has changed yet; item 14 (incoming-link button) is explicitly reported "**settled, spec confirmed**... §1.7 stands" (line 1303) — i.e. it did NOT change the spec, contradicting its inclusion in a "change what this spec says" list — [rechecked: L1816-1818 vs L1301-1303 re-read — item 14 'settled, spec confirmed ... §1.7 stands' did not change what the spec says; item 13 did; item 11 unrun] |
| Y733 | One question open in v6.0 wealth model; two others settled into §1.3 | CONFIRMED | file read: spec §3.13/§1.3 cross-check | Matches framing at lines 1819-1821; only one bulleted open item follows, with a "Settled and moved" parenthetical for the other two |
| Y193 | Open wealth question reframed as design question | CONFIRMED | file read: spec §1.3 | §1.3 (per cross-ref) reads only development/trade-good/four province-state modifiers — matches "and nothing else, so this is now a design question" |
| Y195 | `trade_goods_size`/`trade_goods_size_modifier` granted across 8 named categories | CONFIRMED | file read: `common/` tree | Confirmed matches in buildings, event_modifiers, great_projects, static_modifiers, province_triggered_modifiers, holy_orders, state_edicts, tradecompany_investments — all 8 categories present |
| Y194 | v3.0-v5.0's province-scoped rule was wrong in both independent audits that examined it | CONFIRMED | file read [rechecked by coordinator: both audits located] | audit 1: `../v4-owner-agnostic/validation-v4.md` W041 — v3.0's structural rule missed the fourth modifier (`chinaware` `local_autonomy` -0.1, 'the fourth v3.0 missed'); audit 2: `../v5-owner-agnostic/validation-v5.md` X024/X044/X045 — the two-test version wrong on `industrious_personality`, `production_leader` (not local), `bonus_from_merchant_republics` (not local) |
| Y196 | Re-admitting sources costs 105.30 ducats, ~1% of world wealth either way | CONFIRMED | measurement: `scripts/measure6.py` (regenerated) | `measure6.out`: "deleted apparatus in ducats 105.3", "apparatus pct of on-total 0.98", "apparatus pct of v6 field 0.99" — both ~1% |
| Y734 | `local_production_efficiency` from a trade good is outside wealth (Barcelona example) | CONFIRMED | file read: §1.3 wealth formula + `common/tradegoods/00_tradegoods.txt` + `common/technologies/adm.txt` | Wealth reads only base_tax/base_production/price (no income field); `local_production_efficiency=0.1` for glass confirmed at tradegoods.txt:1953; `production_efficiency=0.02` per early adm tech confirmed; Barcelona (213) trade_goods=glass confirmed. The exact tooltip rendering itself is a carried prior observation, not independently re-rendered here, but every component figure is file-verified |
| Y735 | `TAX_COEFF`=1.0 across dev range, tooltip figures at base_tax 6 and 2 | UNTESTABLE | gap identified | Spec's own §2.3 states `TAX_COEFF` "is in no shipped file that has been found" — confirmed by an independent `defines.lua` search (no such constant exists); the specific tooltip readings require a live game session not among our materials |
| Y736 | `k`, `α_min`, `α_max` remain unresolved | CONFIRMED | file read: spec §1.4 + `scripts/solver.py` | §1.4's formula (line 446) keeps k/α_min/α_max symbolic, no numeric commitment; `solver.py`'s `build_sc` takes `alpha_of_good` as a caller-supplied function rather than hardcoding one |
| Y1137 | `b_w` built from uniform `s_w=1/N` against a `c_w` summing to 1; no scale knob | CONFIRMED | file read: `scripts/wealthmodel.py:28-30`, `scripts/round6.py:bw()` | `phi_w`/`bw` both compute `np.full(N,1/N) - num/num.sum()` — s_w is a hardcoded constant, c_w is normalised to sum 1; both fixed by construction, matching "no scale knob to turn" |
| Y1138 | 1e-11 threshold is `flowop.ZERO_TOL`, a post-solve classification constant | CONFIRMED | file read: `scripts/flowop.py:22` | `ZERO_TOL = 1e-11 # net flow below this counts as zero` — a plain Python constant unconnected to solver internals |
| Y1139 | `flowop.LP_OPTS` bottoms at HiGHS's 1e-10; below that, silently rejected and reverts to 1e-7 default | CONFIRMED | measurement: `scripts/round6.py` (rerun) | `round6.out`: tolerance 1e-11 flagged "rejected: Invalid option value", `success=True` at every tolerance tested |
| Y1140 | Revert measured on copper: None/1e-7 move 8 slots, 1e-8/1e-10 move 0, rejected 1e-11 moves 8 | CONFIRMED | measurement: `scripts/round6.py` (rerun) | Exact reproduction: `None 8`, `1e-07 8`, `1e-08 0`, `1e-10 0`, `1e-11 8` |
| Y738 | α_min sublinear regime reachable for 13/30 goods, unreachable 11, boundary 2 | CONFIRMED | measurement: `scripts/w10.py` (rerun, full `change_price` census) | Output: "BELOW 2.0 (sublinear reachable): 13", "EXACTLY 2.0: 2 (gems, silk)", "NO negative event at all: 11" |
| Y739 | Calibration tracks price more closely than the baseline does; configuration recorded, figures withdrawn | CONFIRMED | measurement: `scripts/final.py` (rerun) + independent spearman check on baseline | `final.out` PART B: config k_exp=2/unclamped/ρ=0.5/tol=3e-4 reproduced exactly, cloves α=16→sink beijing; independently computed baseline spearman = -0.249 vs calibration spearman = -0.473 (fresh run) — calibration is materially more price-correlated, confirming "tracks more closely"; no span/correlation/reach figure is quoted in the current spec text at that point |
| Y197 | Under α=16, cloves sink lands on a high-demand node, not a geographic accident | CONFIRMED | measurement (fresh script) | Computed demand ranks at α=16 directly: hangzhou(1) 0.988, beijing(2) 0.0052, doab(3) — beijing (the actual sink) is demand rank 2, confirming "high-demand node" |
| Y741 | Twig tolerance re-routes a small fraction of a good's mass, costs one good full reach | CONFIRMED | measurement: `scripts/final.py` (rerun) | `final.out`: "lowest reach under calibration: [('cloves','99.9975%'), ...]" (only cloves <100%), "max pruned twig mass: 0.00142" (0.14%, a small fraction) |
| Y737 | Zero-flow tolerance now closed: hazard not reachable from inside the model | CONFIRMED | file read + measurement (round6.py, wealthmodel.py) | Directly supported by Y1137/Y1138's confirmed derivation chain; matches current spec wording exactly (lines 1844-1846) |
| Y198 | v2's "Beijing holds richest province" is wrong — that is hangzhou; no figures quoted | CONFIRMED | measurement (fresh script via `wealthmodel.py`) | Richest single province computed = pid 1821, node hangzhou, wealth 30.4 (Beijing's richest is pid 1816 at 19.5); current spec text at that point carries no numeric figures, matching the claim |
| Y743 | Open multiplayer item is build discipline, not LP pivot determinism (§2.1 retires it) | CONFIRMED | file read: spec §2.1 table (lines 915-921) | "margin... 3.8e-8 worst per good, 7.5e-6 aggregate — 8 to 10 orders above float noise"; "orientation under LP column permutation: 0 flips" — independently reproduced via `round6.py` (margin 3.765e-08/copper, 7.527e-06 aggregate) |
| Y1049 | Open: runtime CPU dispatch / threaded reductions could break bit-identity | CONFIRMED | file read: spec §2.1 item 2 (lines 928-931) | Matches near-verbatim: "No runtime CPU dispatch in the LP solver, and single-threaded... Both must be pinned... not assumed" |
| Y1050 | Open: whether DLL reproduces reference orientation exactly, untestable until DLL exists | CONFIRMED | file read: spec §2.1 item 3 (line 932) | Matches: "§2.8's cross-implementation orientation check. It compares the DLL against the reference" |
| Y744 | AI merchant reassignment cadence is open | CONFIRMED | file read: spec §3.14 cross-ref | §3.13 line 1885 points to §3.14, which states cadence "is undecided and is the one item left for the human" (line 1899) |
| Y740 | Unclamped α² is a demand-model decision because luxuries become court goods | CONFIRMED | file read: spec §1.4 + §3.13 (lines 1872-1873) | §1.4: "α>1... Luxuries concentrate on individually rich provinces" — "court goods" is a direct restatement; matches current wording |
| Y742 | Baseline does not adopt the calibration; adopting it is a §1.4 decision | CONFIRMED | file read: spec line 1876 | Exact text match: "The baseline does not adopt it; adopting it is a §1.4 decision" |
| Y1141 | No span/correlation/reach figure quoted for the calibration; numbers move with the field | CONFIRMED | file read + cross-version comparison | Current spec text (lines 1865-1878) quotes only configuration parameters, no span/correlation/reach; the same calibration's figures differ across `drain-orientation.md` (span 1..5, spearman -0.539), a withdrawn "-0.20" figure, and the fresh current-field run (span 1..6, spearman -0.473) — the numbers genuinely move with the wealth field, as claimed |

## §3.14 — AI merchant assignment (15 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y745 | Two ends of a link never compete: disjoint oriented good-sets | CONFIRMED | derivation + file read: spec §1.6/§1.7 | DRAIN produces one direction per good per undirected edge (confirmed acyclic/oriented in `round6.out`/`final.out`); §1.7 line 733-737 explicitly states merchants at n/m act on disjoint good sets |
| Y746 | One precompute per good: backward DAG pass gives `S_g[n][H]` | CONFIRMED | file read: spec §1.8 cross-ref | §1.8's `collected_share`/transfer formulas are exactly the recursive per-good, per-node structure described |
| Y747 | `collected_share(n,g)` reaches 1 at the sink, terminating recursion | CONFIRMED | file read: spec §1.8 (line 755) | `collected_share(n,g) = 1 if n is a sink for g` — exact match |
| Y748 | All three survival-table inputs are country-independent, so one table not one per nation | CONFIRMED | file read: spec §1.8 | "Trade power and collect/transfer intent are node-wide. What varies per good is what they produce" — consistent with country-independence claim |
| Y749 | ~1.5 MB double precision; v1/v2's "0.75 MB" was the single-precision figure | CONFIRMED | measurement + file read: `v1-laplacian/per-good-trade-spec.md:484`, `v2-drain/per-good-trade-spec.md:788` | Both v1 and v2 state "about 0.75 MB" verbatim; independently computed 29(live goods)×80×80×8 bytes = 1,484,800 bytes = 1.4848 MB, exactly double 0.75 MB |
| Y750 | Scoring reads survival table for both steering and collecting | CONFIRMED | file read: spec §3.14 (line 1893) | Direct text match |
| Y751 | Off-home penalty is a power modifier, not a value haircut | CONFIRMED | file read: `common/defines.lua` | `TRADE_NON_CAPITAL_OFFICE = -0.50` — a trade-power-scoped constant, not a value multiplier |
| Y752 | `value×share×0.5` scoring is wrong; halving belongs on power upstream of the two-stage formula | CONFIRMED | derivation | Halving power before computing `collected_share=P_collect/(P_collect+P_transfer)` changes the denominator too, unlike a post-hoc ×0.5 on the final product — mathematically distinct except in degenerate cases (P_transfer=0) |
| Y753 | Off-home penalty "falls out" of survival table because it's power-derived | CONFIRMED | file read: spec (line 1895) cross-ref | Consistent with Y751/Y752's confirmed mechanism |
| Y754 | Home-node bonus voided entirely by placing a collector outside home | CONFIRMED | file read: `common/defines.lua` + derivation | Restates the -50% `TRADE_NON_CAPITAL_OFFICE` penalty from the opposite framing (no separate mechanic found) |
| Y755 | Greedy scoring can oscillate; damping should hold it | UNTESTABLE | gap identified | §3.14 itself flags this as unverified design intent: "the prototype must verify" (line 1897) — no implementation exists in `scripts/` to test |
| Y756 | Reassignment cadence undecided, left for the human | CONFIRMED | file read: spec (line 1899) | Direct text match |
| Y757 | Mirror-vanilla preference stated; relevant define not located | CONFIRMED | measurement: `common/defines.lua` full search | No merchant-reassignment-cadence define found anywhere in defines.lua (searched all AI/merchant/cooldown/interval keys) — consistent with "not located" |
| Y758 | Computed alternative uses `MERCHANT_SPEED` and the survival table, both of which exist | CONFIRMED | file read: `common/defines.lua` | `MERCHANT_SPEED = 20.0` confirmed present; survival table is the document's own defined construct (§3.14) |
| Y759 | Argument for computing cadence: vanilla's was tuned against a static graph | CONFIRMED | file read: spec (line 1904) | Direct text match |

## §3.15 — Rejected (35 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y760 | v1 Laplacian: topological sink placement, sparse (not large) supply signal | CONFIRMED | file read: spec §3.2 cross-ref | §3.2 (lines 1432-1442): sink rule "(c−s)/deg > mean(neighbour φ) − min(neighbour φ)", "supply is sparse... spices 18/80 nodes, cloves in one" |
| Y200 | v3.0/v4.0 repeated the 10⁷/10²-10³ ratio while v4.0's own §3.2 withdrew it | CONFIRMED | file read: `v3-owner-agnostic/per-good-trade-spec.md:1109`, `v4-owner-agnostic/per-good-trade-spec.md:837,1200` | v3 and v4 both repeat "supply contrast (10⁷) drowns demand contrast (10²–10³)" in their rejected-Laplacian entry, while v4's own §3.2 (line 837) simultaneously qualifies/withdraws the same ratio |
| Y199 | Laplacian entry carries no copy of the measurement (§3.2 carries it); demand side now wider | CONFIRMED | file read: spec §3.2 (lines 1438-1442) | "On the contrast metric itself the demand side is the wider one, not the supply side" — present only in §3.2, not duplicated in §3.15's entry |
| Y201 | `cloves` has a single producer, no contrast to measure | CONFIRMED | file read: spec §3.2 (line 1435) | "cloves in exactly one [node]" |
| Y761 | Laplacian diagnosed/measured/replaced; DRAIN keeps 100% reachability, exact conduit behaviour | CONFIRMED | file read + measurement | `final.out`: "V032 reach==100%: 29/29"; matches spec text (lines 1917-1919) verbatim |
| Y762 | Pure min-cost-flow (no sweep) orients only ~79-edge support, half the map, cyclic aggregate | CONFIRMED | measurement: `scripts/final.py` (rerun) | `final.out`: "V186 support sizes: min 78 max 79 (N-1 = 79)" of 159 total undirected edges (~50%); cyclic aggregate matches §3.9's confirmed "value-weighted net flow... contains directed cycles" |
| Y202 | Ranked orientation wins alignment stats, far higher top-demand share than DRAIN | CONFIRMED | measurement: `scripts/rankrep.py` (rerun) | RANK: P(sink\|top10)=46.6%, mean sinks/good=13.3 vs DRAIN's top-8 alignment 19.4% (`round6.out`) and mean sinks/good 3.7 (`final.out`) — several times higher on both axes |
| Y203 | Seeded basin growth starves demand off supply→seed paths at every tuning tried | CONFIRMED | file read: `v2-drain/drain-orientation.md` (lines 141-345) | Scorecard: BASIN demand-reachable 88.5% (<100%); appendix lists three separate BASIN tunings (0.3941/0.3242/0.2206), all leaving unserved demand |
| Y763 | BASIN's useful ideas (HHI-adaptive k, stall self-correction) survive in DRAIN Phases 1/3 | CONFIRMED | file read: `scripts/drain.py`/`drainrep.py` cross-ref | `drainrep.py` reports HHI and k as DRAIN Phase-1 outputs; fallback-promotion ("stall lemma") logic present in DRAIN's Phase 3 |
| Y764 | DEF-descending rejected: unmet demand identically zero, DEF=total demand, strands flow | CONFIRMED | file read: `v2-drain/drain-orientation.md` §6 | "on the flow subgraph, unmet demand is identically zero... DEF is necessarily total downstream demand... stranding it at their sinks (0.1206 → 0.1415)" — near-verbatim match |
| Y765 | Authored demand weights rejected: authored data in a model needing none | CONFIRMED | file read: spec (trivial restatement) | Consistent with the model's stated zero-authored-data design goal (§1's Goal 6) |
| Y766 | Trade income inside wealth rejected: reintroduces circularity | CONFIRMED | derivation | wealth→Φ_w→orientation→flow/trade-income pipeline; feeding trade income back into wealth would close the loop, matching §1.5's exclusion of all income fields |
| Y767 | Node-level α rejected: concentration becomes a function of map granularity | CONFIRMED | derivation | Internally coherent argument; no contradiction found elsewhere in the document |
| Y768 | Median-relative α anchor rejected: concentration shifts on unrelated price moves | CONFIRMED | file read: spec §3.5 (line 1570) | "Under a median anchor, a good could concentrate because some unrelated commodity got expensive — noise dressed as economics" — direct match |
| Y769 | α floored at 1 rejected: discards the cheap-bulk regime | CONFIRMED | file read: spec §1.4 | α<1 regime ("Bulk goods spread toward populous regions") becomes unreachable if floored at 1 — direct logical consequence |
| Y770 | Production income as supply term rejected: makes world supply depend on idea groups | CONFIRMED | file read: spec §3.4 (lines 1562-1566) | "Substituting production income would make V_g... depend on owners' idea groups and autonomy"; v1 second-strike (α=1 identity) also confirmed moot per text |
| Y771 | τ margin on orientation rejected: manufactures cycles | CONFIRMED | file read: spec §3.6 (lines 1621-1629) | Concrete v1 example: tol=1e-3 on {0,0.0006,0.0012} turned an acyclic prior into A→B→C→A |
| Y772 | Uniform supply (v1 entry, moot in v2): answered nobody's question, destroyed φ₀'s identity | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:515` | "Answers a question nobody asked and destroys the identity that makes φ₀ worth computing" — exact match |
| Y773 | φ₀ as installed graph (v1 entry, moot in v2): not the economy the model runs | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:517`, `v2-drain/per-good-trade-spec.md:847-848` | v1: "It is not the economy the model runs"; v2-drain confirms the dropped "(v2.0 briefly used Φ_ord)" parenthetical was historically accurate, just cut for concision |
| Y774 | Φ_ord (v2.0, superseded v2.1): ends are Phase 3 queue-discipline artifacts, not the world | CONFIRMED | file read: `v2-drain/per-good-trade-spec.md:851-853` + current spec (lines 1966-1968) | v2-drain's old wording (62.7%/53.4% figures + "measured coherence ceiling") vs current spec's new wording ("acyclic for free, but its ends are a function of Phase 3's queue discipline... no figure is kept") — matches the stated rewording exactly |
| Y204 | §3.15's Φ_ord entry drops its figures and "coherence ceiling" role | CONFIRMED | file read: same as Y774 | Current text: "The rejection is structural, so no figure is kept for it" — confirms both the figures and the ceiling-role framing are gone versus v2-drain's version |
| Y205 | 3-mass gravity field reproduces whatever end count it's seeded with (γ small) | CONFIRMED | file read: `v2-drain/per-good-trade-spec.md:855-858` + current spec (lines 1970-1979) | v2-drain quotes a specific "69% vanilla-arrow agreement" figure; current spec explicitly withdraws it ("every agreement percentage... was measured on a superseded wealth field") — matches the described figure-withdrawal |
| Y775 | Emergent-count wealth good replaced the pinned-count fields | CONFIRMED | file read: spec (line 1979) + v2-drain (line 858) | Verbatim in both versions |
| Y776 | Vestigial in-game economy w/ net treasury settlement rejected | CONFIRMED | file read: spec (line 1981) + v2-drain (line 859) | Verbatim match: "Correct treasuries, wrong displays, wrong AI inputs" |
| Y778 | Node-level collect/transfer rejected: split is per good | CONFIRMED | file read: spec §1.8 (line 755-757) | `collected_share(n,g)` formula is explicitly per-(node,good) |
| Y779 | Unsteered-goods-fully-collected rejected: transfer power isn't from merchants; full collection is sink-only | CONFIRMED | file read: spec §1.8 (lines 759-767) | "If no country steers g at n: the outgoing value splits evenly"; "At g's sink: ...100% is collected" — full collection strictly sink-gated |
| Y780 | Undirected shortest path as primary fleet route rejected | CONFIRMED | file read: spec §1.10 fallback ladder (lines 797-801) | Undirected shortest path is explicitly rank-3 fallback, not primary |
| Y781 | Automatic per-good targeting rejected: one arrow click already resolves per-good | CONFIRMED | file read: spec §1.7 (lines 733-737) | A single link assignment "steers every good oriented n→m" — one click, all goods on that link |
| Y782 | Companion-overlay assignment rejected: must stay a game action | CONFIRMED | derivation | Consistent with the model's stated Goal 7 (engine's own numbers/actions) |
| Y783 | Emission-time pruning rejected: peripheral termini are intended consumption | CONFIRMED | file read: spec (lines 1999-2002) | Direct text match, including the named twig-tolerance exception |
| Y784 | Edge conductance/weighted Laplacian rejected: v1's "too much mechanical surface," now no Laplacian to weight | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:535` | v1: "Too much mechanical surface; the unweighted solve routes correctly through conduits" — direct quote source; current text's correction (unweighted metric was the actual cause) matches §3.2's diagnosis |
| Y785 | Staged delivery rejected: different designs sharing a solver | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:536` | Verbatim carried unchanged from v1 |
| Y786 | "Aggregate map not a DAG" still an error; v1's gradient-of-Φ defense contradicted its own §3.9 | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:539` vs `:421-423` | Line 539: "Net flow is the gradient of Φ, hence acyclic"; v1's own §3.9 (line 421): "ΔΦ is not the net value crossing an edge... a link can be oriented n→m under Φ while realized net flow runs m→n" — direct internal contradiction |
| Y787 | Aggregate is a DAG because Φ_w is a DRAIN orientation, acyclic by marking order | CONFIRMED | measurement + file read: spec §3.15/§3.9 | `round6.out`/`final.out` confirm acyclicity of the installed aggregate; matches current text (lines 2013-2015) |
| Y777 | Per-good propagation rejected NOT for breaking factoring (it doesn't) but for writing a fictitious per-node power | CONFIRMED | file read: spec §3.10 (line 1758) | §3.10 confirms the `ps̄_C` identity survives per-good propagation algebraically; current §3.15 text matches the CHANGED wording exactly |

## §3.16 — Evidence standard (24 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y788 | v1's evidence standard: every retraction traced to prose | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:543` | "Every retraction... traced to a premise that entered through prose... Nothing built on adjacency data, file values, or the model's own equations failed" — exact source of the standard being refuted |
| Y789 | At least fifteen non-prose claims failed, by three mechanisms | CONFIRMED | file read: `v1-laplacian/validation.md:71` | "at least fifteen of them... Three distinct mechanisms show up" — exact match |
| Y790 | Mechanism 1: 75% overseas floor is pre-Common-Sense; 1.37 has 90/50/20/0 | PARTIAL | file read: `common/defines.lua` | `COLONY_MIN_AUTONOMY = 50` confirms the "50" figure exactly; no defines.lua constant found for the Territory (90), pasha-state (20) or core (0) floors — these appear to be hardcoded engine values not exposed in any shipped file, so only one of the four figures is independently file-verified — [rechecked: defines.lua carries only COLONY_MIN_AUTONOMY = 50; no 90/20/0 constants and no pre-Common-Sense file among the materials] |
| Y791 | Mechanism 2: v1's grain(1.25)/livestock(1.00) are exactly price/P₀ | CONFIRMED | file read: `v1-laplacian/per-good-trade-spec.md:389` + `common/prices/00_prices.txt` | v1 wrote "grain lands near 1.25 and livestock near 1.00"; actual base prices are grain=2.5, livestock=2.0 (`00_prices.txt`); 2.5/2.0=1.25 and 2.0/2.0=1.00 exactly |
| Y792 | Mechanism 3: ε only preserves α=1 identity if applied to φ₀'s supply too | CONFIRMED | measurement: `scripts/final.py` PART C (rerun) | "V204(a) eps on per-good s only: rel residual 9.11e-06" vs "V204(b) eps on both sides: rel residual 1.22e-15" — ~1e-5 vs machine precision, exactly the described bug pattern |
| Y206 | v1's ε (1e-6) implemented as written leaves α=1 residual at 1e-5 | CONFIRMED | measurement: same as Y792 | Same figures: 9.11e-06 residual against v1's stated ε of 1e-6 — an order of magnitude worse, "would have been diagnosed as a solver bug" |
| Y793 | One of only 3 `verified (method unstated)` claims — Propagate Religion's gating — was wrong | CONFIRMED | file read: `v1-laplacian/validation.md:63` | "Note this claim carries `verified (method unstated)` provenance - it is one of only three such claims in the spec, and it is wrong" — exact match |
| Y794 | 9 of 16 refuted ENGINE claims were UNSOURCED (v2 wrongly said "nine of fourteen"; 16 total, 13 excluding 3 derivation) | CONFIRMED | file read + independent recount: `v1-laplacian/validation.md` + `claims.md` (cross-referenced by script) | Recount confirms: 23 total refuted, 16 ENGINE-typed refutations exactly (matches summary table); of those 16, provenance breakdown = 9 UNSOURCED, 3 derivation, 3 file value, 1 verified(method unstated) — 16−3=13 exactly. v2's "nine of fourteen" independently confirmed at `v2-drain/per-good-trade-spec.md:913-914` |
| Y795 | Rule: not "trust derivations"/"distrust prose" but "unsourced entry is the risk" | CONFIRMED | file read: spec (lines 2038-2039) | Direct text match |
| Y796 | Every engine fact must carry a source; unsourced is a to-do not a fact | CONFIRMED | file read: spec (lines 2040-2041) + broad cross-check | Verbatim match; consistent with the overwhelming majority of ENGINE claims across this slice carrying explicit file/string provenance |
| Y797 | v1 never stated what determines sink placement — nothing to extract or refute | CONFIRMED | measurement: exhaustive grep across `v1-laplacian/claims.md`, `per-good-trade-spec.md`, `validation.md` | Zero hits for "sink placement"/"determines sink"/"what determines" anywhere in any of the three v1 documents |
| Y798 | Audit found the flaw only by running the solver and asking why output looked wrong | CONFIRMED | file read: `v1-laplacian/diagnosis.md:1-7` | "The reference solver places the sole spices sink at saxony... No node containing a top-development province is a spices sink. This document diagnoses why" — the origin story matches exactly |
| Y799 | Standing repair: sink placement, free-edge direction, reachability, acyclicity, scheduler-invariance now stated | CONFIRMED | file read: spec §3.2 (lines 1467-1512) cross-ref | The four numbered guarantees in §3.2 match the list named at line 2048-2050 exactly |
| Y800 | Each property provable/measured-and-labelled, checked at runtime as assertion or monitor | CONFIRMED | file read: spec §2.8 | "Reachability... asserted every tick"; "Containment is a hard assertion, every tick... Equality... is monitored, not asserted... measured exact... but is not a theorem" — exact assertion/monitor distinction confirmed |
| Y801 | Next audit's first question: which property is still unstated | CONFIRMED | file read: spec (line 2054-2055) | Verbatim match |
| Y802 | Cautionary case closed the other way; corrected once, defended by two reviewers, tooltip's second qualifier | CONFIRMED | file read: spec (lines 2057-2063) | Direct text match |
| Y803 | Probe 15: qualifier descriptively false — no provinces/merchant, still receives propagated power | CONFIRMED | file read: `v2-drain/game-session.md:406-429` | Real session log: "France holds no provinces in the Sevilla node and has no merchant there (0.0 in both)... Transfers from traders downstream: +3.1" |
| Y804 | Lesson isn't the filed one; what failed was a binary string, the class §3.16 nominates as sufficient | CONFIRMED | file read: localisation + game-session.md cross-ref | `TRADE_POWER_UPSTREAM_DESC` string confirmed genuine; its implied precondition falsified by the same live probe (Y803) — a string, not prose, was the unreliable source |
| Y805 | A localisation string describes intent, not behaviour | CONFIRMED | file read: `localisation/tradenodes_l_english.yml:150` | `TRADE_POWER_UPSTREAM_DESC:0 "A nation can Transfer Trade Power back upstream to trade nodes where it already has power."` — the string exists and states intent; Y803's probe shows the described precondition is not enforced |
| Y806 | Sources necessary but not sufficient; string-sourced fact settled only when behaviour observed | CONFIRMED | file read: spec (lines 2068-2070) | Direct text match, consistent with the Y803-Y805 case |
| Y807 | Declaration-order test: permuted differed from vanilla on 61/80 nodes | CONFIRMED | file read: `v2-drain/game-session.md:179,584` | "vanilla vs permuted differed on 61 of 80 nodes" — exact match, stated twice in the log |
| Y808 | That measurement was meaningless: two vanilla runs differ on 49/80 nodes by up to 8.96% | CONFIRMED | file read: `v2-drain/game-session.md:187,190` | "local_value 30/79 differ, max 8.96%"; "Two identical vanilla runs disagree on 49 of 80 nodes by up to 8.96%" — exact match |
| Y809 | A measurement without a null comparison is not evidence | CONFIRMED | file read: `v2-drain/game-session.md:590` | Session log's own conclusion: "was still meaningless without a null comparison" |
| Y810 | Every measured claim that could vary run-to-run should carry its noise-floor control | CONFIRMED | file read: spec (line 2078-2079) + cross-document spot-check | Direct text match; consistent with the noise-floor controls observed throughout this slice (fingerprint determinism, ±1% wealth-noise checks, 40-of-40 relabellings, the declaration-order null itself) |
