Y1142	§0	PARTIAL	The folded claim-audit corrections were spot-verified on 7 of 60 in the round-7 audit, with none missing.
Y1185	§0	PARTIAL	The seven claim-audit corrections spot-verified in the round-7 audit are C037, C038, C049, C101, C128, C130 and C132.
Y1081	§1.1	PARTIAL	`props6.py` copied and extended from `val5_pergood.py`; the permutation loop written for this citation — "the figure had been quoted since v2 with nothing in the tree that computed it"
Y258	§1.1	PARTIAL	§2.2 states the connectedness requirement and what the solver does when it is violated.
Y037	§1.3	PARTIAL	`tax_value(p) = TAX_COEFF · base_tax(p)` — no modifier at all (restated L49-50, L349, L955)
Y114	§1.6	PARTIAL	`genua` is a sink, out-degree 0 against in-degree 5, so the western Mediterranean, the Adriatic and the Rhône carry power into it.
Y384	§1.9	PARTIAL	Ship trade power propagates only where the country has a ship-propagation modifier, at the compounded rate: the propagation share multiplied by that modifier.
Y1012	§2.1	PARTIAL	There is no randomness in the solve: an identical output fingerprint over repeated runs, separate processes, and five `PYTHONHASHSEED` values including `random`.
Y1021	§2.1	PARTIAL	Every trade number EU4 writes to a save is quantised to 1/1000: 495 of 495 sampled values land exactly on that grid across `total`, `val`, `p_pow`, `retention`, `collector_power` and `max_pow`.
Y134	§2.2	PARTIAL	v5.0's "0.17–0.21 s for all 29 goods" is not reproducible: the number of twelve runs landing inside that interval varies from 0 to 11 across replicates.
Y483	§2.3	UNTESTABLE	DLC state is a third input axis: treasure-fleet diversion and caravan power are both DLC-conditional, and caravan modifier values are readable even when inert, so key on the DLC flag rather than on the presence of a value.
Y1212	§2.5	PARTIAL	The binary is at build `835bfdf8`, SCM timestamp 2024-10-03, constant across `eu4_rev.txt` and all three crash dumps.
Y505	§2.6	UNTESTABLE	Monthly trade tick runs in three passes: static power/modifiers; end-node propagation pass; origin-node value pass
Y509	§2.6	UNTESTABLE	Country trade income is derived by the engine from the written fields, unless stored
Y512	§2.6	UNTESTABLE	Two deadlines: display immediately after the value pass, because AI consumers read these figures during the month
Y513	§2.6	UNTESTABLE	Payment bounded by month boundary, since treasury reconciles at month start against previous month's income
Y515	§2.7	PARTIAL	Items 1–10 are the v1 probe set, settled with a debugger on a vanilla install in one session, though the claim audit found several are observable without one.
Y549	§2.8	PARTIAL	Caribbean 1650: sugar production income makes it a sink for cloth, tools and wine.
Y173	§3.5	PARTIAL	`verify6.py` checks the census only by requiring the printed total to match a computed one rather than by reconciling per file, and `measure6.py`'s walker still swallows parse failures in a bare `except`.
Y695	§3.10	PARTIAL	Paying countries correctly while leaving the display wrong is strictly weaker: those figures have readers — income-threshold content file-confirmed, the other three engine-internal.
Y1232	§3.10	PARTIAL	AI light-ship building, trade-league behaviour and peace valuation are engine-internal readers.
Y728	§3.13	UNTESTABLE	The `TRADE_PROPAGATE_THRESHOLD` file value and the documented raw requirement differ by exactly the propagation divider, which reconciles if the threshold is expressed in propagated units.
Y749	§3.14	PARTIAL	v1 and v2 both said 0.75 MB, the single-precision figure; the rest of the solver is double precision — 29 × 80 × 80 at 8 bytes is 1,484,800 bytes — and its residuals sit at 1e-16, one ULP of a double (§3.10).
Y754	§3.14	UNTESTABLE	The home-node bonus is voided entirely by placing any collector outside the home node.
Y790	§3.16	REFUTED	Mechanism 1: the 75% overseas floor is pre-Common-Sense; of 1.37's regime floors `COLONY_MIN_AUTONOMY = 50` is the file-exposed one, and the 90/20/0 floors are not in `defines.lua` or any file that has been searched.
Y805	§3.16	PARTIAL	A localisation string describes intent, not behaviour.