## §1.1

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y1080 | props6.py credited for every determinism-bullet figure, core-node count included | CONFIRMED | measurement + file read (p6.out; grep of measure6.py) | p6.out's single run of `props6.py` prints core-node count ("core nodes summed over goods: 2320"), permutations ("scheduler permutations: 0 ... 145 trials"), Phase-1 argmin/cut ties, and reachability/orphan-sink figures all in one script/run; `grep -n "corenodes\|core node\|argmin tie\|reachab\|orphan" scripts/measure6.py` returns no matches, so `measure6.py` computes none of them. |
| Y270 | Across machines only build-discipline is open; §2.1 retires LP determinism | CONFIRMED | file read (spec §2.1, §3.13) | §2.1 (doc lines ~910-960): "a few units in the last place cannot change any decision this solver makes... What remains is not bit-reproducibility of a simplex... but three checks: 1. One binary per platform... 2. No runtime CPU dispatch... 3. §2.8's cross-implementation orientation check." §3.13's "Multiplayer build discipline" bullet: "Not LP pivot determinism, which §2.1 retires... What is open is whether the shipped solver build does runtime CPU dispatch or threads its reductions." |
| Y221 | Every trade good has its own directed network over the same adjacency | CONFIRMED | file read (solver.py, drain.py) | `solver.py` builds one shared `UND`/`EDGES_UND` from the tradenodes file; `drain.run_drain(b)` is called once per good with a different `b` vector (per props6.py's loop `for gi, g in GL: ... run_drain(b)`), reusing the same `EDGES_UND` each time. |
| Y222 | Direction is computed, never authored | CONFIRMED | file read (solver.py lines 44-52) | `solver.py`: `for m in NODES[n]["outgoing"]: ... UND[a].append(b); UND[b].append(a)` — the vanilla tradenodes file's directional `outgoing` list is folded into an undirected adjacency (`EDGES_UND = sorted(set(tuple(sorted(e)) for e in EDGES))`); no direction field survives into the graph structure DRAIN consumes. |
| Y223 | b_g(n)=s_g(n)-c_g(n), oriented by DRAIN in four named phases: peel, select, route, sweep | CONFIRMED | file read (drain.py) | `drain.py` defines exactly `phase0` (peel), `phase1` (select), `phase2` (route: `mincost_flow`), and `sweep`/`sweep_priority` (sweep), matching the four names; Phase 4 (`compile_dirs`, un-peel) completes the peel/un-peel pair and is not one of the "four named phases" listed in this summary sentence. |
| Y224 | Phase 0 orients pendant edges by sign of absorbed subtree balance (exporter\|toward core; importer\|fed from core; zero\|toward core), folds residual into parent | CONFIRMED | file read (drain.py phase0, compile_dirs) | `phase0`: `Plog.append((v,u,beta[v])); beta[u]+=beta[v])`. `compile_dirs`: `for (v,u,bv) in reversed(Plog): directed.append((v,u) if bv>=0 else (u,v))` — `bv>=0` (exporter or zero) emits `v->u` (toward core); `bv<0` (importer) emits `u->v` (fed from core). Matches all three cases exactly, including zero->toward-core. |
| Y225 | Phase 0 exact not heuristic: every removed edge is a bridge; flow on a tree is determined by conservation | CONFIRMED | derivation check | Repeated degree-1 peeling computes the graph's 2-core; every edge removed this way is provably a bridge of the original graph (it lies on no cycle, else its endpoints would keep degree >=2 and never be peeled). On a tree/forest, node balances alone fix every edge's flow uniquely by conservation at each node — standard result, and exactly what `beta[u]+=beta[v]` implements. |
| Y226 | On vanilla map Phase 0 is a no-op: min degree 2, zero bridges | CONFIRMED | measurement (p6.out) | p6.out: "min degree 2  max 8  mean 3.98" and "components=1  bridges=0". |
| Y227 | Phase 0 exists for modded maps | CONFIRMED | file read (drain.py phase0) | `phase0()` is fully general degree-1 peeling with no vanilla-specific gating; it only ever acts when a degree-1 node exists, which p6.out shows is never true on vanilla (min degree 2) but would be true on a map with pendant nodes. |
| Y228 | Phase 1: connected clusters of net demanders, HHI of demand masses, k=clamp(round(1/HHI),1,\|clusters\|), heaviest demander of top-k clusters | CONFIRMED | file read (drain.py phase1) | `drain.py` lines 52-102: `Dset=[v for v in core if beta[v]<0]`; connected components of `Dset` via `UND`; `M=[sum(-beta[v] for v in comp) ...]`; `HHI=sum(x*x for x in q)`; `k=int(min(max(round(1.0/HHI),1),len(comps)))`; `top=sorted(...,key=-M[j])[:k]`; `S.add(min(comps[j],key=lambda v:(beta[v],v)))` — exact match to the stated formula. |
| Y229 | One knob r (default 0); ρ withdrawn, the shipped operator never had it | CONFIRMED | file read (drain.py phase1 signature) | `def phase1(core, beta, dilate_r=0):` is the only knob; no `rho`/`ρ` parameter appears anywhere in `phase1`. |
| Y1075 | Cluster dilation radius r links demanders within r hops before clustering | CONFIRMED | file read (drain.py lines 71-92) | The `dilate_r>0` branch BFS-expands each demander-cluster's frontier up to `dilate_r` hops through `UND` and union-merges any other cluster whose member is reached within that frontier, i.e. demanders within r hops get linked into one cluster before HHI/top-k selection. |
| Y230 | On vanilla 1444, k=1 for 27 of 29 goods at defaults | CONFIRMED | measurement (p6.out) | p6.out: "Phase1 k==1 on 27 of 29 goods (k values Counter({1: 27, 2: 2}))". |
| Y231 | Phase 1's selection is deliberately weak because Phase 3 self-corrects upward | CONFIRMED | file read + derivation (drain.py sweep) | Phase1 selects only `k` heaviest demanders (often 1, per Y230); `sweep`/`sweep_priority`'s stall-promotion branch (`promotions.append(s_star)`) adds further sinks whenever the initial selection under-serves, which is exactly the "self-correction" the claim describes and is what makes the measured mean 3.69 sinks/good exceed the k=1-or-2 selection. |
| Y1076 | ρ was documented as a second Phase-1 knob; the shipped operator has no such parameter — Phase 1 clusters every demander | CONFIRMED | file read (drain.py phase1) | `Dset=[v for v in core if beta[v]<0]` takes literally every core node with negative balance, no quantile/threshold filter; no `rho` parameter exists in the function signature or body. |
| Y1077 | The §3.13 calibration carries its own Phase 1 and implements ρ | CONFIRMED | file read (spec §3.13) | §3.13 "The sink-count-span option": "a demand-mass quantile ρ = 0.5 in its own Phase 1". |
| Y232 | Phase 2 solves the uncapacitated min-cost flow serving b_g and orients every support edge by net flow | CONFIRMED | file read (drain.py phase2, flowop.py mincost_flow) | `phase2()` calls `mincost_flow(b, zeros(N), cost=TIE_COST)` with `bounds=(0,None)` (uncapacitated) in `flowop.mincost_flow`; `net_per_edge(f)` then classifies each edge by the sign of its net flow into `flow_arc` (oriented) or `free`. |
| Y976 | Phase 2 arc costs near-unit, symmetric, wealth-derived: first-order TIE_EPS·(w[u]+w[v])/2 plus a second-order generic tie-break | CONFIRMED | file read (flowop.py lines 58-74) | `TIE_COST = 1.0 + TIE_EPS*(_a1+_a2)/2.0 + TIE_EPS2*_gen` where `_a1,_a2` are normalised `NODEW` (node wealth) for the two endpoints and `_gen=frac(min*max*7919)` — exact match to the stated formula, symmetric in u/v by construction. |
| Y977 | Near-unit costs are used because unit costs give a non-unique, order-dependent optimum; the perturbation leaves one optimum to return | PARTIAL | derivation check + file read (spec §2.3, §2.8) | The general mechanism is sound and matches code (degenerate unit-cost LP; TIE_COST breaks most ties). But the doc's own §2.3 states the full two-term cost still leaves "1 arc on 1 good" with a zero reduced cost off-support, and §2.8's Determinism row calls this "a genuine tie the tie-break did not reach and is the state `paper` is in today" — so the perturbation does not leave one optimum in every case, only in 28 of 29 goods (the residual tie is harmless to the emitted orientation because the tied arc carries no flow in any optimum, but the sentence as stated is unqualified). |
| Y233 | Support is a spanning-tree basis of ≤N-1 edges for a basic (vertex) optimum, which simplex gives | CONFIRMED | derivation check + measurement (f.out V186) | Standard LP flow theory: a basic feasible solution of an N-node balanced transportation/flow LP has at most N-1 basic (non-zero) arcs, i.e. a spanning forest. f.out: "V186 support sizes: min 78 max 79 (N-1 = 79)" — `final.py`'s `supp_sizes.append(len(r["flow_arc"]))`, confirming the measured support never exceeds N-1=79. |
| Y234 | Interior-point without crossover can split flow across parallel paths, returning an undirected cycle in the support | CONFIRMED | derivation check | Standard optimization fact: interior-point methods without crossover converge to a point in the relative interior of the optimal face rather than a vertex, i.e. a convex combination of multiple basic optima, which can assign positive flow to two parallel/opposing paths between the same endpoints, producing an undirected cycle in the support. |
| Y235 | §2.2 therefore requires network simplex or a simplex LP | CONFIRMED | file read (spec §2.2) | §2.2 item 5: "DRAIN per good: min-cost b-flow — **network simplex or a simplex LP, not interior-point without crossover** — because §1.1's spanning-tree-basis property requires a basic optimum". |
| Y1004 | §2.3 additionally requires the solver's optimality tolerance tighter than the tie-break's margin; both are correctness requirements, not settings | CONFIRMED | file read (spec §2.3) | §2.3: "**The solver's optimality tolerance is a correctness requirement, not a performance knob.**" together with the measured margin (3.8e-8) vs HiGHS's default (1e-7) and the pinned `LP_OPTS` (1e-10). |
| Y236 | Support has no directed cycle for any optimum, because strictly positive costs let a cycle be cancelled for lower cost — needs positivity not unit costs | CONFIRMED | derivation check + file read (flowop.py TIE_COST) | Standard min-cost-flow argument: a directed cycle carrying positive flow could be cancelled without violating conservation, strictly lowering total cost whenever every arc on it has positive cost, contradicting optimality; this only needs cost>0. `TIE_COST = 1.0 + TIE_EPS*(...) + TIE_EPS2*(...)` with both extra terms >=0, so `TIE_COST>=1.0` always, strictly positive. |
| Y237 | Edges with zero net flow are free and deferred to Phase 3 | CONFIRMED | file read (drain.py phase2) | `phase2()`: `elif phi < -ZERO_TOL: ... else: free.append(ei)`; `free` is passed into `phase3`/`sweep`/`sweep_priority`. |
| Y238 | Phase 3 marks Kahn-style: ready when every flow out-neighbour marked and (selected sink, has a flow out-arc, or a free edge to a marked node) | CONFIRMED | file read (drain.py sweep/sweep_priority) | `cnt[u]` tracks unmarked flow out-neighbours, decremented via `ins[cand]` when an out-neighbour is marked; readiness test `(u in Sset) or (len(outs[u])>0) or any(w in marked for w in freeadj[u])` gated on `cnt[u]==0` — exact match. |
| Y239 | Sweep pops ready nodes by (DEF ascending, b ascending, index); DEF(v) is total downstream demand on the flow-arc subgraph | CONFIRMED | file read (drain.py sweep_priority, flow_def, run_drain) | `run_drain`'s deterministic path calls `sweep_priority(..., "defasc_beta")`, whose `keyfn = lambda v: (DEF[v], beta[v], pid[v])` is exactly (DEF asc, b asc, index). `flow_def()`: `DEF[v]=max(0,-beta[v])+sum(DEF[u] for u in outs[v])` on the flow-arc subgraph. |
| Y240 | Flow-arc subgraph is acyclic and fixed before any free edge, so DEF has no circularity | CONFIRMED | derivation check | `flow_arc` is entirely determined in Phase 2, before Phase 3 orients any free edge; it is acyclic by Y236's argument, so `flow_def`'s Kahn-style topological pass over it always completes and `DEF` is well-defined with no circular dependency. |
| Y241 | On a stall, promote the heaviest flow-terminal demander among candidates; this self-correction supplies the real sink count | CONFIRMED | file read (drain.py sweep, sweep_priority) | `terminals=[u for u in gated if len(outs[u])==0 and inflow[u]>ZERO_TOL]; if terminals: s_star=min(terminals,key=lambda v:(beta[v],v))` — most negative beta = heaviest demander, promoted into `Sset`. |
| Y242 | If no flow-terminal demander among candidates, fallback promotes highest-wealth candidate, ties by index | CONFIRMED | file read (drain.py sweep, sweep_priority) | `else: s_star=max(gated,key=lambda v:(NODEW[v],-v))` — max NODEW wins; ties resolved by `-v`, i.e. lowest index. |
| Y243 | Node wealth is good-independent, so the fallback branch needs no bootstrap | CONFIRMED | file read (drain.py lines 21-27) | `NODEW` is built once at module load from `ROWS` (`tax+prod_income`), independent of any per-good balance, and reused unchanged across all 29 goods' `run_drain` calls. |
| Y244 | Candidates at a stall are unmarked nodes whose flow out-neighbours are all marked; at least one always exists (flow subgraph acyclic) | CONFIRMED | derivation check + file read (drain.py sweep) | `gated=[u for u in core if u not in marked and cnt[u]==0]`; code raises `RuntimeError("no gated unmarked node: flow support has a cycle")` if `gated` is ever empty. Any finite DAG restricted to its unmarked induced subgraph always has at least one node with no unmarked out-neighbour, so with an acyclic flow-arc subgraph (Y236) this never fires — consistent with p6.out/f.out reporting acyclic 29/29 and no errors. |
| Y245 | A candidate with a flow out-arc is already ready; a candidate with inflow is a flow-terminal demander | CONFIRMED | derivation check | A stall (`cand is None`) only occurs after the scan finds no gated node satisfying `len(outs[u])>0` among candidates, so by construction every genuine stall candidate has `len(outs[u])==0`; a candidate with positive `inflow` and no out-arc is, by `terminals`'s own definition, a flow-terminal demander. |
| Y019 | Fallback fires only when every candidate is support-isolated with zero post-peel balance | CONFIRMED | derivation check | At a stall every candidate has `len(outs[u])==0` (no flow out-arc, from Y245) and, when fallback fires, `inflow[u]<=ZERO_TOL` (no flow-terminal demanders). By Phase 2's flow-conservation identity (`outflow[v]-inflow[v]=beta[v]`, since `mincost_flow` imposes `inflow-outflow=-beta`), zero flow both ways forces `beta[v]=0` exactly — i.e. support-isolated implies zero post-peel balance, matching the claim. |
| Y020 | Priority key reads Phase 0's folded balance, not raw b, so non-zero raw balances can still reach the fallback | CONFIRMED | file read (drain.py phase0, run_drain) | `core, beta, Plog = phase0(b)` returns the folded `beta` (pendant balances summed into parents via `beta[u]+=beta[v]`), and this `beta` — not the original `b` — is what `phase1`/`phase2`/`sweep_priority`'s key read throughout `run_drain`. |
| Y021 | Fallback needs folded balance to vanish across the connected core: per-good, no producer/consumer; aggregate, each node's Σwealth^α_Φ equal | CONFIRMED | derivation check + file read (§3.2 T3, solver.py build_sc) | §3.2's T3 example is exactly a component with `b=0` at all three nodes (no producer, no consumer). `solver.py.build_sc`: `c(n,g)=Σ_{p in n} wealth(p)^a(g) / Σ_world wealth^a(g)`, so for the aggregate graph's `beta_w` to vanish everywhere (given uniform `s_w=1/N`), every node's `Σ_{p in n} wealth(p)^α_Φ` must be equal — the exact condition the claim states. |
| Y022 | Nodes hold between 0 and 72 counted provinces, so equal per-province wealth makes unequal node sums | CONFIRMED | measurement (ran a script against ROWS/ORDER) | Counting `ROWS` (owned, trade-node-mapped provinces) per node: min=0 at `cape_of_good_hope`, max=72 at `mexico`. |
| Y023 | Where the wealth key ties, node index decides | CONFIRMED | file read (drain.py sweep, sweep_priority) | `s_star = max(gated, key=lambda v: (NODEW[v], -v))` — the `-v` secondary key breaks NODEW ties by index. |
| Y024 | §2.8's containment set includes fallbacks because of T3, not the wealth tie; not the reason §2.4 needs canonical node order (a stronger, Phase-2-set requirement) | CONFIRMED | file read (spec §2.8, §3.2) | §3.2 item 1: "written against the narrower containment set, T3 would halt the solver on correct behaviour" (i.e. fallbacks are in the containment set because of T3). §3.2 item 2: "none of them is why §2.4 requires a canonical node order — that requirement came from Phase 2's LP under unit costs, which moved the orientation under relabelling even when no key tie exists anywhere (§2.4 item 1)." |
| Y246 | Free edges orient from later-marked to earlier-marked | CONFIRMED | file read (drain.py compile_dirs) | `directed.append((u,v) if order[u]>order[v] else (v,u))` — higher `order` = later marked, oriented toward the earlier-marked endpoint. |
| Y247 | Phase 4 un-peels the Phase-0 pendants in reverse order | CONFIRMED | file read (drain.py compile_dirs/run_drain) | `for (v,u,bv) in reversed(Plog): directed.append(...)` — `Plog` is built in peel order (leaves first), so `reversed(Plog)` un-peels last-peeled-first. |
| Y1078 | Phase 4 emits the orientations Phase 0 determined; Phase 0 decides, Phase 4 is where they enter the graph | CONFIRMED | file read (drain.py phase0, compile_dirs) | The direction (sign test on `bv`) is fixed at peel time inside `Plog`; `compile_dirs`'s `reversed(Plog)` loop only re-emits that stored sign, computing nothing new. |
| Y1079 | A pendant sink is visible only after Phase 4 (T1, §3.2) | CONFIRMED | file read (spec §3.2 T1) | T1: "Phase 0 peels C, Phase 4 restores the edge B→C, and the actual sinks are {C}" — C is outside the core graph phases 1-3 operate on, so it cannot appear as a sink until Phase 4 restores it. |
| Y248 | Each §1.1 property labelled proved, measured, or true-by-construction; never allowed to stand for each other | CONFIRMED | file read (spec §1.1 property bullets) | Global DAG carries a proof plus "Measured: acyclic 29/29"; Sink placement is explicitly "a measurement on this input, not a theorem"; Reachability is "a feasibility theorem" plus a 1444 measurement; Scan-invariance splits "deterministic by construction" (proved) from "measured, not proved" (index-independence); Efficiency: "carries no measurement and wants none... follows from the construction". |
| Y250 | §1.1 property measurements were regenerated for v6.0 by measure6.py | PARTIAL | file read (spec §1.1 preamble vs later parenthetical; m6.out, p6.out; grep of measure6.py) | The blanket preamble sentence ("measured where measurement applies... regenerated for v6.0 by `measure6.py`") holds only for a subset: Global DAG's acyclic count and Sink-placement's sink/fallback counts are independently reproduced in m6.out ("acyclic goods 29", "sinks per good min/max/mean (2, 8, 3.69)", "fallbacks fired across goods 0"). But the Reachability bullet's 100%/orphan-sink figures and the entire Scan-invariance bullet (core-node count, 145 permutations, 2,320-node key collisions, Phase-1 tie counts) are computed only by `props6.py` — the doc's own line-217 parenthetical says so, and `grep` of `measure6.py` finds none of those computations. |
| Y249 | Labelling discipline caught four over-claims between v2.0 and v3.0 | CONFIRMED | file read (v3-owner-agnostic/changes-v3.md) | Four §1.1-scoped narrowings/removals match: sink-set equality demoted from theorem to measurement (section "V029 / V125 / V126 — the sink identity is not a theorem"); reachability narrowed from "a feasibility theorem" to "on a connected map" (table row V031); spanning-tree-basis narrowed to "a **basic** optimum" (table row V016); efficiency narrowed from "fewest-hop routing" to "fewest-hop **in aggregate**" (table row V036). |
| Y251 | Global DAG: every arc later-to-earlier marked, reversed order is topological, pendant edges (bridges) cannot close a cycle | CONFIRMED | derivation check + file read (drain.py sweep, compile_dirs) | Flow arcs only become ready once their flow out-neighbour is marked, so `order[out-neighbour] < order[u]` always; free edges are oriented later-to-earlier by construction (Y246) — both arc kinds go from later-marked to earlier-marked, making reversed marking order a valid topological order of the core; pendant edges are bridges (Y225) and so cannot participate in any cycle. |
| Y252 | Measured acyclic on 29 of 29 goods | CONFIRMED | measurement (p6.out) | p6.out: "acyclic goods: 29 of 29". |
| Y253 | Every sink is one of four kinds: selected-flow-terminal, stall-promoted, fallback-promoted, or Phase-0 pendant net-importer | CONFIRMED | file read (spec §3.2) | §3.2: "every core node that is neither selected, promoted nor fallback-promoted is given an out-arc by the sweep... Pendant net-importers are the only sinks outside that set" — exactly the four kinds listed. |
| Y025 | On 1444, fallback/pendant cases empty; sink set = {selected∩flow-terminal}∪{promoted} — 29/29 goods, 2-8/good, mean 3.69, zero fallbacks | CONFIRMED | measurement (p6.out) | p6.out: "sinks per good min/max/mean: 2/8/3.69", "fallbacks fired: 0", "sink-set identity holds on 29 of 29 goods; failures []"; Phase 0 no-op already confirmed (bridges=0) so the pendant case is empty too. |
| Y026 | Equality is a measurement on this input, not a theorem; v2 asserted it as one | CONFIRMED | file read (v2-drain/per-good-trade-spec.md) | v2's spec states unconditionally: "the final sinks are exactly `{selected ∩ flow-terminal} ∪ {stall-promoted flow-terminal demanders}`" and separately "Nothing else can be a sink (every other node is given an out-arc by the sweep)" — no measurement hedge, unlike v6's explicit "measured exact... not a theorem". |
| Y027 | Attaching "Phase 0 no-op and no fallback firing" doesn't rescue the theorem; T2 satisfies both and still breaks it | CONFIRMED | file read (spec §3.2 T2) | T2's graph is a 5-cycle plus a chord (min degree >=2, no Phase-0 action) and involves no fallback (both u1, u2 are selected flow-terminal demanders) — it satisfies both proposed conditions yet still breaks the equality (`actual {u2}` vs `formula {u1,u2}`). |
| Y254 | Three constructed cases (T1 pendant leaf, T2 free-edge race, T3 fallback promotion) break the sink-set equality | CONFIRMED | file read (spec §3.2) | §3.2 gives T1 (triangle+leaf C, Phase-0 pendant sink outside the set), T2 (5-cycle+chord, free edge strips u1's sinkhood), T3 (triangle all b=0, fallback promotes A, sink in neither selected nor promoted) — matching the claim's descriptions exactly. |
| Y255 | A node with no outgoing links for g is a sink for g; sinks differ per good; no global end node | CONFIRMED | file read (drain.py sinks_of) + observation | `sinks_of()`: out-degree 0 defines a sink, definitionally per-good since each good gets its own `directed` list from a distinct `b_g`; measured sink lists differ across goods (e.g. m6.out's Φ_w sinks `['genua','hangzhou']` vs f.out/§2.8's per-good spices/cloves sink sets), so no single node is universal. |
| Y256 | Orientation serves 100% of demand because LP imposes node balance and Σb_g=0 identically (s, c are world shares) | CONFIRMED | derivation check + file read (solver.py build_sc) | `build_sc` normalises `S[live]` and (per-good) `C[gi]` to sum to 1 over live/participating nodes, so `Σ(S_g-C_g)=1-1=0` identically for every good; `mincost_flow`'s equality constraints (`AEQ`, conservation) require exactly this to admit a feasible flow. |
| Y257 | LP feasibility premise is connectedness; disconnected map needs per-component balance; cross-component imbalance is infeasible outright | CONFIRMED | file read (spec §2.2a) | §2.2a: "each component must balance separately, and share normalisation does not deliver that. A two-component graph carrying cross-component imbalance is **infeasible outright** — the solver returns no flow at all, not a worse one." |
| Y258 | §2.2a states the connectedness requirement and what the solver does when violated | CONFIRMED | file read (spec §2.2a, doc line 205) | Doc line 205: "§2.2a states the connectedness requirement and what the solver does when it is violated." §2.2a itself: "compute components once at load. On a single component, proceed. On more than one, either renormalise `s` and `c` within each component... or refuse to start." |
| Y259 | Measured on 1444 (one component): 100.0% demand reachable, 29/29 goods, zero orphan sinks | CONFIRMED | measurement (p6.out) | p6.out: "components=1", "demand reachable: min 100.0000% max 100.0000% ; orphan sinks 0". |
| Y260 | Ready-marking is a monotone closure; stall sequence and both promotion branches are provably scheduling-independent | CONFIRMED | derivation check | `cnt[u]` only decreases as more nodes get marked, and the OR-conditions of `ready()` (selected, has a flow out-arc, marked free-neighbour) are each monotone in the marked set — once true, they stay true. A monotone-closure process reaches the same fixed point (same eventual marked/candidate set at any stall) regardless of the order independent ready nodes are processed in. |
| Y261 | Free-edge direction is deterministic by the same closure argument plus the priority key's index tiebreak | CONFIRMED | derivation check + file read (drain.py sweep_priority) | The closure fixes which nodes become ready together as a wave, but not their relative order within a simultaneous wave; `keyfn=(DEF asc, beta asc, pid)` supplies a strict total order (index as final tiebreak) that pins exact marking order within any wave, which the later-to-earlier free-edge rule (Y246) then reads deterministically. |
| Y262 | "Zero orientation changes over 145 scheduler permutations (29 goods x 5)", cited to props6.py | CONFIRMED | measurement (p6.out) | p6.out: "scheduler permutations: 0 orientation changes over 145 trials (29 goods x 5)". |
| Y1005 | Zero exact (DEF,b) key collisions across all 2,320 core nodes — not merely on free edges, which is where earlier versions measured it | CONFIRMED | measurement (p6.out) + file read (v4/v5 measure scripts, final.py) | p6.out: "core nodes summed over goods: 2320 ; exact (DEF,b) key collisions: 0". `grep` of `v4-owner-agnostic/scripts` and `v5-owner-agnostic/scripts` shows `v4measure.py`/`v5measure.py` both print only `"exact (DEF, b) ties on free edges"`; the current tree's `final.py` (f.out) still reports the narrower figure separately: "exact (DEF,b) ties on free edges: 0". |
| Y1006 | Phase 1's within-cluster argmin and top-k cut are untied on the same field, so no index tiebreak in the algorithm fires at all | CONFIRMED | measurement (p6.out) + derivation | p6.out: "Phase-1 within-cluster argmin ties: 0 ; top-k cut ties: 0", "fallbacks fired: 0". For flow-terminal nodes `DEF[v]=max(0,-beta[v])` is a pure function of `beta[v]`, so the measured "0 (DEF,b) collisions across all 2,320 core nodes" also rules out ties in the stall-promotion's `(beta[v],v)` key; with fallback never reached, none of the algorithm's four index-tiebreak sites resolves an actual tie on 1444. |
| Y1081 | props6.py was renamed from a round-5 working file; its permutation loop was written for this citation | PARTIAL | file read (props6.py docstring/comments; v5-owner-agnostic/scripts; no VCS available) | `props6.py`'s own docstring: "Per-good structural battery for **the round-5 audit**", and its in-file comment: "the scheduler-permutation figure section 1.1 quotes lives nowhere else in the tree. It was run once in a throwaway script during the round-5 audit and then cited... Measured here so the citation is true because the harness re-derived it." No prior script computing a 5-permutation/145-trial figure exists anywhere in the tree — `v4measure.py`/`v5measure.py`/`final.py` all instead run only 2 index permutations (`"orientation flips, 2 index permutations x 29 goods"`), corroborating that the 145-trial loop is new. The specific "renamed from" (a literal file rename) cannot be verified: there is no git repository or other version history in this workspace, and no file under a different name matching props6.py's content was found in `v5-owner-agnostic/scripts/`. |
| Y263 | Certificate flow is near-fewest-hop in aggregate: unit-cost objective = Σ(flow×hops); tie-break makes it Σ(flow×cost), cost∈[1,1+TIE_EPS+TIE_EPS2] | CONFIRMED | derivation check + file read (flowop.py TIE_COST, TIE_EPS/TIE_EPS2 values) | `TIE_COST=1.0+TIE_EPS*(w[u]+w[v])/2+TIE_EPS2*frac(...)` with `w` normalised to [0,1] and `frac(...)` in [0,1): the first term ranges over [0,TIE_EPS], the second over [0,TIE_EPS2), so total cost lies in [1, 1+TIE_EPS+TIE_EPS2) — matching the stated (closed) bound; `TIE_EPS=1e-3`, `TIE_EPS2=1e-6` confirmed from `flowop.py` lines 18-19. |
| Y978 | Bound is the interval [1,1+TIE_EPS+TIE_EPS2] itself; no percentage derived, since the spread restates TIE_EPS | CONFIRMED | file read (spec §1.1 Efficiency bullet) | "The bound is that interval itself, which is what an implementer needs; no percentage is derived from it, because the spread relative to the base cost is a restatement of `TIE_EPS` and not a second fact." |
| Y264 | No per-unit shortest-path claim is made and none holds: a unit may detour when sink assignment demands it | CONFIRMED | derivation check | Min-cost flow minimises total cost subject to conservation and the sink set fixed by Phases 1/3; it gives no guarantee that any individual unit travels the graph-shortest path to its assigned sink, since routing must still terminate at the assigned sinks even when a cheaper path would end elsewhere — standard multi-commodity/assigned-terminal flow behaviour. |
| Y265 | Efficiency carries no measurement and wants none: follows from the LP construction; any hop count would re-derive the objective | CONFIRMED | file read (spec §1.1 Efficiency bullet) | "This one carries no measurement and wants none: it follows from the construction of the LP, and any hop count we produced would be re-deriving the objective rather than testing it." |
| Y266 | §3.13 calibration deliberately degrades efficiency, which changes the program being solved rather than being evidence about the property | CONFIRMED | file read (spec §3.13) | §3.13's calibration bullet: "the twig tolerance re-routes arcs carrying a small fraction of a good's mass, and it costs one good full reach" — a documented, deliberate departure from the true LP optimum (pruning small-mass arcs), consistent with §1.1's framing that this is a change to the solved program, not new evidence about baseline efficiency. |
| Y267 | Orientation recomputed on a fixed monthly tick, aligned to the vanilla trade tick | CONFIRMED | file read (spec §2.1, §2.6) | §2.1: "a runtime-attached DLL that each month reads live game state, solves per good". §2.6: "The monthly trade tick runs in three passes..." — the document consistently treats vanilla's own trade tick as monthly and states this mod's solve is aligned to it. |
| Y268 | Orientation is read from the current solve every time, with no memory of the previous one | CONFIRMED | file read (drain.py run_drain) | `run_drain(b, ...)` is a pure function of its `b` argument and the fixed graph (`solver.py`'s `UND`/`EDGES_UND`); no module-level mutable state carries an orientation between calls. |
| Y269 | LP deterministic on one machine/build: six identical solves gave one orientation on the reference implementation | CONFIRMED | measurement (f.out) | f.out: "V037 six identical solves -> one orientation (spices): True" (`final.py`'s `for it in range(6): ...`). |
| Y1082 | Six identical solves are within a single process, blind to cross-process variation; fingerprint6.py covers that separately (§2.1) | CONFIRMED | file read (fingerprint6.py docstring, spec §2.8) | `fingerprint6.py` docstring: "One run hashes ONE process. Repeat under different `PYTHONHASHSEED` values to test what a single process cannot see -- iteration order that varies between processes rather than within one." §2.8's Determinism row: "one fingerprint... was identical across repeated runs, separate processes, and five `PYTHONHASHSEED` values including `random` (`fingerprint6.py`)... it is the separate-process runs above that cover iteration order varying between processes." |
