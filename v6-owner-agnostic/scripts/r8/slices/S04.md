# Slice S04  (37 claims)


## §1.1

- **Y023** [UNCHANGED, type MODEL, spec line 178]
  CLAIM: Where the wealth key ties, the node index decides.

- **Y024** [UNCHANGED, type DESIGN, spec line 179]
  CLAIM: §2.8's containment set includes the fallbacks because of T3 — a fallback promotion that is a sink in neither …

- **Y246** [UNCHANGED, type MODEL, spec line 182]
  CLAIM: Free edges orient from later-marked to earlier-marked.

- **Y247** [UNCHANGED, type MODEL, spec line 185]
  CLAIM: Phase 4 un-peels the Phase-0 pendants in reverse order.

- **Y1078** [UNCHANGED, type MODEL, spec line 185]
  CLAIM: Phase 4 emits the orientations Phase 0 determined for the pendants: Phase 0 decides those directions, Phase 4 is where …

- **Y1079** [UNCHANGED, type MODEL, spec line 186]
  CLAIM: A pendant sink is visible only after Phase 4 (T1, §3.2).

- **Y248** [UNCHANGED, type DESIGN, spec line 189]
  CLAIM: Each §1.1 property is labelled proved, measured, or true-by-construction, and the three are never allowed to …

- **Y249** [UNCHANGED, type MODEL, spec line 193]
  CLAIM: That labelling discipline caught four over-claims between v2.0 and v3.0.

- **Y251** [UNCHANGED, type MODEL, spec line 196]
  CLAIM: Global DAG: every arc points from later-marked to earlier-marked, so reversed marking order is a topological …

- **Y252** [UNCHANGED, type MEASURED, spec line 197]
  CLAIM: Measured acyclic on 29 of 29 goods.

- **Y253** [UNCHANGED, type MODEL, spec line 199]
  CLAIM: Every sink is one of four kinds: a selected demand centre that turned out flow-terminal, a stall-promoted …

- **Y025** [UNCHANGED, type MEASURED, spec line 201]
  CLAIM: On 1444 the fallback and pendant cases are empty and the sink set is exactly `{selected ∩ flow-terminal} ∪ …

- **Y026** [UNCHANGED, type MODEL, spec line 203]
  CLAIM: That equality is a measurement on this input rather than a theorem, and v2 asserted it as one.

- **Y027** [UNCHANGED, type MODEL, spec line 205]
  CLAIM: It does not become a theorem by attaching conditions: "Phase 0 a no-op and no fallback firing" looks …

- **Y254** [UNCHANGED, type MEASURED, spec line 206]
  CLAIM: Three constructed cases break the sink-set equality: a pendant net-importing leaf is a sink outside the set …

- **Y255** [UNCHANGED, type MODEL, spec line 209]
  CLAIM: A node with no outgoing links for `g` is a sink for `g`; sinks differ per good; there is no global end node.

- **Y256** [UNCHANGED, type MODEL, spec line 211]
  CLAIM: The orientation contains a flow serving 100% of every good's demand, because the LP imposes node balance and …

- **Y257** [UNCHANGED, type MODEL, spec line 213]
  CLAIM: The premise that makes the LP feasible is connectedness: on a disconnected map the balance must hold per …

- **Y258** [UNCHANGED, type MODEL, spec line 216]
  CLAIM: §2.2 states the connectedness requirement and what the solver does when it is violated.

- **Y259** [UNCHANGED, type MEASURED, spec line 217]
  CLAIM: Measured on 1444, which is one component: 100.0% of demand reachable from supply, 29/29 goods, zero orphan …

- **Y260** [UNCHANGED, type MODEL, spec line 219]
  CLAIM: Ready-marking is a monotone closure, so the stall sequence and both promotion branches are provably …

- **Y261** [UNCHANGED, type MODEL, spec line 219]
  CLAIM: Free-edge direction is deterministic, by the same closure argument plus the priority key's index tiebreak.

- **Y262** [UNCHANGED, type MEASURED, spec line 223]
  CLAIM: "zero orientation changes over 145 scheduler permutations (29 goods × 5)", cited to `props6.py`

- **Y1005** [UNCHANGED, type MEASURED, spec line 224]
  CLAIM: Measured: zero exact `(DEF, b)` key collisions across all 2,320 core nodes of the 29 per-good solves — not …

- **Y1006** [UNCHANGED, type MEASURED, spec line 226]
  CLAIM: Phase 1's within-cluster argmin and its top-k cluster cut are untied on the same field, so no index tiebreak …

- **Y1080** [UNCHANGED, type PROCESS, spec line 228]
  CLAIM: The determinism bullet’s figures are all `props6.py`’s — the core-node count included.

- **Y263** [UNCHANGED, type MODEL, spec line 233]
  CLAIM: The certificate flow is a near-fewest-hop routing in aggregate: with unit costs the objective would be …

- **Y978** [UNCHANGED, type MODEL, spec line 236]
  CLAIM: the bound is the interval `[1, 1 + TIE_EPS + TIE_EPS2]` itself; no percentage is derived, because the spread relative …

- **Y264** [UNCHANGED, type MODEL, spec line 240]
  CLAIM: No per-unit shortest-path claim is made and none holds, because a unit may detour when sink assignment …

- **Y265** [UNCHANGED, type DESIGN, spec line 243]
  CLAIM: The efficiency property carries no measurement and wants none: it follows from the construction of the LP, …

- **Y266** [UNCHANGED, type DESIGN, spec line 244]
  CLAIM: The §3.13 calibration deliberately degrades efficiency, which is a change to the program being solved rather …

- **Y267** [UNCHANGED, type MODEL, spec line 248]
  CLAIM: The orientation is recomputed on a fixed monthly tick, aligned to the vanilla trade tick.

- **Y268** [UNCHANGED, type MODEL, spec line 248]
  CLAIM: Orientation is read from the current solve every time, with no memory of the previous one.

- **Y269** [UNCHANGED, type MEASURED, spec line 248]
  CLAIM: The LP is deterministic on one machine and one build — six identical solves gave one orientation on the reference impl …

- **Y1082** [UNCHANGED, type MODEL, spec line 250]
  CLAIM: The six identical solves are six solves inside a single process, blind to anything that varies between processes; `fin …

- **Y270** [UNCHANGED, type DESIGN, spec line 253]
  CLAIM: What remains open across machines is §3.13’s build-discipline question (§2.1), not LP determinism, which §2.1 retires.

- **Y1195** [NEW, type PROCESS, spec line 230]  provenance: `diff` of `props6.py` against `val5_pergood.py`
  CLAIM: `val5_pergood.py` is still on disk, and `diff` shows `props6.py` is that file verbatim — zero deletions — plus 27 added lines carrying the permutation loop.
