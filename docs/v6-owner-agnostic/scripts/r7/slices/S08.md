
### Section 2.3 — 49 claims
---
**Y137** | §2.3 | status=CHANGED | type=PROCESS | provenance=the prior spec versions; `audit_modifiers.py` | doc line 1082
CLAIM: v3.0 through v5.0 said neither wealth coefficient was in a file, and a shipped modifier sweep walked past the block holding one of them. || DELTA: old: they "**shipped a whole-install modifier sweep** that walked past the block" — attributed to all three → new: "**v4.0's tree ships the one sweep script, `audit_modifiers.py`**, which walked past the block …, and **v3.0 and v5.0 repeated the claim without shipping a sweep**"
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): v3.0 through v5.0 said neither coefficient was in a file, and shipped a whole-install modifier sweep that … ~ PROCESS ~ 1059 || v3.0 through v5.0 said neither coefficient was in a file, and shipped a whole-install modifier sweep that walked past the block holding one of them. ~ NEW ~ MODEL ~ read from a file (the prior spec versions and their sweep) ~ L1006-1008
---
**Y1032** | §2.3 | status=CHANGED | type=MEASURED | provenance=numerical test | doc line 1159
CLAIM: Over six permutations of the LP's column order, `copper` and `paper` returned differing orientations, with objectives differing by 7.7e-10 relative — unequal-quality answers rather than tied optima. || DELTA: old: "orientations **differing on 12 and 8 edge-slots**" → new: "**4 and 8 orientation flips summed over the six permutations** — copper's on four distinct edge-slots, paper's on two, flipped in four of the six". The 7.7e-10 and the conclusion are unchanged
---
**Y1055** | §2.3 | status=CHANGED | type=MEASURED | provenance=`round6.py` | doc line 1165
CLAIM: Leaving the solver tolerance unset and setting it to 1e-7 agree, which is what pins the effective default independently of the documentation. || DELTA: old: they "give **the same 8 flips over four permutations**" → new: they "give **identical results at every seed tried**", with **no single count quoted**; the agreement, not a flip count, is what pins the default
---
**Y1056** | §2.3 | status=CHANGED | type=MEASURED | provenance=`round6.py` | doc line 1167
CLAIM: 1e-8 gives 0 flips, and is the first value below `copper`'s 3.765e-8 margin. || DELTA: old: "**1e-8 already gives 0**" — one run → new: "**1e-8 gives 0 at every seed**"; the quantifier over seeds is new. The 3.765e-8 margin clause is unchanged
---
**Y1035** | §2.3 | status=CHANGED | type=MEASURED | provenance=numerical test | doc line 1174
CLAIM: What the second-order term costs: self-coherence with the per-good graphs falls and nothing else measured moves. || DELTA: old: falls "**0.1–0.2 points**" → new: falls "**by at most a tenth of a point (0.04 edge-goods, 0.10 value-weighted)**". The "nothing else moves" tail — 2–8 sinks mean 3.69, all 29 acyclic, `Φ_w` unchanged, 0 edges on six noise seeds — is unchanged
---
**Y1037** | §2.3 | status=CHANGED | type=MODEL | provenance=algebraic derivation | doc line 1186
CLAIM: The normalisation of `w` is load-bearing per good and that is a cost of the second-order term; for the first-order term alone it was not. || DELTA: old: not load-bearing **because a sweep found it so** — "the answer is constant over about six orders of magnitude of that (§1.6), so dividing by the maximum, the mean or the world total gave the same orientation" → new: not load-bearing **by algebra rather than by sweep** — `w` enters only through `TIE_EPS · (w[u] + w[v]) / 2` — and the normalisation is recast as a **strength choice**
---
**Y470** | §2.3 | status=UNCHANGED | type=DESIGN | provenance=(carried) | doc line 1062
CLAIM: Constants are read at runtime and never hardcoded.
---
**Y471** | §2.3 | status=UNCHANGED | type=ENGINE | provenance=(carried) | doc line 1064
CLAIM: The nine runtime-read uses map to named defines: `TRADE_PROPAGATE_DIVIDER`, `TRADE_PROPAGATE_THRESHOLD`, …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): The nine runtime-read uses map to named defines: `TRADE_PROPAGATE_DIVIDER`, `TRADE_PROPAGATE_THRESHOLD`, … ~ ENGINE ~ 1041 || The nine runtime-read uses map to named defines: `TRADE_PROPAGATE_DIVIDER`, `TRADE_PROPAGATE_THRESHOLD`, `TRADE_NON_CAPITAL_OFFICE`, `TRADE_POWER_HOME_BONUS`, `MERCHANT_MAX_POWER_BONUS`, `TRADE_MERCHANT_PRESENT`, `TRADE_ADDED_VALUE_MODIFER`, the three caravan terms, and `PS_MOVE_TRADE_PORT`. ~ UNCHANGED ~ INSTALL ~ read from a file ~ L988-998
---
**Y472** | §2.3 | status=UNCHANGED | type=ENGINE | provenance=(carried) | doc line 1071
CLAIM: `TRADE_MERCHANT_PRESENT` is a bonus on income, not trade efficiency.
---
**Y136** | §2.3 | status=UNCHANGED | type=DESIGN | provenance=(carried) | doc line 1076
CLAIM: The two wealth coefficients are not the same kind of constant: the emitter reads `GP_COEFF` rather than …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): The two wealth coefficients are not the same kind of constant: the emitter reads `GP_COEFF` rather than … ~ DESIGN ~ 1053 || The two wealth coefficients are not the same kind of constant: the emitter reads `GP_COEFF` rather than carrying 0.2 because a mod or a patch can change it, and only `TAX_COEFF` must be re-measured against any patch that is not 1.37.5. ~ CHANGED ~ DESIGN ~ stipulated ~ L1000-1006
---
**Y473** | §2.3 | status=UNCHANGED | type=ENGINE | provenance=(carried) | doc line 1089
CLAIM: `GP_COEFF` is 0.2 goods produced per point of `base_production`, measured on four provinces at four …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): `GP_COEFF` is 0.2 goods produced per point of `base_production`, measured on four provinces at four … ~ ENGINE ~ 1065 || `GP_COEFF` is 0.2 goods produced per point of `base_production`, measured on four provinces at four development levels from the `Base Goods Produced` line: Caceres (1747) at 2 gives 0.40, Girona (212) at 3 gives 0.60, Garnatah (223) at 4 gives 0.80 with the itemisation `Base Goods Produced: 0.80 / Base Production: +0.80`, and Barcelona (213) at 5 gives 1.00. ~ UNCHANGED ~ ENGINE ~ measured in-game ~ L1012
---
**Y474** | §2.3 | status=UNCHANGED | type=ENGINE | provenance=(carried) | doc line 1090
CLAIM: `TAX_COEFF` is 1.0 ducat per year per point of `base_tax`, measured on two provinces at two development …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): `TAX_COEFF` is 1.0 ducat per year per point of `base_tax`, measured on two provinces at two development … ~ ENGINE ~ 1066 || `TAX_COEFF` is 1.0 ducat per year per point of `base_tax`, measured on two provinces at two development levels from the `(Yearly …)` parenthetical, and the displayed monthly is the truncation of `base_tax × 0.083333`. ~ UNCHANGED ~ ENGINE ~ measured in-game ~ L1013
---
**Y475** | §2.3 | status=UNCHANGED | type=ENGINE | provenance=(carried) | doc line 1092
CLAIM: Both coefficients are read off the tooltips' base lines, which carry no owner term — Garnatah also has …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): Both coefficients are read off the tooltips' base lines, which carry no owner term — Garnatah also has … ~ ENGINE ~ 1068 || Both coefficients are read off the tooltips' base lines, which carry no owner term — Garnatah also has `local_autonomy = 0`. ~ UNCHANGED ~ ENGINE ~ measured in-game ~ L1015-1016
---
**Y476** | §2.3 | status=UNCHANGED | type=ENGINE | provenance=(carried) | doc line 1093
CLAIM: Neither coefficient is read off a province window, because a window figure carries the owner's modifiers and …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): Neither coefficient is read off a province window, because a window figure carries the owner's modifiers and … ~ ENGINE ~ 1069 || Neither coefficient is read off a province window, because a window figure carries the owner's modifiers and some of those are randomised at game start. ~ UNCHANGED ~ ENGINE ~ measured in-game ~ L1016-1017
---
**Y477** | §2.3 | status=UNCHANGED | type=ENGINE | provenance=(carried) | doc line 1094
CLAIM: Prices come from `common/prices/00_prices.txt` at runtime and are never hardcoded.
---
**Y478** | §2.3 | status=UNCHANGED | type=DESIGN | provenance=(carried) | doc line 1097
CLAIM: The design constants are the excluded-goods list (defaulting to gold), the α price anchor `P₀ = 2.0`, the …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): The design constants are the excluded-goods list (defaulting to gold), the α price anchor `P₀ = 2.0`, the … ~ DESIGN ~ 1073 || The design constants are the excluded-goods list (defaulting to gold), the α price anchor `P₀ = 2.0`, the aggregate-graph exponent `α_Φ = 2.0`, and the Phase-2 tie-break strengths `TIE_EPS = 1e-3` and `TIE_EPS2 = 1e-6`. ~ CHANGED ~ DESIGN ~ stipulated ~ L1020-1022
---
**Y480** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1100
CLAIM: two knobs — `r = 0` and the 1e-11 tolerance; `ρ` withdrawn as never shipped
---
**Y481** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1101
CLAIM: "an absolute threshold rather than a relative one; §3.13 records why that is settled rather than open"
---
**Y482** | §2.3 | status=UNCHANGED | type=DESIGN | provenance=(carried) | doc line 1104
CLAIM: the option "replaces Phase 1, moves the zero-flow tolerance and removes α's clamp"; still recorded in §3.13, still not …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): A measured calibration option that moves all three knobs plus α's clamp is recorded in §3.13, and the baseline does not use it. ~ old: a measured calibration option "moves all three [knobs] plus α's clamp" → new: the option "replaces Phase 1, moves the zero-flow tolerance and removes α's clamp"; still recorded in §3.13, still not used by the baseline ~ DESIGN ~ unsourced ~ 1080 || A measured calibration option that moves all three knobs plus α's clamp is recorded in §3.13, and the baseline does not use it. ~ UNCHANGED ~ DESIGN ~ stipulated ~ L1025-1026
---
**Y479** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1107
CLAIM: `α_Φ`, `TIE_EPS` and `TIE_EPS2` are hyperparameters whose values are developer taste, the document offers no …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): `α_Φ`, `TIE_EPS` and `TIE_EPS2` are hyperparameters whose values are developer taste, the document offers no … ~ MODEL ~ 1083 || `α_Φ`, `TIE_EPS` and `TIE_EPS2` are hyperparameters whose values are developer taste, the document offers no justification for any of them, and every derivation previously offered for `α_Φ` is withdrawn with none replacing it — v2.1 through v4.0's two-sink calibration and v5.0's widest-band argument are both withdrawn. ~ CHANGED ~ MODEL ~ stipulated ~ L1029-1032
---
**Y339** | §2.3 | status=UNCHANGED | type=DESIGN | provenance=(carried) | doc line 1110
CLAIM: Changing any of the three hyperparameters is a design decision, and §1.6 records how the field responds …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): Changing any of the three hyperparameters is a design decision, and §1.6 records how the field responds … ~ DESIGN ~ 1086 || Changing any of the three hyperparameters is a design decision, and §1.6 records how the field responds around them so the decision can be made with the sensitivity in view — documentation for whoever changes them rather than an argument for the current values. ~ CHANGED ~ DESIGN ~ stipulated ~ L1032-1035
---
**Y996** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1115
CLAIM: `TIE_EPS` and `TIE_EPS2` together set the Phase-2 objective, `cost(u,v) = 1 + TIE_EPS·(w[u]+w[v])/2 + …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): `TIE_EPS` and `TIE_EPS2` together set the Phase-2 objective, `cost(u,v) = 1 + TIE_EPS·(w[u]+w[v])/2 + … ~ MODEL ~ 1091 || `TIE_EPS` and `TIE_EPS2` together set the Phase-2 objective, `cost(u,v) = 1 + TIE_EPS·(w[u]+w[v])/2 + TIE_EPS2·frac(min(w[u],w[v])·max(w[u],w[v])·7919)`, with `w` node wealth normalised to [0, 1]; the cost's symmetry and its reading node wealth only are load-bearing — a directional preference is a potential difference whose total is `Σ_n w[n]·b[n]` for every feasible routing and so cannot break a tie, and a wealth-only cost is invariant under relabelling by construction. ~ NEW ~ MATH ~ algebraic derivation ~ L1037-1046, restated L1092-1096
---
**Y1026** | §2.3 | status=UNCHANGED | type=DESIGN | provenance=(carried) | doc line 1126
CLAIM: The two cost terms do different jobs and only the first means anything: the first-order term is the design …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): The two cost terms do different jobs and only the first means anything: the first-order term is the design … ~ DESIGN ~ 1102 || The two cost terms do different jobs and only the first means anything: the first-order term is the design statement that rich corridors cost more, so flow arriving at a wealthy node finds it dear to continue and tends to terminate — wealth as destination rather than thoroughfare. ~ NEW ~ DESIGN ~ stipulated ~ L1048-1050
---
**Y1027** | §2.3 | status=UNCHANGED | type=DESIGN | provenance=(carried) | doc line 1128
CLAIM: The second-order term is tie-breaking and nothing else; its form is arbitrary and no reading should be …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): The second-order term is tie-breaking and nothing else; its form is arbitrary and no reading should be … ~ DESIGN ~ 1104 || The second-order term is tie-breaking and nothing else; its form is arbitrary and no reading should be attached to it. ~ NEW ~ DESIGN ~ stipulated ~ L1050-1051
---
**Y998** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1131
CLAIM: A single cost vector does not make every solve unique, because uniqueness of an LP optimum depends on `b` as …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): A single cost vector does not make every solve unique, because uniqueness of an LP optimum depends on `b` as … ~ MODEL ~ 1107 || A single cost vector does not make every solve unique, because uniqueness of an LP optimum depends on `b` as well as on the objective: a non-tree arc has zero reduced cost exactly when its own cost equals the sum of costs along the tree path between its endpoints, and a different `b` builds a different tree and exposes different coincidences. ~ NEW ~ MATH ~ algebraic derivation ~ L1053-1056
---
**Y1028** | §2.3 | status=UNCHANGED | type=MEASURED | provenance=(carried) | doc line 1134
CLAIM: Measured on zero-reduced-cost arcs outside the support: the aggregate `b_w` goes from 40 under unit costs to …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): Measured on zero-reduced-cost arcs outside the support: the aggregate `b_w` goes from 40 under unit costs to … ~ MEASURED ~ 1110 || Measured on zero-reduced-cost arcs outside the support: the aggregate `b_w` goes from 40 under unit costs to 0 under the first-order term alone, while the 29 per-good `b_g` still carry 41 between them, on 18 of the 29 goods. ~ NEW ~ MODEL ~ numerical test ~ L1056-1058
---
**Y1029** | §2.3 | status=UNCHANGED | type=MEASURED | provenance=(carried) | doc line 1136
CLAIM: Adding the second-order term takes the zero-reduced-cost arcs to 1 arc on 1 good.
---
**Y999** | §2.3 | status=UNCHANGED | type=MEASURED | provenance=(carried) | doc line 1144
CLAIM: BOTH costs make all 159 edge costs distinct (the 3-pairs-equal claim gone); the structured term leaves 11 of 29 goods …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): A structured second term does not do this: `+ TIE_EPS²·abs(w[u] − w[v])` was tried and rejected, because it makes all 159 arc costs distinct where … ~ old: the structured term makes all 159 arc costs distinct where the shipped cost leaves 3 pairs equal, and leaves 72 of 232 per-good supports moving → new: BOTH costs make all 159 edge costs distinct (the 3-pairs-equal claim gone); the structured term leaves 11 of 29 goods admitting an alternative optimum against the shipped term's 1 (`paper`). "Genericity, not distinctness" retained ~ MEASURED ~ `round6.py` ~ 1116 || A structured second term does not do this: `+ TIE_EPS²·abs(w[u] − w[v])` was tried and rejected, because it makes all 159 arc costs distinct where the shipped cost leaves 3 pairs equal and still leaves 72 of 232 per-good supports moving — distinct arc costs are not the obstruction, different routings with equal totals are, so what is needed is genericity rather than distinctness. ~ NEW ~ MODEL ~ numerical test ~ L1062-1065
---
**Y1115** | §2.3 | status=UNCHANGED | type=w[u] − w[v]\ | provenance=(carried) | doc line ` telescopes: summed along a path it collapses to a function of the endpoints, so two routings between t …
CLAIM: `\
---
**Y1030** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1153
CLAIM: The solver's optimality tolerance is a correctness requirement rather than a performance knob, and HiGHS …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): The solver's optimality tolerance is a correctness requirement rather than a performance knob, and HiGHS … ~ MODEL ~ 1125 || The solver's optimality tolerance is a correctness requirement rather than a performance knob, and HiGHS stops when reduced costs are within its dual feasibility tolerance of zero. ~ NEW ~ MODEL ~ unsourced — the stopping rule itself is asserted with no file, version or document cited; the numeric default beside it now carries a citation, recorded separately as `Y1053` ~ L1067-1068
---
**Y1053** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1154
CLAIM: `scipy.optimize.linprog`'s `method="highs"` options document that default as `1e-07`, for both the dual and …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): `scipy.optimize.linprog`'s `method="highs"` options document that default as `1e-07`, for both the dual and … ~ MODEL ~ 1126 || `scipy.optimize.linprog`'s `method="highs"` options document that default as `1e-07`, for both the dual and the primal tolerances, at scipy 1.18.0. ~ NEW ~ MODEL ~ read from a file (cited to the `scipy.optimize.linprog` `method="highs"` option documentation, with the version named) ~ L1068-1070
---
**Y1031** | §2.3 | status=UNCHANGED | type=MEASURED | provenance=(carried) | doc line 1156
CLAIM: The margin by which the tie-break makes the optimum unique runs as low as 3.8e-8 on some per-good solves, so …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): The margin by which the tie-break makes the optimum unique runs as low as 3.8e-8 on some per-good solves, so … ~ MEASURED ~ 1128 || The margin by which the tie-break makes the optimum unique runs as low as 3.8e-8 on some per-good solves, so it sits inside the default tolerance and the solver was free to stop either side of the true optimum. ~ NEW ~ MODEL ~ numerical test ~ L1070-1072
---
**Y1054** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1164
CLAIM: The tolerance mechanism is confirmed rather than inferred, by bisecting the tolerance against `copper`.
---
**Y1057** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1168
CLAIM: The flips therefore appear exactly when the tolerance exceeds the margin, which is the claim.
---
**Y1033** | §2.3 | status=UNCHANGED | type=MEASURED | provenance=(carried) | doc line 1169
CLAIM: `flowop.LP_OPTS` ships 1e-10 — HiGHS's floor for these options, taken for headroom rather than necessity — …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): `flowop.LP_OPTS` ships 1e-10 — HiGHS's floor for these options, taken for headroom rather than necessity — … ~ MEASURED ~ 1139 || `flowop.LP_OPTS` ships 1e-10 — HiGHS's floor for these options, taken for headroom rather than necessity — and the objective spread there is 1.1e-15. ~ NEW ~ MODEL ~ read from a file (`flowop.py`) plus numerical test ~ L1081-1082
---
**Y1034** | §2.3 | status=UNCHANGED | type=PROCESS | provenance=(carried) | doc line 1170
CLAIM: No figure in this document moved when the pinned tolerance went in: the shipped column order was already …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): No figure in this document moved when the pinned tolerance went in: the shipped column order was already … ~ PROCESS ~ 1140 || No figure in this document moved when the pinned tolerance went in: the shipped column order was already reaching the true optimum, and what changed is that every other order now does too. ~ NEW ~ MODEL ~ read from a file (`flowop.py`) plus numerical test ~ L1082-1084
---
**Y1036** | §2.3 | status=UNCHANGED | type=DESIGN | provenance=(carried) | doc line 1176
CLAIM: What the second-order term buys is replacing a tiebreak that was arbitrary and order-dependent — the node …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): What the second-order term buys is replacing a tiebreak that was arbitrary and order-dependent — the node … ~ DESIGN ~ 1146 || What the second-order term buys is replacing a tiebreak that was arbitrary and order-dependent — the node index — with one that is arbitrary but order-invariant. ~ NEW ~ DESIGN ~ algebraic derivation ~ L1088-1090
---
**Y1038** | §2.3 | status=UNCHANGED | type=MEASURED | provenance=(carried) | doc line 1190
CLAIM: dividing by the world total moves the aggregate by 7 of 159 edges, and 13 of 29 per-good graphs move under at least on …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): Measured across the three normalisations — maximum, mean and world total — the aggregate `Φ_w` is unchanged, 0 of 159 edges differing, but 5 of the … ~ old: across the three normalisations the aggregate `Φ_w` is unchanged (0 of 159 edges) and 5 of 29 per-good graphs differ → new: dividing by the world total moves the aggregate by 7 of 159 edges, and 13 of 29 per-good graphs move under at least one candidate ~ MEASURED ~ `round6.py` ~ 1160 || Measured across the three normalisations — maximum, mean and world total — the aggregate `Φ_w` is unchanged, 0 of 159 edges differing, but 5 of the 29 per-good graphs do differ. ~ NEW ~ MODEL ~ numerical test ~ L1101-1103
---
**Y1116** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1195
CLAIM: `w/mean` and `N·w/sum` are algebraically the same vector, and on this field min-max and `w/max` are too, so a sweep ov …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): `w/mean` and `N·w/sum` are algebraically the same vector, and on this field min-max and `w/max` are too, so a sweep over five normalisations is a sweep over three. ~ MODEL ~ derivation ~ 1165
---
**Y1117** | §2.3 | status=UNCHANGED | type=ENGINE | provenance=(carried) | doc line 1197
CLAIM: `cape_of_good_hope` holds no counted province at all: its 20 members are one sea zone and nineteen land provinces, non …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): `cape_of_good_hope` holds no counted province at all: its 20 members are one sea zone and nineteen land provinces, none of them owned at 1444, and §1.3 counts only owned provinces. ~ ENGINE ~ named observation (the map files and the 1444 save, via §1.3's counting rule) ~ 1167
---
**Y1118** | §2.3 | status=UNCHANGED | type=MEASURED | provenance=(carried) | doc line 1199
CLAIM: A re-measuring probe must inherit `flowop.LP_OPTS`: without the pinned tolerance the same sweep undercounts — under `w …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): A re-measuring probe must inherit `flowop.LP_OPTS`: without the pinned tolerance the same sweep undercounts — under `w/mean` it returns 5 goods against the pinned 9, and the 5 are a strict subset of the 9. ~ MEASURED ~ `round6.py` ~ 1169
---
**Y1039** | §2.3 | status=UNCHANGED | type=DESIGN | provenance=(carried) | doc line 1204
CLAIM: The choice of normalisation is a third arbitrary decision with an observable consequence where before it was free; min …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): The choice of normalisation is a third arbitrary decision with an observable consequence where before it was free; min-max is what the implementation … ~ "It is recorded here rather than defended" → "The choice is recorded rather than defended"; same stipulation and same expectation for implementers ~ DESIGN ~ unsourced ~ 1174 || The choice of normalisation is a third arbitrary decision with an observable consequence where before it was free; min-max is what the implementation uses, and an implementer changing it should expect a handful of per-good graphs to move. ~ NEW ~ DESIGN ~ stipulated ~ L1103-1106
---
**Y997** | §2.3 | status=UNCHANGED | type=MODEL | provenance=(carried) | doc line 1207
CLAIM: Every DRAIN solve uses this cost, per good as well as aggregate, and since `w` is node wealth the same cost …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): Every DRAIN solve uses this cost, per good as well as aggregate, and since `w` is node wealth the same cost … ~ MODEL ~ 1177 || Every DRAIN solve uses this cost, per good as well as aggregate, and since `w` is node wealth the same cost vector serves all of them; what keeps unit arc costs is the separate comparison operators — the FLOW and TREE operators in `flowop.py` and the per-good checks in `verify.py` — because `mincost_flow`'s cost argument defaults to unit. ~ NEW ~ MODEL ~ read from a file (the named scripts) ~ L1108-1111
---
**Y483** | §2.3 | status=UNCHANGED | type=ENGINE | provenance=(carried) | doc line 1212
CLAIM: DLC state is a third input axis: treasure-fleet diversion and caravan power are both DLC-conditional, and …
PRIOR-INVENTORY FULL TEXT (for reference only; current document wording governs): DLC state is a third input axis: treasure-fleet diversion and caravan power are both DLC-conditional, and … ~ ENGINE ~ 1182 || DLC state is a third input axis: treasure-fleet diversion and caravan power are both DLC-conditional, and caravan modifier values are readable even when inert, so the model keys on the DLC flag and never on the presence of a value. ~ UNCHANGED ~ ENGINE ~ stipulated ~ L1113
---
**Y1166** | §2.3 | status=NEW | type=MEASURED | provenance=`p3_relabel_pergood.py` | doc line 1140
CLAIM: The earlier 84 → 13 printings mixed seeds: 84 at seeds 1 and 2024, 13 at seed 606, with no single seed yielding both.
---
**Y1167** | §2.3 | status=NEW | type=PROCESS | provenance=`p3_relabel_pergood.py`; `flowop.LP_OPTS` | doc line 1141
CLAIM: Those earlier figures were measured at the HiGHS default tolerance, which the shipped solver does not use.
---
**Y1168** | §2.3 | status=NEW | type=PROCESS | provenance=unsourced | doc line 1142
CLAIM: They are replaced rather than repaired.
---
**Y1169** | §2.3 | status=NEW | type=MEASURED | provenance=`round6.py` | doc line 1165
CLAIM: The `copper` flip count over four column permutations is seed-dependent, taking the values 2, 8 or 6.
---
**Y1170** | §2.3 | status=NEW | type=PROCESS | provenance=§1.6; `epsilon6.py` | doc line 1189
CLAIM: §1.6's `TIE_EPS` sweep is run on the shipped two-term cost and is the sensitivity record for the tie-break's strength.
