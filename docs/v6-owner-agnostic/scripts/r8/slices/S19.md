# Slice S19  (53 claims)


## §3.9

- **Y1132** [CHANGED, type MODEL, spec line 1782]  provenance: derivation
  CLAIM: The flow identity the LP enforces is `flow_in(n) − flow_out(n) = −b_w(n)`.
  CHANGE: old: "**The separating quantity is** the flow identity the LP enforces: `flow_in(n) − flow_out(n) = −b_w(n)`" → new: the identity is still asserted as what the LP enforces, but it is no longer the separator — the text now says what separates a node from an end "**is not flow carriage either**"

- **Y1134** [CHANGED, type MODEL, spec line 1784]  provenance: derivation
  CLAIM: Every net demander absorbs exactly its own deficit and passes the rest on; an end is a node the whole orientation gives no out-arc — no flow arc and no free edge.
  CHANGE: old: "an end is **a node that passes none on**" → new: "an end is **a node the whole orientation gives no out-arc — no flow arc and no free edge**". The absorbs-its-own-deficit half is unchanged

- **Y685** [CHANGED, type MODEL, spec line 1791]  provenance: algebraic derivation
  CLAIM: Wealth pulls but the wealthiest node is not automatically an end: what makes an end is that no arc of either kind leaves, a property of the whole field and the graph rather than of a single node’s rank, and the ends emerge and move when the wealth moves.
  CHANGE: old: "what makes an end is **where the flow *terminates***" → new: "what makes an end is **that no arc of either kind leaves**" — the flow arcs and the free edges together. The whole-field-not-rank clause and the emerge-and-move tail are unchanged

- **Y1131** [REWORDED, type MODEL, spec line 1780]  provenance: derivation
  CLAIM: A node can draw more edges in than it sends out and still be a thoroughfare; the quantity that separates a net demander from an end is not a degree comparison.
  REWORDING: "**The quantity that separates them from an end** is not a degree comparison" becomes "**What separates a node from an end** is not a degree comparison"; the thoroughfare clause between the dashes is byte-identical

- **Y1135** [REWORDED, type MEASURED, spec line 1786]  provenance: `round6.py`
  CLAIM: 18 of 80 nodes have out-degree above zero while carrying no outgoing flow at all.
  REWORDING: "That the two are different questions is visible directly — **18 of 80** nodes **have out-degree above zero while carrying no outgoing flow at all**" becomes "two of the **18 of 80** nodes **with out-degree above zero and no outgoing flow at all**"; the figure, the population and the property are the same, and the sentence now names two of them

- **Y682** [UNCHANGED, type DESIGN, spec line 1772]
  CLAIM: The installed graph exists for the engine's direction-dependent systems — propagation, fleet routes, …

- **Y683** [UNCHANGED, type ENGINE, spec line 1773]
  CLAIM: What vanilla's authored arrows encode is empires pointing at the biggest cities and richest areas, with three …

- **Y177** [UNCHANGED, type MEASURED, spec line 1777]
  CLAIM: On this field `english_channel` is the richest node at 316.6 and is not a sink — it drains to `genua`, 4th at …

- **Y1133** [UNCHANGED, type MEASURED, spec line 1783]
  CLAIM: The identity holds on all 36 net demanders and on all 80 nodes, to a maximum residual of 5.2e-17.

- **Y686** [UNCHANGED, type DESIGN, spec line 1794]
  CLAIM: `Φ_w` reuses the §1.1 operator unchanged: one implementation, one set of guarantees (LP feasibility, …

- **Y687** [UNCHANGED, type MODEL, spec line 1799]
  CLAIM: "Two aggregates were tested and rejected before the third was adopted"

- **Y688** [UNCHANGED, type MODEL, spec line 1801]
  CLAIM: The value-weighted net flow (the sum over goods of `V_g · net_g`) is a flow, flows circulate, and it …

- **Y178** [UNCHANGED, type MODEL, spec line 1803]
  CLAIM: the ends are "a function of the order Phase 3 pops its ready queue, which is a design choice inside the operator" — no …

- **Y689** [UNCHANGED, type MODEL, spec line 1803]
  CLAIM: acyclic for free only; the self-coherence comparison is no longer made anywhere in the document

- **Y180** [UNCHANGED, type DESIGN, spec line 1805]
  CLAIM: "That follows from the definition and needs no measurement" (§3.15: "The rejection is structural, so no figure is kept …

- **Y690** [UNCHANGED, type DESIGN, spec line 1806]
  CLAIM: `Φ_w` is adopted for one operator, one set of guarantees, and ends that move with the world: reusing §1.1 …

- **Y181** [UNCHANGED, type MODEL, spec line 1809]
  CLAIM: v2.1 through v4.0's "two vanilla-like ends at 1444" justification is withdrawn and must not be revived even …

- **Y182** [UNCHANGED, type DESIGN, spec line 1813]
  CLAIM: only the buys half — "What it buys is one operator, one set of guarantees, and ends that sit where the wealth is"

- **Y691** [UNCHANGED, type MODEL, spec line 1816]
  CLAIM: A difference in `Φ_w` across a link is not the net value crossing it.

- **Y692** [UNCHANGED, type MODEL, spec line 1817]
  CLAIM: Realized movement follows vanilla propagation — a good can be diluted by an even split across three links …

- **Y693** [UNCHANGED, type DESIGN, spec line 1819]
  CLAIM: That is why the disagreement rate is measured rather than assumed, and why display policy for negative link …

- **Y694** [UNCHANGED, type MODEL, spec line 1821]
  CLAIM: Link values are realized flows, which makes conservation hold by construction.

- **Y1224** [NEW, type MODEL, spec line 1779]  provenance: `round6.py`; derivation
  CLAIM: `mexico`, `gulf_of_siam` and `sevilla` are not ends for two different reasons, which the flow separates.

- **Y1225** [NEW, type MODEL, spec line 1781]  provenance: derivation
  CLAIM: What separates a node from an end is not flow carriage either.

- **Y1226** [NEW, type MEASURED, spec line 1784]  provenance: `round6.py`
  CLAIM: `sevilla` passes 0.0201 of the 0.0324 it receives.

- **Y1227** [NEW, type MEASURED, spec line 1785]  provenance: `round6.py`
  CLAIM: For `mexico` and `gulf_of_siam` the rest is exactly zero — flow_out 0 — making them two of the 18 of 80 nodes with out-degree above zero and no outgoing flow at all.

- **Y1228** [NEW, type MEASURED, spec line 1787]  provenance: `round6.py`; derivation
  CLAIM: `mexico` and `gulf_of_siam` are still not ends, because each keeps a single free out-edge.

- **Y1229** [NEW, type MODEL, spec line 1788]  provenance: derivation
  CLAIM: Carrying flow out is not necessary for non-endhood.

- **Y1230** [NEW, type MODEL, spec line 1790]  provenance: derivation; §1.1 Phase 3
  CLAIM: The flow arcs terminate where the identity sends them, and the free edges are Phase 3’s completion.


## §3.10

- **Y695** [CHANGED, type ENGINE, spec line 1825]  provenance: the shipped files (`trade_income_percentage`); the defines
  CLAIM: Paying countries correctly while leaving the display wrong is a strictly weaker position: node values, pie charts and the ledger would describe an economy nobody is playing, and those figures have readers.
  CHANGE: old: "**AI light-ship building, trade-league behaviour, peace valuation and income-threshold events all read those figures**" — four readers asserted flat → new: the four are split — income-threshold content is **file-confirmed** (`trade_income_percentage`), the other three are **engine-internal readers** whose enumeration "is named here as missing"

- **Y696** [UNCHANGED, type MODEL, spec line 1827]
  CLAIM: The engine's data model is sufficient at node level for a narrower reason than it first appears: …

- **Y697** [UNCHANGED, type MODEL, spec line 1827]
  CLAIM: What factors out is `powershare_C`, a country's share among collectors, and whether a country collects is a …

- **Y698** [UNCHANGED, type MODEL, spec line 1830]
  CLAIM: `income_C(n)` = the sum over goods of `value_g(n) · collected_share(n,g) · powershare_C(n)` = …

- **Y699** [UNCHANGED, type MODEL, spec line 1833]
  CLAIM: That is an identity rather than a measurement: `powershare_C(n)` carries no `g`, so it factors out of the …

- **Y700** [UNCHANGED, type MODEL, spec line 1833]
  CLAIM: Every term that feeds a collector's power at a node is node-wide — the merchant bonus, the off-home penalty, …

- **Y701** [UNCHANGED, type MODEL, spec line 1833]
  CLAIM: One scalar per node reproduces every country's income exactly, and the engine's own math does the rest.

- **Y702** [UNCHANGED, type PROCESS, spec line 1834]
  CLAIM: v1 through v3.0 quoted floating-point residuals of the income identity — 5.7e-14 here and 1.4e-14 below — produced by constructions none of those documents states, while v4.0 quoted its own 1.3e-16 on a construction it does state.

- **Y1136** [UNCHANGED, type DESIGN, spec line 1834]
  CLAIM: No residual is quoted for the doubles check: a floating-point residual of an exact identity measures the arithmetic, n …

- **Y1180** [UNCHANGED, type PROCESS, spec line 1834]
  CLAIM: v4.0 quoted its own 1.3e-16 on a construction it does state: `gulf_of_siam`, thirteen goods carrying local value, per-good eligibility, and the off-home penalty on two of the three collectors.

- **Y184** [UNCHANGED, type DESIGN, spec line 1836]
  CLAIM: Propagation is kept on a single graph, and the reason is not the one v1 through v6.0's own first draft gave.

- **Y185** [UNCHANGED, type MODEL, spec line 1836]
  CLAIM: "the identity holds by construction." — the doubles residual clause deleted with the withdrawn measurement

- **Y186** [UNCHANGED, type MEASURED, spec line 1836]
  CLAIM: `gulf_of_siam`'s 29 goods leave it by seven distinct downstream sets.

- **Y187** [UNCHANGED, type MODEL, spec line 1836]
  CLAIM: Per-good propagation does not break the income identity: defining `ps̄_C` as the per-good shares weighted by …

- **Y188** [UNCHANGED, type MODEL, spec line 1836]
  CLAIM: Both inputs to `ps̄_C` already exist per good at write time, and §2.6 sums exactly them into `collect_pool`.

- **Y189** [UNCHANGED, type MODEL, spec line 1838]
  CLAIM: The real cost is that `ps̄_C` is not derivable from trade power alone: it is value-weighted, so installing it …

- **Y190** [UNCHANGED, type DESIGN, spec line 1838]
  CLAIM: That is a claim about what the engine exposes rather than about a magnitude, and it is why the single graph …

- **Y191** [UNCHANGED, type PROCESS, spec line 1838]
  CLAIM: Every magnitude previous versions quoted here — v1 through v3.0's "off by 5.96 ducats on a node paying ~250" …

- **Y192** [UNCHANGED, type DESIGN, spec line 1838]
  CLAIM: No figure of the author's own is quoted here, because the identity holds and the objection is structural, and …

- **Y703** [UNCHANGED, type ENGINE, spec line 1840]
  CLAIM: Only the decomposition by good exceeds what the engine can hold.

- **Y1231** [NEW, type ENGINE, spec line 1825]  provenance: the shipped church-aspect, decree and estate files
  CLAIM: Income-threshold content reads trade income in shipped files — `trade_income_percentage` in the church-aspect, decree and estate files.

- **Y1232** [NEW, type ENGINE, spec line 1825]  provenance: the defines; the absence of a shipped rule
  CLAIM: AI light-ship building, trade-league behaviour and peace valuation are engine-internal readers.

- **Y1233** [NEW, type ENGINE, spec line 1825]  provenance: the defines
  CLAIM: The defines expose light-ship constants — cost, maintenance, time, sailors, combat width — but no rule tying the build decision to node value.

- **Y1234** [NEW, type PROCESS, spec line 1825]  provenance: §2.7 item 10
  CLAIM: No §2.7 probe enumerates node-value readers — item 10 enumerates direction-gate call sites, a different question — so the enumeration that would settle those three readers is named here as missing.
