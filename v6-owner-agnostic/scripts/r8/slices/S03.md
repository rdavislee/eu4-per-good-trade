# Slice S03  (37 claims)


## §1.1

- **Y250** [CHANGED, type MEASURED, spec line 190]  provenance: `measure6.py`; `props6.py`
  CLAIM: The §1.1 property measurements were regenerated by `measure6.py` and `props6.py`.
  CHANGE: old: "regenerated **for v6.0** by `measure6.py`" → new: "regenerated by `measure6.py` **and `props6.py`**"; the v6.0 scoping is dropped and a second script is credited

- **Y1081** [CHANGED, type PROCESS, spec line 230]  provenance: `props6.py`; `val5_pergood.py`
  CLAIM: `props6.py` was copied and extended from `val5_pergood.py`, and the permutation loop was written for this citation — the figure had been quoted since v2 with nothing in the tree that computed it.
  CHANGE: old: "`props6.py` was **renamed from a round-5 working file**" → new: it was "**copied and extended from `val5_pergood.py`, which is still on disk**"; the provenance moves from a rename to a copy-and-extend off a named surviving file

- **Y221** [UNCHANGED, type MODEL, spec line 123]
  CLAIM: Every trade good has its own directed network over the same adjacency.

- **Y222** [UNCHANGED, type DESIGN, spec line 123]
  CLAIM: Direction is computed, never authored.

- **Y223** [UNCHANGED, type MODEL, spec line 125]
  CLAIM: For each good `g` the balance is `b_g(n) = s_g(n) − c_g(n)`, oriented by DRAIN in four named phases: peel, …

- **Y224** [UNCHANGED, type MODEL, spec line 126]
  CLAIM: Phase 0 repeatedly removes degree-1 nodes, orienting each pendant edge by the sign of its absorbed subtree balance (ne …

- **Y225** [UNCHANGED, type MODEL, spec line 130]
  CLAIM: Phase 0 is exact rather than heuristic: every removed edge is a bridge and flow on a tree is determined by …

- **Y226** [UNCHANGED, type MEASURED, spec line 131]
  CLAIM: On the vanilla map Phase 0 is a no-op — minimum degree 2, zero bridges.

- **Y227** [UNCHANGED, type DESIGN, spec line 132]
  CLAIM: Phase 0 exists for modded maps.

- **Y228** [UNCHANGED, type MODEL, spec line 134]
  CLAIM: Phase 1 takes the connected clusters of net demanders in the core, computes the Herfindahl index of their …

- **Y229** [UNCHANGED, type MODEL, spec line 136]
  CLAIM: one knob, `r` (default 0); `ρ` withdrawn as a parameter the shipped operator never had

- **Y1075** [UNCHANGED, type MODEL, spec line 136]
  CLAIM: The cluster dilation radius `r` links demanders within `r` hops before clustering.

- **Y230** [UNCHANGED, type MEASURED, spec line 137]
  CLAIM: On vanilla 1444 demand is so ubiquitous that k = 1 for 27 of 29 goods at the default knobs.

- **Y231** [UNCHANGED, type DESIGN, spec line 138]
  CLAIM: Phase 1's selection is deliberately weak because Phase 3 self-corrects upward.

- **Y1076** [UNCHANGED, type PROCESS, spec line 139]
  CLAIM: A demand-mass quantile `ρ` was documented here as a second Phase-1 knob; the shipped operator has no such parameter — …

- **Y1077** [UNCHANGED, type MODEL, spec line 141]
  CLAIM: The §3.13 calibration option carries its own Phase 1 and does implement a quantile; that is where `ρ` is described.

- **Y232** [UNCHANGED, type MODEL, spec line 144]
  CLAIM: Phase 2 solves the uncapacitated min-cost flow serving `b_g` and orients every support edge by its net flow.

- **Y976** [UNCHANGED, type MODEL, spec line 144]
  CLAIM: Phase 2's arc costs are near-unit, symmetric in the arc, and read from node wealth: a first-order term …

- **Y977** [UNCHANGED, type MODEL, spec line 147]
  CLAIM: The costs are not unit because with unit costs the optimum is not unique and which one the solver returns …

- **Y233** [UNCHANGED, type MODEL, spec line 149]
  CLAIM: The support is a spanning-tree basis of at most N−1 edges when the solver returns a basic (vertex) optimum, …

- **Y234** [UNCHANGED, type MODEL, spec line 150]
  CLAIM: An interior-point solve without crossover can split flow across equal-length parallel paths and return a …

- **Y235** [UNCHANGED, type DESIGN, spec line 152]
  CLAIM: §2.2 therefore requires network simplex or a simplex LP.

- **Y1004** [UNCHANGED, type MODEL, spec line 152]
  CLAIM: §2.3 additionally requires the solver's optimality tolerance to be tighter than the margin the tie-break …

- **Y236** [UNCHANGED, type MODEL, spec line 154]
  CLAIM: For any optimum the support contains no directed cycle, because with all costs strictly positive a directed …

- **Y237** [UNCHANGED, type MODEL, spec line 157]
  CLAIM: Edges with zero net flow are free and are deferred to Phase 3.

- **Y238** [UNCHANGED, type MODEL, spec line 159]
  CLAIM: Phase 3 marks nodes Kahn-style: a node is ready when every flow out-neighbour is already marked and it is a …

- **Y239** [UNCHANGED, type MODEL, spec line 161]
  CLAIM: Among ready nodes the sweep pops by the priority key (DEF ascending, b ascending, index), where `DEF(v)` is …

- **Y240** [UNCHANGED, type MODEL, spec line 162]
  CLAIM: The flow-arc subgraph is acyclic and fixed before any free edge, so `DEF` involves no circularity.

- **Y241** [UNCHANGED, type MODEL, spec line 163]
  CLAIM: On a stall the sweep promotes the heaviest flow-terminal demander among the candidates into the sink set, and …

- **Y242** [UNCHANGED, type MODEL, spec line 165]
  CLAIM: If the candidates hold no flow-terminal demander at all, the fallback branch promotes the highest-wealth …

- **Y243** [UNCHANGED, type MODEL, spec line 166]
  CLAIM: Node wealth is a good-independent input, so the fallback branch needs no bootstrap.

- **Y244** [UNCHANGED, type MODEL, spec line 167]
  CLAIM: Candidates at a stall are the unmarked nodes whose flow out-neighbours are all marked; because the flow …

- **Y245** [UNCHANGED, type MODEL, spec line 169]
  CLAIM: A candidate carrying any flow out-arc is already ready, and a candidate with inflow is a flow-terminal …

- **Y019** [UNCHANGED, type MODEL, spec line 171]
  CLAIM: The fallback branch fires only when every candidate is support-isolated with zero post-peel balance.

- **Y020** [UNCHANGED, type MODEL, spec line 172]
  CLAIM: The balance the priority key reads is the one Phase 0 hands on, with each pendant's balance folded into its …

- **Y021** [UNCHANGED, type MODEL, spec line 174]
  CLAIM: On a connected core the fallback needs the folded balance to vanish across the core: for a per-good graph …

- **Y022** [UNCHANGED, type MEASURED, spec line 177]
  CLAIM: Nodes hold between 0 and 72 counted provinces, so equal per-province wealth makes unequal node sums.
