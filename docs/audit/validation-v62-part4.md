## §2.1 — Shape (27 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y427 | one program: runtime DLL, monthly solve, propagate, write back | CONFIRMED | design stipulation; checked consistency against §2.4-§2.6 | §2.4 (tradenodes owned by DLL), §2.6 (monthly tick writes) both match this description; no contradiction found |
| Y428 | ships generated `00_tradenodes.txt` + companion overlay | CONFIRMED | design stipulation; consistency check | §2.4 describes the generated file exactly; overlay is a forward requirement, not contradicted elsewhere |
| Y429 | target platform Windows/Steam | CONFIRMED | file read | `../v2-drain/per-good-trade-spec.md:320` "Windows/Steam" (the DLL precedent); spec text itself line 891 |
| Y430 | achievements off with any mod | CONFIRMED | file read (binary strings) | `grep -rl ACHIEVEMENTS_DISABLED_MODIFIED_GAME` hits `eu4.exe` and `localisation/core_l_*.yml` |
| Y431 | engine loads ironman save in modded game, shipped code path | CONFIRMED | file read (binary strings) | `strings`-equivalent grep for "Loading ironman in modded game" hits `eu4.exe` |
| Y432 | MP is lockstep with checksums; hardware-divergence worry is real | CONFIRMED | file read (binary strings) + derivation | `eu4.exe` contains "Checksum:", "Checksum in HEX:", `checksums.h`, and ships `checksum_manifest.txt` — a real desync-detection mechanism; SIMD/accumulation-order divergence is standard floating-point engineering fact |
| Y434 | DRAIN's exposure differs in kind from v1's dense linear algebra (residual comparisons) | CONFIRMED | file read | `../v1-laplacian/` solves `L·phi=b` (dense linear solve, `solver.py:207-241`'s legacy `solve_phi`); v1 compared solved potentials, which are continuous and residual-bearing, unlike DRAIN's combinatorial LP |
| Y435 | DRAIN compares input-derived quantities (DEF, b, arc costs), not solver residuals | CONFIRMED | file read (drain.py, flowop.py) | `drain.py`'s sweep keys on `(DEF, beta, index)`; `flowop.py`'s `TIE_COST` operates on `w` (node wealth) directly, not on solved potentials |
| Y1011 | MP question now is build discipline, not last-bit arithmetic agreement | CONFIRMED | derivation, following from Y1012-Y1015's measured margins | consistent restatement of measured margins below |
| Y995 | §2.3's two changes move desync from design to verification problem | CONFIRMED | derivation | tie-break (`flowop.TIE_COST`) + pinned tolerance (`LP_OPTS`) both exist and measurably remove the vertex-selection ambiguity (see Y1013-Y1014) |
| Y1012 | no randomness in the solve: identical fingerprint over repeats/processes/5 PYTHONHASHSEED values | CONFIRMED | measurement: ran `fingerprint6.py` under seeds 0,1,2,42,12345,random,None (twice) | all 8 runs produced `8b13dddb9a345aa6d1990cac70d64e0b649b01f8cab1a9ec41cf9c87faa1012c` |
| Y1013 | margin 3.8e-8 worst per-good, 7.5e-6 aggregate, 8-10 orders above unit roundoff | CONFIRMED | measurement: ran `p3_bisect.py`/`p3_margin.py`'s per-good margin scan | worst per-good `copper` 3.7648e-08 (rounds to 3.8e-8); aggregate 7.527e-06 (rounds to 7.5e-6); document's own exponent convention (confirmed self-consistent against Y1024) gives 8 and 10 orders above double epsilon (2.2e-16) |
| Y1014 | 0 flips under LP column permutation, aggregate+29 goods, objective spread 1.1e-15 | CONFIRMED | measurement: ran `p3_perm.py` section A (6 permutations, `LP_OPTS`) | 0 of 30 solves flip; max objective relative spread 1.03e-15 (document states 1.1e-15 — same order of magnitude, minor variance) |
| Y1015 | per-good \|net\| distribution bimodal: 2321 at 0, 2290 above 1e-6, nothing between, min nonzero 6.94e-06 | CONFIRMED | measurement: ran `round6.py` | exact match: "edge-goods at exactly 0: 2321", "above 1e-6: 2290", "strictly between: 0", "smallest non-zero \|net\|: 6.939e-06" |
| Y1016 | a few ULP cannot change any decision; what remains is three checks | CONFIRMED | derivation from Y1013-Y1015's measured margins | margins 6-10 orders above float noise support the conclusion |
| Y1017 | Check 1: one binary/platform, no cross-platform sessions | CONFIRMED | design stipulation | consistent with Y429/Y1018 |
| Y1018 | v2-drain DLL precedent already Windows/Steam-only | CONFIRMED | file read | `../v2-drain/per-good-trade-spec.md:320` "Windows/Steam" |
| Y1019 | Check 2: no runtime CPU dispatch, single-threaded | CONFIRMED | design stipulation (forward requirement, not yet built — consistent with document's own "DLL does not exist yet") | no contradiction; matches §2.1's own framing |
| Y1020 | Check 3: §2.8 cross-implementation check compares DLL vs reference exactly | CONFIRMED | file read | spec §2.8 "Cross-implementation" row (L1387 area): "The DLL and the reference implementation agree on orientation exactly for every save in the historical set" |
| Y1021 | every trade number quantised to 1/1000: 495/495 sampled | CONFIRMED | measurement: ran `val62p4_quant.py`/`val62p4_perm.py` on `VANILLA_start.eu4` and `Castile1444_12_22.eu4` trade blocks | 3,354 of 3,354 values (VANILLA) and 3,810 of 3,810 (Castile) land exactly on the 1/1000 grid across `total,val,p_pow,retention,collector_power,max_pow` — stronger than claimed 495/495; that specific subsample size not independently reproduced, but the phenomenon (100% on-grid) holds on a larger sample |
| Y1022 | quantisation erases divergence below half a grid step | CONFIRMED | derivation | standard rounding-to-nearest-1/1000 argument, correct |
| Y1023 | files can't settle whether rounding is in simulation or serialiser | CONFIRMED | epistemic-limitation check | correct: no memory-read tool is among my materials, only save files, which cannot distinguish the two per the brief's own constraints |
| Y1024 | margins 3.8e-8 to 7.5e-6 are 3-5 orders below 1e-3 grid | CONFIRMED | derivation, exponent-difference convention (self-consistent with Y1013's "8 to 10 orders") | -3-(-6)=3, -3-(-8)=5 using the document's own exponent-only convention |
| Y1025 | reason for single-player shipped changed: build discipline unverified, DLL doesn't exist | CONFIRMED | derivation/consistency check | matches spec text verbatim in substance |
| Y436 | ship single-player only until checks 1-3 done | CONFIRMED | design stipulation | matches spec text (L945) exactly |
| Y1113 | determinism fingerprint = one SHA-256 over Φ_w + all 29 per-good graphs incl. sinks/sources/promotions/fallbacks/Phase-2 objective | CONFIRMED | file read: `fingerprint6.py` | `rec()` hashes `directed, sinks, sources, promotions, fallbacks, cost` for `PHI_W` plus all 29 live goods — matches exactly |
| Y1114 | smallest non-zero per-good \|net\| is 6.94e-06 | CONFIRMED | measurement: `round6.py` | "smallest non-zero \|net\|: 6.939e-06" |

## §2.2 — Solver (23 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y437 | item 1: parser for 00_tradenodes.txt (adjacency, members, path/control, flags) | CONFIRMED | file read: `nodes.json`, `solver.py` | `solver.py` builds `ORDER, NODES, UND, EDGES` from `nodes.json`, itself parsed from the vanilla file |
| Y438 | item 2: parser for non-ironman saves (owner, base_tax, base_production, good, dev) | CONFIRMED | file read: `solver.py` `province_table()` | reads `owner, base_tax, base_production, trade_goods` from `prov1444.json`/save-derived data exactly |
| Y439 | item 3: parser for defines.lua merged with common/defines/ overrides in load order | CONFIRMED | file read: EU4 install | `common/defines/` holds `00_dummy.lua` + four `difficulty_*.lua` override files, a real merge mechanism; this is a forward solver-checklist item, consistent with rest of doc |
| Y440 | GP_COEFF read from static_modifiers rather than hardcoded | CONFIRMED | file read: `solver.py:_read_gp_coeff`, EU4 install | function regexes `provincial_production_size` out of `00_static_modifiers.txt`, raising if absent; file block: `trade_goods_size = 0.2` |
| Y131 | world wealth 10,607.40 over 2,472 counted provinces | CONFIRMED | measurement: `measure6.py` | `measure6.out`: "counted provinces 2472", "world wealth 10607.4" |
| Y441 | item 5: DRAIN per good = min-cost b-flow (simplex/LP not interior-point), sweep, Phase-4 evaluator | CONFIRMED | file read: `flowop.py`, `drain.py` | `linprog(..., method="highs")` (simplex-family, not IPM); `bounds=(0,None)` uncapacitated |
| Y442 | Phase-4 unserved/stranded equal by conservation (Σb_g=0 identically) | CONFIRMED | derivation (MATH type) | correct b-flow mass-balance identity; note: current `drain.py` has no literally-named `unserved`/`stranded` variables (those names survive only in the legacy v1-era `verify.py`), but the algebra the claim asserts is sound regardless |
| Y443 | Φ_w is one more DRAIN run with wealth as good — 30th solve, same code path | CONFIRMED | file read: `round6.py`'s `bw()` + `run_drain` | Φ_w built and solved via the identical `run_drain(b)` call used for all 29 goods |
| Y444 | item 6: survival table S_g[n][H] for AI scoring | CONFIRMED | design stipulation; no contradicting implementation found | forward solver-checklist item, no implementation exists yet in reference tree (consistent with §2.1's stated incompleteness) |
| Y445 | item 7: mutual reachability census, 30 goods × 80 BFS → 80×80 matrix counting goods per path | PARTIAL | measurement: `measure6.py`'s reachability block | the actual computation (feeding "ordered pairs connected 5723 of 6320") is a boolean OR over 29 live goods (not a per-entry count, and not 30 goods) — the entry does not literally "count goods with a directed path" as stated |
| Y446 | item 8: synthetic-shock harness — edit parsed data, re-solve | CONFIRMED | file read: `round6.py`'s "razing a Chinese node" block | zeroes `hangzhou`/`beijing` node wealth and re-runs DRAIN, functionally matching the description |
| Y447 | cost per good = one uncapacitated MCF on 80 nodes/318 arcs + O(V+E) sweep | CONFIRMED | measurement: `N`, `EDGES_UND`, `ARCS` lengths | N=80, edges=159, arcs=318 (=159×2); `bounds=(0,None)` confirms uncapacitated |
| Y133 | repeated 12-run experiments don't reproduce closely enough for a finer figure | CONFIRMED | measurement: own 3×29-good timing run | own quick run gave totals 0.098-0.117s, itself outside v5.0's claimed 0.17-0.21s window, supporting the non-reproducibility finding |
| Y134 | v5.0's "0.17-0.21s" not reproducible: 1, then 0, then 0 runs land inside it | CONFIRMED | consistency with own re-measurement | own totals (0.098-0.117s) also fall outside that window every time, consistent with the claim |
| Y448 | "milliseconds each" holds already with a generic LP; no specific projection for native network simplex | CONFIRMED | measurement/derivation, consistent with own timing (3.4-4.0 ms/good average) | own run: 3.4-4.0 ms/good, well under "tens of milliseconds" |
| Y449 | two implementations of one spec: reference solver + DLL (items 4-7 in host language) | CONFIRMED | design stipulation; consistency w/ §2.8 | §2.2 text describes this exactly; DLL doesn't exist yet, consistent with §2.1 |
| Y450 | must agree on orientation exactly, no tolerance band, replaces v1's identity check | CONFIRMED | file read: §2.8 "Cross-implementation" row | matches almost verbatim |
| Y451 | parsers/harness stay reference-only; DLL never reads a save | CONFIRMED | design stipulation, consistent | no contradiction; DLL reads live memory per §2.2's own text |
| Y452 | inland is derived (no coastal province among members), not trusted from flag | CONFIRMED | file read: `nodes.json`, `coastal.json` (via `val62p4_inland.py`) | derivation logic reproduced exactly |
| Y453 | flag/derivation disagree at exactly one node, siberia (1781/1782 Arctic-coast); 25 vs 26 | CONFIRMED | measurement: `val62p4_inland.py` | "flag inland count: 26 derived inland count: 25", "flag minus derived: ['siberia']", "1781 coastal: True 1782 coastal: True" |
| Y129 | wealth = TAX_COEFF·base_tax + GP_COEFF·base_production·(1+goods mods)·price, no tax modifier | CONFIRMED | file read: `solver.py:province_table` | `tax=TAX_COEFF*base_tax*(1+tmod)` with `tmod=0.0` always (comment: "tax term takes NO province-state modifier"); `gp=GOODS_PRODUCED_FACTOR*base_production*(1+gmod)` |
| Y130 | 4 modifiers reach goods_produced; at 1444 only devastation live (11 provinces); unrest defined, not read | CONFIRMED | file read: `solver.py` + EU4 static_modifiers file | `STATE_GOODS_MOD` has exactly 4 keys (devastation/prosperity/occupied/under_siege); `LIVE_STATE_MODS=('devastation',)`; measure6.out "devastated provinces counted 11"; game file's `unrest={...}` block exists (line 487) with `local_tax_modifier=-0.02` but `STATE_TAX_MOD={}` (empty) — defined and not read |
| Y132 | ~0.1s for 29 goods, ms not tens of ms, "single-digit" deliberately withdrawn | CONFIRMED | measurement: own timing run | 0.098-0.117s total (order 0.1s), 3.4-4.0 ms/good average, consistent with stated withdrawal of "single-digit" given documented replicate variance |

## §2.2a — What map this is for (17 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y454 | v2 called target "map-agnostic" while proving properties only on the measured map | CONFIRMED | file read: `../v2-drain/per-good-trade-spec.md` | line 5: "Target: EU4 (final patch, 1.37.5 Inca), extended timeline compatible, **map-agnostic**"; v2's properties are all measured on vanilla 1444 only |
| Y455 | Premise 1: reachability = LP feasibility, feasible because Σb_g(n)=0 identically | CONFIRMED | derivation | standard b-flow feasibility fact on a connected graph |
| Y456 | on >1 component, global balance insufficient; cross-component imbalance is infeasible outright | CONFIRMED | measurement: `toys.py` T5 | "LP success: False \| The problem is infeasible. (HiGHS Status 8...)" on a disconnected per-component-imbalanced graph |
| Y457 | vanilla 1444 is one component | CONFIRMED | measurement: `solver.py` `COMPS` | `len(COMPS)==1`, containing all 80 nodes |
| Y458 | solver must compute components once at load; single component proceeds | CONFIRMED | design stipulation (forward requirement) | consistent with document's stated incompleteness elsewhere (§2.1); current `drain.py` has no component-aware logic for DRAIN specifically (only the legacy v1 Laplacian path does), but this is stated as a requirement, not a built fact |
| Y459 | solver must not silently hand an infeasible program to the LP | CONFIRMED | design stipulation | same as Y458; not yet enforced in `drain.py` but stated as a requirement |
| Y460 | v1 had per-component renormalisation; v2 dropped it without replacement; v3 restores it | CONFIRMED | file read: `../v1-laplacian/per-good-trade-spec.md`, `../v2-drain/per-good-trade-spec.md` | v1 line 31: "Solve per connected component, renormalizing `s` and `c` within each so they balance. Skip isolated nodes."; v2 spec has zero mentions of "component" |
| Y461 | Premise 2: Phase 0 is a no-op, or map-dependent properties read as measurements | CONFIRMED | design/model framing, consistent | matches §1.1's structure; vanilla confirmed Phase-0-inert (see Y462/props6.py) |
| Y462 | where Phase 0 acts, three properties weaken and spec says so | CONFIRMED | file read: table at spec L1019-1024 | matches the table's own three rows exactly |
| Y463 | Global DAG still proved where Phase 0 acts (pendant edges are bridges, cannot close a cycle) | CONFIRMED | measurement: `toys.py` T1 (pendant net-importer, V029/V126) | directed graph `[('A','B'),('A','D'),('B','C'),('D','B')]` is acyclic |
| Y464 | sink-set equality measured exact 29/29 on 2-core, fails where Phase 0 acts | CONFIRMED | measurement: `props6.py` (2-core: "sink-set identity holds on 29 of 29 goods; failures []") + `toys.py` T1 (Phase-0-active: actual sinks `['C']` != formula set `['B']`, EQUAL: False) | both halves independently confirmed |
| Y135 | free-edge determinism unaffected by Phase 0; index-independence not (key reads post-fold β) | CONFIRMED | file read: `drain.py` sweep key construction + table at spec L1024 | key is `(DEF, beta, index)` computed on peeled/folded balances, matching the claimed mechanism |
| Y466 | T2 (selected flow-terminal loses sinkhood to free edge) and T3 (fallback promotion) both break equality inside the 2-core | CONFIRMED | measurement: `toys.py` T2, T3 | T2: actual sinks `['u2']` vs formula `['u1','u2']`, EQUAL False; T3: actual sink `['A']`, formula set empty, sink reached only via FALLBACK |
| Y467 | stated target: connected maps, min degree ≥2 → every §1.1 property proved/measured | CONFIRMED | design stipulation, consistent w/ vanilla's measured min-degree 2 (`props6.py`) | "min degree 2 max 8 mean 3.98" |
| Y468 | on connected map with pendants, algorithm still runs, still acyclic/fully-oriented/demand-serving | CONFIRMED | measurement: `toys.py` T1 | T1 ran to completion, produced a fully-oriented acyclic directed graph |
| Y469 | on disconnected map, solver must renormalise per component or refuse | CONFIRMED | design stipulation; T5 confirms the underlying infeasibility premise | `toys.py` T5 shows the current (un-renormalised) LP is infeasible on a disconnected imbalanced graph, consistent with the stated requirement not yet being built |
| Y465 | marking order reproduces DAG on 2-core, fails where Phase 0 acts (pendants have no marking order) | CONFIRMED | derivation, consistent w/ table at L1023 | pendant edges are peeled before the core sweep runs, so no marking-order comparison applies to them |

## §2.3 — Constants (44 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y470 | constants read at runtime, never hardcoded | CONFIRMED | file read: `solver.py` | `PRICES` and `GOODS_PRODUCED_FACTOR` both loaded from install files at import time |
| Y471 | nine runtime-read uses map to named defines | CONFIRMED | file read: `defines.lua` | all nine (`TRADE_PROPAGATE_DIVIDER=5`, `TRADE_PROPAGATE_THRESHOLD=2`, `TRADE_NON_CAPITAL_OFFICE=-0.50`, `TRADE_POWER_HOME_BONUS=0.1`, `MERCHANT_MAX_POWER_BONUS=2.0`, `TRADE_MERCHANT_PRESENT=0.1`, `TRADE_ADDED_VALUE_MODIFER=0.05`, `CARAVAN_FACTOR=3.0`/`CARAVAN_POWER_MAX=50`/`CARAVAN_POWER_MIN=2`, `PS_MOVE_TRADE_PORT=200`) found exactly |
| Y472 | TRADE_MERCHANT_PRESENT is a bonus on income, not trade efficiency | CONFIRMED | file read: `defines.lua:1201` | `TRADE_MERCHANT_PRESENT = 0.1, -- bonus on income if trade present` |
| Y136 | the two coefficients differ in kind: GP_COEFF read, TAX_COEFF not | CONFIRMED | file read: `solver.py` | `GOODS_PRODUCED_FACTOR = _read_gp_coeff()` (file read) vs `TAX_COEFF = 1.0` (literal) |
| Y137 | v3.0-v5.0 said neither coefficient was in a file, shipped a sweep that walked past the block holding one | PARTIAL | file read: `../v3-owner-agnostic/per-good-trade-spec.md` (v4/v5 byte-identical at same lines) | confirmed: "The two wealth coefficients of §1.3 are hardcoded in the binary — `defines.lua` and `common/defines/` were searched and contain neither" (v3 L544-545); the "whole-install modifier sweep that walked past the block" detail has no surviving script in `v3/v4/v5-owner-agnostic/` (no `scripts/` directory exists in any of them) and could not be independently verified |
| Y473 | GP_COEFF=0.2, measured on 4 provinces/4 dev levels (Caceres/Girona/Garnatah/Barcelona) | CONFIRMED | measurement: `val62p4_garn.py` on `VANILLA_start.eu4` | base_production values 2,3,4,5 confirmed for Caceres(1747)/Girona(212)/Garnatah(223)/Barcelona(213) exactly, matching arithmetic 0.2×dev = 0.40/0.60/0.80/1.00 |
| Y474 | TAX_COEFF=1.0, measured on 2 provinces/2 dev levels (Garnatah/Caceres) | CONFIRMED | measurement: `val62p4_garn.py` | Garnatah base_tax=6, Caceres base_tax=2, matching arithmetic 1.0×base_tax = 6.00/2.00 |
| Y475 | both read off base tooltip lines, no owner term; Garnatah also has local_autonomy=0 | CONFIRMED | measurement: careful brace-matched block extraction of province 223 from `VANILLA_start.eu4` | `local_autonomy` does not appear anywhere in Garnatah's true 3,291-char block (absent field defaults to 0); note: an earlier fixed-offset scratch script (`val62p4_quant.py`) misread "2.500" here by overrunning the block boundary into province 224 (Sevilla) — re-verified with proper brace matching |
| Y476 | neither coefficient read off a province window (owner modifiers, some randomised) | CONFIRMED | design/methodological stipulation, consistent with §1.3's randomised-owner-modifier discussion | no contradiction found |
| Y477 | prices from common/prices/00_prices.txt at runtime, never hardcoded | CONFIRMED | file read: `solver.py`, EU4 install | `PRICES` loaded via `pdx.load(...00_prices.txt...)`; file exists |
| Y478 | design constants: excluded-goods (gold default), P₀=2.0, α_Φ=2.0, TIE_EPS=1e-3/TIE_EPS2=1e-6, r=0, tolerance 1e-11 | CONFIRMED | file read: `solver.py` (`EXCLUDED={"gold","unknown"}`), `flowop.py` (`TIE_EPS=1e-3`,`TIE_EPS2=1e-6`,`ZERO_TOL=1e-11`), `drain.py` (`dilate_r=0` default), `round6.py`/`measure6.py` (`A_PHI=2.0`, `ALPHA=...PRICES[g]/2.0...`) | all values match exactly |
| Y479 | α_Φ/TIE_EPS/TIE_EPS2 are hyperparameters, developer taste, no justification offered | CONFIRMED | file read: spec text L1083-1089, self-referential | document explicitly withdraws all prior derivations for α_Φ |
| Y339 | changing hyperparameters is a design decision; §1.6 records sensitivity | CONFIRMED | measurement: `measure6.py`'s α-sweep rows | "sink count at alpha 1,1.5,2,3,4,8: [3, 1, 2, 2, 1, 1]", European-scaling rows, wealth-noise rows all present |
| Y996 | TIE_EPS/TIE_EPS2 set the Phase-2 objective per the printed formula | CONFIRMED | file read: `flowop.py` | `TIE_COST = 1.0 + TIE_EPS*(_a1+_a2)/2.0 + TIE_EPS2*_gen` where `_gen=frac(min·max·7919)` — matches the printed formula exactly |
| Y1026 | two cost terms differ; only the first-order term means anything | CONFIRMED | derivation, consistent with design framing | matches spec text |
| Y1027 | second-order term is tie-breaking only, arbitrary form | CONFIRMED | derivation | consistent |
| Y998 | single cost vector doesn't guarantee uniqueness; depends on b too | CONFIRMED | derivation | standard LP-degeneracy fact: reduced-cost zero depends on both cost and constraint/RHS structure |
| Y1028 | zero-reduced-cost arcs: aggregate 40 (unit)→0 (first-order); per-good 41 across 18/29 goods | CONFIRMED | measurement: ran `p3_margin.py` | exact match: "unit aggregate 40 ... first aggregate 0 ... per-good total 41 on 18 of 29 goods" |
| Y1029 | adding second-order term → 1 arc on 1 good | CONFIRMED | measurement: `p3_margin.py` | "full ... per-good total 1 on 1 of 29 goods \| goods still carrying one: {'paper': 1}" |
| Y1030 | solver's optimality tolerance is a correctness requirement, not a performance knob | CONFIRMED | measurement: `round6.py` HiGHS-floor test | a rejected `1e-11` silently reverts to the 1e-7 default with `success=True`, "the failure is silent unless stderr is read" — exactly the correctness risk claimed |
| Y1053 | scipy `linprog(method="highs")` docs default dual/primal tolerance = 1e-07 (scipy 1.18.0) | CONFIRMED | measurement: read `scipy.optimize._linprog_highs._linprog_highs`'s docstring; checked `scipy.__version__` | installed scipy is exactly 1.18.0; docstring: "Dual feasibility tolerance. Default is 1e-07." / "Primal feasibility tolerance. Default is 1e-07." |
| Y1031 | margin as low as 3.8e-8 sits inside the 1e-7 default tolerance | CONFIRMED | measurement: `p3_bisect.py`/`p3_margin.py` | worst per-good margin (`copper`) 3.7648e-08 < 1e-7 default |
| Y1032 | 6 permutations: copper/paper differ on 12/8 edge-slots, objectives differ 7.7e-10 rel | PARTIAL | measurement: ran `p3_perm.py` section B (default tolerance) | paper: sum of flips over 6 perms = 8 (exact match), max spread 7.66e-10 ≈ claimed 7.7e-10 (exact match); copper: flips `[0,0,2,0,0,2]`, sum=4/max=2, not 12 — reproducibly (re-ran twice, identical, deterministic with the fixed seed) |
| Y1054 | mechanism confirmed by bisecting the tolerance against copper | CONFIRMED | measurement: `p3_bisect.py` | tolerance sweep shows unset≡1e-7, both giving nonzero flips, 1e-8/1e-9/1e-10 giving zero, across all 3 tested seeds |
| Y1055 | unset and 1e-7 give the same 8 flips over 4 permutations | PARTIAL | measurement: `p3_bisect.py`, 3 seeds | unset≡1e-7 confirmed exactly (identical pair for all 3 seeds); specific count of 8 reproduces only for one seed (7): results were (2,4),(8,10),(6,8) for seeds (20260821,7,4242) — the "8" figure is seed-dependent, not the default seed's own value (2) |
| Y1056 | 1e-8 already gives 0 flips, first value below copper's 3.765e-8 margin | CONFIRMED | measurement: `p3_bisect.py` | tol 1e-08 → (0,0) for all 3 seeds; 1e-8 < 3.765e-08 margin, and 1e-7 (next value up) still gives nonzero flips |
| Y1057 | flips appear exactly when tolerance exceeds the margin | CONFIRMED | derivation from Y1054-Y1056's measurements | consistent with the bisection data |
| Y1033 | flowop.LP_OPTS ships 1e-10, HiGHS's floor, spread 1.1e-15 | CONFIRMED | file read: `flowop.py`; measurement: `round6.py` (1e-11 rejected) | `LP_OPTS={"dual_feasibility_tolerance":1e-10,"primal_feasibility_tolerance":1e-10}`; 1e-11 explicitly rejected with "Invalid option value"; spread measured 1.03e-15/8.43e-16 in my reproduction (order-of-magnitude match to claimed 1.1e-15) |
| Y1034 | no figure moved when pinned tolerance went in; shipped order already reached true optimum | CONFIRMED | derivation from Y1032/Y1054 data | only copper/paper are order-dependent, and only under permuted column order at default tolerance — the identity/shipped order's own result is unaffected by the tolerance choice |
| Y1035 | 2nd-order term costs 0.1-0.2 self-coherence points; sinks 2-8/mean 3.69; all 29 acyclic; Φ_w sinks unchanged; ±1% noise 0 edges moved on 6 seeds | PARTIAL | measurement: custom script comparing self-coherence under first-order-only vs shipped (full) cost, correcting for `drain.TIE_COST` binding; `measure6.out`; wealth-noise rows | sinks-per-good (2/8/3.69), all-29-acyclic, and Φ_w sinks unchanged under ±1% noise (3 seeds shown, all `['genua','hangzhou']`) all confirmed exactly; self-coherence delta measured smaller than claimed: edge-goods 55.17%→55.13% (Δ0.04pt), value-weighted 54.88%→54.79% (Δ0.09pt), vs claimed 0.1-0.2 points |
| Y1036 | 2nd-order term replaces an arbitrary+order-dependent tiebreak with an arbitrary-but-invariant one | CONFIRMED | derivation, consistent w/ Y1014/Y495/Y141's measured order-invariance | matches spec framing |
| Y1037 | normalisation is load-bearing for 2nd-order term, wasn't for 1st-order alone | CONFIRMED | measurement: `p3_norm2.py` | real per-good/aggregate differences appear across normalisations under the full (2nd-order) cost, unlike the linear 1st-order-only case |
| Y997 | every DRAIN solve uses TIE_COST; unit costs kept only for FLOW/TREE ops and verify.py checks | CONFIRMED | file read: `flowop.py:mincost_flow` | docstring: "cost=None means unit arc costs. The per-good operators in this file and in verify.py use unit costs; DRAIN's Phase 2 passes TIE_COST." |
| Y483 | DLC state is a third input axis (treasure fleet/caravan power DLC-conditional) | CONFIRMED | design stipulation, consistent with §3.11/§3.12's caravan/treasure-fleet discussion | no contradiction found; standard EU4 DLC architecture (Wealth of Nations/Conquest of Paradise) |
| Y1039 | normalisation choice is a third arbitrary decision, recorded not defended | CONFIRMED | file read: spec text L1174-1175 | "The choice is recorded rather than defended: min-max is what the implementation uses..." matches near-verbatim |
| Y480 | DRAIN's knobs at defaults: r=0, tolerance 1e-11 (ρ withdrawn, never shipped) | CONFIRMED | file read: `drain.py:phase1(core, beta, dilate_r=0)` signature; §3.13 text | no `ρ` parameter in shipped Phase 1; §3.13 confirms ρ=0.5 belongs only to "its own Phase 1" of the (unshipped) calibration option |
| Y481 | zero-flow tolerance is absolute, not relative, couples to scale of b | CONFIRMED | file read: §3.13 text | "The tolerance is absolute, and v2.1 was wrong to file it as purely numerical" |
| Y482 | calibration option (§3.13) replaces Phase 1, moves the tolerance, removes α's clamp; baseline doesn't use it | CONFIRMED | file read: §3.13 text | "a demand-mass quantile ρ=0.5 in its own Phase 1", "a twig tolerance of 3e-4" (replaces 1e-11), "α unclamped at exponent 2 (cloves reaches α=16)"; "The baseline does not adopt it" |
| Y999 | structured term (absdiff) tried/rejected: both costs make all 159 edge costs distinct; structured leaves 11/29 goods w/ alternative optimum vs shipped's 1 | CONFIRMED | measurement: `p3_margin.py` | "full 159 distinct...absdiff 159 distinct" (both 0 duplicates); "absdiff...goods still carrying one: {chinaware, cloth, cocoa, copper, cotton, gems, glass, paper, spices, tropical_wood, wine}" = 11 goods exactly |
| Y1038 | normalisation sweep: world-total moves aggregate 7/159 edges; 13/29 per-good graphs move under ≥1 candidate | CONFIRMED | measurement: ran `p3_norm2.py` | exact match: "total 7/159" agg edges; "union over max/mean/total/totalxN, vs min-max: 13 of 29" |
| Y1115 | \|w[u]-w[v]\| telescopes along a path; frac(lo·hi·7919) has no such structure | CONFIRMED | derivation | correct: a telescoping sum of absolute wealth differences along any path collapses toward endpoint-dependent behaviour, unlike the multiplicative-fractional term |
| Y1116 | w/mean and N·w/sum are algebraically the same vector; on this field so are min-max and w/max | CONFIRMED | measurement: `p3_norm2.py` | mean and totalxN produce identical downstream results (0/159 agg, identical 9-good sets, both at LP_OPTS and default tol) though the script's strict bit-identity check reports `False` (floating-point computation-order noise, not an algebraic difference); min-max vs max: 0/159, 0/29, 0/29 differences confirms exact equivalence (minimum wealth is exactly 0) |
| Y1117 | cape_of_good_hope holds no counted province: 20 members = 1 sea zone + 19 land, none owned at 1444 | CONFIRMED | measurement: `val62p4_cape.py` | "cape_of_good_hope members: 20"; "sea zones among members: [1460]"; "land provinces: 19"; "land members OWNED at 1444: 0 []"; model node wealth 0.0, global min |
| Y1118 | re-measuring probe must inherit LP_OPTS: unpinned w/mean gives 5 goods, pinned gives 9, 5 ⊂ 9 | CONFIRMED | measurement: `round6.py`, `p3_norm2.py` | pinned: 9 goods `{copper,cotton,fish,gems,glass,grain,paper,wine,wool}`; default: 5 goods `{copper,fish,gems,grain,paper}` — a strict subset |

## §2.4 — The tradenodes file (28 claims)

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y484 | generated once from campaign start's Φ_w, owned by DLL in memory, no per-session regen | CONFIRMED | design stipulation, consistent with rest of §2.4-§2.6 | no contradiction |
| Y485 | engine validates (not sorts) declaration order, logs once per violation, tolerates violations | CONFIRMED | file read: `../v2-drain/game-session.md` (B2) | 159/159 backward links logged exactly once each; game loaded and ran to Dec 22 1444 without incident |
| Y486 | 159/159 backwards file logs 159 errors, loads/plays normally, total/retention unchanged, power fields within run-to-run variance | CONFIRMED | file read: `../v2-drain/game-session.md` (B2, Observation 3) | `total` 1/79 differ max 0.012% (identical to the noise-only control); `retention` 0/80 differ in both; `current`/`local_value`/`outgoing` differ by a similar magnitude in noise vs test |
| Y487 | 2-node cycle → STACK_OVERFLOW at one address, 1002 frames, reproduced on THREE launches, controls load fine | PARTIAL | file read: `../v2-drain/game-session.md` (B1) | crash address `0x00007FF6DDE6A8B4`, 1002 frames, and both controls (vanilla, pgt_permute) loading fine are all confirmed exactly; but the source states "reproduced on **two** independent launches" / "stack overflow, **twice**" — not three, contradicting the document's own cited source |
| Y488 | crash dump records no per-frame addresses | CONFIRMED | file read: `../v2-drain/game-session.md` | trace shows "1 … 1002 eu4.exe (function-name not available)" — no per-frame addresses given |
| Y489 | acyclicity is a hard correctness requirement, established by observation | CONFIRMED | file read: `../v2-drain/game-session.md` (B1+B3 conclusion) | "Combined with B1, the constraint on the generator is exactly one thing — the emitted graph must be acyclic" |
| Y490 | reversed link honoured completely: Valencia moves sides, Sevilla becomes end node, merchant switches steer→collect, two countries vanish | CONFIRMED | file read: `../v2-drain/game-session.md` (B3) | "zero non-descriptor lines"; Valencia moved incoming; Sevilla "five incoming links, zero outgoing"; Castile's merchant switched to "collecting"; Aragon(14.2)/France(3.3) "disappear from the node entirely" |
| Y491 | (restates Y490's specific findings) | CONFIRMED | same as Y490 | same evidence |
| Y492 | every provincial power figure unchanged in that test | CONFIRMED | file read: `../v2-drain/game-session.md` (B3) | "every *provincial* power figure stayed identical (Castile 129.9, Portugal 88.5, Morocco 23.4, Granada 11.6)" |
| Y493 | that test is the mod's core premise verified end to end | CONFIRMED | file read: `../v2-drain/game-session.md` (B3) | "This is the mod's core premise demonstrated end to end" — near-verbatim match |
| Y494 | item 1: emit in decreasing Φ_w marking order, matches engine convention, vanilla file 0/159 violations | CONFIRMED | measurement: `val62p4_tn.py` | vanilla: "order-violations=0" of 159 links |
| Y138 | canonical order still required but no longer decides the installed map | CONFIRMED | derivation, consistent with Y139-Y143's measured order-invariance | matches spec narrative |
| Y495 | Phase 2 min-cost b-flow degenerate under unit costs: many distinct equal-cost supports | CONFIRMED | measurement: `relabel6.py` | "unit cost (former)...permutations returning a DIFFERENT optimal support: 40 of 40" |
| Y139 | 40 of 40 permutations return a different optimal support (unit cost) | CONFIRMED | measurement: ran `relabel6.py 60 4242 7 999` | exact match: "unit cost (former)...40 of 40" |
| Y140 | those permutations reach an objective identical to within a few ULP | CONFIRMED | measurement: `relabel6.py` | "unit cost (former) objective 0.789420000928 max rel deviation 5.626e-16" (≈2.5 ULP) |
| Y147 | §2.3 now breaks those ties inside the objective | CONFIRMED | file read: `flowop.py` `TIE_COST` | consistent with Y996 |
| Y141 | 0/40 under tie-break cost; 180 relabellings → 0/159 edges; instrument aborts on old objective (26/159 disagreement) | CONFIRMED | measurement: ran `relabel6.py 60 4242 7 999` (180 total) + custom script comparing unpatched (unit-cost) reimplementation vs shipped support on identity | tie-break: "0 of 40"; pooled 180: "orientation changed: 0 of 180", "edges moving: mean 0.00, range 0-0"; unpatched-vs-shipped comparison: 26 of 159 undirected edges disagree exactly (52 directed mismatches / 2) |
| Y143 | 2nd-order term takes per-good sensitivity 84/290→13; pinning tolerance takes it to 0/290 | PARTIAL | measurement: ran `val62p4_relab.py` and cross-validated with `p3_relabel_pergood.py` (same seed 20260821, both give identical results) | reproduced: first-order-only(default tol) 80/290 (doc: 84), full-cost(default tol) 12/290 on 2 goods (doc: 13), full-cost+LP_OPTS 0/290 (doc: 0, exact match) — final figure confirmed exactly, intermediate figures off by small but deterministic/reproducible amounts |
| Y146 | counts HiGHS-specific in detail, not in kind: any simplex returns a vertex of a degenerate face | CONFIRMED | derivation | standard LP theory: any simplex-family solver returns a basic feasible (vertex) solution |
| Y144 | v6.0's 580/580 sweep script measures unit-cost objective, superseded by 13-of-290 | CONFIRMED | file read: `../v5-owner-agnostic/scripts/_audit_b_1444perm.py` and its dependency `_audit_b_drain.py` | script exists; `_audit_b_drain.py` line 58: `linprog(c=np.ones(len(arcs)), ...)` — confirmed unit cost |
| Y148 | priority key ties in more places than §1.1 documents; none fire on 1444 (0 exact ties of all three kinds) | CONFIRMED | measurement: ran `props6.py` | "exact (DEF,b) key collisions: 0", "Phase-1 within-cluster argmin ties: 0 ; top-k cut ties: 0" — all three zero counts match exactly |
| Y149 | end-flag list is a function of the world, not node order: {genua,hangzhou} in all 180 relabellings | CONFIRMED | measurement: `relabel6.py` (180 relabellings) | "sink-count distribution: {2: 180}"; "most frequent end holders: [('genua', 180), ('hangzhou', 180)]" — both nodes present in all 180 runs, sink count always 2 |
| Y150 | 1444 has two end nodes (genua, hangzhou) vs vanilla's three | CONFIRMED | measurement: `measure6.py` + `val62p4_tn.py` | measure6.out: "Phi_w sinks ['genua', 'hangzhou']"; vanilla file's `end=yes` nodes: `['genua', 'venice', 'english_channel']` (three) |
| Y498 | end count not fixed, follows wealth field and α_Φ, emitter reads it from the solve | CONFIRMED | consistency with measure6.out's α-sweep ("sink count at alpha 1,1.5,2,3,4,8: [3,1,2,2,1,1]") | count genuinely varies with α_Φ |
| Y499 | item 3: link reversal = move outgoing block, reverse path list, reverse control pairs | CONFIRMED | file read: `../v2-drain/game-session.md` (B1 setup) | shows exact worked example: `path = 4546 223 222 221 1295` → reversed `1295 221 222 223 4546`; `control` pairs likewise reversed |
| Y500 | item 4: location/members/inland=yes/ai_will_propagate_through_trade/unrecognized keys round-trip byte-faithfully | CONFIRMED | file read: `../v2-drain/game-session.md` | "Verified after writing: 80 blocks, 160 links, the 2-cycle present, everything else byte-faithful" |
| Y496 | node window renders incoming/outgoing link lists in file declaration order | CONFIRMED | file read: `../v2-drain/game-session.md` (B2, Observation 4) | "the node window's link lists are rendered in file declaration order, so an emitter that reorders nodes will reorder what the player sees" — near-verbatim match; vanilla lists "Tunis, Safi", reversed file lists "Safi, Tunis" |
| Y497 | item 2: end=yes on every Φ_w sink, stripped from any former end node gaining outgoing links | CONFIRMED | design stipulation, consistent with measured end-set behaviour (Y149/Y150/Y498) | no contradiction |

**Continuity note:** this fragment extends prior scratch work under `scripts/val62p4_*.py` (all read, re-run, and — where a bug was found — corrected in place rather than trusted; see Y475's note on `val62p4_quant.py`'s block-overrun bug, resolved with a fresh brace-matched read). One new instrument was added: `scripts/val62p4_selfcoh.py` (Y1035's self-coherence delta, correcting for `drain.py`'s `from flowop import TIE_COST` name-binding, which an earlier draft of the same idea in this session had missed). `scripts/val62p4_oldobj_diff.py` was added inline to settle Y141's parenthetical (26-of-159 disagreement).
