# Validation — Per-Good Trade Network Spec v6.6, round 11 (delta-scoped)

**Document under test:** `per-good-trade-spec.md`, 2,283 lines, MD5 `48414cb316bd6b3c3355b1b87afdc3e2`, version stamp 6.6.

**Census validated:** `claims-delta-round11.md` — 1,190 rows (`Y001`–`Y1372`, with gaps), of which 1,189 are live.

---

## Carry statement

This round is **delta-scoped**. The prior validation, `validation-round10.md`, graded
`claims-delta-round10.md`'s 1,172 rows against spec v6.5 (MD5 `f9a70dfd859e1c97b266c35de4a1b228`)
and returned **1,156 CONFIRMED / 8 PARTIAL / 0 REFUTED / 8 UNTESTABLE**. All 8 of those PARTIALs
are in this round's scope — `Y483`, `Y1275`, `Y1291`, `Y1331`, `Y1341`, `Y1342` and `Y1352` are
CHANGED, and `Y375` is pulled in by the brief — so **no PARTIAL is carried into this file
ungraded**.

**Every row of the current census not listed in the scope below carries its round-10 verdict
unchanged, recorded once here and not re-asserted per row.** That carry is licensed by the
reconstruction check in *Footing* below: `round11.diff` applied to the frozen v6.5 baseline
reproduces the current spec byte-for-byte, so every line outside the diff's 13 hunks is identical
to the text round 10 graded.

**Carried: 1,157 rows** — the 1,158 UNCHANGED rows less `Y375`, which is pulled into scope because
its round-10 verdict was PARTIAL. Their carried verdicts are **1,149 CONFIRMED and 8 UNTESTABLE**.
The 8 UNTESTABLE rows are not re-investigated; each is examined below only for whether anything
this round changed its testability.

**Freshly graded here: 32 rows** — 9 CHANGED, 4 REWORDED, 18 NEW, and `Y375`. Plus the soundness
of the 1 REMOVED row (`Y1353`), which is a removal check rather than a claim verdict.

---

## Footing — the census's own accounting

| check | result |
|---|---|
| Spec MD5 | `48414cb316bd6b3c3355b1b87afdc3e2`, 2,283 lines — matches the brief and the census header. |
| Frozen baseline MD5 | `per-good-trade-spec-v6.5-round11-frozen.md` = `f9a70dfd859e1c97b266c35de4a1b228`, 2,270 lines — matches the brief, and matches the document round 10 states it graded. |
| **Diff reconstructs the spec** | **YES.** `patch --binary` of `round11.diff` (144 lines, 13 hunks) onto a fresh copy of the frozen baseline yields MD5 `48414cb316bd6b3c3355b1b87afdc3e2`; `cmp` against `per-good-trade-spec.md` reports no difference. All 13 hunks applied clean — no fuzz, no offsets, no rejects. Text outside the hunks is therefore byte-identical, which is what licenses the carry. (`scripts/r12/_recon.md` is the reconstructed artifact.) |
| Census row count | 1,190 rows matching `^\| Y\d+ \|`; 1,190 distinct IDs, none used twice; highest ID `Y1372` — matches the summary line. |
| Status partition | U 1,158 + RW 4 + C 9 + R 1 + N 18 = 1,190 ✓. Live rows 1,189 (1,190 less the retired `Y1353`) — matches the census's own arithmetic (1,172 live in, 1,171 carried live out, +18 NEW). |
| **Carry integrity against round 10** | **YES.** All 1,172 of round 10's live IDs appear in this census; round 10 retired nothing. The only IDs new to this census are `Y1355`–`Y1372`. No number was reused. |
| **CHANGED set = brief's list** | **YES** — exactly 9 IDs, set-equal to the brief: Y483 Y1158 Y1275 Y1291 Y1326 Y1331 Y1341 Y1342 Y1352. |
| **REWORDED set = brief's list** | **YES** — exactly 4: Y541 Y616 Y783 Y1159. |
| **NEW set = brief's list** | **YES** — exactly 18, contiguous `Y1355`–`Y1372`, no gaps in the span and no NEW row outside it. |
| **REMOVED set** | **YES** — exactly 1: `Y1353`. |

**Footing verdict: CONFIRMED.** The diff reconstructs the spec from the frozen baseline byte-for-byte,
and the work list matches the census's C/RW/N/R sets exactly.

---

## Method

Each in-scope claim was graded against primary sources: the EU4 1.37.5 install at
`C:\Program Files (x86)\Steam\steamapps\common\Europa Universalis IV` — its `interface/`,
`common/`, `localisation/`, `patchnotes/` and `dlc/` trees plus `eu4.exe`'s string table — the two
readable saves (`VANILLA_start.eu4`, `Castile1444_12_22.eu4`), the reference implementation in
`scripts/`, and the lineage directory `..\v1-laplacian\`. Measured figures were re-derived by
instruments written for this round rather than by re-running the round-11 probes alone.

Fresh instruments for this round are in `scripts\r12\`:

- `lv.py` — independent reconstruction of node `local_value` from the save (`Y1331`, `Y1363`, `Y1364`).
- `aut.py` — the autonomy half of §3.4's evidence note (`Y1366`, `Y1367`, `Y1369`).
- `byowner.py` — engine/model ratio grouped by province owner, the control that separates owner
  modifiers from autonomy.
- `cells.py`, `miss.py`, `pblk.py` — cell membership and province-block reads behind the above.
- `gate.py` — the per-link both-ends measurement on `Castile1444_12_22.eu4` (`Y1158`, `Y1326`, `Y375`).
- `_recon.md` — the patched reconstruction used in *Footing*.

Four file-search probes were run by subagents (model sonnet): the patchnote archive, the
`our_from_this` string search, the both-ends evidence sweep, and the DLC gating sweep. **Every
finding those probes returned that produced a non-CONFIRMED verdict was re-derived by the lead
before entering this file** — specifically the round-5 gate measurement (re-run as
`scripts/r12/gate.py`), the v1 `C102` lineage rows (re-read from `../v1-laplacian/claims.md` and
`validation.md`), and `1.16 Patchnotes.txt` L42 (re-read from the file).

---

## CHANGED — the 9 rows whose assertion moved

| ID | Verdict | Method | Evidence |
|---|---|---|---|
| Y1275 | **CONFIRMED** | Read §1.8 L817-823; re-derived the `local_value` identity with `scripts/r12/lv.py`; read the autonomy blocks of `common/static_modifiers/00_static_modifiers.txt`. | The re-scoped warrant is accurate and is a strict weakening of what it replaced. The identity the sentence now leans on — Σ_g `trade_goods_size`(n,g) × price(g) ÷ 12 — has no efficiency and no autonomy factor in its form, and it reproduces the engine's own `local_value` digit-for-digit on 57 of the 79 nodes that carry the field. The substance is independently corroborated from a shipped file the sentence does not cite: `00_static_modifiers.txt` L501-508's `local_autonomy_multiplicative` block — the block that scales with autonomy — grants `local_manpower_modifier`, `local_sailors_modifier`, `local_tax_modifier`, `local_production_efficiency = -1.0`, `province_trade_power_modifier = -0.5` and two forcelimit modifiers, and grants **no** `trade_goods_size_modifier`. Autonomy therefore reaches production efficiency and trade power but not goods produced. The delegation clause is true: §3.4 L1734 does carry the evidence note (`Y1359`). |
| Y1158 | **PARTIAL** | Re-read §1.8 L850-855; independent grep of `common/defines.lua`, `common/defines/`, all of `localisation/` and `eu4.exe`'s string table; reproduced the round-5 per-link gate measurement myself as `scripts/r12/gate.py` on `Castile1444_12_22.eu4`; read the save's `meta` and `../v2-drain/game-session.md`. | **The file half is exhaustively confirmed.** No define states the rule — the trade block of `defines.lua` (L1190-1206, inside `NEconomy`; there is no `NTrade` block) names nothing about two nodes or a receiving-side condition. In `localisation/`, `both ends`, `both nodes` and `power in both` return zero hits; `both have power` returns exactly two, `TRANSFER_TRADE_POWER_TO_DESC` and `TRANSFER_OTHERS_TRADE_POWER_TO_DESC` (`core_l_english.yml:281,284`), which belong to the country-to-country **diplomatic action** `transfer_trade_power` (`common/new_diplomatic_actions/00_diplomatic_actions.txt:21`), a different mechanic. The one trade-adjacent `both` string in `eu4.exe` is a `tradenodedatabase.cpp` data-validation message about a province listed in two node definitions. **The v1 lineage half is confirmed verbatim** (see `Y1360`). **The half that fails is the new absence clause "and no session has observed it."** Read narrowly — no live session has watched a node window and seen the gate — it is true, and it matches §2.7 item 19's own use of "session". But the tree holds a reproducible save-derived measurement of exactly this rule, recorded in its own validation history at `validation-v6-round5-part2.md:195,199` (graded CONFIRMED there), and I reproduced it figure-for-figure: over all 159 links of `Castile1444_12_22.eu4`, 50 carry zero value and **33 of those have no country holding power at both ends**, while of the 109 value-carrying links **only 3 lack one** (χ² = 78.3, 1 df). That save is the recorded artifact of the session in `../v2-drain/game-session.md` (its own L170 and L617 name it), so the blanket absence passes over evidence the document itself produced. The same run also carries **three direct counterexamples to the rule as an absolute** — `cuiaba → brazil` 0.085, `rio_grande → mississippi_river` 0.129, `california → mississippi_river` 0.012, each between nodes whose power-holder sets are disjoint — and 17 zero-value links with up to 9 both-ends holders. **What would settle it:** §2.7 probe 19 as written, plus a decision on whether save-derived per-link evidence counts against the clause; the save in hand is a modded run (`mod/pgt_permute.mod`, per its `meta`) and the session's own finding is that EU4 trade values are not reproducible run to run, so the measurement bounds the rule without settling it. |
| Y1291 | **CONFIRMED** | Read `interface/tradeinterface.gui` around L246-256 and L714-724; grepped the whole install for `our_from_this`; grepped `localisation/` for the key and its plausible variants. | The re-scoping is exactly right. `our_from_this` is a real widget name at `tradeinterface.gui:248` (inside `TradeNodeInterface`) and `:716` (inside `TradeCompanyNodeInterface`); its block carries `text = "0"` — a literal placeholder, not a localisation key — and no `tooltip`, `tooltipText`, `tooltip_text` or `desc` property. Its four sibling value fields each have a paired `*_label` widget carrying a real `TN_*` key; `our_from_this` and `piracy_value` have none. So nothing in the shipped files glosses the field, and "the country's own take" is indeed an inference from the widget's name and nothing else. The flat gloss it replaced asserted a reading the files do not supply. |
| Y483 | **CONFIRMED** | Read §2.3 L1319; read `common/disasters/decline_of_mali.txt`; enumerated every `caravan_power` and `treasure_fleet` occurrence across `common/`, `events/`, `decisions/`, `missions/`, `localisation/`, `interface/` and checked each bearing file for an enclosing `has_dlc`; listed and grepped the `dlc/` archives. | This was round 10's one carried PARTIAL, and the re-scoping closes it. Each factual component holds. The cited gate is real: `decline_of_mali.txt` L4 puts `has_dlc = "Origins"` in the disaster's `potential` block, which controls the whole disaster including the `modifier` block at L36-41 carrying `caravan_power = -0.33`. "Named by no shipped file" holds for the mechanics themselves: of the 34 script-side `caravan_power` occurrences across 16 files, only `decline_of_mali.txt` has an enclosing DLC gate and it gates a modifier *value*; `merchant_republic`'s `caravan_power = 0.33` sits in an unconditional `modifiers` block while the `has_dlc = "Res Publica"` beneath it gates `can_use_trade_post`. For treasure fleets, **no file under `common/`, `events/`, `decisions/` or `missions/` contains both `treasure` and `has_dlc`**, and no shipped script carries the diversion logic at all. "Readable even when inert" holds: `CARAVAN_POWER_MAX = 50` / `CARAVAN_POWER_MIN = 2` / `CARAVAN_FACTOR = 3.0` sit in `defines.lua` L1220-1222 unconditionally, as do `TREASURE_FLEET_OPINION_HIT` and `TREASURE_FLEET_INFLATION`. The `dlc/` archives are art assets only — `dlc110_origins`, `dlc033_el_dorado` and `dlc095_golden_century` unzip to `gfx/` and `interface/*.gfx` with no script — so the logic really does live engine-side. *Residual, not a defect in this row:* the `dlc_load.json` toggle run the sentence names is not carried as a numbered §2.7 probe item (item 9 touches the DLC flag only for diverted gold), unlike the both-ends rule which this same round routed to item 19. |
| Y1326 | **PARTIAL** | Read §2.7 item 19 at L1502-1505; same searches and same reproduction as `Y1158`. | The probe item now mirrors §1.8, so the two sites agree — which is what the change set out to do — and the file half is confirmed on the same evidence. The failure is the same one, and it bites harder here because this site's verb is broader: §1.8 says "no session has **observed** it", but item 19 says "no define, string, searched file or recorded session **supports** it". Support is exactly what the round-5 save parse supplies. `Castile1444_12_22.eu4` is the recorded artifact of `../v2-drain/game-session.md`; parsing its 159 `incoming` blocks gives 33 of 50 zero-value links with no both-ends holder against 3 of 109 value-carrying links, χ² = 78.3 — a recorded-session-derived result that supports the rule, and which the tree recorded as CONFIRMED at `validation-v6-round5-part2.md:199` before rounds 9 and 10 re-graded the same claim without citing it. **What would settle it:** either run probe 19 as written, or narrow the clause to what is true — that no session *observed* the rule, and that the per-link save evidence is associative and carries three counterexamples. |
| Y1331 | **CONFIRMED** | Wrote `scripts/r12/lv.py` from scratch: parsed `change_price` from `VANILLA_start.eu4` for the slot-ordered price vector, parsed all 80 `node` blocks for `trade_goods_size` and top-level `local_value`, and compared under several rules. | Both halves of the correction check out, and the figure it replaced does not. The save has **80 node blocks, 79 of which carry a top-level `local_value`**; `cape_of_good_hope` is the sole node with no such field anywhere in its block. The save prints `local_value` to **3 decimals on all 79**. Truncating the reconstruction Σ_g `trade_goods_size` × price ÷ 12 to those same 3 decimals and comparing digit-for-digit gives **57 of 79** — and truncating both sides to 3dp independently gives the same 57, so the count is stable across two readings of §1.3's truncation convention. The old "58 of 80" is reachable under no tolerance rule I could construct — the neighbouring counts are 59 (2dp), 61 (abs tol 5e-3), 63 (abs tol 1e-2), 13 (exact float) — and my data corroborates round 10's account of the two ad-hoc routes that do reach 58, both of which require admitting a node that should not count: `philippines`, whose reconstruction is 3.010 against a printed 3.009 and which sits in my miss list, or `cape_of_good_hope`, which carries no field at all. Aggregate: recon 326.4253 against engine 337.7970, **−3.366%**, i.e. the unchanged "3.4% low", with the seven largest shortfalls all New World nodes (`chesapeake_bay`, `st_lawrence`, `ohio`, `mississippi_river`, `rio_grande`, `james_bay`, `california`). |
| Y1341 | **CONFIRMED** | Read §3.4 L1734; ran `scripts/r12/lv.py` and `scripts/r12/aut.py`; read `00_static_modifiers.txt` L501-508. | The row's own content is accurate. Replacing "by vanilla's own construction (verified two ways on the save)" — an unsourced warrant plus an unquantified gesture — with a named identity and a named direct check is a real improvement, and both named checks exist and were reproduced here: the identity (57 of 79, above) and the autonomy check (245 singleton cells, r = −0.0971). The exclusion the row asserts holds on independent file evidence too, since the autonomy-scaled static-modifier block grants no `trade_goods_size_modifier`. *The figure this row's sentence quotes alongside — "autonomy-heavy provinces reproducing the autonomy-free prediction exactly" — is censused separately at `Y1366` and is graded PARTIAL there; it does not travel on this row.* |
| Y1342 | **CONFIRMED** | Read §3.4 L1736-1738, §1.6 L557-570 and §1.3 L328-336; read `scripts/solver.py` L155-190 and `scripts/measure6.py` L18-25. | Naming the two excluded quantities instead of writing "neither quantity" is accurate on both counts. §1.6 defines `Φ_w = DRAIN(b_w)` with `s_w(n) = 1/N` uniform and `c_w(n) = Σ_{p∈n} wealth(p)^α_Φ / Σ_{q} wealth(q)^α_Φ` — so it reads the wealth field and nothing else. `V_g = Σ_n inject_g(n)` is defined three lines above and is used for display, link values and AI, never for `Φ_w`. The implementation agrees: `measure6.py` L19 builds `W = tax + prod_income` and L22-25 derives `cv()` and `ph()` from `W` alone. Production income is read nowhere — see `Y1370` for the summand qualification the row adds. |
| Y1352 | **CONFIRMED** | Opened `patchnotes/1.16 Patchnotes.txt` and read L42 myself; grepped the whole file for `75` and for `autonomy`. | The re-scoping is a correction, and a needed one. `1.16 Patchnotes.txt:42` does read "Implemented a new system called \"States &amp; Territories\", where states gives most benefits of being non-overseas, while territories have autonomy and is considered to be overseas for many rules." — so the L42 read is exact. But the string `75` **does not occur anywhere in `1.16 Patchnotes.txt`**, and no line of that file says the overseas autonomy floor is removed or superseded. The supersession is therefore genuinely an inference and not a quoted sentence, exactly as the new text now says; the old "the tree's own version archive settling both ends" claimed the archive closed a question it never addresses. |

---

## REWORDED — the 4 rows whose words moved but whose proposition did not

| ID | Verdict | Method | Evidence |
|---|---|---|---|
| Y1159 | **CONFIRMED** | Read §1.8 L854 against the frozen baseline's L853; read §3.16's evidence rule at L2242-2243; read §2.7 item 19 at L1502-1505. | The proposition survives word-for-word: "a probe-class fact under §3.16's own rule" is unchanged, and §3.16's rule does classify it that way — "Every engine fact in this spec must carry its source — a file path, a binary string, or a named observation — and a claim without one is a to-do, not a fact." The both-ends rule has none of the three, so it is a to-do. The dropped "recorded as such" loses nothing, because the recording it named is the §2.7 carry that survives in the same clause, and §2.7 item 19 exists and is that recording. |
| Y541 | **CONFIRMED** | Read §2.8's Malacca↔Cape pre-1500 row at L1516 against the baseline; checked the pointer's two referents; traced the v1 ancestry in `../v1-laplacian/claims.md`. | The proposition is word-for-word — "Corridor withheld by range and the power-at-both-ends gate … not by direction" — and the inserted "(both-ends: the model's reading, §1.8; probe §2.7 item 19)" names two referents that both exist and both say what the pointer says they say. The insertion is well targeted: this row's v1 ancestor is `claims.md:518` C299, whose own dependency list is "C100, C101, C102" — so the site really is a consequence of the rule now scoped at §1.8. |
| Y616 | **CONFIRMED** | Read §3.2 L1693-1695 against the baseline; same referent checks; traced the ancestry. | Sentence otherwise word-for-word; pointer accurate. v1 ancestor `claims.md:619` C385 — "Value only arrives where someone holds power at both ends of the link", provenance `derivation`, dependency `C102` — is the same sentence with the same parent, so the pointer restores a lineage the prose had lost. |
| Y783 | **CONFIRMED** | Read §3.15 L2190-2193 against the baseline; same referent checks; traced the ancestry. | The rejection, its two reasons and the twig-tolerance exception are word-for-word; pointer accurate. v1 ancestor `claims.md:963` C664 — "The power-at-both-ends gate already withholds unworked corridors", `derivation` from `C102`. Together with `Y541` and `Y616` this is exactly the set of three v1 claims that derive from C102, so "the both-ends rule's three consequence sites" is the right three and there is no fourth. |

---

## NEW — the 18 propositions `Y1355`–`Y1372`

| ID | Verdict | Method | Evidence |
|---|---|---|---|
| Y1355 | **CONFIRMED** | Read all 13 hunks of `round11.diff` and classified each. | No hunk changes an equation, a constant, a definition or a design decision. Hunk 1 is the version stamp; hunk 2 is the changelog paragraph itself; hunks 3-7, 11 and 13 are re-scopings; hunk 8 corrects a figure; hunks 9, 10 and 12 insert a pointer. Nothing in `scripts/` was edited to produce any of it — the 57 figure was already in the round-11 instrument's own output (`scripts/r11/L_localvalue.out`, "nodes at exactly 12.00x: 57"), so the correction is spec-side transcription, not a computation move. |
| Y1356 | **CONFIRMED** | Scanned every hunk for numeric changes. | Exactly one hunk changes a figure that was already in the document: hunk 8, "exactly on 58 of 80 nodes" → "digit-for-digit on 57 of the 79 nodes that carry the field". Hunk 11 introduces figures (57/79, 91%, pid 213, −0.1, 245) but as new text, not corrections. And the corrected figure does match its instrument: 57 of 79, reproduced independently at `scripts/r12/lv.py`. |
| Y1357 | **CONFIRMED** | Mapped the seven named statements to hunks. | All seven exist and each is re-scoped by a hunk: §3.4's aggregate parenthetical and save-evidence note (hunk 11, both), §1.8's `inject` evidence (hunk 3) and both-ends rule (hunk 4), §3.16's supersession inference (hunk 13), §1.12's `our_from_this` gloss (hunk 5), §2.3's DLC conditionality (hunk 6). *Noted, not charged:* hunk 7 re-scopes §2.7 item 19 as an eighth edited site, but it restates §1.8's both-ends re-scoping at its mirror rather than re-scoping an independent statement, which is how the census classifies it too. |
| Y1358 | **CONFIRMED** | Read hunks 9, 10 and 12. | Three consequence sites, three insertions, one identical pointer: §2.8's Malacca↔Cape row (L1516), §3.2 (L1694-1695), §3.15 (L2190). Each gains "(both-ends: the model's reading, §1.8; probe §2.7 item 19)" and nothing else. These are exactly the three sites the census carries as REWORDED, and exactly the three v1 claims descending from C102. |
| Y1359 | **CONFIRMED** | Read §1.8 L820-821 and §3.4 L1734. | §1.8's clause says "§3.4 carries the evidence note"; §3.4 L1734 carries it — "The evidence, at its class: the save's `local_value` identity … has no efficiency and no autonomy term in it; and the autonomy half is checked directly as well". The pointer resolves. |
| Y1360 | **CONFIRMED** | Opened `../v1-laplacian/claims.md` and `../v1-laplacian/validation.md` myself and read the rows. | Verbatim on every element. `claims.md:261`: `\| C102 \| §1.8 \| No transfer occurs into a node where nobody holds power at both ends. \| ENGINE \| UNSOURCED \| — \|`. `validation.md:1320` opens the section `### C102 — NEEDS_GAME`, restating the claim as `*ENGINE / UNSOURCED*` and recording "No file states this rule; it is not a define, a static modifier, or a scripted trigger." So C102 exists, its text **is** the both-ends rule rather than a cousin of it, its provenance is UNSOURCED and its verdict is NEEDS_GAME. |
| Y1361 | **CONFIRMED** | Grepped the whole install for `our_from_this`; grepped `localisation/` for the key and four variants; read the widget block and its neighbours; searched `eu4.exe` and the root DLLs. | The absence is exhaustive. `our_from_this` occurs in exactly two places in the entire install, both widget `name` fields in `interface/tradeinterface.gui` (L248, L716). `localisation/` has no `our_from_this`, `OUR_FROM_THIS`, `OURFROMTHIS` or `our_from` (the only `from_this` hits are `NOT_FROM_THIS_GOV`, a government-conversion string). The widget carries no tooltip property, and `our_from_this_label` does not exist anywhere. The single `eu4.exe` hit sits in a null-terminated C++ identifier table immediately after the source-path string `...graphics\tradeinterface.cpp`, among sibling widget names — a symbol, not a gloss. |
| Y1362 | **CONFIRMED** | Grepped the mod tree for `dlc_load`; read `../v2-drain/game-session.md` L32-34 and L598-601; read the `meta` of `Castile1444_12_22.eu4`. | Unprobed, as claimed. The only `dlc_load.json` use anywhere in the tree is the v2-drain session writing one **to select mods**, not to toggle DLC for a caravan or treasure-fleet comparison. The Castile save's `meta` lists all 22 DLC enabled, so no toggle contrast exists even incidentally. No session record anywhere tests the caravan grant mechanic or treasure-fleet diversion against a DLC flag. |
| Y1363 | **CONFIRMED** | `scripts/r12/lv.py`: parsed all 80 node blocks and counted `local_value` occurrences at node top level and anywhere in the block. | 80 node blocks; 79 carry a top-level `local_value`; no node carries two. The single node with **no `local_value` anywhere in its block** is `cape_of_good_hope`, and its `trade_goods_size` array sums to 0.0 — it produces nothing in 1444, which is why the field is absent. So the 79-of-80 denominator is right and the named exception is the right one. |
| Y1364 | **CONFIRMED** | `scripts/r12/lv.py`, written independently of the round-11 probe. | Both halves. The identity is as written: the save's `change_price` block is slot-ordered `["nogoods"] + GOODS` where `GOODS` is the 32-entry order of `common/tradegoods/00_tradegoods.txt` (verified equal), and every node's `trade_goods_size` array has exactly 33 slots. Σ_g size × price ÷ 12, truncated to the save's own 3 decimals, equals the engine's printed `local_value` digit-for-digit on **57 of the 79** nodes that carry it. |
| Y1365 | **CONFIRMED** | Read the identity as written; corroborated against `00_static_modifiers.txt` L501-508. | True of the identity's form on inspection — its only inputs are `trade_goods_size` and `price`. The claim is typed MODEL and is a statement about the written identity, and at that scope it is simply correct. Corroboration that the *inputs* are also clean: the autonomy-scaled static modifier block grants `local_production_efficiency` and `province_trade_power_modifier` but no `trade_goods_size_modifier`, so neither quantity can reach `trade_goods_size` through the shipped modifier keys. |
| Y1366 | **PARTIAL** | `scripts/r12/aut.py` and `scripts/r12/byowner.py`: built the 245 singleton (node, good) cells, took each cell's sole province's `local_autonomy` from the save, and compared the engine's `trade_goods_size` against the autonomy-free model prediction at every autonomy threshold; then grouped the same ratios by province owner as a control. | **The named instance holds exactly; the plural generalisation does not.** Barcelona reproduces the autonomy-free prediction to the digit (model 1.0000, engine 1.0000) at 91% autonomy. But of the **7 singleton cells whose sole province carries ≥50% `local_autonomy`, only 3 reproduce exactly** — `valencia`/glass (1.0000), `ganges_delta`/ivory (1.0000), `chengdu`/silk (1.0000) — while four miss: `lahore`/salt 1.1000, `crimea`/fish 1.1333, `crimea`/cloth 1.1333, `chengdu`/copper 0.7000. No threshold rescues the statement: ≥5% gives 45 of 77, ≥20% gives 14 of 27, ≥30% gives 6 of 15. High-autonomy cells in fact reproduce **less** often (3/7 = 43%) than zero-autonomy cells (77/128 = 60%). **The misses are not autonomy-shaped, and the control shows why**, which is the finding the sentence should be carrying instead: grouped by owner, GEO's four singleton cells all sit at ratio ≈1.133 — the two at 51-52% autonomy and the two at **0%** autonomy alike — so the deviation is an owner-level goods-produced modifier that autonomy does not touch. Three of the four misses are the engine sitting *above* the prediction, a direction autonomy cannot produce, and the one below (0.70 at 61% autonomy) is nowhere near the 0.39 a proportional autonomy penalty would give. This is stronger evidence for the exclusion than "reproduces exactly", and it is also the claim the document's own "no empirical absolutes" convention would prefer. **What would settle it:** restate as what was measured — e.g. that the engine/model ratio is flat in autonomy within owner, with r = −0.10 across the 245 cells and the 91% cell exact — or scope the plural to the named province. |
| Y1367 | **CONFIRMED** | `scripts/r12/aut.py`: read province 213's save block directly and cross-checked the localisation name and the model's cell assignment. | Every element checks. `VANILLA_start.eu4` province −213: `name="Barcelona"`, `owner="ARA"`, `trade="valencia"`, `trade_goods=glass`, `base_production=5.000`, `local_autonomy=91.000`. `localisation/prov_names_l_english.yml` carries `PROV213:0 "Barcelona"`. The model puts pid 213 in the `valencia` node on `glass` with `gp = 1.0`, and `scripts/r12/cells.py` confirms it is the **only** province in the `valencia` node carrying `glass`, so the cell really is a singleton and the engine's node quantity is attributable to it alone. |
| Y1368 | **CONFIRMED** | Read §2.3's `GP_COEFF` row at L1190 and §3.13's tooltip note at L2020-2027. | Both citations are already there. §2.3 L1190: "Four provinces, four development levels, from the `Base Goods Produced` line: Caceres (1747) … Girona (212) … Garnatah (223) … **Barcelona (213) 5 → 1.00**". §3.13 L2022-2023: "**Barcelona's production tooltip** read `Production Efficiency: +12.0% / From Technology: +2.0% / Producing Glass: +10.0%`". The two are also mutually consistent with the save: `base_production` 5 × `GP_COEFF` 0.2 = 1.00, which is exactly the engine's `trade_goods_size` for the cell — and §3.13's point, that glass's +10% books on production income rather than on goods produced, is precisely why the cell's engine/model ratio is 1.0000 despite the modifier. |
| Y1369 | **CONFIRMED** | `scripts/r12/aut.py`, recomputing the Pearson correlation from the save rather than trusting the round-11 run. | Both figures. The model has 693 (node, good) cells, of which **245 are singleton and all 245 carry an engine quantity** — so the denominator is exact and needs no qualification. Pearson r(`local_autonomy`, engine/model ratio) over those 245 = **−0.0971**, which rounds to the quoted −0.1. |
| Y1370 | **CONFIRMED** | Read §1.3 L329-336, §1.6 L565-570 and §3.4 L1736-1738; read `scripts/solver.py` L155-166 and `scripts/measure6.py` L19. | Exactly right, and it is a distinction worth the words. §1.3 L329-331 defines `trade_value(p) = goods_produced(p) · price(good(p))` and `wealth(p) = tax_value(p) + trade_value(p)`, and L336 already says "The `trade_value(p)` defined here is a component of `wealth(p)` and feeds orientation only." The implementation matches and flags the trap in its own comment: `solver.py` computes `prod_income = gp * price` with the note "prod_income is MISNAMED: goods_produced * price is the trade_value component of wealth (sec 1.3), NOT production income — no efficiency, no autonomy enters." `measure6.py` L19 builds the field as `W = tax + prod_income`. So the wealth field does carry `trade_value(p)` as a summand, and it is the orientation-side quantity rather than §1.8's `inject`. |
| Y1371 | **PARTIAL** | Opened `patchnotes/1.16 Patchnotes.txt` and read L42 myself; grepped the whole file for `autonomy`, `territor` and `75`. | **The autonomy half is verbatim; the replacement half is not in the archive and is in mild tension with the same line.** L42 reads "… while territories **have autonomy** and **is considered to be overseas for many rules**." So "territories carry autonomy" is a direct read. "In place of the overseas rule" is not: no line of the file says the overseas rule is displaced, `75` does not appear in the file at all, and L42's own second clause says territories remain overseas for many rules — which is the opposite of a clean replacement. The census types this row ENGINE with provenance "read from a file (the tree's patchnote archive)", and half of it is not read from the file; it is the inference the enclosing sentence declares. **What would settle it:** split the row, or carry the replacement half under the sentence's own inference provenance rather than as a file read. |
| Y1372 | **CONFIRMED** | Grepped every file in `patchnotes/` for `autonomy` co-occurring with `75`, and for `overseas` across all files for versions after 1.16; listed the directory to check the archive's coverage. | True, and the archive is complete enough to support it. Across all 30 patchnote files (1.8 through 1.37, one per minor version, **no gaps**), exactly one line pairs an autonomy floor with 75: `1.8 Patchnotes.txt:40`. The only other `75`+`autonomy` line in the tree is `1.32 Patchnotes.txt:751`, about an estate privilege, unrelated. Every `overseas` hit in 1.17-1.37 is about war goals, subjects, AI behaviour or mission text — none names a numeric autonomy floor. The claim is scoped to notes later than 1.16 and the coverage for that span is complete. |

---

## Y375 — carried UNCHANGED, re-graded because its round-10 verdict was PARTIAL

| ID | Verdict | Method | Evidence |
|---|---|---|---|
| Y375 | **PARTIAL** | Read the current gate sentence at §1.8 L850-851 and confirmed it byte-identical to the frozen baseline; re-ran the trade-range string checks; reproduced the per-link gate measurement as `scripts/r12/gate.py`. | **The trade-range half is file-sourced and reproduces**, unchanged from round 10: `HINT_TRADERANGE_TEXT`, `TRADE_RANGE_IRO` and `TRADE_NODES_OUT_OF_RANGE` all exist and all describe reach. **The both-ends half is still not established by the files** — no define, no string, no shipped file states it, on my own exhaustive sweep. The round-11 fix does not touch Y375's own words; it moved into the adjacent parenthetical, which now labels the rule the model's reading and routes it to probe 19. That is a real improvement in disclosure and it is the second consecutive round in which the disclosure improved without the evidence moving. **What is new this round is on the other side of the ledger:** the tree's one body of per-link save evidence, which I reproduced, both supports the gate statistically (33 of 50 zero-value links lack a both-ends holder; 3 of 109 value-carrying links do; χ² = 78.3) and **falsifies the sentence's absolute form** — `cuiaba → brazil`, `rio_grande → mississippi_river` and `california → mississippi_river` each carry value between nodes whose power-holder sets are disjoint, and 17 zero-value links have up to 9 both-ends holders. So the claim as written ("there is no transfer into a node where nobody holds power at both ends") is stronger than the only evidence in hand supports, in a document whose stated convention forbids universal quantifiers asserted as facts. **What would settle it:** §2.7 probe 19 — one session, node window, observing whether transfer enters a node where the receiving side holds power at only one end. Failing that, the sentence could be softened to the association the save shows. |

---

## REMOVED — is the retirement of `Y1353` sound?

| ID | Verdict | Method | Evidence |
|---|---|---|---|
| Y1353 | **CONFIRMED — removal sound** | Grepped the current spec for `1.12` and inspected every hit; read hunk 13's before and after; opened `patchnotes/1.12 Patchnotes.txt` and grepped it for `autonomy` and `overseas`. | The deleted clause — "1.12's notes carry no overseas-floor change" — **is asserted nowhere in the current text.** The spec's only `1.12` occurrences are section cross-references to §1.12 (L89, L98, L950, L1435, L1565, L1925); no line mentions EU4 patch 1.12. What stands in the deleted clause's place, `Y1372`'s "the floor appears in no later note", is scoped to notes after 1.16 and says nothing about 1.12, so it does not silently re-assert it. Two further checks: the deleted clause was **true** when it stood — `1.12 Patchnotes.txt`'s only `autonomy` lines are L74 (gold production reduced by autonomy), L265 (sounds) and L304 (`set_local_autonomy` effect), and its only `overseas` lines are L64, L376 and L433, none a floor change — so nothing false was smuggled in by deleting it, and nothing true was lost that another row now needs. The retirement removes a claim the document no longer makes, which is exactly what a REMOVED row should do. |

---

## Prior UNTESTABLE rows — did anything this round change their testability?

Each row's anchor line was compared **byte-for-byte** against the frozen baseline rather than
inferred from hunk arithmetic; all six distinct anchor lines occur verbatim and exactly once in the
baseline.

| ID | § | changed? | note |
|---|---|---|---|
| Y350 | §1.7 | **No — carried UNTESTABLE.** | L787 is byte-identical to the baseline. No hunk falls in §1.7, and no probe added or amended this round covers a one-merchant-per-country-per-node cap. Still needs a debugger session or an engine-code read. |
| Y370 | §1.8 | **No — carried UNTESTABLE.** | L845 byte-identical. §1.8's two hunks re-scope the `inject` warrant and the both-ends parenthetical; neither supplies a source for the steered outgoing-division rule, and item 19 is the both-ends rule, not this. *Observation, not a change of verdict:* `scripts/r12/gate.py` shows that per-link realised values **are** extractable from `Castile1444_12_22.eu4` (159 `incoming` blocks, all `from` indices resolving), and round 5 graded this family PARTIAL/CONFIRMED off exactly that data. Nothing this round changed that either way, so the carry stands — but "untestable" is doing more work here than the available save warrants, and a future round may want to revisit the family on that data. |
| Y371 | §1.8 | **No — carried UNTESTABLE.** | Same line, same gap, same observation as `Y370`. |
| Y372 | §1.8 | **No — carried UNTESTABLE.** | Same. |
| Y373 | §1.8 | **No — carried UNTESTABLE.** | L847 byte-identical; same gap. |
| Y505 | §2.6 | **No — carried UNTESTABLE.** | L1414 byte-identical. §2.7 still records "none of the ten has been run", so probes 1 and 2 — which name this pass structure as unresolved — remain unrun. |
| Y512 | §2.6 | **No — carried UNTESTABLE.** | L1432 byte-identical; probe 3 remains unrun. |
| Y513 | §2.6 | **No — carried UNTESTABLE.** | L1433 byte-identical; same unrun probe 3. |

---

## Summary

| group | rows | CONFIRMED | PARTIAL | REFUTED | UNTESTABLE |
|---|---|---|---|---|---|
| **Carried** (round-10 verdicts, unre-graded) | 1,157 | 1,149 | 0 | 0 | 8 |
| **Freshly graded — CHANGED** | 9 | 7 | 2 | 0 | 0 |
| **Freshly graded — REWORDED** | 4 | 4 | 0 | 0 | 0 |
| **Freshly graded — NEW** | 18 | 16 | 2 | 0 | 0 |
| **Freshly graded — `Y375`** | 1 | 0 | 1 | 0 | 0 |
| **Fresh total** | **32** | **27** | **5** | **0** | **0** |
| **Census total (live rows)** | **1,189** | **1,176** | **5** | **0** | **8** |

Plus one removal check (`Y1353`) — **sound** — and the census footing — **CONFIRMED**.

**Movement against round 10.** Round 10 closed with **8 PARTIALs**; round 11 closes with **5**.
Every one of round 10's eight was in this round's scope, and **seven of the eight are now closed**
— `Y483`, `Y1275`, `Y1291`, `Y1331`, `Y1341`, `Y1342` and `Y1352`, all CONFIRMED against re-run
instruments and re-read files. That is the round working as intended: the diff went to exactly the
sites the prior round flagged, and re-scoping each at its evidence class settled it. `Y375` is the
one that did not close, for the third round running — its fix again moved the disclosure rather
than the evidence.

The three new PARTIALs are all on material this round *wrote*, so they could not have been graded
before it existed: `Y1366` (a plural the measurement does not support), `Y1371` (a premise typed as
a file read that is not in the file), and the shared absence clause on `Y1158`/`Y1326`. The pattern
across all three is the same and is worth naming: **each is a re-scoping that landed slightly wide
of its own evidence** — two claiming more support than the source gives, one claiming less
evidence exists than the tree holds. The round's method is sound; the residual is calibration.

### Every non-CONFIRMED fresh verdict, one line each

- **`Y1158`** (§1.8) — PARTIAL. The file half is exhaustively confirmed, but the new clause "no session has observed it" asserts an absence the tree contradicts: the recorded session's own save yields a reproducible per-link measurement of the rule (33/50 vs 3/109, χ² = 78.3), which also carries three links transferring value with no both-ends holder.
- **`Y1326`** (§2.7 item 19) — PARTIAL. Same gap, sharper: this site says no "recorded session **supports** it", and the save-derived measurement is support, recorded in the tree's own round-5 validation as CONFIRMED before later rounds re-graded the claim without citing it.
- **`Y1366`** (§3.4) — PARTIAL. Barcelona reproduces the autonomy-free prediction exactly, but only 3 of the 7 singleton cells at ≥50% autonomy do, and high-autonomy cells reproduce less often (43%) than zero-autonomy cells (60%); the owner-grouped control is the better evidence and is not what the sentence says.
- **`Y1371`** (§3.16) — PARTIAL. "Territories carry autonomy" is verbatim at `1.16 Patchnotes.txt:42`, but "in place of the overseas rule" is in no file and sits against that same line's "and is considered to be overseas for many rules" — it is the inference, not the file read the row's provenance claims.
- **`Y375`** (§1.8) — PARTIAL. Trade-range half file-sourced; both-ends half still stated by no define, string or shipped file, and the only save evidence available contains three counterexamples to the sentence's absolute form.
