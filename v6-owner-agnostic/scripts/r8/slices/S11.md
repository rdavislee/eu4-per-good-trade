# Slice S11  (60 claims)


## §2.1

- **Y1017** [CHANGED, type DESIGN, spec line 962]  provenance: algebraic derivation
  CLAIM: Check 1 — one binary per platform and no cross-platform sessions, because a single compiled instruction stream gives identical IEEE-754 results on any x86-64 host it runs on.
  CHANGE: old: "identical IEEE-754 results on **any x86-64 host**" → new: "on any x86-64 host **it runs on**"; the universal over hosts is narrowed to the hosts the binary actually runs on

- **Y427** [UNCHANGED, type DESIGN, spec line 925]
  CLAIM: The implementation is one program: a runtime-attached DLL that each month reads live game state, solves per …

- **Y428** [UNCHANGED, type DESIGN, spec line 925]
  CLAIM: It ships with a generated `00_tradenodes.txt` for load time and a companion overlay for what the engine …

- **Y429** [UNCHANGED, type DESIGN, spec line 927]
  CLAIM: The target platform is Windows/Steam.

- **Y430** [UNCHANGED, type ENGINE, spec line 927]
  CLAIM: Achievements are off with any mod (`ACHIEVEMENTS_DISABLED_MODIFIED_GAME`).

- **Y431** [UNCHANGED, type ENGINE, spec line 928]
  CLAIM: The engine will load an ironman save in a modded game — `Loading ironman in modded game` is a shipped code …

- **Y432** [UNCHANGED, type ENGINE, spec line 932]
  CLAIM: EU4 multiplayer is lockstep with checksums, so every client must reach the same answer; the classical worry …

- **Y434** [UNCHANGED, type MODEL, spec line 934]
  CLAIM: DRAIN's exposure is different in kind from v1's dense linear algebra, which was badly exposed to it: v1 …

- **Y435** [UNCHANGED, type MODEL, spec line 939]
  CLAIM: DRAIN's comparisons are of input-derived quantities (`DEF`, `b`, arc costs) rather than of solver residuals, …

- **Y1011** [UNCHANGED, type DESIGN, spec line 941]
  CLAIM: The multiplayer question is no longer whether the arithmetic agrees to the last bit but whether the build is …

- **Y995** [UNCHANGED, type MODEL, spec line 945]
  CLAIM: §2.3's two changes move the desync question from a design problem to a verification one: the largest exposure …

- **Y1012** [UNCHANGED, type MEASURED, spec line 952]
  CLAIM: There is no randomness in the solve: an identical output fingerprint over repeated runs, separate processes …

- **Y1113** [UNCHANGED, type MODEL, spec line 952]
  CLAIM: The determinism fingerprint is one SHA-256 over `Φ_w` and all 29 per-good graphs, including sinks, sources, promotions …

- **Y1013** [UNCHANGED, type MEASURED, spec line 953]
  CLAIM: The margin by which the optimum is unique is 3.8e-8 worst per good and 7.5e-6 on the aggregate — 8 to 10 …

- **Y1014** [UNCHANGED, type MEASURED, spec line 954]
  CLAIM: Orientation under LP column permutation: 0 flips on the aggregate and on all 29 goods, with an objective …

- **Y1015** [UNCHANGED, type MEASURED, spec line 956]
  CLAIM: The per-good `abs(net)` distribution is bimodal — 2,321 edge-goods at exactly 0 and 2,290 above 1e-6, with …

- **Y1114** [UNCHANGED, type MEASURED, spec line 956]
  CLAIM: The smallest non-zero per-good `\|net\|` magnitude is 6.94e-06.

- **Y1016** [UNCHANGED, type MODEL, spec line 958]
  CLAIM: A few units in the last place cannot change any decision this solver makes, so what remains is not …

- **Y1018** [UNCHANGED, type MODEL, spec line 965]
  CLAIM: The `../v2-drain/` DLL precedent is already Windows- and Steam-only, so the one-binary rule matches practice …

- **Y1019** [UNCHANGED, type DESIGN, spec line 967]
  CLAIM: Check 2 — no runtime CPU dispatch in the LP solver and single-threaded: this is the live risk, because …

- **Y1020** [UNCHANGED, type DESIGN, spec line 971]
  CLAIM: Check 3 — §2.8's cross-implementation orientation check compares the DLL against the reference implementation …

- **Y1021** [UNCHANGED, type ENGINE, spec line 975]
  CLAIM: Every trade number EU4 writes to a save is quantised to 1/1000: 495 of 495 sampled values land exactly on …

- **Y1022** [UNCHANGED, type ENGINE, spec line 977]
  CLAIM: Quantisation of that kind erases any divergence below half a grid step, which is the standard cheap defence.

- **Y1023** [UNCHANGED, type ENGINE, spec line 978]
  CLAIM: The files cannot settle whether the rounding happens in the simulation or only in the serialiser; that needs …

- **Y1024** [UNCHANGED, type MODEL, spec line 980]
  CLAIM: Quantisation would not rescue this solver either way: the orientation margins of 3.8e-8 to 7.5e-6 are three …

- **Y436** [UNCHANGED, type DESIGN, spec line 984]
  CLAIM: Until checks 1–3 are done, ship single-player only.

- **Y1025** [UNCHANGED, type MODEL, spec line 984]
  CLAIM: The reason for shipping single-player has changed: it is no longer "vertex selection is machine-dependent" …

- **Y1199** [NEW, type DESIGN, spec line 962]  provenance: algebraic derivation
  CLAIM: Running on *any* host requires building to the architecturally-mandatory baseline (SSE2).

- **Y1200** [NEW, type DESIGN, spec line 963]  provenance: algebraic derivation; §2.1 check 2
  CLAIM: Targeting an optional extension turns "any host" into "any host with that extension" — check 2’s live risk restated at build scope.


## §2.2

- **Y132** [CHANGED, type MEASURED, spec line 1013]  provenance: named observation (the replicate batches)
  CLAIM: Measured on the reference implementation (scipy/HiGHS plus the deterministic sweep, one machine): of order 0.1 s for all 29 goods.
  CHANGE: old: "of order 0.1 s for all 29 goods, **and milliseconds rather than tens of milliseconds per good on average**", with the 10.5 and 10.8 ms replicate figures attached → new: **"of order 0.1 s for all 29 goods"** alone, "the paragraph’s one figure-shaped assertion"; the per-good half and both replicate figures are withdrawn

- **Y133** [CHANGED, type MEASURED, spec line 1015]  provenance: named observation (the replicate batches)
  CLAIM: Repeated 12-run experiments on one machine do not reproduce each other closely enough for anything finer; across every replicate batch to date every all-29 median is of order 0.1 s while per-good-average maxima land on both sides of 10 ms.
  CHANGE: old: "three replicates gave per-good averages spanning **3.5–10.5, 3.5–10.8 and 3.1–4.7 ms**" → new: "**across every replicate batch to date**, every all-29 median is of order 0.1 s while per-good-average maxima land on **both sides of 10 ms**"; the three spans are withdrawn for a bounding statement over all batches

- **Y134** [CHANGED, type MEASURED, spec line 1020]  provenance: named observation (the replicate batches)
  CLAIM: v5.0’s "0.17–0.21 s for all 29 goods" is not reproducible: the number of twelve runs landing inside that interval varies from 0 to 11 across replicates.
  CHANGE: old: "across **three replicates of twelve runs** the number of runs landing inside that interval was **1, then 0, then 0**" → new: that number "**varies from 0 to 11 across replicates** — the variance is the refutation, and no counting triple is maintained in either direction"

- **Y448** [CHANGED, type DESIGN, spec line 1023]  provenance: unsourced
  CLAIM: That order of magnitude is reached with a generic LP; the all-29 figure is what a native network simplex would have to improve on, and no measurement in this project supports a specific projection, and none is offered.
  CHANGE: old: "**“Milliseconds each” therefore holds already**, with a generic LP" → new: "**That order of magnitude** is reached with a generic LP"; the subject moves from the withdrawn per-good figure to the all-29 order of magnitude. The simplex and no-projection tail is unchanged

- **Y449** [CHANGED, type DESIGN, spec line 1027]  provenance: the reference implementation; §2.9
  CLAIM: Two implementations of one specification: the list above specifies the solver deliverable, the reference implementation (standalone, run against parsed saves, the thing every validation in §2.8 is measured on) ships a stated subset of it, and the shipped DLL carries a second implementation of items 4–7 in the host language reading live memory instead of save files.
  CHANGE: old: "**The list above is the reference solver**" → new: "The list above **specifies the solver deliverable**", with the reference implementation’s coverage enumerated as a subset (items 1 less `path`/`control`, 4, 5, 7 as the boolean projection, and 8) and its gaps handed to §2.9. The DLL half is unchanged

- **Y437** [UNCHANGED, type DESIGN, spec line 990]
  CLAIM: Solver item 1 is a parser for `common/tradenodes/00_tradenodes.txt` reading adjacency, `members`, …

- **Y438** [UNCHANGED, type DESIGN, spec line 991]
  CLAIM: Solver item 2 is a parser for non-ironman saves reading province owner, `base_tax`, `base_production`, trade …

- **Y439** [UNCHANGED, type DESIGN, spec line 992]
  CLAIM: Solver item 3 is a parser for `common/defines.lua` merged with `common/defines/` overrides in load order.

- **Y129** [UNCHANGED, type MODEL, spec line 993]
  CLAIM: `TAX_COEFF · base_tax + …` — "The tax term takes no modifier at all"

- **Y130** [UNCHANGED, type MEASURED, spec line 995]
  CLAIM: four modifiers in scope, all four reaching `goods_produced`; at 1444 only `devastation` is live, on eleven; `unrest` d …

- **Y440** [UNCHANGED, type ENGINE, spec line 998]
  CLAIM: `GP_COEFF` is read from `common/static_modifiers/00_static_modifiers.txt` rather than hardcoded.

- **Y131** [UNCHANGED, type MEASURED, spec line 1002]
  CLAIM: World wealth is 10,607.40 annual ducats over 2,472 counted provinces.

- **Y441** [UNCHANGED, type DESIGN, spec line 1003]
  CLAIM: Solver item 5 is DRAIN per good: a min-cost b-flow using network simplex or a simplex LP rather than …

- **Y442** [UNCHANGED, type MODEL, spec line 1005]
  CLAIM: The Phase-4 evaluator's `unserved` and `stranded` must be equal by conservation, since the sum of `b_g` over …

- **Y443** [UNCHANGED, type MODEL, spec line 1007]
  CLAIM: `Φ_w` is one more DRAIN run with wealth as the good — the 30th solve, same code path.

- **Y444** [UNCHANGED, type DESIGN, spec line 1008]
  CLAIM: Solver item 6 is a survival table `S_g[n][H]` for AI scoring, one table serving every country.

- **Y445** [UNCHANGED, type DESIGN, spec line 1009]
  CLAIM: Solver item 7 is a mutual reachability census: 30 goods × 80 BFS producing an 80×80 matrix whose entry counts …

- **Y1164** [UNCHANGED, type DESIGN, spec line 1009]
  CLAIM: The 30 goods × 80 BFS mutual-reachability census, as an 80×80 counting matrix, is the DLL deliverable.

- **Y1165** [UNCHANGED, type MODEL, spec line 1009]
  CLAIM: The reference implementation ships the boolean projection of that census over the 29 live goods, and that projection is what feeds §3.8's 5,723.

- **Y446** [UNCHANGED, type PROCESS, spec line 1010]
  CLAIM: Solver item 8 is a synthetic-shock harness that edits parsed province data and re-solves.

- **Y447** [UNCHANGED, type MODEL, spec line 1012]
  CLAIM: Cost per good is one uncapacitated min-cost flow on 80 nodes and 318 arcs plus an O(V+E) sweep.

- **Y450** [UNCHANGED, type DESIGN, spec line 1027]
  CLAIM: The two implementations must agree on orientation exactly — a combinatorial comparison with no tolerance band …

- **Y451** [UNCHANGED, type PROCESS, spec line 1029]
  CLAIM: The parsers and the harness stay reference-only, and the DLL never reads a save.

- **Y452** [UNCHANGED, type DESIGN, spec line 1031]
  CLAIM: Inland is derived rather than trusted from the flag: a node with no coastal province among its `members`.

- **Y453** [UNCHANGED, type ENGINE, spec line 1032]
  CLAIM: The derivation and the flag disagree at exactly one node — `siberia` carries `inland=yes` but has two …

- **Y1201** [NEW, type PROCESS, spec line 1027]  provenance: the reference implementation
  CLAIM: The reference implementation ships items 1 (less `path`/`control`), 4, 5, 7 (as the boolean projection) and 8 of §2.2’s list.

- **Y1202** [NEW, type PROCESS, spec line 1027]  provenance: `nodes.py`; the shipped node file; §2.4 item 3
  CLAIM: The shipped node file carries `path` and `control` and `nodes.py` discards both, while §2.4 item 3 nevertheless obliges the emitter to reverse both.

- **Y1203** [NEW, type PROCESS, spec line 1027]  provenance: the reference implementation
  CLAIM: The reference implementation builds its province table from `history/provinces`, reading the save only for the twenty rolled trade goods.

- **Y1204** [NEW, type PROCESS, spec line 1027]  provenance: the reference implementation
  CLAIM: The reference implementation reads three defines by regex rather than by the merged parse of item 3.

- **Y1205** [NEW, type PROCESS, spec line 1027]  provenance: the reference implementation
  CLAIM: The reference implementation does not build the survival table.

- **Y1206** [NEW, type PROCESS, spec line 1027]  provenance: §2.9
  CLAIM: §2.9’s build order is where those reference-implementation gaps are owned.
