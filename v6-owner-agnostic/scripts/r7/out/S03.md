## §1.2

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y271 | `s(n,g) = goods_produced(n,g)/Σ_m goods_produced(m,g)` | CONFIRMED | derivation check against solver.py | `solver.py:build_sc()` computes `S[live]=gp[live]/world[live]` where `gp` sums `r["gp"]` per node and `world=gp.sum(axis=1)` — exactly this ratio |
| Y272 | `goods_produced` is a physical quantity — pre-production-efficiency, pre-autonomy | CONFIRMED | file read (solver.py) | `solver.py:province_table()`: `gp = max(0.0, GOODS_PRODUCED_FACTOR * s["base_production"] * (1.0+gmod))` — no `production_efficiency` or `local_autonomy` term anywhere in the expression |
| Y273 | `goods_produced` moves with devastation, occupation, prosperity (`00_static_modifiers.txt`'s `devastation`,`occupied`,`under_siege`,`prosperity` all carry `trade_goods_size_modifier`) | CONFIRMED | file read | `common/static_modifiers/00_static_modifiers.txt`: `devastation{trade_goods_size_modifier=-2}` L454, `occupied{trade_goods_size_modifier=-0.5}` L435, `under_siege{trade_goods_size_modifier=-0.25}` L445, `prosperity{trade_goods_size_modifier=0.25}` L466 |
| Y274 | No regularizer: v1 mixed in `s ← (1−ε)·s+ε/N`; DRAIN doesn't need it | CONFIRMED | file read (v1 spec) + script read | `v1-laplacian/per-good-trade-spec.md:43-46`: "A regularizer is mixed in on every solve: s ← (1 − ε)·s + ε/N   ε ≈ 10⁻⁶"; `measure6.py`'s `ph()`/`cv()` for Φ_w use no `eps` term at all, and `run_drain()` in `drain.py` takes no supply regularizer |
| Y275 | DRAIN's free edges oriented combinatorially by drainage sweep, not by comparing near-equal solved potentials | CONFIRMED | file read (drain.py) | `drain.py` implements `sweep`/`sweep_priority` (Kahn-style ready-queue over degree-peeled core) per its own docstring "sweep (orients ALL free edges) -> un-peel" — no potential-comparison step; contrasted with v1's retired Laplacian-potential approach per `v3-owner-agnostic/per-good-trade-spec.md` lineage note |
| Y276 | One node has `b=0` exactly at 1444 — `cape_of_good_hope` — handled as an ordinary conduit | CONFIRMED | measurement (own script over solver.py) | Built S,C via `solver.build_sc`: `cape_of_good_hope` has S=C=0.0 for all 29 goods exactly, because it is the **only** one of 80 nodes with zero counted (owned+trade-node) provinces (`Counter(r['node'] for r in ROWS)` gives 0 for `cape_of_good_hope`, nonzero for every other node); `r6.out` §3.2 independently shows its in/out degree nonzero for 29/29 goods and in/out flow nonzero for 28/29 (conduit behaviour) |

## §1.3

| ID | claim (short) | verdict | method | evidence |
|---|---|---|---|---|
| Y030 | Owner-agnosticism true by construction; v4.0/v5.0 defended with two-test classifier, v3.0 with a structural rule (§0) | CONFIRMED | file read (v3, v4 specs + current spec's own §0) | `v3-owner-agnostic/per-good-trade-spec.md:163-166`: "a trade good's `province = { … }` block is province-scoped… its `modifier = { … }` block is country-scoped… Only the first kind is local" (a structural, data-model rule); `v4-owner-agnostic/per-good-trade-spec.md:180`: "**Which modifiers are local, and which of those enter wealth.** Two tests…" (the two-test classifier); current spec L19-20: "The two-test classifier is v4.0's; v3.0 used a structural rule about which block of a trade-good definition a modifier sits in" |
| Y293 | Excluded non-local items: Reform Iqta +5% (govt), Clergy +5% observed (estate, loyalty-scaled per `01_church.txt`), national ideas +15%, tech production efficiency +2%, owner's goods-produced mods | CONFIRMED | file read | `common/government_reforms/01_government_reforms_monarchies.txt:783` `iqta` reform: `global_tax_modifier=0.05`; `common/estates/01_church.txt:44-46` "# These scale with loyalty & power" / `global_tax_modifier=0.2`; `common/ideas/00_country_ideas.txt:3736-3747` `GRA_ideas` start bonus (trigger tag=GRA, free=yes): `global_tax_modifier=0.15`; `common/technologies/adm.txt` repeated `production_efficiency = 0.02` per ADM level |
| Y277 | Demand assembled per province, then summed to the node | CONFIRMED | file read (solver.py) | `solver.py:province_table()` builds per-province `ROWS`; `build_sc()` aggregates with `np.add.at(C[gi], pn, w/tot)` where `pn` maps province→node index — a per-province quantity summed into per-node shares |
| Y028 | Wealth owner-agnostic; reads development, trade good, own condition | CONFIRMED | derivation check | Matches `wealth(p)=TAX_COEFF·base_tax + GP_COEFF·base_production·(1+goods mods)·price`; every input is `base_tax`, `base_production`, `good`, or a province-state modifier — no owner field anywhere in `solver.py:province_table()` |
| Y278 | Wealth is a property of the place, before government touches it | CONFIRMED | derivation check | Same as Y028 — no owner-side term in the formula |
| Y279 | Wealth reads no autonomy/production efficiency/ideas/estate-govt mods/technology | CONFIRMED | file read (solver.py) | `province_table()`'s `tax`/`gp` expressions contain only `TAX_COEFF`,`base_tax`,`GOODS_PRODUCED_FACTOR`,`base_production`,`gmod` (state modifier) — none of the excluded terms appear |
| Y029 | Same dev/good/condition ⇒ same wealth regardless of owner | CONFIRMED | derivation check | Trivial consequence of the wealth formula having no owner input (Y028/Y279) |
| Y280 | Province wealth doesn't change when conquered | CONFIRMED | derivation check | Same — no owner input, so a change of `owner` alone leaves `wealth(p)` unchanged |
| Y031 | `base_tax`,`base_production`,trade good are bare attributes needing no classifying | CONFIRMED | derivation check | These are the only province-place fields read (Y279); no "is this modifier local" test is applied to them in `province_table()` |
| Y032 | What's given up: `gems`' `local_tax_modifier`, `incense`' `trade_value_modifier`, great projects, permanent modifiers, DLC state | CONFIRMED | file read (apparatus6.py) | `apparatus6.py`: `LOCAL_TAX_MOD={"gems":0.15}`, `LOCAL_TV_MOD={"incense":0.10}`, `MON_FLAT`/`MON_GPMOD`/`MON_TVMOD` (great projects: falun, grand_canal_1-4, krakow_cloth_hall), `PERM_FLAT` (permanent province modifiers) — none of these appear in `solver.py`'s live wealth path |
| Y033 | Dropped apparatus live on 89 of 2,472 — 43 gems+31 incense+16 great-project, less 1 (province 542) | CONFIRMED | measurement (apparatus6.py run + own script) | `apparatus6.py` in `scripts/r7/w03`: `apparatus provinces touched = 89`; own script: gems-good provinces=43, incense-good provinces=31 (30 authored + province 4856 rolled), great-project/permanent-modifier pids=16 (`FLAT_GOODS`∪`MON_GPMOD`=15+1); province 542 (`trade_goods=gems`, also in `PERM_FLAT`) is the double-count: 43+31+16=90, union=89 |
| Y034 | Count is field-dependent: 87 under withdrawn `is_city` filter; 89 not 88 because province 4856 rolled `incense` | CONFIRMED | measurement (own script) | Restricting the 89 touched provinces to `is_city=="yes"` removes {4856, 1207} → 87; province 4856 (`trade_goods=unknown` in history) is touched **only** because `ROWS` resolves its good to `incense` via the roll (`solver.ROLLED[4856]=="incense"`; confirmed not in `FLAT_GOODS`/`MON_GPMOD`/`MON_TVMOD`) — without the roll incense-count drops to 30, giving 43+30+16-1=88 |
| Y1083 | Apparatus figures reproduced by `apparatus6.py`, frozen classifier constants | CONFIRMED | file read + measurement | `apparatus6.py` docstring: "FROZEN TABLE of twenty-two constants, copied verbatim from `../../v5-owner-agnostic/scripts/solver.py:59-75`"; running it in `scripts/r7/w03` reproduces `10607.4 / 10712.7 / 105.3 / 89` matching `m6.out` exactly |
| Y1084 | Frozen constants record v5's input surface, sit in own file so nothing wires them back | CONFIRMED | file read | `solver.py` (the live wealth path) contains no reference to `apparatus6`; `measure6.py:256` comment: "constants live in apparatus6.py so they cannot be wired back into the live wealth path here" |
| Y1085 | `measure6.py` imports the apparatus figures rather than restating them | CONFIRMED | file read | `measure6.py:258`: `import apparatus6 as _AP` |
| Y281 | Model trades fidelity for an input surface with no classification question | CONFIRMED | derivation check | Consistent with Y031/Y279 — only bare place-attributes are read, no local/enters-wealth test exists in `solver.py` |
| Y035 | `goods_produced(p)=GP_COEFF·base_production(p)·(1+Σ province-state goods mods)`, no flat-bonus term | CONFIRMED | file read (solver.py) | `province_table()`: `gp = max(0.0, GOODS_PRODUCED_FACTOR * s["base_production"] * (1.0 + gmod))` — multiplicative only, no additive flat term |
| Y036 | `trade_value(p)=goods_produced(p)·price(good(p))`, no trade-value modifier term | CONFIRMED | file read (solver.py) | `province_table()`: `prod_income=gp*price` — no modifier multiplier |
| Y037 | `tax_value(p)=TAX_COEFF·base_tax(p)` — no modifier at all | CONFIRMED | file read (solver.py) | `province_table()`: `tax=TAX_COEFF*s["base_tax"]*(1.0+tmod)` with `tmod=0.0` always (`STATE_TAX_MOD={}`) |
| Y282 | `wealth(p)=tax_value(p)+trade_value(p)` | CONFIRMED | file read (measure6.py) | `measure6.py`: `W = np.array([r["tax"]+r["prod_income"] for r in ROWS])` |
| Y283 | `c(n,g)` = node wealth^α(g) share of world wealth^α(g) | CONFIRMED | file read (solver.py) | `build_sc()`: `w=wealth**a; np.add.at(C[gi],pn,w/tot)` where `tot=w.sum()` |
| Y038 | `GP_COEFF` is a shipped file value from `provincial_production_size`, localised "Base Production" | CONFIRMED | file read | `common/static_modifiers/00_static_modifiers.txt:251-254`: `provincial_production_size={trade_goods_size=0.2 ship_recruit_speed=-0.01}`; `localisation/EU4_l_english.yml:815`: `provincial_production_size:0 "Base Production"` |
| Y284 | `GP_COEFF`/`TAX_COEFF` different provenance | CONFIRMED | derivation check | `GP_COEFF` is regex-read from the file at runtime (Y038/Y039); `TAX_COEFF` is a Python literal `1.0` with no source file (Y040) |
| Y039 | `GP_COEFF` moddable, read at runtime not hardcoded | CONFIRMED | file read (solver.py) | `solver.py:_read_gp_coeff()` opens `00_static_modifiers.txt` and regex-extracts `trade_goods_size` at import time; `GOODS_PRODUCED_FACTOR=_read_gp_coeff()` |
| Y040 | `TAX_COEFF` in no file found — not `defines.lua`, `common/defines/`, static-modifier block | CONFIRMED | file read (negative search) | `grep -in "tax_coeff" common/defines.lua common/defines/*.lua` and recursive `grep -rin tax_coeff common/` — zero hits; `solver.py:83`: `TAX_COEFF = 1.0` (a bare literal, no file read) |
| Y285 | Tax/trade terms share time basis: engine tooltips give both as annual/12 for display | CONFIRMED | derivation check (underlies Y041-Y046) | The `×0.0833333` (≈1/12) truncation model reproduces both tax observations and is consistent with the production tooltip's divisor bound (11.73,12.14] — see Y041,Y044 |
| Y041 | Tax tooltip schema `Base: trunc(base_tax×0.0833333) (Yearly base_tax)`; `0.49/6.00` and `0.16/2.00` | CONFIRMED | derivation check (arithmetic) | `python`: `6*0.0833333=0.4999998→trunc 0.49`; `2*0.0833333=0.1666666→trunc 0.16` — matches both observations exactly; `history/provinces` + save (`check_save.py`) confirm base_tax=6 (223) and base_tax=2 (1747) |
| Y042 | Parenthetical is `base_tax` itself; Base is truncated twelfth; not 12×displayed (would give 5.88, 1.92) | CONFIRMED | derivation check (arithmetic) | `12*0.49=5.88`, `12*0.16=1.92` — computed, matches claim's counterfactual |
| Y043 | v4.0/v5.0 wrote schema `Base: X (Yearly 12·X)`, false on own data; v3.0 has neither | CONFIRMED | file read (v3,v4,v5 specs) | `v4-owner-agnostic/per-good-trade-spec.md:163`, `v5-owner-agnostic/per-good-trade-spec.md:170`: literally `Base: X (Yearly 12·X)`; `grep "Yearly 12\|0.6125" v3-owner-agnostic/per-good-trade-spec.md` → no matches |
| Y044 | Production tooltip `Trade Value` 3.52→+0.29 fixes divisor to (11.73,12.14] | CONFIRMED | derivation check (arithmetic) | `python`: `3.52/0.30=11.7333`, `3.52/0.29=12.1379` → interval (11.73,12.14] rounds exactly |
| Y045 | Both monthly figures being annual/12 lets annual forms add; tax pair establishes it at two dev levels | CONFIRMED | derivation check | Both `base_tax=6` and `base_tax=2` observations fit the same `×0.0833333` truncation model (Y041) — two independent development levels agreeing |
| Y286 | Coefficients measured on Garnatah (223: base_tax 6, base_prod 4, silk, autonomy 0) and Caceres (1747: base_tax 2, base_prod 2, wool) | CONFIRMED | file read + measurement | `history/provinces/223 - Granada.txt`: `base_tax=6 base_production=4 trade_goods=silk`, `add_core=GRA` (cored ⇒ autonomy 0 consistent); `1747 - Caceres.txt`: `base_tax=2 base_production=2 trade_goods=wool`; `check_save.py` confirms both against the save exactly |
| Y287 | Only tooltips' `Base` lines used | CONFIRMED | file read (current spec §2.3) | Spec's own §2.3: "Both coefficients are read off the tooltips' **base** lines, which carry no owner term" |
| Y288 | Province window `Trade Value` also carries owner's `global_trade_goods_size_modifier` | CONFIRMED | file read | `global_trade_goods_size_modifier` is a real, widely-used engine key (`common/advisortypes/00_advisortypes.txt:1098`, `common/ages/00_default.txt:520`, `common/ancestor_personalities/00_core.txt`, etc.) |
| Y289 | Garnatah window read 3.52 not 3.20 because Industrious personality +10% | CONFIRMED | file read + arithmetic | `common/ruler_personalities/00_core.txt:1359-1406` `industrious_personality={... global_trade_goods_size_modifier=0.1 ...}`; `0.80*4.00=3.20`, `3.20*1.10=3.52` exact match; `history/countries/GRA - Granada.txt` scripts no personality for the ruling 1444 monarch (Muhammad IX, from the 1419.1.1 entry), consistent with a random roll |
| Y290 | Ruler personalities rolled at start where history scripts none; window figure is one sample, Base lines/ratio are not | CONFIRMED | file read | `history/countries/GRA - Granada.txt` contains no `personality` key on any monarch entry (checked via grep), consistent with random-roll behaviour; Base lines (Y041) and the /12 ratio hold identically on two independent provinces |
| Y291 | Modifiers apply after coefficient: base from development first, then percentage | CONFIRMED | derivation check | Underlies Y046's exact reproduction of 0.49→0.62 via the untruncated-monthly-value path |
| Y046 | `Base 0.49`→`0.62` at 125%; 0.49×1.25=0.6125→trunc 0.61≠0.62; engine multiplies untruncated 6×0.0833…=0.49999…×1.25=0.62499…→0.62 | CONFIRMED | derivation check (arithmetic) | `python`: `0.49*1.25=0.6125`, floor-trunc→`0.61`; `6*0.0833333=0.4999998`, `×1.25=0.62499975`, trunc→`0.62` — both figures reproduce exactly |
| Y047 | Example establishes only the ordering, nothing finer | CONFIRMED | derivation check | Appropriately scoped — the single observation pins ordering (Y291) but not (e.g.) the exact truncation point beyond what Y041/Y046 already pin |
| Y048 | v4.0/v5.0 read "0.6125 shown as 0.62" (rounding) while §2.3 requires truncation — both cannot hold | CONFIRMED | file read (v4,v5 spec + current §2.3) | `v4-.../per-good-trade-spec.md:178`, `v5-.../per-good-trade-spec.md:185`: "giving 0.6125, which the province window shows as 0.62" (requires rounding 0.6125→0.62); current spec §2.3: "The displayed monthly is the truncation of `base_tax × 0.083333`" (requires truncation); `0.6125` truncates to `0.61`, not `0.62` — the two cannot both hold |
| Y049 | Flat goods bonuses would add pre-price-multiply; additive "Base Goods Produced" above multiplicative "Goods Produced Efficiency"; unexercised under §1.3 | CONFIRMED | file read (localisation) | `localisation/EU4_l_english.yml:1283`: `BASE_TRADE_GOODS_SIZE:0 "Base Goods Produced"`; `localisation/text_l_english.yml:737`: `GOODS_PRODUCED_EFFICIENCY:0 "Goods Produced Efficiency"` — both localisation keys exist verbatim; no flat-bonus term appears in `solver.py`'s `gp` expression (Y035) |
| Y050 | Province condition: 4 static modifiers applied (all in `00_static_modifiers.txt`), all reach `goods_produced`; 5th (`unrest`) defined but not read | CONFIRMED | file read | `00_static_modifiers.txt` defines `devastation`,`prosperity`,`under_siege`,`occupied` (all with `trade_goods_size_modifier`, Y273) and `unrest` (L487-491); `solver.py:STATE_GOODS_MOD` holds exactly the four, `STATE_TAX_MOD={}` |
| Y979 | `unrest` deliberately not read; 1444 21-province liveness no longer asserted (withdrawn) | CONFIRMED | file read (current spec + changes-v6.md) | Current spec text (L243-453, read in full) makes no mention of unrest liveness or a 21-province count; `changes-v6.md:4277,329-338` confirms an earlier draft *did* implement/measure `unrest` live on 21 provinces (16 authored + 5 Shirvan-owned runtime) and then reverted it — the withdrawal is real |
| Y051 | Four-row table: `occupied` enters `goods_produced` only, tax half "granted by file, not read"; no `unrest` row | CONFIRMED | file read | `00_static_modifiers.txt:433-439` `occupied={local_tax_modifier=-0.5 trade_goods_size_modifier=-0.5 ...}` — both keys present in file, but `solver.py:STATE_TAX_MOD={}` confirms the tax half is not read |
| Y054 | `devastation`'s scaling law not settled by file (only row without a scaling comment); wiki gives `-2×level/100`, linear at 100% | PARTIAL | file read + (wiki barred as a source) | File-confirmed: `devastation={...}` (L453-461) carries no comment at all; contrast `unrest{regiment_recruit_speed=0.1 #10% longer time to build troops for each rr ...}` (L487-490) and `nationalism={local_unrest=0.5 #for each year revolt risk!}` (L493-495) both carry per-unit comments — devastation is uniquely bare. The wiki-sourced scaling law itself is UNTESTABLE under this audit's rules ("A wiki is not a source" — BRIEF FORBIDDEN sources) |
| Y053 | No modifier reaches tax term; all four rows enter `goods_produced` | CONFIRMED | file read (solver.py) | `STATE_TAX_MOD={}`; `STATE_GOODS_MOD={"devastation":-2.0,"prosperity":0.25,"occupied":-0.5,"under_siege":-0.25}` — all four only feed `gmod`, which only multiplies `gp` |
| Y1086 | `occupied`'s `local_tax_modifier` granted by file, not read; production half already carries the effect | CONFIRMED | file read | `00_static_modifiers.txt:434` `local_tax_modifier=-0.5` present in file; `solver.py:STATE_TAX_MOD={}` confirms it is not read; `trade_goods_size_modifier=-0.5` (L435) is read via `STATE_GOODS_MOD["occupied"]` |
| Y981 | `STATE_TAX_MOD` is empty, kept as empty declaration | CONFIRMED | file read | `solver.py:89`: `STATE_TAX_MOD = {}` |
| Y1087 | `STATE_TAX_MOD` kept empty rather than deleted so exclusion's shape stays legible for a later editor | PARTIAL | file read (solver.py) | The empty-declaration fact holds (Y981), but `solver.py`'s actual inline comment gives a **different** stated reason: "Kept as an empty declaration rather than deleted so verify6 has a subject to assert against" (lines directly above `STATE_TAX_MOD={}`) — not "so the shape of the exclusion stays legible... a later editor has to infer" as the claim states |
| Y1088 | `prosperity`,`under_siege`,`occupied` live on no counted province at 1444 | CONFIRMED | measurement (round6.py, `r6.out`) | `r6.out` §1.3: "prosperity live on 0 counted province(s) <== no input on this field"; "under_siege live on 0"; "occupied live on 0" |
| Y1089 | Wealth rule carries four modifiers, one exercised by reference field | CONFIRMED | measurement | `r6.out`: `devastation` live on 11, the other three on 0 — exactly one of four live |
| Y980 | Revolt risk not a property of the place: carries separatism/culture/religion/nationalism, all owner-relative | CONFIRMED | derivation check | Consistent with `changes-v6.md:329-333`'s account: "revolt risk carries separatism, culture and religion terms that are relations between a province and its owner" |
| Y1090 | Province in revolt still has development's buying power; collection is owner's problem | CONFIRMED | derivation check | Consistent with wealth formula reading no owner/unrest term (Y279) |
| Y984 | Effect unrest would buy already bought: devastation/occupied/under_siege applied | CONFIRMED | file read (solver.py) | `STATE_GOODS_MOD` contains all three (`devastation`,`occupied`,`under_siege`) plus `prosperity`; `changes-v6.md:337-338` states the same conclusion when unrest was reverted |
| Y1091 | No figure quoted for unrest exclusion cost, none reconstructed | CONFIRMED | file read (current spec) | Full text of doc lines 243-453 read; no ducat figure for an "unrest exclusion" appears anywhere in the current §1.3 |
| Y1092 | Earlier drafts carried such a figure; keeping it meant parsing `revolt_risk` from the save | CONFIRMED | file read (changes-v6.md) | `changes-v6.md:329-338` "### unrest: implemented, measured, reverted" — quotes costs "12.23 ducats (0.115%) reading the save, 9.40 (0.089%) reading only the authored 16" — the save-reading is exactly the parsing burden the claim describes |
| Y1093 | Exclusion is a decision about what wealth means; a measured cost wouldn't bear on it | CONFIRMED | derivation check | Consistent design argument; matches `changes-v6.md`'s stated reason for reverting (owner-agnosticism test, not a cost threshold) |
| Y058 | Condition modifiers make map answer to war: §1.2 volatility, §3.3's "besieged province genuinely produces less", §2.8 war rows are their test | CONFIRMED | file read (current spec §3.3, §2.8) | Current spec §3.3 (L1570 area): "A besieged province genuinely produces less"; §2.8/`r6.out` has the razing-a-Chinese-node war-scenario rows ("zeroing hangzhou/beijing" tests) |
| Y059 | 11 counted provinces begin devastated — Bohemia 50, Erzgebirge/Moravia 20 — no province-history file says so | CONFIRMED | file read (11 province histories) + solver.py | `solver.py:ON_STARTUP_DEVASTATION` lists exactly 11 pids (5 at 50.0, 6 at 20.0); `grep -i devastat` on all 11 corresponding `history/provinces/*.txt` files (Brno, Prague, Plsen, Erz, Eger, Budejovice, Hradecko, Olomouc, Pardubice, Jindrichuv Hradec, Ostrava) returns zero matches |
| Y060 | Devastation applied by `on_startup`→`flavor_boh.15` via the `on_startup_effect` chain | CONFIRMED | file read | `common/on_actions/00_on_actions.txt:33`: `on_startup_effect=yes` inside `on_startup`; `common/scripted_effects/01_scripted_effects_for_on_actions.txt:4787-4795`: `if{limit={tag=BOH...}} country_event={id=flavor_boh.15}`; `events/flavorBOH.txt:939` "#The Aftermath of the Hussite Wars" `id=flavor_boh.15`, `immediate` block: `bohemia_area{add_devastation=50}`, `erzgebirge_area{add_devastation=20}`, `moravia_area{add_devastation=20}` |
| Y061 | Start devastation costs 13.40 ducats across the 11 provinces | CONFIRMED | measurement (`m6.out`) | `m6.out`: "devastation cost in ducats 13.4" |
| Y062 | Start state is what engine produces not history files; costs 3 separate reads | CONFIRMED | file read (current spec structure) | Matches the doc's own three enumerated points (on_startup effects; `add_base_*` accumulation; `is_city` not a filter) that immediately follow this sentence in the current text |
| Y063 | `on_startup` also fires `flavor_mng.42`,`flavor_mos.1`,`flavor_geo.1` from its own `events={}` list | CONFIRMED | file read | `common/on_actions/00_on_actions.txt:23-32`: `events={ muslim_school_events.20 flavor_got.1 flavor_mng.42 flavor_mos.1 flavor_fra.206 flavor_geo.1 flavor_mam.111 }` — all three present |
| Y064 | History parse matches save on 2,472/2,472 for `base_tax`,`base_production`,owner; only `trade_goods` differs, on 20 provinces | CONFIRMED | measurement (own script `check_save.py` against `VANILLA_start.eu4`) | Comparing `provinces.py`'s history-derived state against the save's `gamestate` top-level province fields for all 2,472 owned provinces: 0 owner mismatches, 0 base_tax mismatches, 0 base_production mismatches; exactly 20 `trade_goods` mismatches, all `history="unknown"` vs `save=<rolled good>` |
| Y065 | `flavor_geo.1` doesn't carry `add_base_tax`; its whole effect is legitimacy+modifier+flag; those keys are in `flavor_geo.3`, fired by a mission not `on_startup` | CONFIRMED | file read | `events/FlavorGEO.txt:7-44` `flavor_geo.1` immediate/option: `add_legitimacy=-20`, `add_country_modifier={name="geo_powerful_nobles"...}`, `set_country_flag=geo_received_starting_event` — no `add_base_tax`; `flavor_geo.3` (L99-137) option b: `capital_scope={add_base_tax=2 add_base_production=2 add_base_manpower=1}`; `grep -rn flavor_geo.3 missions/` → `missions/KoK_Georgian_Missions.txt:2043: country_event={id=flavor_geo.3}` (a mission, not `on_startup`) |
| Y066 | `add_base_*` in a dated pre-start block accumulates; v5- overwrote; Uppland `base_tax=5` undated +1 at 1436.4.28 = 6 | CONFIRMED | file read + script | `history/provinces/1-Uppland.txt`: `base_tax=5` (undated) and `1436.4.28={...add_base_tax=1...}`; `provinces.py`'s `ADD` accumulation logic sums these; `check_save.py` confirms the save's resolved `base_tax=6.0` for province 1 exactly |
| Y067 | `is_city=yes` not a filter: 20 owned provinces omit/comment it; province 265 among them, also devastated | CONFIRMED | measurement (own script) | `provinces.py` run: "owned at 1444.11.11: 2472", "is_city=yes and owned: 2452" (diff=20); own script lists the 20 pids including 265 (Brno); 265 is also in `ON_STARTUP_DEVASTATION` (20.0, Erzgebirge/Moravia group) |
| Y068 | Model counts owner+trade-node: 2,472 not 2,452 | CONFIRMED | measurement | `provinces.py`: "owned at 1444.11.11: 2472"; `solver.py`'s `ROWS` (owner+`PNODE` membership, no `is_city` check) has length 2472 |
| Y069 | 20 counted provinces have `trade_goods=unknown`; engine assigns from `chance={}` block | CONFIRMED | measurement + file read | Own script: exactly 20 pids with `trade_goods=="unknown"`; `common/tradegoods/00_tradegoods.txt` has a `chance={...}` block for every good (20+ occurrences) |
| Y070 | Model reads the engine-rolled good; pricing at zero understates world wealth by 12.70 | CONFIRMED | measurement (own script against `solver.ROWS`) | Sum of `prod_income` over the 20 rolled-good provinces = **12.70** exactly (computed in `scripts/r7/w03`) |
| Y071 | The 20 rolled 7 fur, 5 grain, 3 wool, 2 livestock, 1 each cotton/incense/naval_supplies | CONFIRMED | measurement (`solver.ROLLED` against `VANILLA_start.eu4`) | `Counter(solver.ROLLED[p] for p in the 20 unknown pids)` = `{fur:7, grain:5, wool:3, livestock:2, naval_supplies:1, cotton:1, incense:1}` — exact match |
| Y294 | `Core`(+75%)/`City`(+25%) not excluded, already inside `TAX_COEFF` | CONFIRMED | file read | `00_static_modifiers.txt:99-104` `city={local_tax_modifier=0.25...}`, `00_static_modifiers.txt:334-338` `core={local_tax_modifier=0.75 #90% more in a core...}` |
| Y295 | Engine tax multiplier = sum of itemised %: Garnatah 125%=75+25+5+5+15; Caceres 105%=75+25+5 | CONFIRMED | file read + arithmetic | Core 75 (file) + City 25 (file) + Iqta 5 (file, Y293) + national ideas 15 (file, Y293) + Clergy 5 (observed) = 125; 75+25+5=105; both are elementary sums of file-confirmed and observed components |
| Y296 | Cored city province with nothing else sums to 0.75+0.25=1.00, yields `base_tax` ducats/yr, the `TAX_COEFF=1.0` reference | CONFIRMED | file read + derivation | `0.75+0.25=1.00` (trivial); `solver.py:83`: `TAX_COEFF=1.0` |
| Y072 | `TAX_COEFF=1.0` applied to every counted province; ownership not modelled | CONFIRMED | file read (solver.py) | `province_table()`: `tax=TAX_COEFF*s["base_tax"]*(1.0+tmod)` uses the constant `TAX_COEFF=1.0` uniformly, no per-province core/city/owner check anywhere in the function |
| Y297 | Carrying Core or City again would double-count | CONFIRMED | derivation check | Trivial — `TAX_COEFF` already includes both (Y296) |
| Y073 | Known cost: `TAX_COEFF=1.0` rests on two cored-city readings at base_tax 2 and 6 | CONFIRMED | file read | `history/provinces/223 - Granada.txt`: `add_core=GRA`,`is_city=yes`,`base_tax=6`; `history/provinces/1747 - Caceres.txt`: `add_core=CAS`,`is_city=yes`,`base_tax=2` — both cored and city |
| Y074 | `base_tax` at 1444 runs up to 15 (province 1821), total dev 33 there | CONFIRMED | measurement | `solver.PROV[1821]`: `base_tax=15.0, base_production=15.0, base_manpower=3.0`; sum=33.0; confirmed as the max `base_tax` among owned provinces via `max()` over `solver.PROV` |
| Y298 | Unowned provinces outside model: `s`,`c` computed over owner+trade-node provinces | CONFIRMED | file read (solver.py) | `province_table()`: `if not s.get("owner"): continue` and `if node is None: continue` |
| Y299 | Owner-agnostic demand: responds only to what's there; conquest doesn't move demand same-day; only dev/goods/prices do | CONFIRMED | derivation check | Consequence of Y279/Y280 — no owner term, so only `base_tax`,`base_production`,`good`,`price`,condition-modifiers move `wealth` |
| Y075 | Owner-agnostic wealth removes hidden owner-dependence from §1.6's aggregate graph, built from same wealth field | CONFIRMED | file read (measure6.py) | `measure6.py`: `W=np.array([r["tax"]+r["prod_income"] for r in ROWS])` is the same wealth array used both for §1.3's per-province wealth and (via `cv()`) for Φ_w's aggregate demand shares |
| Y1146 | Base-line schema, truncation ordering, window's modifier composition are recorded observations from tooltip sessions | CONFIRMED | derivation check / self-consistency | Matches the doc's own framing and is corroborated by every arithmetic reconstruction above (Y041-Y049) reproducing the quoted figures exactly from the stated tooltip values |
| Y1147 | Round-7 audit re-verified save-side inputs and every arithmetic step | CONFIRMED | this audit's own work | This validation pass re-derived `base_tax`/`base_production`/goods for Garnatah and Caceres from the save (`check_save.py`) and re-computed every truncation/multiplication in Y041-Y049 independently, reproducing all quoted figures |
| Y1148 | On-screen strings are the observation itself; named observation is a source §3.16 admits | CONFIRMED | file read (current spec §3.16) | `per-good-trade-spec.md:2064` §3.16: "Every engine fact in this spec must carry its source — a file path, a binary string, **or a named observation**" |
| Y1149 | `01_church.txt` grants `global_tax_modifier=0.2` under "scale with loyalty & power"; Clergy 5 is a scaled instance | CONFIRMED | file read | `common/estates/01_church.txt:44-46`: `# These scale with loyalty & power` / `country_modifier_happy={global_tax_modifier=0.2 ...}` / `country_modifier_neutral={global_tax_modifier=0.2}` |
| Y1150 | Of Garnatah's 125%: 75,25,Iqta5,ideas15 file-confirmed (120/125); Clergy 5 the loyalty-scaled remainder observed | CONFIRMED | file read | Core 75 (`00_static_modifiers.txt:335`), City 25 (`:100`), Iqta 5 (`01_government_reforms_monarchies.txt:783`), national ideas 15 (`00_country_ideas.txt:3738`) — all four file-confirmed, summing to 120; remaining 5 (Clergy) is the tooltip-observed, loyalty-scaled instance (Y1149) |
