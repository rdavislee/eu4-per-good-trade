# Round-8 fixes — draft for negotiation, nothing applied

Answering `validation-round7.md`: **958 CONFIRMED · 26 PARTIAL · 0 REFUTED · 18 UNTESTABLE** over
1,002 claims. The spec is frozen at `4150af72da9ea1868b29fdd941bea604` (2,127 lines) until this list
is negotiated to unconditional confirmation and separately preconfirmed. Of the 18 UNTESTABLE rows,
sixteen are DLL/era-save/engine-internal gaps whose rows name what would settle them and are not
addressed here — they are the loop's floor, not its backlog; the two exceptions are Y1142 (a
process-artifact gap, discharged by B6 below) and Y1119 (an unrecorded pre-tie-break calibration
state, unrecoverable and left as graded). R1 is untouched by every row.

## A. Wrong or outrun conclusions (the two substantive ones first)

| # | ID | fix |
|---|---|---|
| A1 | Y631 | §3.3's closing conclusion is corrected: the slicing distortion is larger on the aggregate than on the **worked α = 1.5 example** — and larger still on price-clamped goods: the shipped `α = clamp(price/2, 0.2, 3.0)` puts `cloves` (base price 8.0, live at 1444) at α = 3.0, exponent 2.0, where the same two node comparisons read **(77/19)² ≈ 16.4×** and **(68/33)² ≈ 4.25×**, both above the aggregate's 4.1× and 2.1×. "Larger on the aggregate graph than on any per-good one" goes; what replaces it says the aggregate sits between the mild per-good example and the clamp ceiling. |
| A2 | Y177 | §3.9's sentence about the three rich non-sinks is corrected to what the flow shows — and so is the paragraph's own definition, which the counterexample falsifies. `sevilla` passes flow on (0.0201 of 0.0324 in); `mexico` and `gulf_of_siam` absorb every unit they receive (flow_out exactly 0, two of the same paragraph's "18 of 80") and are still not ends, because each has a single **free** out-edge. So carrying flow out is **not necessary** for non-endhood, and the sentence "an end is a node that passes none on" is corrected in the same edit: an end is a node the whole orientation gives no out-arc — no flow arc **and no free edge** — and the flow identity says why the flow arcs stop where they do, while the free edges are Phase 3's completion. **Three sites in the paragraph, one edit**: the three-node sentence, the "an end is a node that passes none on" definition, and — four lines below — "What makes an end is where the flow *terminates*", which restates the same falsified claim and becomes: the flow *arcs* terminate where the identity sends them; what makes an end is that no arc of either kind leaves, a property of the whole field and the graph rather than of a single node's rank. Wealth figures unchanged (all five reproduce). |

## B. Figures re-scoped to their instruments and constructions

| # | ID | fix |
|---|---|---|
| B1 | Y1169 | §2.3's seed-dependence parenthetical states the one counting rule and its seed-dependence honestly: the count of orientation flips over four column permutations is a function of the seed that draws the permutations — `p3_bisect.py`'s three seeds (20260821, 7, 4242) give **2, 8 and 6**, and seeds 0–30 span **4–14** with 8 the most common — same counting rule throughout (distinct per-permutation flips; the two instruments agree wherever seeds coincide), different seed sets. The load-bearing clause — seed-dependent, so no single count is quoted — is unchanged. |
| B2 | Y133, Y134 | §2.2's timing paragraph stops quoting **any** fixed replicate span, counting triple **or bracketed range** as a maintained figure — including in this row's own replacement text, where a median bracket was twice drafted and twice broken by the next batch of replicates. The replicate record — the original three, the validator's twelve (per-good-average maxima 4.4–11.4 ms, medians 0.105–0.240 s), the preconfirmation's nine (three maxima above 10.8), and round-7's three subagent runs (maxima 9.8–10.3) — carries no total, because a replicate tally is the next figure a new batch moves: every median is **of order 0.1 s**, maxima land on both sides of 10 ms, and runs inside v5.0's old interval vary from 0 to 11 of twelve per replicate. The paragraph therefore asserts exactly one figure-shaped thing: **of order 0.1 s for all 29 goods** — no bracket, no span, no triple; per-good averages are machine-and-scheduler noise, so **neither "single-digit" nor any finer figure is asserted**; and v5.0's 0.17–0.21 s was noise quoted as a figure — refuted by the inside-count variance, not by any one triple. The original 3.5–10.5/3.5–10.8/3.1–4.7 spans and the 1/0/0 triple go, as one machine's readings at one time. |
| B3 | Y972 | §0's v6.1 note scopes the sink-set move to what moved it: the installed set went from `{english_channel, hangzhou}` (v6.0's, under the unit-cost solver) to `{genua, hangzhou}` — **the tie-break and α_Φ moved together**, and the tie-break alone re-decides the α = 1.5 answer (the shipped operator at 1.5 gives a single sink, consistent with §1.6's own count row). The sentence no longer reads as α_Φ's sole effect. |
| B4 | Y738 | §3.13's α_min echo carries the full partition: reachable for **13**, exactly on the boundary for **2**, and unreachable for **15** — 11 with no negative event and 4 whose largest negative stops above the anchor — matching §3.5 and `measure6.py`'s four buckets. |
| B5 | Y250 | §1.1's preamble: "regenerated for v6.0 by `measure6.py`" → "regenerated by `measure6.py` and `props6.py`" — the preamble's blanket attribution matches the per-bullet citations that already split them. |
| B6 | Y214 | The lineage sentence names its sample so the spot-check is checkable: "(spot-verified on 7 of 60 in the round-7 audit — C037, C038, C049, C101, C128, C130, C132 — none missing; no exhaustive re-check has been made)". This also discharges Y1142's UNTESTABLE, whose gap was exactly that the seven were unrecorded. |
| B7 | Y011 | §0's R3 rationale: "re-measured and re-refuted in three successive audits" → re-measured and re-refuted **across the v3 and v5 audits** (v4's audit touched one gravity figure and passed it). The independence half — no rejection argument depends on the numbers — is untouched (confirmed). |
| B8 | Y125 | §1.10: "everything without a cooldown, which is most of the ladder" → "the undamped rows of the table — both trade-conflict casus belli rows and privateer blocking, **three of its seven**": four rows are damped (two by the policy cooldown, two by the trade-company defines), so "most" was false against the table it describes. |

## C. Scope and characterisation

| # | ID | fix |
|---|---|---|
| C1 | Y437, Y438, Y439, Y444 | §2.2 gains one implementation-status sentence after the list, replacing "The list above is the reference solver: standalone, run against parsed saves": **the list specifies the solver deliverable; the reference implementation ships items 1 (less `path`/`control`, which the shipped file carries and `nodes.py` discards — §2.4 item 3 nevertheless obliges the emitter to reverse both), 4, 5, 7 (as the boolean projection) and 8; builds its province table from `history/provinces` with the save read only for the twenty rolled trade goods; reads three defines by regex rather than a merged parse; and does not build the survival table.** And §2.9's solver track is extended to own every named gap, which today it does not: it lists the defines parser first but never mentions the save parser (item 2), the `path`/`control` read (item 1's remainder) or the survival table (item 6) — those join its solver track explicitly, so "the build order is where the gaps are owned" becomes true by making it true. Each of the four rows' facts lands; no item's stipulation changes. |
| C2 | Y498 | §2.4 item 2's emitter clause marked as specification: "the emitter reads it from the solve rather than assuming a number **(emitter behaviour — specification; the premise, a varying count, is measured)**". |
| C3 | Y503 | §2.5: "The binary is frozen — offsets found stay found" scoped to the build it is true of: frozen at build `835bfdf8` (2024-10-03, constant across `eu4_rev.txt` and all three crash dumps); offsets found stay found **for that binary, and a patch is a new binary** — which is §2.3's re-measure-on-patch rule applied to offsets. |
| C4 | Y533 | §2.7 item 10's parenthetical: "both nation-pair gates compare trade capitals" → the treasure-fleet gate compares **two trade capitals**; the sell-province gate compares **the buying country's main trading port against the province being sold** — `$WHO$` is the buyer, established not by the string alone but by its sibling keys (`DIPLO_SELLPROV_ABOVE_TREASURY` "Price is above $WHO$ treasury", `_NOPORT_NOCORE` "cannot be made into a core of $WHO$"), all AI-acceptance reasons evaluated on the counterparty. The colonisation-absence half is untouched (confirmed). |
| C5 | Y665 | §3.7: the per-nation universal is cut to its measured base plus intent: it returns true for some goods **at every node** — measured, every node holds an out-arc for at least 18 of the 29 goods — so no node is globally inert; that **no nation** is ever globally inert follows only through its merchant and collection placement, which is the design intent the AI section implements, not a measured fact. |
| C6 | Y695 | §3.10: the four consumers split by evidence: income-threshold content reads trade income **in shipped files** (`trade_income_percentage` in the church-aspect/decree/estate files); AI light-ship building, trade-league behaviour and peace valuation are **engine-internal readers** — the defines expose light-ship constants (cost, maintenance, time, sailors, combat width) but no rule tying the build decision to node value — and **no current §2.7 probe enumerates node-value readers** (item 10 enumerates direction-gate call sites, a different question), so those three stay engine-internal claims with the enumeration that would settle them named as missing rather than pending. |
| C7 | Y1017 | §2.1 check 1 gains the baseline qualifier: a single compiled stream gives identical results on any x86-64 host **it runs on**; running everywhere requires building to the architecturally-mandatory baseline (SSE2) — targeting an optional extension (the AVX2 check 2 names) turns "any host" into "any host with that extension", which is check 2's live risk restated at build scope. |
| C8 | Y1030 | §2.3's stopping-rule sentence carries its evidence class: scipy documents the option and default; the termination behaviour is established **behaviourally by the bisection below** (identical flip sets at unset and 1e-7, none at 1e-8), not from HiGHS's internals, which are outside the materials. |
| C9 | Y1081 | §1.1's parenthetical: "renamed from a round-5 working file" → **copied and extended from `val5_pergood.py`, which is still on disk**: `diff` shows props6.py is that file verbatim — zero deletions — plus 27 added lines carrying the permutation loop. The round-5 provenance is the shared docstring; the built-for-this-citation half is the in-file comment at the loop ("the scheduler-permutation figure section 1.1 quotes lives nowhere else in the tree… Measured here so the citation is true"). "Renamed" goes because the predecessor still exists; the derivation needs no version history — the two files establish it. |
| C10 | Y1087 **[instrument]** | §1.3's legibility reason stands unchanged — it is the true one. **`solver.py`'s comment is corrected**, because its stated reason is false of the instrument it names: `verify6.py` asserts nothing against `STATE_TAX_MOD` and its own D8 comment says reading the dict proves nothing; the only reader outside `solver.py` is `round6.py`, which reports the dict's state as a figure. The corrected comment: kept as an empty declaration rather than deleted so the exclusion's shape stays legible in the code; `round6.py` reports it, and `verify6.py` checks the tax rule behaviourally rather than by reading this dict. |
| C11 | Y054 | §1.3's devastation note keeps its honest flag and gains the probe: the scaling law rests on community documentation, which this audit's own rules bar as a source — so it is **an unverified scaling assumption, testable in one live session** (the stock 1444 start already carries two devastation levels — Bohemia 50, Erzgebirge/Moravia 20 — so one session, two province windows), added to §2.7 as item 18. **Two sites**: §2.7 gains the item, and §2.9's memory-track count moves "thirteen (1–11, 16 and 17)" → **"fourteen (1–11 and 16–18)"** — the only other place that counts open probes. |
| C12 | Y735 | §3.13's settled-and-moved parenthetical marks its tooltip readings as the recorded observations they are, pointing at §1.3's observation note — same treatment D1 gave the source block. |

## What negotiation must settle

- Every row: CONFIRMED or REJECTED, no conditions; anything conditional comes back.
- A1 and A2 change conclusions, not figures — verify my restatements against your own evidence cells before confirming.
- B2 quotes no fixed span, triple or bracket in either direction — the row itself records two drafted brackets broken by the next replicates. The record spans four runs' replicates (the original three, the validator's twelve, the preconfirmation's nine, round-7's three subagent replicates) with no total counted, every median of order 0.1 s and maxima on both sides of 10 ms; the refusal cuts both ways and rests on the spread, not on any single run.
- C1 compresses four rows into one sentence; check no row's fact is dropped.
- C11 adds probe 18 and B6 discharges Y1142 — confirm both side effects are correctly claimed.
